# 仓根临时脚本盘点 · 用途 + 三档归档建议(非破坏性 · 刷新版)

> 交付:**谷歌学术人机认证-140**（执行）｜工单来源:**总指挥 -142**（刷新 146 版盘点）｜2026-07-02。
> 本文件**取代**《仓根临时脚本盘点-可否归档建议-146.md》(已过时)。命名沿用总指挥指定的 `-142`。
> **边界（重要）**:**只产建议,不实际删/移任何文件**;不动 `fulltext_fetcher/` 生产代码;他人在飞的临时脚本一律列【待确认】,不单方处置。
> 口径:仓根 `_*.py` + `fs_shim_mainloop_141.py` 共 **43 个**临时/探针脚本(146 版 33 个 → 本波 **+10**);逐个读文件头 docstring 判用途,并只读比对 `fulltext_fetcher/` 判断是否已被生产吸收。

---

## 〇、TL;DR

- **43 个临时脚本**,四类分流:**可删 16 · 可归档 14 · 需保留/待转正 3 · 待确认(他人在飞)10**。
- **可删的判据**=一次性探针,结论已 **100% 落进生产代码 + 经验记录**,无残余参考价值:
  - JA3/浏览器内直下的根因与真修复 → 已并入 `fulltext_fetcher/render_fetch.py`(`_patch_nodriver_cdp_compat` 第 54–82 行、页内 `fetch().arrayBuffer()`+`FileReader` 第 396–404 行、`render_download_pdf_bytes` 第 570 行);
  - 内容 QC/覆盖口径 → 已在 `tools/qc_content_match.py`、`tools/build_coverage.py`、`tools/regress_qc_union_189.py`。
- **可归档**=方法/证据有参考价值,建议 `git mv` 进 `archive/scratch-2026-07/`(保 git 历史、随时可回),**不建议直接 `rm`**(北极星:留证)。
- **需保留 3 个**:`_smoke_render_bytes.py`(Runbook-173 依赖)、`fs_shim_mainloop_141.py`(修好 [Errno 22] 的可用 shim,待评估合入 `tools/`)、`_naming_audit.py`(可复用运维审计,待转正 `tools/`)。
- **待确认 10 个**属 **-152**(route-B / publisher_direct 缺口,9 个)与 **-150**(回收覆盖聚合,1 个)在飞的活,**不单方处置**,交总指挥 -142 排期后由属主定夺。

| 类别 | 数量 | 处置口径 |
|---|:--:|---|
| 可删 | 16 | 结论已固化,无残值,拍板后可直接删(或一并归档) |
| 可归档 | 14 | `git mv` → `archive/scratch-2026-07/` |
| 需保留/待转正 | 3 | 留仓根或转正 `tools/` |
| 待确认(他人在飞) | 10 | 交 -152 / -150 属主定夺,勿动 |
| **合计** | **43** | |

---

## 一、对照 146 版:本波新增 10 个

| 脚本 | 归属 | 本盘点分类 |
|---|---|:--:|
| `_agg_recover_150.py` | -150 回收覆盖聚合(dry-run,并 run_all 回收 coverage 入 999 净口径) | 待确认(-150) |
| `_route_b_b2_152.py` | -152 RSC 端到端(过盾→B2 Fetch.enable RESPONSE 拦截+导航 PDF) | 待确认(-152) |
| `_route_b_probe_152.py` | -152 route-B 根因(CDP 版本错配 monkeypatch 证伪) | 待确认(-152) |
| `_route_b_diag_152.py` | -152 route-B 分步诊断(定位 no-pdf-captured 卡点) | 待确认(-152) |
| `_route_b_qc_152.py` | -152 route-B 落盘 PDF 内容 QC(抽首页核 DOI/标题) | 待确认(-152) |
| `_route_b_cfwait_152.py` | -152 验证「过盾期开 Fetch 拦截卡死 CF」假设(纯导航轮询 cf_clearance) | 待确认(-152) |
| `_find_rsc_oa_152.py` | -152 找真金 OA 的 RSC 样本(Crossref RSC Adv → Unpaywall 核 is_oa) | 待确认(-152) |
| `_rb_test_144.py` | route-B 方法B(页内 fetch().arrayBuffer())聚焦测试 | 可删 |
| `_oa_check_144.py` | 单 shim(8199)测 1 篇 ACS 能否过 CF | 可删 |
| `_fs_test_144.py` | Unpaywall 找 1 条 RSC 金 OA 且 PDF 托 pubs.rsc.org 样本 | 可删 |

> 说明:`-152` 的 route-B 探针**结论看似已并入** `render_fetch.py`(第 65 行注释「见 152 冒烟」即出自此波);若 -152 已收口,这批可整体从【待确认】转【可归档】。是否收口以 -152 为准,故本文件不擅自降档。
> 146 版所列 33 个脚本本波**无删除/无失效**,全部仍在仓根(见下三档)。

---

## 二、可删(16)· 结论已 100% 固化,无残余参考价值

### 2.1 FlareSolverr / nodriver shim 诊断(-141,已并入 `fs_shim_mainloop_141.py` + 经验记录 N.1)
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_fs_probe_141.py` | 复现 `_solve` 各 CDP 步定位 [Errno 22] | 结论已产出可用 shim 变体,纯 traceback |
| `_tmp_fs_probe2_141.py` | 诊断并发模型跨线程 [Errno 22] | 同上 |
| `_tmp_fs_probe3_141.py` | 真实 `_solve` 全流程 traceback | 同上 |
| `_tmp_fs_probe5_141.py` | 精确复现 shim HTTP 路径失败行 | 同上 |

### 2.2 CDP/JA3/字节捕获一次性探测(已并入 `render_fetch.py`)
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_probe_cdp_api.py` | 打印 nodriver `Tab.add_handler/evaluate/get` 签名 | 纯 API 探查,一次性 |
| `_diag_capture.py` | 逐步打印浏览器内抓字节各阶段卡点 | 机制已落 `render_download_pdf_bytes` |
| `_spike_bc_smoke.py` | nodriver 能否在本环境启动+抓页 | throwaway 冒烟 |
| `_spike_browser_capture.py` | 浏览器内 PDF 字节捕获 SPIKE | 已落 `render_fetch.py` 第 396–404 行 |

### 2.3 -144 一次性单样本核验
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_rb_test_144.py` | route-B 方法B 页内 fetch 聚焦测试 | 结论已并 `render_fetch.py`,单样本 |
| `_oa_check_144.py` | 单 shim 测 1 篇 ACS 过 CF | 一次性诊断,自述 task-144 诊断用 |
| `_fs_test_144.py` | 找 1 条 RSC 金 OA 样本 | 一次性取样,已被 `_find_rsc_oa_152` 取代 |

### 2.4 QC 口径复原临时统计(自述用后即删)
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_qc_bucket_stats.py` | 统计 union 错论文审计集 host/桶分布 | 自述「用后即删」 |
| `_tmp_qc_bucket189.py` | 复原「同域错论文 189 条」口径 | 自述「临时」 |
| `_tmp_qc_report_probe.py` | 探查 qc_content_report 结构 | 自述「临时」 |

### 2.5 其它已被生产吸收
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_verify_cov_147.py` | 校验 b4cf 黑名单是否已从 coverage 剔除 | 自述可删,一次性校验 |
| `_title_probe_content_qc.py` | title_probe 内容级 QC | 功能已在 `tools/qc_content_match.py` |

---

## 三、可归档(14)· `git mv` → `archive/scratch-2026-07/`

### 3.1 JA3 / 浏览器内直下 根因与真修复探针(-144,有方法参考价值)
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_ja3_probe_144.py` | JA3 只读探针(浏览器内 fetch 绕 JA3 拿 PDF) | 结论已落 `render_fetch.py`,但方法可复用 → 留证 |
| `_tmp_ja3_diag_patched.py` | ClientSecurityState 容缺 monkeypatch 后定位卡点 | 根因验证有参考价值 |
| `_tmp_ja3_monkeypatch_probe.py` | 证实 CDP schema 漂移是抓不到 PDF 根因 | 同上 |
| `_tmp_ja3_realfix_probe.py` | 实证不启 Fetch 域拦截同会话 fetch 正文 PDF | 同上 |

### 3.2 内容 QC / 门③④⑤ 验证与回归(-142/-147/-157)
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_tmp_verify_b4cf_qc_142.py` | 独立重算 recover_b4_cf 34 条 verdict 对账 | 审计证据 |
| `_tmp_manual_check4_142.py` | 4 条 uncertain/scanned + 1 SI 边界人工核验 | 审计证据 |
| `_tmp_b4cf_fail_rootcause_157.py` | b4cf 失败 DOI 根因分析(产 md) | 根因写作参考 |
| `_tmp_verify_gates345_147.py` | 门③④⑤ 拦截率/必要性/误杀 | 门验证证据 |
| `_tmp_verify_gate_live_147.py` | 34 条 success 跑真门 end-to-end 拦截率 | 同上 |
| `_tmp_verify_escape_mech_147.py` | 区分两种逃逸机制(整源豁免 vs 进门 match) | 同上 |
| `_tmp_regress_nofalsekill_147.py` | 广样本不误杀回归 | 建议并入 `tools/regress_qc_union_189.py` |
| `_tmp_regress_fixtures_147.py` | §9.7 四条不误杀正例 fixture 断言 | 建议并入 selftest |

### 3.3 批次切分(一次性数据工具)
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_split_batch4.py` | batch_4 500 条切 5 chunk | 用完可删,留作切分模板 |
| `_split_batches.py` | 测试集 csv/xlsx 切 N chunk | 同上 |

---

## 四、需保留 / 待转正(3)· 别删

| 脚本 | 为什么保留 | 建议动作 |
|---|---|---|
| `_smoke_render_bytes.py` | **被《路线B-浏览器内直下PDF验证Runbook-173.md》第 2 步显式引用**(冒烟 ACS-OA+RSC 各 1 条) | 留仓根;或随 Runbook 迁 `tools/` 并同步路径 |
| `fs_shim_mainloop_141.py` | **修好 Windows 工作线程 Proactor [Errno 22]** 的可用 shim 变体(主线程 loop + HTTP 守护子线程) | 评估合入 `tools/`(或并进 `tools/flaresolverr_nodriver.py`);合入前别删 |
| `_naming_audit.py` | 只读审计 `out/*/pdfs/` 文件名是否符合 `download.py` 命名标准——**可复用运维工具** | 转正 `tools/` 或 `scripts/` |

---

## 五、待确认(10)· 他人在飞,**勿单方处置**

> 判据:脚本归属仍在协同组内在飞的成员(-152 / -150),归档/删除须其属主拍板;本文件仅登记,交总指挥 -142 排期路由。

| 脚本 | 归属 | 现状 |
|---|:--:|---|
| `_route_b_b2_152.py` | -152 | route-B 端到端;结论疑已并 `render_fetch.py`,待 -152 确认收口 |
| `_route_b_probe_152.py` | -152 | route-B 根因;同上 |
| `_route_b_diag_152.py` | -152 | route-B 分步诊断;同上 |
| `_route_b_qc_152.py` | -152 | route-B 落盘 QC;可能转正为 route-B 验收工具 |
| `_route_b_cfwait_152.py` | -152 | 过盾期 Fetch 拦截假设验证 |
| `_find_acs_oa_152.py` | -152 | 找真金 OA 的 ACS 样本(证路线B) |
| `_find_rsc_oa_152.py` | -152 | 找真金 OA 的 RSC 样本 |
| `_gap_pubdirect_152.py` | -152 | P1 publisher_direct 缺口扫描 |
| `_gap_pubdirect_152_detail.py` | -152 | 缺口明细(建模板 backlog) |
| `_agg_recover_150.py` | -150 | 回收覆盖聚合(dry-run);可能转正为覆盖审计工具 |

---

## 六、附:临时 `.txt/.json` 产物(随对应任务归档,非脚本盘点主体)

| 文件 | 性质 | 建议 |
|---|---|---|
| `_filelist.txt` / `_naming_audit_report.txt` / `_tmp_final_check.txt` / `_run_all_demo_input.txt` | 脚本临时输出/输入 | 随 2.x 可删项一并处置 |
| `titles_probe.txt` / `titles_probe_truth.json` / `subset_elsevier_probe_149.txt` | 探针输入/真值 | 探针归档则一并归档 |
| `recover_*_input.txt` / `recover_b4_cf_smoke10.txt` / `batch7_*.txt` / `rerun141_other_cf_input.txt` | 回收任务**数据输入清单**(非脚本) | 属数据,建议归 `inputs/` 或随批次目录 |

> 注:git 快照中出现的 `smoke170_input.txt` 当前 glob 未见(疑已删),不计入。

---

## 七、执行建议(供总指挥 -142 / 属主拍板)

1. **可删 16 个** → 拍板后可 `rm`(结论已固化);保守做法仍是先 `git mv` 进 `archive/scratch-2026-07/` 留一版。
2. **可归档 14 个** → `git mv` 进 `archive/scratch-2026-07/`;其中 `_tmp_regress_*_147.py` 两条建议并入 `tools/regress_qc_union_189.py` / selftest 后再归档。
3. **需保留 3 个** → `_smoke_render_bytes.py` 留仓根(Runbook 依赖);`fs_shim_mainloop_141.py`、`_naming_audit.py` 走转正 `tools/` 评审。
4. **待确认 10 个** → 交 -152 / -150 属主确认收口后再归档/转正;总指挥可 `submit_plan` 编排或 `send_to_session` 知会属主。
5. 归档后可在 `.gitignore` 加约定 `_*.py` / `_tmp_*` / `_spike_*` / `_probe_*` / `_route_b_*`,减少后续临时件污染仓根。

---

*核验 2026-07-02｜-140 执行 · -142 工单｜取代 146 版。只读盘点+建议,未删/未移/未改任何脚本或生产代码。*
