# 经验记录完整性审计 · 本波关键主题落档核对（只读产出）

> 交付：**谷歌学术人机认证-140**｜taskId=`task-ee120786-50e7-48f3-a115-fa9879e0aa5a`｜2026-07-02
> 触发：总指挥派单——按本组原则#4（实现路径/原理/踩坑/成功经验/新发现完整落档），只读审计《经验记录-踩坑与发现.md》（已改动未提交），核对本波 5 大关键主题是否都有条目，产【已落/缺失/需合并去重】三分类清单 + 具体补登建议。
> **边界：只读，不改《经验记录》正文**（避免与两份 route-B 待归档暂存片段冲突）；仅本审计报告为新增文件。审计同时对照了当前**工作树** `fulltext_fetcher/download.py`（未提交），以“代码现状”而非“文档口径”定 门③/route-B 落盘的真实状态。
> 事实源：`经验记录-踩坑与发现.md`（L1–L400）、`路线B-真机冒烟结果-152.md`、`真实净增回写coverage-Step1核验-143.md`、`验证-门③④⑤拦截率-recover_b4_cf-147.md`、`验证-QC并集门对route-B产出无假阳无假杀-147.md`、`经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md`、`选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md`、工作树 `download.py`。
>
> ℹ️ **净覆盖率口径（173 冻结）**：本审计文内出现的净覆盖 / miss 数字（如 620/628 等）均为成文时快照，属**【历史口径】**。**【历史快照】当前权威见 `out/coverage.json`：326 success / 673 miss / 999 = 32.63%**（generated_ts 2026-07-03 12:50:24, allow_override=10）。见 **《基线口径冻结说明-388-173.md》**。

---

## 〇、TL;DR（给总指挥，5 主题一句话结论）

| # | 主题 | 落档结论 | 动作 |
|---|---|---|---|
| 1 | **route-B 抓字节 Network 域 vs 过盾后 Fetch** | **缺失**（定论只在两份暂存草稿，正文 N.8 只到 “B2 过盾后开 Fetch”，未收 A/B 决定性结果 + PreflightWarn bug） | 新增 **N.9**（缺失 A+B） |
| 2 | **CF 过盾根因 + 修法** | **已落且充分**（N.7 现象 + N.8 双路径根因定论 + 5 条修法铁律） | 无需补 |
| 3 | **JA3 / 指纹** | **已落且充分**（N.3 ACS 不绑/RSC 绑分水岭 + N.4 浏览器内直下 + curl_cffi 背景） | 无需补 |
| 4 | **QC 并集门** | **大部已落**（L/M/O/N.5）；**两处待修**：①N.5「已补门③」与代码不符（门③仍未实现）；②route-B 落盘曾绕过 QC 门（147 核出）+ 现已修，坑与修法未落档 | 更正 1 + 补 缺失 C |
| 5 | **coverage 二级扫描回写** | **数字已落**（L.4 净 448/999）；**机制 + 口径坑缺失**：二级(嵌套)扫描机制、“+71→442/561”证伪、stale 黑名单双向错（误杀 4 真全文 / 漏 3 假阳） | 补 缺失 D |

**净结论**：主题 2、3 完整；主题 1 基本缺失（待 N.9）；主题 4、5 主体已落但各有 1 处**需更正** + 若干**踩坑/新发现未落档**。共 **4 项缺失（A/B/C/D）+ 1 项需更正 + 2 项去重/一致性**。

---

## 一、审计方法

1. 通读《经验记录》A–O 全 15 节，建立“已有条目 → 主题”索引。
2. 对 5 主题各取 1–2 份本波权威佐证文档，逐条比对“文档里的实现路径/原理/踩坑/新发现”是否在正文有对应条目。
3. 对**门③状态**与**route-B 落盘是否过 QC 门**两个易漂移点，直接查工作树 `download.py`（未提交）定论，不采信文档旧口径。
4. 归入三类：**已落**（有等效条目、口径一致）/ **缺失**（无条目或只在暂存草稿）/ **需合并去重·需更正**（有条目但与代码/其他条目矛盾、或多份重复）。

---

## 二、三分类清单

### 2.1 【已落】—— 有等效条目、口径一致，无需补

| 主题 | 正文条目 | 覆盖度 |
|---|---|---|
| CF 过盾根因（FS/shim 路径 0 产出 = 多 shim thrash） | **N.7 + N.8 #1** | ✅ 现象 + 根因定论 + “单 shim 单桶串行”修法 |
| CF 过盾根因（route-B 路径卡 CF = 过盾期开 CDP Fetch/Network 拦截） | **N.8 #2**（5 条修法铁律） | ✅ 真因、与 KeyError 串联、`acsomega.6c04195` 6.6s 过 + 13.7MB %PDF 实证 |
| JA3 分水岭（ACS 不绑→回放可过 / RSC 绑→回放必 403） | **N.3** | ✅ 机理 + 实测 + curl_cffi「非解题真 Chrome 的 JA3」背景 |
| JA3 绑定站正解 = 浏览器内直下（B1 同源 fetch） | **N.4** | ✅ 机制 + ACS-OA 已闭环 + ROI 诚实口径 |
| QC：websearch 系统性抓错论文假阳 + 双法定案 + 净覆盖修正 | **L（L.1–L.6）** | ✅ 假阳率 68.5%、四类错误模式、交集→并集教训、数据回归 |
| QC：同题非全文四类新假阳（citation-report/poster/SI/同题他刊） | **M（M.1–M.2）** | ✅ 逐类实锤 + 闸门启示 |
| QC：ACS authorchoice `/doi/pdf/` 高 SI 假阳 ≈93% + SI 门漏检 7 条 | **O（O.1–O.5）** | ✅ 机制 + 三桶净增结算 + 门④漏检三机理 |
| QC：FS/路线B 回收必须与内容 QC 联用 | **N.5** | ✅ FS 成功≠正确全文、pypdf 缺失 → 门 no-op 铁律 |
| coverage 净覆盖率修正（净 448/999≈44.8%，不认 71.4%） | **L.4 + L.6** | ✅ 黑名单感知、以 coverage.json `success_after_qc` 为准 |
| route-B B1/B2 按域分（ACS-OA=B1 同源 fetch / RSC=B2 过盾后 Fetch） | **N.8 #2 修法 #3** | ✅ 已记“先 B1、遇 CSP/跨源回落 B2、B2 的 Fetch.enable 只在过盾后开” |

### 2.2 【缺失】—— 无条目或仅在暂存草稿，建议补登

#### 缺失 A（主题 1，P0）· route-B 方法A「Network 域 vs 过盾后门控 Fetch.enable」分叉**定论**未入正文
- **现状**：正文 N.8 #2 只说到“B2 的 Fetch.enable 只在过盾后开”，**未收**本波 A/B 决定性实测：
  - **当前工作树（-154）把方法A 改成纯 Network 域**（`ResponseReceived`+`LoadingFinished`→`network.get_response_body`，删了 `fetch.enable`）；
  - 对 RSC `10.1039/d5ra08493h` 导航 articlepdf：**Network 域 = FAIL（size=0、`no-pdf-captured`、188s）**；**过盾后门控 `Fetch.enable` RESPONSE（同 tab、cf_clearance 后才 enable）= PASS（484,829B、%PDF-1.6、QC match）**；ACS-OA `acsomega.6c04195` B1=PASS（13.78MB）。
  - **定论**：同源 PDF→B1 页内 fetch；**跨源/viewer 导航 PDF（RSC articlepdf）→ 过盾后门控 Fetch.enable RESPONSE，Network 域取不到导航 PDF 的 body**。-154「整体删 Fetch 改 Network 域」对同源 OK、对 RSC **不成立**，需把过盾后门控 Fetch **引回**。
- **只在**：`经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md` §四 + `选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md` §四（两份均标“待写锁合入”）。
- **补登建议**：新增 **N.9「route-B 方法A 定论：同源 B1 / 跨源过盾后门控 Fetch；Network 域取不到导航 articlepdf body」**（草稿见 §三）。

#### 缺失 B（主题 1，P1）· PreflightWarn 枚举漂移 bug（Network 域抓不到 body 的直接机制之一）未落档
- **现状**：正文 N.4/N.8 只记了 `KeyError:'localNetworkAccessRequestPolicy'`（字段**缺失**）；**未记** `_patch_nodriver_cdp_compat._tolerant_from_json` 只兜 KeyError、**不兜“字段存在但值是 nodriver 0.50.3 不认的新枚举”**——Chrome 133 发 `PreflightWarn` → `LocalNetworkAccessRequestPolicy.from_json` 抛 **`ValueError`** 洪水（单跑约 270–上千行）→ `parse_json_event` 崩 → **网络事件被丢弃**，这正是 A（Network 域）在 RSC 抓不到 `LoadingFinished`/body 的直接机制之一。探针 `_route_b_b2_152.py` 的 `_tol`（`try/except`→`ALLOW`）不崩，故能抓到字节。
- **补登建议**：并入 N.9 的“附带必修 bug”；修法 = `_tolerant_from_json` 的枚举转换包 `try/except`，未知值降级 `ALLOW`（与探针 `_tol` 对齐）。

#### 缺失 C（主题 4，P0 载荷性）· route-B 落盘曾**结构上绕过内容 QC 门**（147 核出）+ **现已修**，坑与修法未落档
- **现状**：`验证-QC并集门对route-B产出无假阳无假杀-147.md` 核出——内容 QC 门只在 ① `_download_pdf_core` 跑；route-B 走 **②b `_browser_capture_fallback`（B1/B2）** 与 **⑥ `_browser_pdf_download`（Akamai/MDPI）**，二者校 `%PDF`+体积+结构后**直接 `_save()`、不过 `_content_qc_gate`** → 回收波 route-B 产出的 SI/错论文假阳会裸奔落盘。
- **代码现状（工作树 `download.py`，未提交）：已修** —— `_browser_capture_fallback`（L1049）与 `_browser_pdf_download`（L1252）落盘前均已 `_content_qc_gate(..., force=True)`（`force=True` 关键：route-B 源多为 DOI 绑定源、会被 `_source_needs_content_qc` 豁免，故必须强制过门）。
- **落档缺口**：这一“P0 结构缺口 → force=True 强制过门修法”的**踩坑 + 修法**在《经验记录》**完全无条目**（N.5 只讲“联用”，未讲“route-B 落盘路径本身曾绕过门、现已接门”）。
- **补登建议**：在 **N.5 末**或 N.9 补一条（草稿见 §三）：记“route-B 三出口（②b B1/B2、⑥ Akamai）落盘前必过 `_content_qc_gate(force=True)`；`force=True` 是为压过 DOI 绑定源豁免；回收波置 `content_qc_non_article_hard_reject=True`”。

#### 缺失 D（主题 5，P1）· coverage 二级扫描**机制** + “+71”口径证伪 + stale 黑名单双向错
- **现状**：正文只有 L.4 的**最终净数字**（448/999），**缺**以下本波实现路径/踩坑/新发现（全在 `真实净增回写coverage-Step1核验-143.md`）：
  - **新发现/机制**：`build_coverage.py::list_batch_dirs` 早已 `os.walk` 递归收录任意深度 `out/<dir>/fetch/metadata.jsonl`，`include_nested=True` 默认，带 `--no-nested` 逃生门 + `--extra-dirs` 定向纳入 + demo/smoke 夹具剔除；`--selftest` 含嵌套翻转用例。**“修扫描”其实是已完成态**（Step1 无需改码）。
  - **口径坑**：任务简报 **“+71 → 371→442 / 628→561” 在 QC 口径下不成立、严重偏乐观**——71 行 = 62 条 websearch 抓错论文假阳（QC 正确拦截）+ 4 条 route-B 无 metadata 裸 PDF（不进 coverage）+ 5 条翻 success（仅 1 条可信）。`--no-qc` 硬凑到 506（50.7%）是**把 websearch 错论文全算进来的污染值，绝不可当净覆盖**。含嵌套+QC 实测 **376/623**（仅比一级 371/628 净增 +5）。
  - **黑名单坑（双向错）**：① **stale 黑名单误杀 4 条真全文**（`j.jechem.2020.06.007 / j.apcatb.2021.119925 / j.jcou.2022.102356 / j.jece.2025.119153`，`judge()` 判 match 89.1–100，黑名单基于**早期错版**建、rerun 已重下正确版 → false-kill 应解救）；② **rerun 新产的 uncertain 未被“整池拒收”快照盖到 = 并集门对新回收波的盲区**；③ 3 条共享同一假 PDF（md5 碰撞，EHS《氢化安全须知》）不在任何黑名单 → 会漏进 376。→ “**376 里同时含 3~4 假阳 + 漏 4 真全文，两错抵消凑成 +5，逐条都是错的**”。
- **补登建议**：新增 **P 节「coverage 二级扫描机制 + 口径/黑名单双向错」**（草稿见 §三）；净数字仍以 L.4/L.6 的 coverage.json 为准，**不把中间 dry-run 的 376 当终值**（见去重/一致性 3）。

### 2.3 【需合并去重 · 需更正】

**更正 1（主题 4，必改）· N.5「已补门③」与代码不符 —— 门③仍未实现**
- 正文 **N.5（L315）** 写“-173 建议 + 142 复核 + 147 实测**已补门③**（文内/元数据 DOI≠期望）/门④/门⑤”。
- **实证反证**：`验证-门③④⑤拦截率-147.md` §四明列“**P0 · 门③ 未实现**”，唯一漏网 `cctc.200900261`（同题他刊，Beilstein DOI 不在首 500 字，须 `meta-doi-mismatch` 读 XMP/Info `/doi`）；`验证-QC并集门对route-B-147.md` 同判门③为“老缺口未实现”。
- **代码现状**：工作树 `download.py` 的 `_content_qc_verdict`/`_content_qc_non_article_reject` 只有 **门①（content-title-mismatch）、门②（url-doi-conflict）、门④（SI）、门⑤A/B/C（citation-report/poster/TOC）**；DOI 强正只查“期望 DOI 是否在正文/URL”（L633–638），**无读 PDF 元数据 `/doi`·`prism:doi` 与期望 DOI 反向比对的门③**。
- **改法**：把 N.5（L315）表述更正为“**门④⑤ 已落地 end-to-end；门③（文内/元数据 DOI≠期望，meta-doi-mismatch）仍未实现（P0，`cctc.200900261` 漏网）**”，别把“建议补”写成“已补”。（此更正需《经验记录》写锁属主执行，本审计不改正文。）

**去重 2（主题 1）· 两份 route-B 分叉暂存草稿高度重叠**
- `经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md` 与 `选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md` 记的是**同一 A/B 定论 + 同一 PreflightWarn bug**。合入 N.9 时**取单份口径**、别把两份都抄进正文造重复；两份原文件可在 N.9 落定后归档（保留其一作事实源指针即可）。

**一致性 3（主题 5）· coverage 净数字多口径需锚定**
- 正文/文档并存 **448/999（L.4 权威）、376/623（143 Step1 dry-run 中间态）、71.4%（虚高逐批求和）** 三个数。补登缺失 D 时，**必须明示：净数字以 `out/coverage.json` 的 `summary.qc.success_after_qc` 为准（随 cleanup/黑名单增删演进），376 只是某次 dry-run 快照，不与 448 并列当终值**——与 L.6 现有“勿引用陈旧快照”口径对齐。

---

## 三、具体补登建议（可直接合入的成段草稿，交写锁属主）

> 以下为**建议文案**，本审计不写入正文。写锁属主（-146 / 总指挥指派）持锁后按需合入；合入后可归档两份 route-B 暂存草稿 + 143 coverage 核验文档为事实源指针。

**建议新增 N.9（缺失 A+B）**
```
### N.9 route-B 方法A 定论：同源 B1 / 跨源过盾后门控 Fetch（Network 域取不到导航 articlepdf body）（2026-07-02，A/B 收口；源 -152 真机 · -140/-142 起草）
- 分叉：过盾后抓 PDF 字节两条 CDP 实现——(A) 当前工作树 -154「删 Fetch.enable、纯 Network 域」；(B) 探针「过盾后门控 Fetch.enable RESPONSE、同 tab」。
- A/B 实测（同机单头 nodriver 0.50.3 + Chrome 133 有头）：
  · ACS-OA acsomega.6c04195 → B1 页内 fetch = PASS（13.78MB、%PDF、QC match）。
  · RSC d5ra08493h · A 端(Network 域) = FAIL（size=0、no-pdf-captured、188s）；B 端(过盾后门控 Fetch.enable RESPONSE) = PASS（484,829B、%PDF-1.6、QC match）。
- 定论：同源 PDF→B1；跨源/viewer 导航 PDF（RSC articlepdf，用 publisher_direct 构造）→ 过盾后门控 Fetch.enable RESPONSE（仅 cf_clearance 后 enable、enable/paused/get_response_body/continue_request 全同一 tab、抓到 %PDF 即 disable）；Network 域对导航 PDF 取不到 body，不可用。-154「整体删 Fetch」对同源 OK、对 RSC 需把过盾后门控 Fetch 引回。落地归 -144/-154。
- 附带必修 bug（PreflightWarn）：_patch_nodriver_cdp_compat._tolerant_from_json 只兜字段缺失(KeyError)，未兜“字段在但枚举值 nodriver 0.50.3 不认”（Chrome 133 发 PreflightWarn → LocalNetworkAccessRequestPolicy.from_json 抛 ValueError 洪水 → 网络事件被丢弃 → 即 A 端抓不到 body 的直接机制之一）。修法：枚举转换包 try/except、未知值降级 ALLOW（探针 _route_b_b2_152.py 的 _tol 即此写法）。
- 出处：路线B-真机冒烟结果-152.md UPDATE/UPDATE-2；经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md；选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md。
```

**建议在 N.5 末补一条（缺失 C）**
```
- route-B 落盘曾结构上绕过内容 QC 门（147 核出，现已修）：内容 QC 门原只在 ① _download_pdf_core 跑；route-B 走 ②b _browser_capture_fallback(B1/B2) 与 ⑥ _browser_pdf_download(Akamai/MDPI)，二者曾直接 _save() 不过门。已修：两函数落盘前均 _content_qc_gate(..., force=True)；force=True 是为压过“route-B 源多为 DOI 绑定源会被 _source_needs_content_qc 豁免”，否则接了也豁免。回收波须置 content_qc_non_article_hard_reject=True。
```

**建议新增 P 节（缺失 D）**
```
## P. coverage 二级(嵌套)扫描机制 + “+71”口径证伪 + stale 黑名单双向错（2026-07-02，143 Step1 只验）
- 机制/新发现：build_coverage.list_batch_dirs 早已 os.walk 递归收 out/<dir>/fetch/metadata.jsonl，include_nested 默认 True（--no-nested 逃生 + --extra-dirs 定向 + demo/smoke 剔除；--selftest 含嵌套用例）。“修扫描”其实已完成态。
- 口径坑：任务简报 “+71→442/561” 在 QC 口径下不成立——71 = 62 条 websearch 假阳(QC 正确拦) + 4 条 route-B 无 metadata 裸 PDF + 5 条翻 success(仅 1 可信)。--no-qc 的 506(50.7%) 是污染值，绝不可当净覆盖。含嵌套+QC 实测 376/623（仅 +5 vs 一级 371/628）。
- 黑名单双向错：① stale 黑名单(基于早期错版)误杀 4 条真全文(judge 判 match 89.1–100)→ 应解救；② rerun 新产 uncertain 未被“整池拒收”快照盖到 = 并集门对新回收波盲区；③ 3 条共享假 PDF(md5 碰撞)不在黑名单会漏进 376。→ 376 里同时含 3~4 假阳 + 漏 4 真全文，两错抵消凑 +5，逐条皆错；真回写前必须按当前盘上文件重跑内容 QC。
- 口径锚定：净数字以 out/coverage.json 的 summary.qc.success_after_qc 为准，376 仅 dry-run 快照，勿与 L.4 的 448/999 并列当终值。
```

---

## 四、给写锁属主 / 总指挥的执行清单（按优先级）

1. **P0 更正 1**：N.5（L315）「已补门③」→ 更正为「门④⑤ 已落地、门③（meta-doi）仍未实现」。（防误导后续放弃 `cctc.200900261` 一类回收判断。）
2. **P0 缺失 C**：N.5 补“route-B 落盘现已 `force=True` 过门”一条（坑+修法已发生在代码，未落档）。
3. **P0/P1 缺失 A+B**：合入 N.9（route-B 方法A 定论 + PreflightWarn bug），并把两份暂存草稿归档为事实源指针（去重 2）。
4. **P1 缺失 D**：新增 P 节（coverage 二级扫描机制 + “+71”证伪 + stale 黑名单双向错），锚定净数字口径（一致性 3）。
5. 以上均需《经验记录》**写锁**；本审计只读，未改正文，草稿已备好可直接粘贴。

---

## 五、边界声明

- **未改任何 `.py`、未改《经验记录-踩坑与发现.md》正文、未动他人产物**；仅新增本审计报告。
- 门③状态、route-B 落盘是否过门两点，以**工作树 `download.py`（未提交）**实测为准（非文档旧口径）：门③未实现、route-B 落盘已 `force=True` 过门。
- 若在本审计出具后 `download.py` 再被并发改动，第 1/2 条结论需按最新代码复核。

*核验 2026-07-02｜谷歌学术人机认证-140｜task-ee120786｜只读审计，未改正文/未改码/未动他人产物。*
