# A5 放量执行顺序 · 441 分片按 route-A / route-B 分类(证据化)

> 交付:**谷歌学术人机认证-145**(持续巡检 AI)｜2026-07-04 01:00｜承用户令「把 441 分片按 route-A/route-B 分类,出凭据到位后的放量执行顺序」
> 数据源(只读):`out/coverage.json`(368/631/36.84% @22:07:22)每条 `error` 机制 × `a5_shards/*.txt`(-157 属主产)逐条交叉。
> 边界:**纯分析,不改任何生产码/coverage/凭据**;放量命令均待用户凭据到位后执行。

---

## 〇、结论(一句话)

**先 route-A(Elsevier 312 + Springer 14 + 小众 8 = 334 条,占 76%)——HTTP 机构直链、无需浏览器、最快最稳;再 route-B(ACS 28 + Wiley 13 + T&F 3 = 44 条)——CF 站页内注入;最后 route-B+governor(RSC 46)——JA3 绑定最脆、需 -165 governor 补丁。** 每条命中与否最终取决于**你的机构对该刊是否有订阅**(441 是「A5 可寻址上限」,非「保证命中」)。

---

## 一、证据化分类(coverage.json error 机制 × 分片)

| 分片 | 条数 | 主导失败机制(证据) | route 归属 | 脆弱度 |
|---|---:|---|---|---|
| `elsevier.txt` | **312** | no-candidates 228 · landing-no-pdf 43 · cf-403 14 · paywall-403 11 · content-mismatch 9 | **route-A**(EZproxy 改写 + Crossref PII→/pdfft) | ★低 |
| `springer.txt` | 14 | landing-no-pdf 14(link.springer.com /content/pdf) | **route-A** | ★低 |
| `aip.txt` | 3 | cf-403 3 | route-A/B | ★中 |
| `csj.txt` | 3 | cf-403 3 | route-A/B | ★中 |
| `pleiades.txt` | 3 | landing-no-pdf 3 | route-A | ★低 |
| `iop.txt` / `ecs.txt` | 1+1 | landing-no-pdf | route-A | ★低 |
| `other.txt` | 14 | pdf-missing 5 · no-candidates 3 · landing 2 · cf-403 2 · http-412 1 | 视域名 | ★中 |
| `acs.txt` | **28** | **cf-403 27** + no-response 1 | **route-B cf-only**(ACS 不强绑 JA3,注入机构 Cookie 页内直下) | ★中 |
| `wiley.txt` | **13** | **cf-403 13** | **route-B cf-only** | ★中 |
| `taylorfrancis.txt` | 3 | cf-403 3 | route-B cf-only | ★中 |
| `rsc.txt` | **46** | pdf-missing 31 · cf-403 8 · **rsc-governor 5** · no-pdf 2 | **route-B + governor** | ★★高(JA3 绑定 + 限速) |

> 注:`rsc.txt` 的 31 条 "success-metadata-but-pdf-missing" 与我 -145 隔离真跑的 36 条 pdf-missing 桶**高度重叠**(免费路线已实测 0/36),坐实 RSC 归 route-B。

---

## 二、放量执行顺序(凭据到位后,从稳到脆)

### 阶段① route-A 主力(334 条,无需浏览器,先跑)

```powershell
# Elsevier(312)——A5 最直接:HTTP 层 EZproxy 改写 + 机构 Cookie 注入,Crossref PII→/pdfft
python run_all.py -f a5_shards/elsevier.txt -o out/a5_elsevier --no-resume --institutional -c 2
# Springer(14)+ 小众(aip/iop/ecs/pleiades)可合并或逐分片
python run_all.py -f a5_shards/springer.txt -o out/a5_springer --no-resume --institutional -c 2
```
- **预期**:命中率取决于机构 Elsevier/Springer 订阅覆盖;landing-no-pdf 与 no-candidates 在机构会话下应大量转直链成功。
- **护栏**:`-c 2` 低并发防 bulk 风控;先小样(如 `head -30`)验证 Cookie 生效再全量。

### 阶段② route-B CF 站(44 条,需 nodriver + 有头显示)

```powershell
# ACS(28)+ Wiley(13)+ T&F(3)——CF 软盾,注入机构 Cookie 后页内 fetch 直下
python run_all.py -f a5_shards/acs.txt   -o out/a5_acs   --no-resume --institutional --route-b cf-only -c 1
python run_all.py -f a5_shards/wiley.txt -o out/a5_wiley --no-resume --institutional --route-b cf-only -c 1
```
- **前置**:装 nodriver + 有头/xvfb 显示环境;缺依赖时 route-B 优雅 no-op(不报错但零命中)。

### 阶段③ route-B + governor 最脆(RSC 46,最后)

```powershell
# RSC(46)——JA3 绑定 CF + rsc-governor 限速;须 A5 授权会话 + route-B 页内直下叠加
python run_all.py -f a5_shards/rsc.txt -o out/a5_rsc --no-resume --institutional --route-b cf-only -c 1 --browser-pdf-wait 18
```
- **风险**:RSC 5 条已触发 `rsc-governor` 限速;`-165 governor 补丁` 若未落盘需先确认;放量务必 `-c 1` + 长 wait。

---

## 三、每阶段回写纪律(红线)

- 每分片用**独立 `-o` RUNROOT**,跑完 `python run_all.py --verify -o <RUNROOT>` 自证复现。
- **不直接写权威 `out/coverage.json`**;各分片净增经内容 QC(门①–⑤ + 开卷)复核后,由**单写者 `build_coverage --write`** 并入(承总指挥 GO + 写前 backup),与 writeback149 同纪律。
- 凭据仅在 `.ftf_institutional.local.json`(gitignore),严禁入库/贴日志。

---

## 四、诚实预期(勿过度承诺)

- 441 = **A5 可寻址上限**(miss 631 − QC 剔抓错论文 190);**非保证命中数**。真实净增 = 441 ∩(你的机构实际订阅)。
- 若机构 Elsevier+ACS+RSC+Wiley+Springer 订阅齐全,乐观可回收大部分 → 净覆盖 36.84% 有望向 50%+ 迈进(《A5即跑清单-189》放量建议);订阅不全则按覆盖折算。
- route-B(ACS/Wiley/RSC 共 87 条)受**有头显示环境**约束,无桌面/xvfb 时该 87 条无法自动跑,须人工浏览器补或配显示环境。

---

*145｜纯只读分析:441 分片 × coverage.json error 机制逐条交叉,route-A 334(Elsevier 主力)/ route-B 44(ACS/Wiley/T&F)/ route-B+governor 46(RSC);放量顺序从稳到脆。未改任何生产码/coverage/凭据;命令待凭据到位后执行。*
