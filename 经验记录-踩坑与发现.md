# 经验记录 — 踩坑 / 成功经验 / 新发现

> 把所有经验性的东西沉淀下来,供后续少走弯路。持续追加。版本 1.0 起 · 2026-07-01 · 谷歌学术人机认证-141

---

## A. 多源全文抓取的踩坑(fulltext_fetcher 实现期实测)

1. **落地页冒充 PDF(头号坑)**
   `oa_url` / `bibjson.link.url` / 部分 `best_oa_location.url` 返回的是 HTML 落地页而非 PDF。
   ✅ 对策:下载后校验 `Content-Type` 非 html + 内容前 1KB 含 `%PDF` 魔数 + 大小达标;失败记 `landing-page`/`not-pdf`。

2. **某些 host 在特定网络环境连不通会拖垮整体**
   实测本环境对 `pmc.ncbi.nlm.nih.gov` 持续 `SSLEOFError`,默认重试导致**每条输入白等 ~25s**。
   ✅ 对策:加**按域熔断器**——连续 3 次(非 SSL)连接类错误即本次运行跳过该 host(阈值原为 2,后提至 3 以减少高峰误熔)。改完总耗时 40s→7s(对命中靠前源的样本)。

3. **`--no-download` 汇报了错误的 top 候选**
   初版只在 `landing` 列表里挑"top",忽略了直链,导致 peerj 明明有 Unpaywall 8 个直链却报了个 `doi.org` 落地页。
   ✅ 对策:累积**全部**候选,按 `(is_direct, confidence)` 排序取 top。

4. **OpenAIRE 旧版 JSON 杂质极多**
   深层嵌套里递归抓 url 会抓到出版商/机构首页(如 `universityofcalifornia.edu`)这种垃圾。
   ✅ 对策:只保留含 `pdf`/`/download` 或已知 OA host 的链接,其余丢弃;OpenAIRE 仅作低优先兜底。

5. **Unpaywall 占位邮箱 422**
   `@example.com` 会被拒(422);`@example.org` 实测可用,但生产**必须填真实邮箱**。无邮箱时 Unpaywall(命中率最高的源)直接废掉。

6. **arXiv 合成 DOI 在 OpenAlex 不一定解析**
   `10.48550/arxiv.XXXX` 走 OpenAlex 单条可能拿不到元数据(title 为空)。
   ✅ 对策:arXiv 源直接用 arxiv_id 构造 `arxiv.org/pdf/{id}` 直链,不依赖元数据富化即可命中。

7. **arXiv API 限速 3s/次**
   官方要求 ≥3s/请求、单连接。对非 arXiv 论文做标题搜索会白白等 3s。
   ✅ 对策:为 `export.arxiv.org` 单独设 3s 间隔;下载模式靠"首个直链即短路",命中靠前源时根本不会触发 arXiv。

8. **作为子程序的 JSON 出口要强制 UTF-8 字节**
   `--print-json` 若用 `print()`,在 Windows 上被父进程捕获时易受控制台/区域编码影响;再叠加 PowerShell `>` 重定向会写成 **UTF-16 BOM**(`0xff 0xfe`),父程序按 utf-8 解析直接崩。
   ✅ 对策:CLI 用 `sys.stdout.buffer.write(json.encode("utf-8"))` 强制 UTF-8 字节;父程序用 `subprocess(capture_output=True)` 拿 bytes 再 `.decode("utf-8")`,绕开 shell 重定向编码。已验证可稳定 `json.loads`。

9. **Crossref link[] 是高噪声 TDM 链(候选率最高却几乎下不动)**
   实测 Crossref 候选产出率 48.6%(最高),但 `link[]` 多为出版商 text-mining 链 → 普通请求 403(30 次);是"定位到却下不动"的主因。
   ✅ 对策(2026-07-01 落地):抽 `_score_crossref_link` 纯函数给 Crossref 候选**分层降权**——基础分压到 `application/pdf=40` / `unspecified=24`(均低于真 OA 直链最低 68),对 `intended-application=text-mining/similarity-checking` 再 −12、已知订阅出版商域名(elsevier/acs/rsc/wiley/oup/sage/t&f…)再 −10,下界 5。**保留为低分候选不丢弃**(下载到落地页仍可由 landing 二次回收,实测能回收一部分)。效果:`--no-download` 定位模式 top 不再被 Crossref 兜底链抢占;下载模式源内先试更可能成功的链。`python -m fulltext_fetcher.sources.aggregators` 有 selftest 防回归。

## B. 成功经验

1. **"走开放 API 正门"是正解**:Unpaywall/OpenAlex/Europe PMC 直接给直链,命中率最高、零验证码、合规。实测多个 OA DOI 100% 拿到真实 PDF。
2. **源优先级 + 首个直链成功即短路**:兼顾高成功率(回退覆盖)与高速率(命中即停)。
3. **回退链真实有效**:peerj 从 Unpaywall 直链失败 → 自动落到 Europe PMC `?pdf=render` 成功,验证了多源冗余的价值。
4. **结构化日志驱动调试极其高效**:`attempts.jsonl` 里每源 `ok/n/ms/error` 一目了然,上面的坑 2/3/4 全是读日志当场定位的。
5. **元数据富化一次、多源复用**:OpenAlex 单条(按 DOI 免费)拿到 pmcid/arxiv_id,下游源直接命中,省请求。

## C. 新发现 / 时效性提醒(2026-07 核验)

1. **OpenAlex**:2026-02 起 polite pool(`mailto`)弃用,改 API key + 计费;但**按 DOI 取单条 singleton 永久免费**——本程序正是只用单条查询,免费。
2. **PMC**:OA Web Service(`oa.fcgi`)+ FTP 将于 **2026-08 停用**,迁 AWS S3(`pmc-oa-opendata`)。现走 Europe PMC `?pdf=render` 规避;8 月后可加 S3 直取。
3. **DOAJ** 升 v4(`/api/v4/`);**Semantic Scholar** 生产需申请 key(无 key 共享池易被节流);**CORE** 必须 key(免费,约 150 次/15 分)。
4. 详尽各源字段路径/限速/坑见 `fulltext_fetcher资料-各源接口速查.md`。

## D. 协作 / 环境经验(PChat 工作组)

1. **PChat 工具调用别手工 `\uXXXX` 转义**:直接传中文纯文本,手工 Unicode 转义会导致 `delegate_task` 等调用失败(组内多次踩到)。
2. **会话显示编号会变、sessionId 才稳定**:系统会对掉线成员的任务自动负载均衡改派,taskId 保持稳定;以 sessionId 为准。
3. **公共 backlog 池可能跨组**:`list_backlog` 里出现过别的项目任务,push 前要确认归属,避免进错池。
4. **Windows PowerShell 控制台中文 GBK 乱码**:仅显示问题,落盘文件均 UTF-8,功能不受影响。
5. **CLI help / 日志里的 emoji 会在 GBK 控制台直接崩溃(不是乱码,是崩)**:argparse `--help` 打印含 `⚠️`(U+26A0)的帮助串时,Windows GBK 控制台报 `UnicodeEncodeError: 'gbk' codec can't encode character '\u26a0'` 而**整条命令失败**(`--selftest` 不打印 help,故长期未暴露;真机跑 `--help` 才发现)。⚠ 注意区别:中文本身 GBK 可编码(不崩、正常显示),**emoji 才是元凶**。✅ 对策:CLI 帮助串 / 日志告警只用 GBK 可编码字符,把 `⚠️` 换成「注意:」。已修 `cli.py`(help+warning)与 `scholar/cli.py`(help);`python -m fulltext_fetcher --help` / `... scholar --help` 恢复正常(退出 0)。(模块 docstring 里的 emoji 无妨——不会经控制台编码。)

## E. 规模化验证实测(80 真实 DOI,paywall 为主语料,2026-07-01)

数据来自 `title_doi_500_batch_6.xlsx`(Elsevier 240 / ACS 105 / RSC 52 / Springer 29 / Wiley 23 / MDPI 15…),取前 80 条 DOI 用 fulltext_fetcher 实跑(并发10、含下载)。

**结果:成功 9/80 = 11.2%**(命中源:Unpaywall 5、Europe PMC 2、OpenAIRE 1、S2 1);平均 17.8s/条;总 151.5s。

**各源候选命中率 vs 实际下载**:
| 源 | 产出候选率 | 说明 |
|---|---|---|
| Crossref | 48.6%(最高) | 但几乎都下不动:`link[]` 多为出版商 TDM 链 → **403**(30 次)。高噪声、低产出 |
| Unpaywall | 21.2% | 真正贡献 5 篇;部分是 bronze 落地页/403(10 次) |
| OpenAlex | 16.0% | — |
| Semantic Scholar | 12.3% | 贡献 1 篇 |
| Europe PMC | 2.7% | 化学语料生物医学占比低 |
| CORE | 0 | **未配 key 被跳过**(本可能补绿色 OA) |
| DOAJ/HAL/Zenodo/PMC | 0 | 该语料几乎无命中 |

**关键发现**:
1. **47/80 "定位到候选" 但仅 9 下成**——38 篇定位到却下不动,绝大多数是**出版商 403(付费墙)**,不是程序缺陷。订阅型化学语料的免费全文天花板本就低。
2. **11% 是诚实的免费源覆盖率**:Elsevier/ACS/RSC 多数无免费版,其 Crossref 链是付费 TDM。要更高只能上**角度4 商业服务**或机构订阅(正是七角度研究的结论)。
3. **可提升项**:① 配免费 **CORE key**(36M+ 全文,补绿色 OA);② 实现 **HTML 落地页内 PDF 解析**(可回收一部分 ct=text/html 的落地页);③ 真实机构邮箱可小幅提升 Unpaywall;④ Crossref 候选分层降权(高噪声)——✅ 已落地(见 A.9)。
4. **诚实边界**:付费墙内全文任何免费手段都拿不到;本程序对**开放获取/绿色 OA**的论文命中良好,对纯订阅论文返回"无免费版"是正确行为。
5. **HTML 落地页解析已落地并验证有效**:`landing.py`(144 写,提取 `citation_pdf_url`/内嵌 `.pdf` 链接)集成进 `download.py`(拿到 HTML 时抠出内嵌 PDF 再下一层、`%PDF` 二次校验)。同一 80 条样本 **9→11(11.2%→13.8%,相对 +22%)**,且 **Crossref 从 0 变 2**(落地页里的 citation_pdf_url 被成功回收)。结论:landing 解析能稳定回收一部分"定位到却返回 HTML"的失败;对硬 403 无效(那是付费墙)。

## F. 谷歌学术爬虫子系统 fulltext_fetcher/scholar/(2026-07-01 构建 + 真实验收)

> 目标:输入标题/DOI → 避免或自动过人机验证 → 抓 Scholar 元数据 + 可及 PDF → 文件名标准化 → 批量。经「研究(R1 反爬栈 / R2 Scholar抓取·CAPTCHA·下载)→ 架构与选型(冻结接口契约)→ 14 模块并行构建」完成,离线 selftest 全绿,并做了真实 Scholar 联网验收。详见 `谷歌学术爬虫-架构与选型.md` / `谷歌学术爬虫-使用说明.md`。

**架构要点**
- 三层+两翼反爬:`curl_cffi`(L1 TLS 指纹 impersonate)→ `nodriver`(L3 无头浏览器);翼A 住宅代理、翼B 限速/打码。`mode=auto`(有 SerpApi key 走合规商业,否则自建)。
- 大量复用父包(download/landing/render_fetch/sources/resolve/report/logsetup),不重造轮子。
- 默认最安全:代理/打码/Sci-Hub/OA兜底 **全关**;Scholar 抓取串行 + 强限速(页间 45–90s、命中风控冷却 900s)。

**踩坑 / 新发现**
1. **数据中心 IP 直连 Scholar 必被 403**(实测):真跑 "Attention is all you need" → 即便 curl_cffi(impersonate=chrome)也**即刻 403**。结论:TLS 指纹伪装过不了 **IP 信誉**这关;真实抓取必须住宅代理或直接上 SerpApi(合规)——印证 R1/R2 调研。
2. **403 未被判「被拦」的 bug(靠 run+读日志 当场抓到)**:`fetcher._is_blocked` 初版只认 429/503、漏了 403,而 `serp.detect_captcha` 是认 403 的——不一致导致 403 被当普通失败、跳过 冷却/换代理/升级浏览器。✅ 已修:`_is_blocked` 统一走 `detect_captcha(html,url,status)`;修后 403→blocked=True + 触发 block 事件 + 冷却。**这就是「改程序→跑→读日志→再改」闭环的价值**。
3. **离线 mock selftest 验的是「编排正确性」,不是「真实页面选择器准确性」**:`serp.py` 用合成 SERP HTML 自测(SERP_OK),但真实 Scholar 现行 HTML 的选择器(`.gs_r/.gs_rt/.gs_a/.gs_fl/.gs_or_ggsm`)**未在真实页面校验过**——因为被 403 挡着、从没拿到真实 SERP。这是**唯一未闭合、且属环境物理边界(非代码缺陷)**的差距。
4. **对策:原始 SERP HTML 快照**(ARCH §6,本轮补齐 pipeline 实现):每次取回把原始页面落 `out_scholar/serp/<idx>_<engine>_<status>.html`。在**非被拦环境**跑一次即存下真实页面,据此离线校对/调优 `serp.py` 选择器,无需反复联网。
5. **OA 兜底默认关**(用户拍板):北极星是「Scholar 所能达到的」,闭源/付费墙用户用别的渠道;故 `config.oa_fallback` 默认 False,`--oa-fallback` 显式开启。

**成功经验 / 协作**
1. **研究先行 + 先冻契约再并行**:R1/R2 调研 → 综合《架构与选型》**冻结 models/config 接口契约(P0)** → 多成员并行实现 P1–P4,几乎无接口漂移。**教训**:个别模块(serp/naming/query)在 ARCH 前抢先起步、用了 dict 接口,后需回头对齐 models,略有返工——**契约先冻结再并行**能免掉。
2. **可选依赖一律函数内延迟导入 + 统一「不可用信封」**`{available:False, reason}`:curl_cffi/nodriver/patchright/2captcha/capsolver 全不进强制依赖,缺失即优雅降级。
3. **一键回归覆盖全项目**:`run_all_selftests.py` 现含 36 项(compileall + 35 模块 selftest,其中 scholar 子系统 11 项),`python run_all_selftests.py` 一条命令验全项目(PASS=36 / FAIL=0 / SKIP=0)。

**入口**:`python -m fulltext_fetcher.scholar "标题或DOI" [-f 列表] [--serpapi-key … | --proxy]`;产物落 `out_scholar/`。

## G. 方向拍板:暂缓付费凭据、全力免费方法(2026-07-01,用户拍板 + 记忆)

> 用户明确:**「本项目先不考虑需 SerpApi key / 住宅代理,这点纳入记忆,SerpApi/代理纳入后期待办。」** 并追问:**「搜索引擎你不用用么?无头浏览器什么的过人机验证。有很多方法探索了么?你有分工给其他成员任务么?」**

**记忆锚点(后续所有决策遵循)**
1. **SerpApi key / 住宅代理 = 后期待办,当前一律不依赖**。凡"需付费凭据才能验收"的路(Scholar 真实抓取的商业/代理通路)暂缓,不作为当前交付门槛。
2. **当前主攻「免费」拿 PDF**:① 通用搜索引擎(DuckDuckGo HTML / Bing 优先,Google 难)按 标题/DOI 找作者自存稿·机构库·ResearchGate·预印本;② 无头浏览器隐身(patchright/nodriver,**本地 IP、无代理**)驱动搜索引擎/落地页过人机验证;③ FlareSolverr(免费自托管)绕 Cloudflare;④ oa.works/OpenAccess Button 等免费聚合 API。
3. **困难任务先全面检索开源**(时效/维护/热度/许可/功能)再决定直采或吸收——已就"免费搜索找 PDF""免费无头浏览器过验证"各派 1 份调研文档。
4. **总指挥必须把活拆开、并行派给成员**(用户特别强调"你有分工给其他成员任务么")——本轮已 push_backlog 6 项(websearch / browser_search / oa_button / flaresolverr + 2 调研),各自**新文件、离线 selftest、禁改共享文件**,集成由总指挥统一做(本仓已 git,可 diff/回退)。

**batch_06 基线实跑(500 条,进行中)**:`python -m fulltext_fetcher -f …title_doi_500_batch_6.xlsx -o out/batch6 -c 8`。数据以 Wiley/Elsevier/ACS 订阅刊为主;无邮箱→Unpaywall 被禁(422)。此基线用于**对照新免费方法上线后的命中率提升**(改程序→重跑 miss 项 `--retry-failed`→读日志比对)。

## H. 免费方法大突破 + 无人值守收尾 + 前沿阻塞(2026-07-01 下午,实测·证据口径)

### H.1 【突破】websearch + publisher_oa 把免费公开边界成功率从 10.8% 拉到 ~82%
- **口径必须讲清(否则数字自相矛盾)——真相源永远以各 `out/<batch>/summary.json` 为准**:
  - 早期"纯 OA-API 聚合"口径(仅 unpaywall/openalex/s2/crossref…,**无 websearch**):`val500_out/summary.json` 处理 498、成功 54 = **10.8%**。
  - 上线 **websearch**(DuckDuckGo/Bing 找作者自存稿·机构库·预印本)+ **publisher_oa**(OA 出版商 DOI→PDF 直构)后,**同一 500 DOI 语料**:`out/batch6/summary.json` 本轮 success=336 + skipped_resume=74(前轮已成功续跑跳过)= **累计 410/500 = 82.0%**;去重合并另 +2 = **412/500 = 82.4%**(见 `最终交付清单.md`)。
  - by_source(batch6):**websearch 260**、publisher_oa:acs-authorchoice 52、unpaywall 11、europe_pmc 7、s2 2、crossref 2 → **websearch 单源约占成功 72%**(本轮 260/336≈77%),是拉高命中的绝对主力。
- **结论**:**不依赖 SerpApi key / 住宅代理**,仅靠免费搜索引擎兜底 + OA 出版商直构,就能把"公开网络可及"的免费 PDF 命中率从个位数拉到 ~82%(订阅刊化学语料);付费墙内纯订阅全文仍是物理边界(需机构订阅 A5)。
- ⚠️ **口径提醒(给读报告的人)**:82% 是"累计覆盖口径(含续跑已成功)";按"单轮 processed"口径 batch6=336/425=79.1%、batch7=108/213=50.7%。三者不矛盾,是口径差异。
- ⚠️ **代价**:websearch 每条 50–90s(`out/batch7/run.log` 实测),吞吐低、依赖第三方搜索引擎稳定性。

### H.2 【坑+修复】批处理收尾被单条 straggler 挂起 → summary 不落盘(已加看门狗)
- **坑**:并发批跑收尾时,若个别输入的 future 卡在阻塞 IO(如 `10.1039/d1ta08016d` 在 websearch/落地页阶段长时间无返回),`ThreadPoolExecutor` 收尾整体挂起 → `summary.json` 迟迟不落盘,无人值守跑批"看起来死了"。
- ✅ **修复(已落地,证据 `fulltext_fetcher/pipeline.py`)**:尾部卡死看门狗——
  - `_straggler_timeout()`(L228):无进展阈值秒 = `max(180, per-request 超时×8)`(或 `cfg.item_timeout`)。
  - 主循环(L264–282)`wait(pending, timeout=straggler_timeout, return_when=FIRST_COMPLETED)`;一个窗口内**零完成**→ 判卡死,`log.error("尾部 N 条…判定卡死(straggler)")`,`cancel()` 未启动项 + `_record_straggler()`(L242)把卡死项记为 `error="straggler-timeout"` 失败结果并落盘,`break` 停等;`finally: ex.shutdown(wait=False, cancel_futures=True)` 不等卡死线程。
  - 效果:**绝不因单条 straggler 让整体挂起**,summary/metadata/results.csv/report 收尾必写;straggler 命中可重试提示,`--retry-failed` 续跑会自动重试该条。
- **补丁(149 修复,task-97a459c2):CF/浏览器兜底感知的超时放大(≥900s)**。原阈值 `max(180, per-request×8)` 在**开启 CF/浏览器兜底重活时会误杀慢但合法的尾部条目**:FlareSolverr 解 CF、有头渲染等待、多候选逐个下载/重试会让单条合法耗时涨到**数分钟**,被旧阈值当成 straggler 记假失败、压低回收率。已改 `_straggler_timeout()`(`pipeline.py` L363–386):`_browser_fallback_active()`(检测 `use_flaresolverr`/`flaresolverr_url`/`FLARESOLVERR_URL` 环境变量/`render_fallback`/`browser_pdf_download`/`browser_search` 源任一)为真时,阈值放大为 `max(900, base×30, (fs_timeout+browser_wait)×6)`;**默认(无 CF/浏览器兜底)行为不变**。selftest 已锁两分支(默认 240s、CF 感知 ≥900s),`run_all_selftests.py` 全绿。
- 备注:`out/batch7` 的 `recovered=true`(两并发写同目录互撞、summary 由 metadata 重建)与 straggler 是相邻但不同的收尾问题;**并发写目录保护(out_dir 文件锁)已由 -141 落实,详见 I.1**。

### H.3 【前沿阻塞】Cloudflare JS 质询 / SSL 证书链 / 死主机(免费手段当前天花板)
- **Cloudflare Managed Challenge(http-403/503)**:Elsevier/RSC(如 `pubs.rsc.org`)整站 JS 质询,HTTP 与 curl_cffi 都过不了(`out/batch7/summary.json` miss_reason 实见 `download-failed:cloudflare-challenge(http-403)`)。
  - ✅ 已精确识别:`download.py._is_cloudflare_challenge()`(L73,认 403/503 + Server=cloudflare + `just a moment`/`__cf_chl` 特征,力求不误伤普通 403),失败串 `cloudflare-challenge(http-{code})`(L322),据此**跳过对同域无谓的 curl_cffi/publisher 重试**。
  - ✅ 兜底方向(可选、默认关):`_flaresolverr_fallback()`(L540)用 FlareSolverr(免费自托管无头浏览器)解 JS 质询拿 `cf_clearance` 再重下;需 `cfg.use_flaresolverr` 或 `FLARESOLVERR_URL`,未启用零副作用。
- **SSL 证书链失败**:部分学术站(如 `chem.pku.edu.cn`)证书链不完整/过期 → `SSLError`,与 A.2 `pmc SSLEOFError` 同类。
  - ✅ 对策方向:**按已知学术域定向证书回退,绝不全局关 TLS 校验**(全局关引入中间人风险);沿用 A.2 按域熔断避免拖垮整体。
- **死主机 / CDN 502/504**:RSC suppdata 502、sci-hub.box 504(注:**sci-hub 已由总指挥裁定越界**,-157 加影子库域名黑名单,此处仅记现象)。
- **总结**:免费手段当前主矛盾 = "定位到候选却下不动"(Cloudflare 质询 + 出版商 403 + 死链),而非定位能力;FlareSolverr / 证书定向回退是下一步收益点,机构订阅仍是唯一破付费墙根本途径。

## I. 并发写同一 out/ 撞车(batch7 实锤)+ out_dir 写锁(2026-07-02)

### I.1 【坑+修复】两次并发跑批写同一 out/ 互相打断 → summary 丢失/被重建
- **现象**:两次跑批同时写 `out/batch7`,在 `summary.json` 落盘前互相打断,最终 summary 只能事后由 `metadata.jsonl` 重建才补回。`out/batch7/summary.json` 实锤字段:
  - `"recovered": true`
  - `"recover_note": "Rebuilt from metadata.jsonl after two concurrent runs on out/batch7 interrupted each other before summary.json was written; dedup per raw_input with success-wins; elapsed_sec spans full wall clock of both runs."`
  - 连带失真:`elapsed_sec=2885.5` 跨两次运行墙钟(单跑口径失真)、`by_source`/`miss_reasons` 需按 `raw_input` 去重(success-wins)后才可信。
- **根因**:`Pipeline` 对 `out_dir` **无任何互斥**。两进程各自 append `metadata.jsonl`(行交错)、最后各写一份 `summary.json`(后写覆盖先写、被打断则干脆不写),`attempts.jsonl` 亦交错;断点续跑 `_load_done` 还可能读到对方半截写入而误判。本质是"多写者共享同一可变输出目录"缺锁。相关但不同层面的收尾坑见 H.2(单进程内单条 straggler 挂起);并发撞车是**多进程**层面。
- **规避 / 修复(已落地,证据 `fulltext_fetcher/pipeline.py`)**:
  1. **首选:每个跑批用唯一 `-o` 输出目录**(如 `out/batch4_p5`),这也是分片并行的既定约定——绝不多进程共写一个 out/。
  2. **兜底:out_dir 文件锁(本次新增)**。`Pipeline.__init__` 早于打开任何产物即 `_acquire_out_lock()`:在 `out_dir/.lock` 上用 **OS 建议锁**(Windows `msvcrt` / POSIX `fcntl`,仅标准库、只锁 byte 0 作锁位、pid+时间写其后便于冲突方读出属主)独占该目录;冲突(锁被另一存活进程持有)→ 抛 `RuntimeError` 让第二个并发跑批**快速失败**、不去写坏第一个。`run()` 收尾 `_release_out_lock()` 释放(关 fd 后 Windows 才能删该目录),`__del__` 兜底防泄漏。进程崩溃/退出时锁由 OS **自动释放**、不留死锁残留(优于"锁文件存在即占用");锁机制自身任何异常一律**降级放行**(绝不因加锁失败阻断正常单跑),两平台锁实现都不可用时亦降级不加锁。
  3. **selftest 固化**:`pipeline.py` 内置 selftest 新增两断言——持锁时同目录第二次获锁必为 `False`、`run()` 收尾后可再次获锁(证明已释放);`python run_all_selftests.py` 全绿(PASS=40 / FAIL=0 / SKIP=0)。
- **口径提醒**:batch7 的 `recovered=true` 数字已是"重建去重"口径,读报告以此为准;加锁后不应再出现同目录并发,若仍见 `recovered/recover_note`,说明有人绕过了唯一 `-o` 约定。

## J. browser_search(无头浏览器驱动搜索引擎)对 Elsevier still-miss 的探针实测(2026-07-02,143)

**目的**:验证 H.3 结论的一个具体分支——对 batch6 残差里的 **Elsevier A 类(10.1016 / ScienceDirect 渲染墙)**、且 **batch6 里 websearch 已找到候选但下载失败** 的那部分 miss,改用 `browser_search`(无头隐身浏览器渲染 Bing/DDG 结果页找 PDF)能否把它们捞回、能否拿到 `%PDF`。

**方法(注意命令口径)**:输入 `subset_elsevier_probe_149.txt`(5 条:ijhydene.2019.03.015 / supflu.2016.11.013 / apsusc.2022.154379 / ijhydene.2015.01.031 / apcatb.2023.122531)。
- ⚠️ **派单里的 `--browser-search` flag 不存在**(`cli.py` 无此参数);`browser_search` 源**只能经 `--sources browser_search` 显式开启**(它刻意不进 `DEFAULT_SOURCE_ORDER`)。实跑实际命令即:`python -m fulltext_fetcher -f subset_elsevier_probe_149.txt --sources browser_search --email test@test.edu -o out/browser_elsevier_probe_149`(并发=1,`run.log` 实证"源=browser_search")。
- 证据目录 `out/browser_elsevier_probe_149/`(该目录另含前一组 jcou/rser/jpowsour/supflu2006,两轮合计 10 条记录)。

**结果:0/5(两轮合计 0/10),无一命中**。`summary.json` success=0 / miss=5;`run.log`+`attempts.jsonl` 每条 browser_search `ok=false, n=0`,result `error="no-candidates"`、browser_search 内部 `reason=no-pdf-candidates`;单条 ~27–39s(5 条 ~2.5 min)。

**关键分析(为什么捞不回)**:
1. **不是被拦、也不是浏览器起不来**:reason 一律 `no-pdf-candidates`(而非 `search-engine-blocked` / `browser-unavailable`)——无头隐身浏览器**渲染 Bing 成功、没吃验证码**,问题在**召回为 0**,不在访问。
2. **对照 batch6 原始失败模式(`out/batch6/attempts.jsonl` 实锤)**:这 5 条在 batch6 里 websearch **确有候选**(n=3~8),但候选几乎全是 **ResearchGate 落地页 → HTTP 403**(数据中心 IP / 需登录),外加 ScienceDirect AM(`/article/am/pii/…`)→ 403、一条 scispace `.pdf` → 405、doi.org 落地页 → `landing-no-embedded-pdf`。即这一桶瓶颈是 **"定位到候选却下不动"(下载环 403)**,与 H.3 总结完全一致。
3. **browser_search 在召回上反而更差**:它默认只打 `"DOI" filetype:pdf` 与 `"DOI"` 两式、主攻 Bing;对这些冷门催化论文 Bing 的 DOI 检索本就召回低,`filetype:pdf` 又把 ResearchGate(非 .pdf)整类排除 → 候选 0。
4. **架构性要害(决定它帮不上这一桶)**:`browser_search` **只渲染"搜索引擎结果页"抽候选 URL,不渲染/不下载候选"落地页"**(下载仍走普通 HTTP 下载器)。所以哪怕它抽到 ResearchGate 链,下载环照样 403——**用浏览器驱动"搜索引擎"救不了一堵长在"下载/落地页"上的墙**。

**结论 / 拍板**:
- 对 Elsevier ScienceDirect 渲染墙这一桶,`browser_search`(现设计)**净增 0、且每条多花 ~30s**,应**继续留在 `DEFAULT_SOURCE_ORDER` 之外**(现状正确)。**%PDF**:0 候选 → 0 次下载 → 从未取到 `%PDF`。
- 想真正回收这一桶,ROI 排序:① **在浏览器里直接渲染+下载"落地页"**(ResearchGate / ScienceDirect AM),但需登录会话/住宅 IP —— 属 G 已拍板的"后期待办";② 机构订阅(A5)是破纯付费墙的唯一干净途径;③ 若仍要用 browser_search,须**去掉 `filetype:pdf`、补 DDG/Scholar 检索式、并配套"浏览器落地页下载"**,否则纯是成本。
- 协作备注:本探针 143 与 148 被重复派单;probe 产物已存在,故**未重复重跑**(规避 141 的 out/ 写锁),结论在此**加锁单份记录**,避免两人重复写。

## K. batch6「非Elsevier长尾」9 条回收实测:实为 Cloudflare 桶、非 arXiv/wayback 桶(2026-07-02,143)

**背景**:『下载层A类修复』分桶回收;176 判「非 Elsevier 长尾 9 条」为 arXiv/wayback/自存稿桶、约 45% 命中、无需 Docker。143 实跑核验(`out/recover_b6_tail/`,全默认免费源,`--email test@test.edu -c 3`)。(注:该任务票据中途被 hub 改派离开 143,但 143 已完成实跑与分析,产物与结论如下,供接手者与总指挥直接引用、勿重复重跑。)

**结果:0/9**,且**候选并不缺**——crossref/publisher_oa/websearch 都产候选,9 条全部**卡在下载环**:8 条 `cloudflare-challenge(http-403)`、1 条 IOP `/pdf` 返 HTML 壳(`landing-no-embedded-pdf`,~14KB)。**arXiv**:9 条 resolve 全 `arxiv:null`(无预印本);**wayback**:0/9 且单条 20–134s 极拖时间。

**桶画像更正(要点)**:这是 **Cloudflare 桶**,不是 arXiv/wayback 桶。命中墙的出版商 = **AIP(pubs.aip.org)、Wiley(onlinelibrary/chemistry-europe/aces)、Oxford Academic(academic.oup.com)、Taylor & Francis(tandfonline.com)**;更关键——**连自存稿/预印本兜底也进了 Cloudflare**:本次实见 **ResearchGate、ChemRxiv(chemrxiv.org)** 返 `cloudflare-challenge(403)`。H.3「Cloudflare 是主矛盾」由此扩大:免费逃生口(RG/ChemRxiv)亦被 Cloudflare 封。

**杠杆更正**:此桶真实回收杠杆是 **FlareSolverr(解 Cloudflare JS 质询,需 Docker)**,而非 arXiv/wayback——与派单「无需 Docker、~45%」前提相反。候选齐全、只差过 Cloudflare,FlareSolverr 上线后 8 条(除下述不完整 DOI)大概率可观回收。`download.py._render_fallback` / `browser_pdf_download` 虽存在但**未经 CLI 暴露**(需 cfg 标志 + 浏览器/显示环境),本次未用。

**数据坑**:`10.1002/1099-0739(200012)14:12` 为**截断/不完整 DOI**(resolve `via=none`、title=null),websearch 还据残串误配到无关 ChemRxiv/Wiley 文——已按派单**标记跳过**;此类老式带括号 DOI 入库前需补全校验。

## L. 【重大】websearch 主力源系统性抓「错论文」假阳 + 双法定案 + 净覆盖率修正(2026-07-02,150 沉淀;审计 151/153/140 + coverage 147)

> **一句话**:下载校验只看 `%PDF`+体积、**无内容/DOI/标题比对**,导致主力源 **websearch** 系统性把「另一篇论文」当成功记账。内容级复扫实锤:websearch 的「success」里过半是**错论文**;剔错后**真实唯一正确论文净覆盖 = 448/999 ≈ 44.8%**(权威去重口径),而非逐批求和的 866/1213≈71.4%(后者含错论文假成功 + 跨批重复,虚高);**websearch 主线内容可信率点估 ≈ 33%**。DOI-keyed 源(unpaywall/openalex/europe_pmc/crossref/s2/publisher_oa)**0% 假阳、干净**。

### L.1 假阳规模(证据:`tools/qc_content_match.py` → `out/qc_content_report.{json,md}`,生成 2026-07-02)
- 纯读 `out/*/metadata.jsonl` 里 `success 且 source_used 含 websearch` 的记录,抽 PDF 首2页文本+元数据 title,与该记录的 resolved title/DOI 模糊比对(rapidfuzz;阈值 match≥70 / mismatch<50 / 边界带±8),五档 verdict。
- **扫描 645 条 websearch success**:match **166** / mismatch(疑似错论文) **361** / uncertain **88** / scanned(抽不出正文) **29** / unreadable **1**。
- **假阳率 A(mismatch / 可判定 527)= 68.5%**;**假阳率 B(mismatch / 全部 645)= 56.0%**(保守下界)。即**可判定的 websearch 命中里约 2/3 是错论文**。
- **uncertain 带不是「误杀的真命中」,而是「漏网的假阳」**:-153 对 uncertain/border 池随机抽 40 条人校(`out/qc_uncertain_sample_verify.{csv,md}`,seed=42),**match 0 / mismatch 40 → 100% 错论文**(Wilson 95% CI 下界 91.2%)。故 uncertain 应计入不可信,主线可信率从区间 33–43% **收窄为点估 ≈ 33%**(区间下沿)。

### L.2 四类系统性错误模式(证据:同报告 `systematic_patterns` + `out/qc_merge_*`)
websearch 是「关键词/DOI 串搜索引擎 → 取首个 `%PDF`」,故错配呈强规律:
- **① junk_domain(垃圾域,40 条)**:同一下载域被塞给 ≥3 种不同 DOI 前缀且 0 命中。实测 Top:`escholarship.org`(13 条/4 前缀)、`pubs.acs.org`(11/4)、`patentimages.storage.googleapis.com`(8/4)、`govinfo.gov`、`tandfonline.com`。另 `frontiersin.org` 是典型通配域(同一 public-pages PDF 被配给 `10.1016/j.cej…`、`10.1039/b915667d` 等多个不相干 DOI,见 `qc_merge_151match_url_conflict.csv`)。
- **② same_pub_wrong_doi(同社近似 DOI 错论文,155 条)**:正文含同前缀异后缀 DOI(如把某 nature/RSC PDF 当成同社另一篇的答案)。
- **③ cross_pub_wrong_doi(跨社错论文,193 条)**:正文含**他社**前缀 DOI(物理领域 DOI 配到 PNAS/NEJM/皮肤病学等完全跨领域文)。
- **④ future_year(未来年份,92 条)**:served(URL/正文 DOI)年份 > 目标 DOI 年份(如 2025/2026 的 DOI 配给旧文),**物理不可能**、铁证错配。
- 经典实例:`10.1002/cssc.201601217`(ChemSusChem)落盘成 `jaad.org` 皮肤病学论文;两个不同 Elsevier DOI 被 openaire 指到**同一筑波大学仓库 PDF**(同 MD5,见 `检索成果-标题输入路径-端到端实测与错文件揭示.md`)。

### L.3 双法定案(交集→并集返工,140/151 落地;precision 优先)
单一方法各有盲区,**取并集最全**:
- **法A｜151 内容标题法**(`tools/qc_content_match.py`,pypdf 抽正文比标题/DOI):mismatch 精确率 ~100%,能揪**同社错论文(publisher 62)**与**仓库托管错论文(repository 129)**——这两桶正是审计域法判不了的(域相符 or 域是中立仓库)。
- **法B｜审计 URL 域 × DOI 前缀 / 嵌入 DOI 法**:靠「下载域 vs DOI 归属出版商不符」揪**跨社**铁证,是下限。
- **产物(供 `build_coverage` 消费,`out/`)**:`qc_merge_highconf_wrong.csv`(**54** 条两法都判错的硬黑)、`qc_merge_union_wrong.csv`(**391** 条并集)、`qc_merge_151match_url_conflict.csv`(**16** 条 151 判 match 但下载域与 DOI 冲突的**假匹配**,需人核)、`qc_rejected_manifest.csv`(**391** 条已由 cleanup 物理移入各批 `rejected/` 的地面真相)。
- ⚠️ **口径教训**:早期用「两法**交集**」会漏掉大量单法命中的错论文(交集太小);**140 已返工为并集**(union ⊇ hard),并补「同社错论文」selftest 锁定正确性。

### L.4 净覆盖率修正(权威口径,证据:`tools/build_coverage.py` → `out/coverage.json`)
- `build_coverage` 现**黑名单感知**:去重 + PDF 落盘实证后,再消费 `qc_merge_*_wrong.csv` 把命中者从 success 剔为 miss(进 `still_missing.txt`)。
- **净成功 = 448 / 999 唯一 DOI ≈ 44.8%**(`success_before_qc=460` → 剔 12 仍在盘的错论文 → 448;其余错论文早被 cleanup 移出 `pdfs/`,已不计落盘成功)。
- **对比虚高口径**:core 三主语料批**逐批 metadata 求和 = 866/1212 ≈ 71.4%**(审计基线,但 metadata 不剔错论文、且含 batch7↔batch6 跨批重复);落盘实证求和已随 cleanup 移错论文 PDF 降到 484。**读报告认净 44.8%,不认 71.4%**。
- 主力成功源(净、去重后):websearch 217 / unpaywall 52 / publisher_oa:acs-authorchoice 47 / semantic_scholar 37 / openalex 31 / europe_pmc 22——websearch 剔错后仍是第一,但已非「7 成」的假象。

### L.5 根因与修复(已落地:下载即校的内容 QC 门,140)
- **根因**:`download.py` 记 success 只校 `%PDF` 魔数 + 体积,**从不比对下到的 PDF 是不是这个 DOI/标题的文**;websearch 候选质量参差(搜索引擎首个 PDF),故系统性假阳。DOI-keyed 源因链接由 DOI 权威解析,天然不会错配,故 0 假阳。
- **修复(已落地,证据 `fulltext_fetcher/download.py::_content_qc_gate`)**:非 DOI-keyed 来源(websearch/wayback/browser_search 及任何经 `+landing` 解析者)在记 success 前过**双门并集**:
  - 硬拒 = 门①**内容标题明确不符**(能抽出正文且标题分 < 50)**OR** 门②**跨社第二信号**(URL 嵌异 DOI / 正文首 1200 字异社 DOI / URL host 跨社);任一命中 → `error=content-mismatch(...)`,不落盘。
  - 期望 DOI 出现在正文/URL → match 压过两门;**uncertain[50,70) / 扫描件 / 出版商未知 → 放行打标 `qc_uncertain`,绝不误杀 undecidable**;DOI-keyed 源(unpaywall/openalex/crossref/S2…)一律豁免;缺 pypdf/rapidfuzz 自动降级放行;总开关 `cfg.content_qc`(默认开)可整体回退。
  - 跨社按**出版商标签**判而非裸 DOI 前缀(Wiley 10.1002/10.1111 同社多前缀不算跨社)。
- **⚠️ 交集→并集的判定教训(与 L.3 呼应,代价:一轮返工)**:第一轮实现曾疑"标题法过判"而取**交集**(标题不符 AND 跨社信号)。审计逐条交叉验证推翻:content-mismatch(标题分<50)≈100% 准,且 **189 条同域错论文(publisher 61 + repository 128:Springer DOI→另一篇 Springer 文、或仓库托管他篇)跨社信号根本不触发**——交集会整批放行。定稿**并集**,并新增「同社错论文」selftest(Springer→Springer 仅标题不符、无跨社信号 → 必判 mismatch;先断言第二信号不触发,证明交集在此必漏)锁死不回退。
- **负样本 fixture**:`out/qc_uncertain_sample_verify.csv` 的 40 条(含生物学/索引页/未来年份/无关配合物)可作**内容校验回归的负样本**——回收管线若把这些 DOI 再判 success 即回归失败。

## M. 复盘:自动 QC 的 match 也不可全信——「同题非全文」四类新假阳模式(2026-07-02,142 实测;样本 out/recover_b4_cf)

> **一句话**:对 recover_b4_cf(batch4 CF 桶 full-80 回收)的 13 条自动判 **match** 逐条人工开 PDF,**仅 5 条是真全文**;标题匹配法(甚至 DOI-in-text 强信号)对下面四类假阳**零免疫**,因为这些文件里就含正确标题/DOI。净口径:该目录 34 条 unique success → 真全文 net_gain 仅 **5** 条。

### M.1 四类新假阳模式(全部有实锤,证据在 `out/recover_b4_cf_qc.md`『142 深核补记 v2』)
- **① citation_report(引用报告)**:`exaly.com/paper-pdf/*/citation-report.pdf` 被 websearch 当正文抓回——文件首页印着**正确标题+正确 DOI**(骗过 doi_in_text),正文全是引用列表。实锤:10.1021/acs.chemrev.7b00776(242页)、10.1016/j.jcou.2017.08.019。**exaly.com 应入垃圾域黑名单**。
- **② poster(会议海报)**:zenodo 托管的同项目同题海报(meta_title=`Free research poster template`,1页)。实锤:10.1016/j.jcou.2018.01.028。可用「页数=1 + poster 关键词」拦。
- **③ si_only(正确论文的 SI 附件,非正文)**:**publisher_oa:acs-authorchoice 源在本目录 5/5 全部落盘 SI**(pdf_url 形如 `pubs.acs.org/doi/pdf/10.1021/…` 却服务 SI 文件;首页 `S-1/Supporting Information` 开头)。实锤:acsanm.1c00959 / acscatal.4c04824 / acscatal.8b02371 / nl401568x / acscatal.0c01253。**该源需修复或在记 success 前加 SI 判识**(首页含 "Supporting Information" 且无正文版式 → 拒或降级)。
- **④ same_title_other_journal(同作者同题他刊双发)**:期望 ChemCatChem 10.1002/cctc.200900261,落盘为 Beilstein JOC 10.3762/bjoc.7.159(同作者同题综述)。标题 100% 匹配,只有比对**文内 DOI ≠ 期望 DOI** 才能识破。
- 另:待人工的 scanned 两条开卷后也全是错件(copernicus 图1 EPS、Nature Catal SI)——**scanned ≠ 无害**,URL 域×DOI 前缀比对可判。

### M.2 CF 回收路线的净收益(证据:summary.json + 142 复核)
- full-80 跑 73 条 processed,summary 报 success 27(37%);QC 三层甄别后**真全文仅 3 条**(ep.670220410 / apcata.2015.10.041 / acs.iecr.5c04764),smoke10 再 +2(apcatb.2021.120319 / c1gc15503b)。**真·净增益率 ≈ 3/73 ≈ 4%**,与「CF 免费路线到顶」结论互证。
- **flaresolverr_recovered 真救回 = 0**:27 条 success 无一来自 FS 直破 CF;全部是绿 OA(ACS authorchoice/unpaywall/crossref)与 websearch 碰运气。
- 净口径影响:142 把 8 条假 match 补入 `qc_uncertain_reject.csv`(90→98 唯一 DOI)后,全局净成功 **379 → 371**(-8,37.1%);此前 12 条 mismatch/scanned 已由 147/153 协同补入(78→90)。
- **工具边界**:`qc_move_rejected.py` 只消费 union/hard 两份 CSV,**不消费 qc_uncertain_reject.csv** → 这 20 条错件 PDF 仍在 `pdfs/` 未物理隔离(coverage 口径不受影响,靠黑名单剔;目录卫生待 cleanup 扩展)。
- **闸门启示(补 L.5)**:内容闸门光有「标题比对」不够,还要 **文内 DOI ≠ 期望 DOI 即拒**、**SI/poster/citation-report 版式判识**、**exaly 等垃圾域黑名单**,否则①②③④照样漏进 success。

### L.6 防退化数据回归(可重跑,140)
- `tools/regress_qc_union_189.py`:重放审计产物到现门,两桶断言——① 189 条同域错论文(union CSV 里 mismatch 且 publisher/repository 桶)**并集全拒 189/189**(旧交集口径对照只拒 58、漏 131,证明现实现是并集);② 34 条 **title 假匹配**(151 判 match/uncertain 但 url_wrong=True,如 frontiersin 通配文标题分 91.5)**门②全拒 33/33**(1 条 PDF 缺失跳过)。
- 已收编进 `run_all_selftests.py` 可选项:默认 SKIP(依赖 out/ 审计产物与已隔离 PDF,非纯离线),**置 `RUN_DATA_REGRESS=1` 才真跑**;改 QC 门判定逻辑后务必开着跑一次(约 40s)。
- 数字口径备忘:`qc_merge_union_wrong.csv` 有 391 行,但含 match 16 + uncertain 18(url_wrong 的 title 假匹配),mismatch 实为 357——**引用"错论文数"别把 391 当 mismatch 数**;coverage 净成功以 `out/coverage.json` 的 `summary.qc.success_after_qc` 为准(该值随 cleanup/uncertain 拒收清单演进,勿引用陈旧快照)。

## N. CF 免 Docker 破盾落地 + JA3 绑定三层拆解 + 路线B 浏览器内直下(2026-07-02 固化,146;源 -145/-173/-177/-179/-149/-150/-148)

> **一句话**:免费过 Cloudflare 已**免 Docker 端到端跑通**(仓内 nodriver shim);但"过 CF"≠"下到 PDF"——必须把 **过 CF / cf_clearance 是否绑 JA3 回放 / 是否真 OA(付费墙)** 三层分开看。**ACS 不绑 JA3(cookie→curl_cffi 回放可过)、RSC 绑 JA3(回放必 403;免费正解方向=路线B 浏览器内同会话同 JA3 直下,但该路径端到端落盘本机尚未实证,-152 有头冒烟卡 CF,见 N.4)**。本节把散落 -179/-177/-149/-173 的已验证结论去重固化一处,并按 -152 真机结果标注 route-B 的未证边界;详版见各源文档。

### N.1 免 Docker 过 CF = 仓内 nodriver shim(起法 + 端口 + FLARESOLVERR_URL,-179/-177 实测)
- **别急着装 Docker / clone 外部项目**:本仓 `tools/flaresolverr_nodriver.py` 就是**免 Docker、FlareSolverr `/v1` 逐字段兼容**的求解端点,用 `nodriver`(uc 现代继任者、驱动真 Chrome)实现。本机 `nodriver 0.50.3 + curl_cffi` 已装,健康检查 + 端到端真解全通过(`SOLVE_OK`)。这是 -145 撞 `undetected-chromedriver 3.5.5` 与现代 Chrome 版本错配时的**零安装、零改码**解。
- **起法(PowerShell,两终端)**:
  ```powershell
  # 终端A —— 起 shim(默认有头,CF 通过率更高;无显示环境才 --headless)
  python tools/flaresolverr_nodriver.py --port 8191
  # 终端B —— 指向它并跑回收
  $env:FLARESOLVERR_URL = "http://127.0.0.1:8191/v1"
  python -m fulltext_fetcher -f <input>.txt -o out\... --email you@org.edu
  ```
- **零改码启用机制**:客户端 `flaresolverr.py::_endpoint()` 只认端点(`cfg.flaresolverr_url` > env `FLARESOLVERR_URL` > 默认 8191,自动补 `/v1`);`download.py::_flaresolverr_enabled()` = `use_flaresolverr` OR `flaresolverr_url` OR env `FLARESOLVERR_URL` 任一为真即启用 → **只 set 一个环境变量就开兜底**,`cli`/`config.py` 不用改。
- **⚠️ 坑1(PowerShell env)**:-177 早稿的 `set FLARESOLVERR_URL=...` 是 **cmd/bat 语法,PowerShell 里静默不生效**(不报错、变量没设上、`_flaserolverr_enabled` 仍 False、兜底静默不触发)。PowerShell 必须 `$env:FLARESOLVERR_URL="..."`;坚持用 cmd 窗口才能 `set`。
- **⚠️ 坑2(端口冲突)**:-145 常驻 FS 可能已占 `8191`,多人同机各用不同端口(`--port 8199`)并相应改 `$env:FLARESOLVERR_URL`。停服务后端口残留:`Get-NetTCPConnection -LocalPort <port> -State Listen` 找 `OwningProcess` → `taskkill /PID <pid> /T /F` 连子树清。
- **⚠️ 坑3(无 CF 小页等满超时)**:对无 CF 页(如 example.com)单次 ~59s(循环到 ~0.9×maxTimeout)。**真实回收不受影响**:shim 只在 `download.py` 已检测 CF 质询后才兜底调用,真 CF 站拿到 `cf_clearance` 提前 break,且**按 origin 缓存(默认 1200s)**→ 80 条 DOI 通常只落 4~5 个域,每域首条付一次解题成本。
- **selftest**:`python -m tools.flaresolverr_nodriver --selftest` → `FLARESOLVERR_NODRIVER_OK`(~21s);`run_all_selftests.py` 默认 **SKIP**(离线全绿 PASS=40 / SKIP=1),`$env:RUN_ONLINE_SELFTESTS="1"` 才真跑联网自检。
- **许可证**:`nodriver = AGPL-3.0`(内部批量回收/个人研究无碍;对外服务须开源或过法务)。
- **变体阶梯(-177)**:仓内 shim(★零安装首选) → **byparr**(Camoufox/Firefox,`pip install uv`+`git clone`+`uv sync`;nodriver headed 仍被特定 Turnstile 识别时上) → 住宅代理(cf_clearance 绑 IP,规模化时)。
- **✅ 纠正 K 节旧结论**:K「batch6 长尾 CF 桶回收杠杆是 FlareSolverr(需 Docker)」里的**「需 Docker」前提已被 -179 推翻**——仓内 nodriver shim **免 Docker** 即可解 CF JS 质询。

### N.2 "过 CF"≠"下到 PDF":三层必须分开看(过 CF / JA3 回放 / 付费墙)
- **shim 过 CF 稳定有效**(-179 smoke10 本机有头):对拿到的 4 个出版商 origin **全部 `cf_clearance=YES`**——`pubs.rsc.org` 9.9s、`pubs.acs.org` 54.1s、`researchgate.net` 54.8s、`onlinelibrary.wiley.com` 54.4s,按 origin 缓存命中正常。
- 但**能否把 PDF 下全**取决于两件**与 CF 无关**的事:① 该站 `cf_clearance` 是否绑 JA3;② 该条是否真 OA(订阅墙救不了)。
- **判"回收率"必须把三层拆开**,否则数字自相矛盾:recover_b4_cf full-80 summary success 27(37%),但内容 QC 甄别后真全文仅 5、`flaresolverr_recovered` 真救 CF = 0(见 N.5 / M 节)。

### N.3 【关键发现】ACS 不绑 JA3(回放可过)/ RSC 绑 JA3(回放必 403)—— 分水岭
- **机理**:FS/shim 解 CF 拿 `cf_clearance`+UA 后,`download.py` 把 cookie 交给 **`curl_cffi` 第三方 HTTP 客户端回放**下 PDF。`cf_clearance` 除绑 IP+UA 外,**强 CF 站还绑 JA3/TLS 指纹**。
- **ACS(中等 CF 站,不绑 JA3)→ 回放可过**:实测 `10.1021/acscatal.0c01253`(author-choice OA)FS 真解落 **4.41MB PDF**(`out/recover_b4_cf/`,-145/-149/-179)。
- **RSC(强 CF 站 `pubs.rsc.org`,绑 JA3)→ 回放必 403**:shim 侧 `cf_clearance=YES`,但 `curl_cffi` 回放那步因 **JA3 ≠ 真 Chrome** 仍 403;实测 3 条 RSC `articlepdf` 全 `flaresolverr_failed(cloudflare-challenge http-403)`。**换 byparr(Firefox JA3)/FlareBypasser 同链同理救不了 RSC**(其 JA3 也非回放客户端的 JA3)。
- **一句话**:只要是「solve 拿 cookie → 异客户端回放」这条链,**绑 JA3 的强 CF 站(RSC)必 403**;真解 = 浏览器内直下(N.4)。
- **curl_cffi 背景(-148)**:项目反指纹取回统一走 `curl_cffi`(lexiforest,MIT,`impersonate="chrome"`,与 `scholar.fetcher` 同栈);但它的 JA3 是「curl-impersonate 复刻的 Chrome」,**≠「解题那个真 Chrome 实例」的 JA3**——强 CF 站校验会识破。
- **cookie 回放正确性(中等 CF 站仍适用)**:回传 cookie 直下**必须带 `solve()` 返回的 `user_agent` + 同一出口 IP**,别自己另换 UA;引擎定 UA 家族(shim/FlareBypasser=Chrome UA、byparr=Firefox UA),各自一致、勿混;HTTP/2 站用 `httpx` 而非 `requests`(偶发 403)。

### N.4 JA3 绑定站的免费正解 = 路线B 浏览器内直下 PDF(**机制已实证闭环**;初测卡CF→真因=过盾期开 CDP 拦截,-152/-148,详见 N.8)
- **机理**:同一 nodriver 真 Chrome 会话内 `fetch(pdfUrl).arrayBuffer()` 取字节,**不把 cookie 回放到 curl_cffi** → 下载与挑战在**同一真 Chrome 指纹**内完成 → 破 JA3 死结。
- **✅ 端到端状态(2026-07-02,已闭环,详见 N.8)**:route-B「页内直下真能落 %PDF」**已实证**——修正「过盾期不开 CDP Fetch/Network 拦截」后,`10.1021/acsomega.6c04195`(金OA)**CF 6.6s 过、落 13.7MB/17 页 %PDF、QC=match、Chrome133 + nodriver0.50.3 免升级**(-152/-148 真机)。**JA3 死结路径已打通**。
- **📜 定论前的初测经过(-152,留档)**:-152 初测用 **Chrome133 有头**跑 `render_download_pdf_bytes` 曾 **4/4 卡在 CF 质询页**、ACS happy path 0/4(且原样本 `acsanm.1c00959` 经 Unpaywall 实为 `is_oa=False` 闭刊)。**当时误判**复验需「升 Chrome→140+」——**已被 N.8 推翻**:Chrome 版本 / 指纹 / KeyError 均非 CF 拦路虎,**真因 = 过盾期开了 CDP Fetch/Network 拦截**(见 N.8 #2)。curl_cffi 回放绑 JA3 站仍 403 这半(-145/-179)始终成立。另一相关根因(仓内探针 `_spike_browser_capture.py`/`_tmp_ja3_monkeypatch_probe.py` + -142 印证):**nodriver 0.50.3 的 typed CDP Network 事件在 Chrome133 上 `KeyError:'localNetworkAccessRequestPolicy'` 崩**(Chrome↔nodriver CDP schema 漂移),`--capture-bytes` 一走 CDP Network 域即中招——规避 = 避开 CDP Network 域(纯页内 fetch)/ monkeypatch `ClientSecurityState.from_json` 容忍缺字段 / 升 Chrome+nodriver。
- **⚠️ 与 N.1/N.2 不矛盾但要分清**:N.1/N.2 过 CF 的是 **FS nodriver shim(`tools/flaresolverr_nodriver.py`)的 solve 路径**(cf_clearance=YES,-179);而本节 **route-B 的 `render_fetch` 直下路径**却卡 CF——**同为 nodriver,差别就在:FS shim 全程不开 CDP 拦截、route-B 过盾期开了**(根因已定位,见 **N.8 #2**;修法 = 过盾期不开拦截、以 cf_clearance 判过后再 B1 页内 fetch)。详见 `路线B-真机冒烟结果-152.md`。
- **落点**:`render_fetch.py::render_download_pdf_bytes()` + `--capture-bytes`;`download.py::_nodriver_fetch_pdf_bytes` + `cfg.browser_pdf_download`(骨架见 -177 `选型2026-强CF浏览器内直下PDF实现者骨架-renderfetch-CDP.md`)。
- **验证 Runbook**:`路线B-浏览器内直下PDF验证Runbook-173.md`——1.1 ACS-OA 应落 `%PDF`≥100KB;1.2 RSC 订阅墙应 `no-pdf`/`blocked`(**不误报假 %PDF**);第4步 JA3 对照(回放链 403 + 页内直下**有 PDF** = JA3 死结被解开 = 高价值样本,存档 DOI+hash);第5步内容 QC 必做。
- **ROI(诚实口径,-173/-144)**:JA3 绑定型 CF 后 OA/免费正文桶规模仅 **~5~15**;路线B 全量 still_missing 净增**点估 +20 篇(+2pp)**,区间 +10~35。**价值在「JA3 从 0 到 1 + 提质」**(从权威落地页直下,绕开 websearch 68.5% 错论文假阳),**不在清空 still_missing**。
- **边界**:RSC 在 batch6 口径**净 MISS≈0**(websearch 已从别处兜回),为 RSC 单独上 CF 破盾**边际≈0**;路线B 主要点亮机制与提质。
- **→ 根因定论见 N.8**:本节「端到端本机未证」已被 -152 闭环——**真因 = 过盾期开了 CDP Fetch/Network 拦截(非 env/Chrome 版本)**;修法落地后 `acsomega.6c04195` 已落 13.7MB %PDF、Chrome133 免升级。

### N.5 ⚠️ FS / 路线B 回收必须与内容 QC 联用(否则忠实下载错候选)
- **FS 成功 ≠ 正确全文**:FS 会忠实地把 websearch 错候选也"成功"下载——实锤 FS「救回」的 `jaad.org`(皮肤病学)正是 `10.1002/cssc.201601217`(ChemSusChem)的假阳(-149/-150 交叉印证)。
- **"跑批 success 率"≠"真全文净增"**:recover_b4_cf full-80 success 27(37%)→ 内容 QC 三层甄别后**真全文仅 5**、真净增益 ≈4%;`flaresolverr_recovered` 真救 CF = 0(-142/-149;详 M 节)。
- **QC 门现状**:已落地门①(标题)/门②(跨社 URL/嵌入 DOI)对「像这篇但不是正文」零免疫;-173 建议 + 142 复核 + 147 实测已补**门④(SI 判识 + acs-authorchoice 强制 QC)/门⑤(citation-report/poster/TOC 非正文版式)** end-to-end,147 广样本回归 **143 真全文 0 误杀 + 净清 51 SI**,达翻默认门槛(待 140 拍板)。
- **⚠️ 更正 → 落地(-143):此前把门③「建议补」误记「已补」,-143 核码确认原缺失、并已【补齐落地】**。门③ = 元数据 DOI 反向比对:`_extract_pdf_meta_dois` 从 PDF XMP(`prism:doi`/`dc:identifier`)+ `/Info` 抽内嵌 DOI,`_content_qc_verdict`(门②之后)判——期望 DOI 已知、且 PDF 自述 DOI【全部】≠期望且期望不在其中 → `meta-doi-mismatch` → mismatch。**精度优先**:仅在①强正未命中(期望 DOI 不在 URL/正文区)时才走门③,元数据含期望/无内嵌 DOI → 不触发、不误杀;专拦「同题他刊」(如 `cctc.200900261`:标题近同、期望 DOI 不在正文首部,靠内嵌 Beilstein DOI 识破)。门②的「文内异 DOI 反证」(body-embeds ~1500 字窗口)属反证侧位置规避,与门③(PDF 自身元数据)互补。落地详见 **W.4**(承 T.6、《经验记录完整性审计-本波关键主题落档核对-140.md》更正1)。
- **运维铁律**:**运行时缺 `pypdf` → 整个内容 QC 门静默变 no-op、100% 放行**;CF 重跑环境必须装 `pypdf`,并置 `content_qc_non_article_hard_reject=True`(详见 L.5/M 与 `选型2026-QC并集门增强建议-recover_b4_cf假阳-173.md`)。

### N.6 运维速查(主 Runbook 指针)
| 场景 | 权威文档 | 归属 |
|---|---|---|
| 免 Docker CF 回收起法/端口/坑/selftest | `选型2026-FlareSolverr免Docker-仓内nodriver-shim实测与落地-179.md` | -179 |
| 免 Docker 变体阶梯(byparr/FlareBypasser) | `选型2026-FlareSolverr免Docker变体-给145.md` | -177 |
| 路线B 机制验证(照做) | `路线B-浏览器内直下PDF验证Runbook-173.md` | -173 |
| CF/免费到顶全景 + websearch 假阳 | `回收实测结论-CF与免费路线到顶.md` | -149/-150 |
| CF/JA3 桶 ROI 与排期 | `检索成果-still_missing-CF-JA3桶ROI深挖-173.md` | -173 |
| 反指纹 HTTP/TLS 客户端选型 | `选型2026-隐身HTTP与TLS指纹客户端.md` | -148 |
| 内容 QC 门③④⑤ 规格 | `选型2026-QC并集门增强建议-recover_b4_cf假阳-173.md` | -173/-142/-147 |

> 说明(146):本节为「主 Runbook 指针」式刷新——CF/JA3/route-B/FS 的**单一事实源**集中在此表 + N.1~N.8,不重写各成员正在维护的专项 runbook(避免与 -173/-152 撞车);如需独立"总运维 Runbook"合稿,由总指挥指派。

### N.7 nodriver FS shim 长跑后 solve 全挂 OSError Errno 22(-140 实测 2026-07-02)
- **现象**:`python tools/flaresolverr_nodriver.py --port 8191` 进程仍 `[ready] listening`,但管线侧每次 CF 质询调 FS 均报 `FlareSolverr 非 ok 响应: status=error message=OSError: [Errno 22] Invalid argument`;`flaresolverr_recovered=0`,CF 桶(wiley/aip/acs/T&F)几乎全部 `cloudflare-challenge(http-403)`。
- **连带**:acs 全量 95 条跑到 ~28 条时进程 **exit_code=1** 中断(未出 run_all 总结);须 `--resume` 续跑或修 FS 后重跑。
- **假阳**:FS 失效时 websearch 偶发落盘错 PDF(aip `10.1063/1.1647050`→ESOMAR 指南,wiley 1 条被 QC 并集门拒);说明 **FS 挂掉时 CF 桶 ROI 趋近 0,不能靠 websearch 凑数**。
- **对策(待验证)**:长跑前/中定期重启 nodriver FS;排查 Windows 下 nodriver 与并发 solve 的 Invalid argument 根因;CF 桶在 FS selftest 不过时**不要扇出全量**。
- **→ 根因定论见 N.8**:本波实证此 OSError / CF≈0 的**根因 = 多 shim 重载 thrash(非 FS 真挂)**,对策 = **单 shim 单桶串行降载**。

### N.8 CF 过盾根因定论(本波实证闭环,2026-07-02 固化 142;源 -144/-148/-152 实证,-146 授权写锁)

> **一句话**:本波把「CF 过不去」拆成**两条独立代码路径、两个独立根因**,均已实证闭环——(A) FS/shim 路径 0 产出 = **多 shim 重载争用**(非 CF 真破);(B) route-B `render_fetch` 路径卡 CF = **过盾期开了 CDP Fetch/Network 拦截**(非 env/指纹/Chrome 版本)。二者修法各异,勿混。

**#1 FS/shim 路径:0 产出 = 多 shim 重载 thrash(N.7 OSError 的根因定论)**
- 根因:同机并发起了 **7 个 shim(3×flaresolverr + 4×fs_shim_141)**互相 thrash → nodriver 被拖垮 → 全组 CF≈0、solve 报 `OSError:[Errno 22]`(即 N.7 现象)。**非 FS 真挂、非 CF 真破**。实证:-144 kill 掉一个 FS shim 竟释放 ~10 个 chrome 进程。
- 修法:**CF 桶必须单 shim 单桶串行,严禁多头浏览器 / 多 shim 同机**。降载后 `pubs.acs.org` **CF 200 + cf_clearance @ 6.9s**(-144 实证)。

**#2 route-B 路径:卡 CF 真因 = 过盾期开了 CDP Fetch/Network 拦截(N.4「本机未证」的定论 + 修正)**
- 真因:`render_fetch` route-B 在**过 CF 质询期开着 CDP Fetch 拦截(`fetch.enable`/`network.enable`,render_fetch.py:445-449 导航前)**→ CF 敏感 → 卡 'Just a moment';而 FS shim **全程不开拦截**、只轮询 cf_clearance,故**同机同 Chrome133(甚至 headless)能过**。**指纹 / Chrome 版本 / KeyError 均非 CF 拦路虎**——推翻 N.4「需升 Chrome140+」的旧判。
- **同一根因串联**:N.4 记的 `KeyError:'localNetworkAccessRequestPolicy'` 正是**过盾期开了 CDP Network 域**才触发的 typed 事件崩;不开 Network/Fetch 域即根本走不到那条崩。
- **修法(5 条铁律,-148/-152 实证)**:
  1. 过盾期**绝不开** Fetch/Network 拦截;
  2. 用 **cf_clearance cookie + 可见文案(title / body innerText)** 判过盾——**别**用 HTML 里 `cf-chl`/`challenge-platform` 脚本标记(过盾后残留 → 假阳);
  3. 过盾后**优先 B1**:页内同源 `fetch(pdfUrl,{credentials:'include'}) → FileReader → data-URL`(**不需 `Fetch.enable`**);B1 遇 CSP/跨源失败(`TypeError: Failed to fetch`)才回落 **B2**(过盾后开 `Fetch.enable` RESPONSE + 导航 PDF 直链、网络层 `getResponseBody`)。**拓字节子路径按域分(-152 实测)**:ACS-OA 走 **B1**;**RSC 走 B2**——B1 被 CSP/跨源挡、且 RSC 页无明链 PDF → B2 导航目标用 `publisher_direct._rsc()` 构造 `articlepdf` URL;实现口径=先 B1、遇 CSP/跨源失败回落 B2,B2 的 `Fetch.enable` 只在过盾后开;
  4. **不用** `Page.printToPDF`(重渲染失真、不利 QC);
  5. nodriver `KeyError`(Chrome146 把 `privateNetworkRequestPolicy`→`localNetworkAccessRequestPolicy`)→ `nd.start` 前 **monkeypatch `ClientSecurityState.from_json` 双键兼容**兜底(同 N.4)。
- **实证**:`10.1021/acsomega.6c04195`(ACS Omega 金OA)**CF 6.6s 过、B1 拿 13.7MB/17 页 %PDF、QC=match、Chrome133 + nodriver0.50.3 免升级**(-152 真机)。⇒ **route-B「本机未证」已闭环为「已实证」**,JA3 死结路径打通。

**#3 dual-key monkeypatch**:见 N.4(Chrome↔nodriver CDP schema 漂移的双键兼容),此处仅指针不重复。

**#4 QC 联用(防假阳)**:搜索类源(websearch)先拒、DOI 绑定源再过内容 QC;门③④⑤(文内/元数据异 DOI、SI、citation-report/poster/TOC)见 **L/M 节 + `选型2026-QC并集门增强建议-recover_b4_cf假阳-173.md`**;运行时缺 `pypdf` → 门 no-op(见 N.5)。

**边界(诚实)**:#2 实证的是**机制通**(金OA 样本);A 段 rsc 67 多为**订阅墙** → route-B 对订阅墙 `no-pdf` 是**正确行为非 bug**,真回收 = 真 OA 子集(如 RSC Advances);净增益仍按 N.4 ROI(点估 +20 / +2pp,价值在 JA3 从 0 到 1 + 提质)。

*出处:`选型2026-JA3绕过与浏览器内直下-选型建议.md §七`、`路线B-真机冒烟结果-152.md §5.1`;源 -144/-148/-152 实证,142 固化(task-fe3de730),-146 授权写锁,B1/B2 分域补记 -148/-152。*

### N.9 浏览器无头「双默认」定论:过 CF 解题器【默认有头+移屏外】vs route-B 抓字节/Akamai 下载【默认无头】,统一由 `FTF_HEADLESS` 翻转(2026-07-03 固化 142;源 -146 用户要求/-157 实现,`_verify_headless_157` 实证)

> **一句话**:「浏览器自动化默认无头」这条**只对抓字节/下载路径成立**;**过 CF 的解题器必须默认【有头】**(无头过 managed-challenge 通过率≈0),二者共用**同一个 `FTF_HEADLESS` 全局开关**但默认取向**相反**——把「默认无头」当铁律套到 CF 解题器上会直接废掉主功能,务必分清。

**两条路径、两个相反默认(动机都是 -146『办公不被弹窗打扰』)**:
1. **过 CF 解题器**(`tools/flaresolverr_nodriver.py` `_resolve_headless`/`_offscreen_args`,81–102 行)——**默认【有头】**。真机实测 `pubs.aip.org` 等 managed-challenge 站**无头过 CF 通过率极低**(本机 headless 81.5s 拿不到 `cf_clearance`,有头 ~8s 即过)。为兼顾「不弹窗」→ **默认有头 + 窗口移出可视区**(`_offscreen_args` 返回 `--window-position=-2400,-2400`);排障要看窗口设 `FTF_BROWSER_SHOW=1`;服务器/xvfb 真要无头设 `FTF_HEADLESS=1`。
2. **route-B 抓字节**(`render_fetch.py` `_resolve_headless(default=True)`/`_nodriver_capture_fn`,656–710 行)——**默认【无头】**(不弹窗);`FTF_HEADLESS=0` 才强制有头(调 CF 通过率/排障)。`default` 形参**已不影响结果**、仅留 API 向后兼容(旧调用 `_resolve_headless(headless)` 仍合法)。
3. **Akamai/MDPI 下载·门⑥**(`download.py` `_nodriver_fetch_pdf_bytes`,1279–1285 行)——自身默认按 `cfg.browser_pdf_headless`(默认 False=有头,过 Akamai bm-verify 需真 Chrome),但**尊重同一 `FTF_HEADLESS` 覆盖**(`from .render_fetch import _headless_env_override`;**env 优先于 cfg**)。

**`FTF_HEADLESS` 全局开关(统一口径)**:`=1/true/yes/on`→无头;`=0/false/no/off`→有头;**未设**→各路径自取默认(CF 解题器=有头、route-B=无头、门⑥=按 cfg 有头)。

**坑/铁律**:要「不弹窗打扰办公」又不塌 CF 通过率,正解是**有头 + 移屏外**(`FTF_BROWSER_SHOW` 缺省即移屏),**不是**把 CF 解题器改无头。无头只安全用于 route-B 页内抓字节 / 门⑥ 下载路径。

**实证/复现**:`python -X utf8 _verify_headless_157.py` —— 断言 `_resolve_headless(False)==False`(有头) + `_offscreen_args` 含 `--window-position`(移屏外) + 真机 solve `pubs.aip.org` 拿到 `cf_clearance`(证有头+移屏外仍过 CF)。

*出处:`render_fetch.py:_headless_env_override/_resolve_headless/_nodriver_capture_fn`、`download.py:_nodriver_fetch_pdf_bytes(门⑥ 1279–1285)`、`tools/flaresolverr_nodriver.py:_resolve_headless/_offscreen_args(81–102)`、`_verify_headless_157.py`;-146 用户要求(办公不被弹窗打扰)、-157 实现,142 固化落档。*

## O. 【重锤】ACS authorchoice `/doi/pdf/` 高 SI 假阳(≈93%)+ CF/FS 回收三桶真净增结算 + 生产 SI 门漏检定位(2026-07-02,145;task-d4154e0d)

> **一句话**:CF/FS-shim「免费回收」三桶(-144 的 acs/wiley/aip 共 120 条 still_missing)跑批 `success=57`,但**全页内容复核后真·正文净增仅 5 篇、flaresolverr 直破 CF 真救回 = 0**。主桶 ACS 56 条 success 里 **51 条是 ACS 免费 Supporting Information 冒充正文、1 条错文件,真正文仅 4**。再次证实并放大 M.2/N.5「跑批 success 率 ≠ 真全文净增」。

### O.1 acs-authorchoice 是高 SI 假阳源(≈93%)
- **机制**:`publisher_oa.py::_acs()`(L167)对所有非金牌 OA 的 ACS DOI 一律构造 `https://pubs.acs.org/doi/pdf/{doi}`(conf 38, tag `acs-authorchoice`)。对**订阅正文**,ACS 免费开放的是 **SI**,该 URL(或重定向)落到 SI PDF → 下载器只校 `%PDF`+体积 → 记 success。真 author-choice/OA 才给正文。
- **实测(rerun_acs_144 全 56 success 全页复核)**:真正文 **4**(`acscatal.0c04429`/`ja509214d`/`acs.energyfuels.5c06101`/`acs.langmuir.7b03998`,均 acs-authorchoice 真 OA)、SI-only **51**、错文件 **1**(`j100144a026` 得到「USER MANUAL for PFMS GPF Module」扫描件,源 websearch)。acs-authorchoice「success」**51/55≈93% 是 SI**。真净增率 4/95≈4.2%(对比跑批 56/95=59%,虚高 14×)。
- **flaresolverr 真破 CF = 0**:56 全是 acs-authorchoice `/doi/pdf/` 直构碰运气 + 1 wayback + 1 websearch,无一来自 FS;另 39/95 硬 miss=`cloudflare-challenge(http-403)`。

### O.2 CF/FS 回收三桶真净增(诚实口径)
| 桶 | 跑批 success | 真·正文净增 | FS 真破 CF |
|---|---:|---:|---:|
| acs(rerun_acs_144) | 56/95 | **4** | 0 |
| wiley(rerun_wiley_144) | 1/22 | 1(源 semantic_scholar 绿 OA) | 0 |
| aip(rerun_aip_144) | 0/3 | 0 | 0 |
| **合计** | 57/120 | **5** | **0** |
- 坐实「CF 免费路线到顶」:**FS-shim 对订阅正文墙无效,真正文仍需机构订阅 A5**。**不应为 ACS/CF 桶铺大 FS 走量**。route-B 对 ACS 桶边际≈0(ACS 不绑 JA3、且拿到的仍是免费 SI,正文在付费墙内)。

### O.3 生产 SI 门 `download._content_qc_non_article_reject` 漏检 7 条 —— 三个精确机理(交 -140 修,-147 验)
逐条实测(首页首 500 字信号),漏因归三类:
- **(A) 只认 "supporting information",未认 "supplementary information"** → `acs.joc.4c00727`、`jacs.0c12689`。
- **(B) `and not has_body` 过度抑制**:SI 前 2 页含 Introduction/Experimental 等词 → has_body=True → SI 判定被压掉 → `acs.jpclett.4c00005`、`acscatal.0c02146`。
- **(C) 粘连/空白变体**:'S1SupportingInformationfor'(全粘连,`jacs.2c13784`)、'S1Efficient…'(仅 S1 贴标题、S-head 正则 `^\s*s-?\d+\s*[.\)]` 要求数字后跟 `.`/`)`→ 不命中,`es501021u`)、多空格 'Supporting  information'(`acscatal.6b00397`)。
- **建议修法(保守不误杀真正文——真正文首页是标题页,不以 Supporting/Supplementary Information 开头)**:① SI 短语集加 `supplementary information`/`supplementary materials`;② 首 ~120 字即以 `^\s*\d?\s*S-?\d*\s*(supporting|supplementary)\s*info` 开头 → 优先判 SI,不被 has_body 抑制;③ 判前对首页做空白归一+去粘连('S1Supporting'→'S1 Supporting')。
- **负样本回归(修门后必判 non-article-si)**:7 条 leak DOI 见 `out/rerun_acs_144_reclass_145.json` 的 `prod_leaks`;正样本(不可误杀)= 4 条真正文。

### O.4 QC 黑名单双向纠错(与 -151 coverage 回写对齐)
- 我的 content-QC 恰好抓出现有 QC 黑名单的**双向错误**,-151 已编入回写口径:① `acsami.9b14097`(SI)是现有黑名单**漏判**的假阳 → 补入 `--qc-extra-reject`;② 我判真正文的 4 条被旧 union/highconf **误拉黑**(假阳)→ -151 新加 `--qc-allow` 白名单(优先级高于黑名单、不凭空造成功)翻正。**ACS 桶权威净贡献 = +4**。
- **副本陷阱(务必精确到源/ md5)**:`acs.energyfuels.5c06101` 有三副本——batch6 与 rerun_acs_144 **字节完全相同**(md5 c467680f,真正文);但 **batch7/rejected 那份(2128231B,md5 dce2eca5)是另一篇错论文**(RSC《Sustainable Energy & Fuels》被 websearch 误配给 ACS《Energy & Fuels》同名不同刊 DOI,已 reject)。白名单**必须精确到 md5/源**,别误白到错文件。

### O.5 复用与产物(纯读、可重跑、未改核心码/PDF)
- **复用生产门做审计**:`download._extract_pdf_text_meta` / `_pdf_page_count` / `_content_qc_non_article_reject` / `_content_qc_verdict` 可被审计脚本直接 import 复用(与 pipeline 完全同逻辑),**不重造匹配/判定**。
- 产物:主报告 `验证-rerun_acs_144-真全文净增与SI假阳-145.md`;`out/rerun_acs_144_screen56_145.csv`(56 条逐条)、`_deduct_dois_145.txt`(应扣 52)、`_whitelist_back_145.txt`(翻正 4)、`_si_reject_145.txt`(51 SI)、`_{qc_scan,reclass}_145.json`(证据);探针 `_qc_scan_acs144_145.py`/`_acs_reclassify_145.py`/`_screen56_145.py`。

*出处:145 task-d4154e0d(总指挥 142 派)｜证据 `out/rerun_acs_144_*_145.*`｜与 -151(coverage 回写,+4 已编入)、-147(QC 门回归)、-140(门代码)对齐｜-145 获写锁追加。*

## P. 【重锤】RSC route-B 死结定论:签名 CDN 内联 viewer → 唯「导航后同源页内 fetch」能落字节(方法D,2026-07-02,141;task-d4def0b5)

> **一句话**:RSC(JA3 绑定型 CF)route-B 长期 `no-pdf-captured` 的真因**不是 CF 没过**(CF+SSO 都过了),而是 articlepdf 会 **302 到签名 CDN 直链(`rscj.silverchair-cdn.com/…pdf?Expires=`)并被 Chrome 内联 PDF viewer 打开**:既不触发下载(`set_download_behavior` 无效)、`get_response_body`(Network 域 on_finished + Fetch 域 on_paused,即 -148 的 b2/b2-fetch)也读不到 viewer 流 body → 全 0 字节。**制胜法=导航【落到 CDN .pdf 页后】在其同源上下文 `fetch(location.href)`**(方法D/`b2-viewerfetch`),-141 真机实证 484KB %PDF-1.6。

### P.1 根因三连(-141 探针逐段实证)
- **过盾链路带 SSO 中转**:`https://doi.org/10.1039/d5ra08493h` → CF 质询 → `sso.rsc.org/connect/authorize?client_id=…`(OAuth/SSO 授权回跳)→ 回落 `pubs.rsc.org/ra/article/16/17/15523/…`(真文章页)。**cf_clearance 会在仍停在 SSO 中转页时就置上**——旧 AND 判据(只查 cf_clearance+质询文案)会"偷跑"到 SSO 页、找不到 PDF。
- **articlepdf = 签名 CDN 内联流**:导航 `pubs.rsc.org/en/content/articlepdf/2025/ra/d5ra08493h` → 302 到 `rscj.silverchair-cdn.com/…/d5ra08493h.pdf?Expires=…`,`document.contentType='application/pdf'`,被 Chrome viewer **内联渲染**。故:①无下载事件 → 下载目录空(探针 `saw_partial=False, dir_files=[]`);②Network/Fetch 域 `getResponseBody` 读不到 viewer 流 → -148 的 b2/b2-fetch 必 0 字节(**这就是 -148「Fetch.enable RESPONSE / JA3 B1 fail→b2-fetch」在 RSC 无效的定论**)。
- **B1 为何也不行**:B1 从**文章页(pubs.rsc.org)跨源 fetch** articlepdf,fetch 跟随 302 到 silverchair-cdn(跨源),最终响应无 CORS 头 → 读不到。**唯有导航到 CDN .pdf 页后**,页内 `fetch(location.href)` 才是**同源**且带 cookie → 拿到字节。

### P.2 修法(render_fetch.py `_nodriver_capture_fn`,局部两处,零改 download.py)
- **① SSO 回落等待**:JA3 站过盾判据追加「已离开 SSO 中转页」——`location.href` 含 `/connect/authorize`|`sso.`|`/login`|`/idp` 即 `on_interstitial=True` 视同未过盾、继续等(deadline 兜底不死锁)。
- **② 方法D(`b2-viewerfetch`)**:导航 pdf_url 后轮询,若当前页 `contentType=application/pdf` 或 href 含 `.pdf`,则 `_inpage_fetch_pdf_js(location.href)`(await_promise)→ `_data_url_to_pdf_bytes` → 命中 %PDF 即收;复用现成 B1 机具,**A/C 拦截器并行保留兜底**(非 CDN 内联型站仍走老路,不回归)。
- **改动面**:纯 `render_fetch.py`,零改 `download.py`(避开 P8 重构冲突);opt-in route-B 路径;selftest 绿(`RENDER_BYTES_OK`/`RENDER_OK`)。

### P.3 实证(真机 nodriver0.50.3 + Chrome133 + 生产路径 `_route_b_verify_152.py`)
- **RSC** `10.1039/d5ra08493h`:`ok`(how=b2-viewerfetch)、**484,829B %PDF-1.6**、QC verdict=match(doi in text)、25.3s;与 -152 记录的 484KB 吻合。
- **ACS-OA B1 无回归** `10.1021/acsomega.6c04195`:13,780,580B %PDF-1.4、QC=match、9.4s(SSO 判据对非 SSO 站零影响)。
- **两样本齐绿 GATE=PASS** —— A 集金OA 发射门(RSC 金OA~8 + MDPI7)前置闸门打通。

### P.4 边界(诚实)
- 修法针对**签名 CDN 内联 viewer 型 OA**(RSC Silverchair 系);订阅墙 RSC 仍 `no-pdf`(正确行为、非 bug)。真回收 = RSC 真金OA 子集(RSC Advances 等)。
- 探针留档 `_route_b_download_probe_141.py`(一次性;走同一把 `_single_head_guard` 单头锁,不并发弹窗踩 CF)。产物 `out/runbook_b/verify_152/10.1039_d5ra08493h.pdf`、`out/runbook_b/probe141_10.1039_d5ra08493h.pdf`。

### P.5 统一化补丁:方法C(Fetch RESPONSE)触发放宽 + `*/pdf*` 模式(-149 MDPI 发现联动)
- **-149 MDPI 发现**:MDPI(Akamai,非 CF)同属『过盾后 citation_pdf_url 302 跨域到 CDN attachment PDF』——B1 页内 fetch 跨域 CORS 被挡、Network 域 get_response_body 拿不到 body;但【过盾后 Fetch.enable RESPONSE 拦截(方法C)】能抓 MDPI 7/7。生产旧码只对 `is_ja3_bound_cf_host` 开方法C → MDPI 非 JA3 不触发 → no-pdf。
- **本次 render_fetch 放宽(局部2处,不动 download.py)**:① `_enable_fetch_capture()` 触发从『仅 is_ja3』改为『B1 失败即开』(覆盖 RSC/MDPI/其他过盾后跨域 CDN 同类站);② `_PDF_URL_PATTERNS` 加 `*/pdf*`(MDPI `/pdf?version=` 直链旧模式漏网,-149 实证需加;实际抓取仍由 `_looks_pdf_response` content-type/URL 二次把关,过宽命中不误抓)。
- **回归复验**:RSC d5ra08493h 放宽后仍 PASS(484,829B %PDF-1.6,QC=match,23.2s);selftest 绿(RENDER_BYTES_OK/RENDER_OK)。
- **边界(未我方证,诚实)**:MDPI 生产覆盖还需 **download.py 路由**把 mdpi 纳入 browser-capture host 集(属 P8/download.py,非本会话范围);MDPI 7 条经 `render_download_pdf_bytes` 的复验建议由 -149(MDPI 主跑手)在本 render_fetch 更新后执行。本会话只保证 **render_fetch 侧能力就位 + RSC 无回归**。
- **注**:总指挥转述『_PDF_URL_PATTERNS 不用改』与 -149 实测脚本 `_diag_mdpi_b2_143.py` 注释(MDPI `/pdf?version=` 不匹配旧模式、需加 `*/pdf*`)矛盾;以 -149 **能跑通的探针**为准 → 加 `*/pdf*`。

*出处:141 task-d4def0b5(总指挥 -144 派)｜-141 真机三探针定论(SSO 回落 + CDN 内联 viewer + 同源页内 fetch)｜续 N.2/#2 的 B1/B2 分域(此为 B2 的「签名 CDN 内联」子类补锤)｜P.5 系 -149 MDPI 发现联动的统一化放宽(方法C 触发 + `*/pdf*`)｜-141 获写锁 `fulltext_fetcher/render_fetch.py` + `经验记录-踩坑与发现.md` 追加。*

## Q. 【本轮收敛·2026-07-02 晚】一键活体冲烟成立 + route-B 机制横切定论 + MDPI7/T0 翻案净增(153 沉淀;总指挥 -144 派 task-ecb07009)

> **一句话**:把本轮散点结论收敛成四条可复现定论——①`run_all` 默认路径一键**活体冲烟 5/5、零假阳、标准化名、无人工断点**(北极星"程序化默认路径"成立);②**route-B 机制横切分层定论** + 生产触发从"仅 JA3 host"放宽为"**B1 失败即试 B2**";③**MDPI7 全 7/7 真净增**(count 381→388 定;机制复用 B2 同款「Fetch RESPONSE 拦截」但归类 ⑥ browser_pdf_download,详见 Q.3/P.5);④**T0 翻案 +2 铁净增/+4 边界**,websearch 兜底源漂移严重、QC 并集门 100% 拦截零污染。RSC 字节级根因见 **P 节**(不重复);净覆盖官方口径见 Q.5。

### Q.1 一键活体冲烟成立(证北极星默认路径程序化闭环)
- `python run_all.py`(默认配置:不带 `--institutional`、无付费凭据、无灰色开关)对 demo 输入(`_run_all_demo_input.txt`)端到端 **5/5** 跑通:每条产真 %PDF、内容 QC verdict=match(**零假阳**)、文件名标准化落 `out/`、`summary.json` 落盘,**全程无人工断点**(无交互阻塞、无手动续跑)。
- 意义:证明"输入 → 程序化默认路径 → 直取 PDF"在**默认最安全配置**下即成立,不依赖任何按需/灰色能力(SerpApi/代理/scihub/FS 全默认关)——北极星路径**程序化闭环**已达成。

### Q.2 route-B 机制横切分层定论 + 生产触发放宽(RSC 字节级根因详见 P 节)
- **同源直取(GO)**:ACS 等**不绑 JA3**的站,B1「文章页同源 `fetch`」直接拿到字节(见 N.3);这是 route-B 的顺路径。
- **JA3 绑定站过盾后仍拿不到 body 的共因**:过盾后 PDF 走**跨源 CDN**,B1 跨源 `fetch` 被 **CORS 挡**、Network/Fetch 的 `getResponseBody` 读不到流 → 全 0 字节。据 PDF 交付方式再分两子类,**解法分叉**:
  - **(a) 跨源 attachment/下载型**(过盾后〔CF 或 Akamai〕PDF 走可下载的跨源 CDN attachment):过盾后 `Fetch.enable`(RESPONSE 阶段)拦截即可落字节 = **方法C/B2**(-148/-149;render_fetch 触发已放宽为「B1 失败即开」,见 P.5)。**MDPI7 即属此类**(方法C 侧 7/7);惟其入库 coverage 的产物 source 记为 ⑥ `browser_pdf_download`(生产落盘路径,详见 Q.3)。
  - **(b) 签名 CDN 内联 viewer 型**(**RSC** Silverchair):PDF 被 Chrome viewer 内联渲染,**连 Fetch RESPONSE 也读不到 viewer 流** → 方法C/B2 在此**无效**,须**方法D=导航到 CDN `.pdf` 页后在其同源上下文 `fetch(location.href)`**(-141 真机 484KB,**根因与修法全见 P 节**)。
  - ⚠️ 关键区分:RSC 与 MDPI **同属过盾后跨域 CDN**,但 **MDPI=attachment(方法C/B2 可抓,7/7)**、**RSC=内联 viewer(方法C 也读不到,须方法D)**;早期「RSC/MDPI 一并方法C 解决」对 MDPI 成立、对 RSC 不成立。以本节 + P 节(P.5=MDPI / P.1–P.4=RSC)为准。
- **生产触发放宽(一句话横切,细节见 P.5)**:方法C/B2 触发从「**仅 JA3 host**」放宽为「**B1 失败即开**」以覆盖名单外同 failure 站——`_PDF_URL_PATTERNS` 加 `*/pdf*`、-149 MDPI 联动、RSC 无回归复验等实现细节**全在 -141 的 P.5**,此处不重复展开(避免两处漂移)。

### Q.3 MDPI7 全 7/7 真净增(count 381→388 确定;归类 browser_pdf_download⑥、机制复用 B2 同款拦截;清单 `routeB_mdpi.txt`)
- MDPI(`10.3390`)**全 OA** 子集 **7 条**(app14114959 / catal10070741 / catal13091244 / catal15111028 / catal16020163 / catal16030270 / en11092276)**全部 7/7** 落真 %PDF、**真净增**(非 SI、非同题假阳);7 条已过总指挥新强制门(逐条开卷 expected-doi-present=True、非 SI)。
- **机制**:MDPI 站走 **Akamai(非 CF)**+ 全 OA(无付费墙),过盾后 `citation_pdf_url` 302 跨域到 CDN **attachment** PDF——B1 跨域 fetch 被 CORS 挡/`getResponseBody` 拿不到 body;**技术上复用与 route-B B2 同款「过盾后 `Fetch.enable` RESPONSE 拦截」** 抓到 7/7(-149 发现;-141 render_fetch 放宽触发『B1 失败即开』+ 加 `*/pdf*`,详见 **P.5**)。⚠️ **但归类以 coverage 为准 = `browser_pdf_download`(⑥),不叫 B2/CF 过盾**——对象是 Akamai 反爬非 CF 付费墙、MDPI 全 OA 无墙(脚本虽名 `_batch_mdpi_b2_143.py`、docstring 自称 B2,系历史命名;-151 确认 `source_used=browser_pdf_download`)。
- **入库数据(-151 定稿,coverage 重跑落盘 2026-07-02 22:31)**:`count 381 → 388`、`allow_override 6 → 13`(7 条全 allow_override)、`still_missing 618 → 611`、`by_source.browser_pdf_download` **= +7**(7 条全归 `browser_pdf_download/routeB_mdpi`)。回写用 `allow_override`(`out/routeB_mdpi_qcallow_151.txt`)。〔历史注(坑):`catal15111028`、`en11092276` 曾被 `build_coverage`「取最大 bytes」暂占**历史 websearch 错文大版**(batch=recover_b4_cf/batch6),致 by_source 一度只 +5;经 -165(task-a65ef369)归置该 2 份铁证错文旧 PDF + -151 重跑后已收敛归位——**教训:跨批『取最大 bytes』合并会被历史大体积错文压过真源,须与 QC 黑名单联动。**〕
- 与 RSC 对照:同为过盾后跨域 CDN,但 **MDPI=attachment**(拦截可抓)、**RSC=内联 viewer**(拦截也读不到、须方法D,见 P.1–P.4),解法分叉;route-B 方法C 的生产 `download.py` 路由尚待接入(P.5 边界)。

### Q.4 T0 预筛 still_missing 翻案 + 兜底源可信度 + QC 门效果
- **翻案净增**:T0 翻案 **+2 铁净增**(双 `match100`——内容 QC 双向 100% 命中,**已回写、已核验、计入当前 381**);**+4 边界**(在盘净源、真全文,但卡 QC 黑名单**假阳**,**-147 终裁**为「可救、需白名单免剔」,暂未强回写)。
- **websearch 兜底源漂移严重**:抽 6 条复核 **3 条抓错**(源漂移/同题非目标全文)——兜底搜索源**不可全信**,必须过内容 QC。
- **QC 并集门零污染**:硬黑 → 并集返工的 QC 门对 route-B/T0 产出 **100% 拦截、零污染**(无假阳漏网、无真源误杀),precision 优先口径成立(详见 L.3/L.4/M)。

### Q.5 官方净覆盖率诚实基线(以 `out/coverage.json` summary 为准,**当前权威快照 2026-07-02 22:31:41**;详见《基线口径冻结说明-388-173.md》)
- **`total_unique_dois=999`,`success`(QC 门后)`=388`,`miss=611`,`success_rate=38.84%`**(当前临时权威基线,generated_ts 22:31:41)。链路:原始去重成功 **512** → QC 剔抓错 **124**(硬黑 4 + 软黑 120) → 白名单免剔 **13** → 净 **388**。〔前一快照 381/618/38.14%(22:08,allow6)为【历史口径】〕
- **诚实路径**:**379**(旧基线,37.9%,历史)→ 381(22:08,历史)→ **388**(当前临时权威,T0+2 与 MDPI7 已回写,allow13)→ **391**(T0 终裁 **+3**,-147 采纳 apcatb/s1872/anie、驳回 1.5053761、待 -151 回写)→ **~400**(RSC 金OA 子集);**免费天花板 ≈ 39–41%**。〔原「392(T0+4)」已作废:-147 终裁驳回 1.5053761,T0 实为 +3〕
- **剩余大头**:`miss≈618`(≈600)绝大多数是**付费墙内全文**,免费路线物理边界;根本解 = **A5 机构订阅/EZproxy/Shibboleth(需凭据)**,非「更强反爬」。

*出处:153 task-ecb07009(总指挥 -144 派)｜四条结论源汇:一键冲烟(`run_all.py`+`_run_all_demo_input.txt`)、route-B 机制(续 N.2/N.3/N.8 + O.2,RSC 根因归 P 节)、MDPI7(机制复用 B2 同款拦截·归类 ⑥ browser_pdf_download〔P.5 -149/-141;-151 确认〕;count 381→388、by_source.browser_pdf_download +7(-165 归错文 + -151 重跑收敛,22:31 落盘定稿);证据 `routeB_mdpi.txt`+`_verify_mdpi_151.py`)、T0 翻案(-147 终裁 + QC 门 L.3/L.4/M)｜净覆盖口径 `out/coverage.json` summary(`success_after_qc=381/999=38.14%`,快照 22:08)｜与 -141 P 节(RSC 字节级根因)分节对齐、不重复｜-153 获《经验记录》整文件写锁串行追加,写毕即释放。*

## R. 【回写强制前置门·总指挥「规矩③」】qc-allow / 内容QC 判据回写 coverage 前必须开卷读落盘 PDF 复验 expected-doi-present 且非-SI(禁信 metadata / 标题页 / 下载期 verdict)(2026-07-02,154;总指挥 -144 派 task-1c1b2d05;承 -147『1.5053761』+ -145 O 节)

> **一句话**:凡要把某 DOI 以 **success 回写 coverage**(尤其经 `--qc-allow` 免死金牌翻正、或据下游内容QC verdict),回写属主(**-151 / -140**)**必须先开卷读该 DOI 的落盘 PDF 正文**、确认 `expected-doi-present`(正文/元数据含期望 DOI)**且不是 SI / 顶包 / 错论文**——**禁止直接采信** metadata.jsonl 的下载期 `success/verdict`、文件名编码的 DOI、或标题页看着「像这篇」。两条铁证:①`1.5053761` metadata 记 match 实为 AIP 用户指南顶包(-147 驳回);②本波 3 条 ACS `allow_override` 被 -145 判「真正文」实为 SI-only(见 R.2)。**「content-QC 说是正文」≠「落盘就是正文」**,回写前的开卷复验是不可跳过的前置门。

### R.1 规矩③(回写强制前置门)判据与归属
- **归属**:coverage 回写属主 **-151(qc-allow / coverage 回写)** 与 **-140(生产内容QC门代码)**。任何 `--qc-allow` 清单在喂 `build_coverage` 前,清单上**每条**都要过开卷复验。
- **判据(与生产门③/④同源,precision 优先·不误杀真正文)**:开卷抽全页文本(`pypdf`),PASS 需**同时**满足 ① `expected-doi-present`:正文或元数据含期望 DOI 全串 / 去前缀后半 / 数字尾号,**或**首页标题与期望题名吻合;② **非 SI**:不满足「通篇仅标题页 + `Figure S*/Table S*/Supporting|Supplementary Information` 图注、零主正文章节(Abstract/Introduction/Results/Experimental/References 章节体)」。任一不满足 → **SUSPECT**,不得回写为 success,交 -151 复核。
- **禁止采信的伪信号**(全部踩过坑):metadata.jsonl 下载期 `success=true`(只校 `%PDF`+体积)、`pdf_path`/文件名里的 DOI、标题页题名(**SI 也印同题**)、页数多 / 含 introduction·references 关键词(**SI 也有**)。
- **副本必须精确到 coverage 的 `pdf_path` / md5**:同一 DOI 常有多副本(pdfs/ 真源、rejected/ 隔离错文、跨批重复),**只读 coverage 记的那份**;按文件名 glob「取最大体积」会误读 rejected/ 里的错论文大版(见 R.3 -140 脚本教训、O.4 副本陷阱)。

### R.2 铁证二:3 条 ACS `allow_override` 实为 SI-only(开卷复核推翻 O.1/O.4「真正文 4」)
- **背景**:coverage(22:31 基线)13 条 `allow_override` 中 4 条 ACS(`publisher_oa:acs-authorchoice`)源自 -145 白名单 `out/rerun_acs_144_whitelist_back_145.txt`,O.1/O.4 记为「真正文 4、ACS 净贡献 +4」。**逐条开卷读 coverage `pdf_path`** 结果:

| DOI | coverage pdf(md5) | 页 / 正文字符 | 开卷实况 | 裁决 |
|---|---|---|---|---|
| `10.1021/acscatal.0c04429` | rerun_acs_144(2acf1a6b) | 14p / 60996 | 多章节正文,含 DOI 全串 +「ACS Catal. 2021.11:2398-2411」 | **PASS 真正文** ✓ |
| `10.1021/acs.energyfuels.5c06101` | batch6=rerun_acs_144(**c467680f**) | 7p / 3354 | p1 标题页 + p2–p7 全 Figure S1–S5 /「Supplementary Information S2」,**零主正文** | **SI-only** ✗ |
| `10.1021/acs.langmuir.7b03998` | rerun_acs_144(344f7027) | 8p / 2370 | p1 标题页 + p2–p8 全 Figure S1–S7,meta.title 为 Word .docx,**零主正文** | **SI-only** ✗ |
| `10.1021/ja509214d` | rerun_acs_144(1a2f3883) | 42p / 63593 | p1 标题+「Supplementary Information」、p2 SI「Table of Contents」、p3+ Figure S1–S24 / Table S1,33/42 页 SI 标记(「introduction/references」是 SI 散文里的词、非章节) | **SI-only** ✗ |

- **关键**:`energyfuels.5c06101` 读的正是 O.4 亲标「md5 c467680f 真正文」那份(batch6 与 rerun_acs_144 字节相同)——**同一份文件,-145 判正文、开卷是 SI**。-145 的依据「论文标题页(非SI封面)…确为正文」「全页正文(42页)」正是**只看标题页 / 页数 / 关键词、未开卷读正文区分主文 vs SI** 的失效(规矩③要防的靶心)。
- **修正**:O.1/O.4「真正文 4 / ACS 净贡献 +4」应更正为 **真正文 1 / +1**;coverage 应把这 3 条移出 `--qc-allow`(移出后回落黑名单 union/hard → miss)。**success −3(388→385)、allow_override 13→10**。异常清单已交 -151 修 coverage、heads-up -147(基线冻结)。
- **复现**:直连 coverage `pdf_path` 的开卷探针(md5 + 逐页 Figure S 铁证);另见 R.3。

### R.3 复现探针与一个必修的坑(-140 回扫脚本 find_pdf 误读 rejected/ 副本)
- **可复用探针**:`_t0_adjudicate147_probe.py` / `_mon169_verify_plus2_probe.py`(-147/-169,开卷验 expected-doi + 注释 /XMP / 文件名伪信号溯源)、`_allow_override_openbook_rescan_140.py`(-140,遍历 coverage `_qc_paths.allow_dois` 逐条开卷)。
- **坑(务必修)**:`_allow_override_openbook_rescan_140.py::find_pdf` 是「按 DOI 文件名 glob 全 `out/` 树、取最大体积那份」——会**误读 `rejected/` 里的隔离错论文大版**(实测把 energyfuels 读成 batch7/rejected 的石墨烯综述、langmuir 读成 batch4_p5/rejected),致其 SUSPECT 的「读到的文件」与 coverage 真源不符。**正解:直接读 coverage `records[].pdf_path`**(本波即用此法,结论可信)。此坑也殃及 catal15111028 / en11092276 等非 ACS 项的回扫判读,-151 复核须以 coverage `pdf_path` 为准。

*出处:154 task-1c1b2d05(总指挥 -144 派;本会话名册暂显示 -149)｜纯只读开卷复核 `out/coverage.json` 的 4 条 ACS `allow_override`(直连 `pdf_path`,md5 + 逐页 Figure S 铁证)｜承 -147『1.5053761 顶包』终裁 + -145 O.1/O.3/O.4｜异常清单交 -151(coverage 回写)、heads-up -147(388 基线冻结)、对齐 -153(Q.3/Q.5 数字连带 −3)｜-154 获《经验记录》整文件写锁串行追加,写毕即释放。*

## S. 【本波·2026-07-02 晚】备忘录秘书接手范式 + RSC 金OA 本波 8 条 route-B 全 0 净增(netgain=0/8) + content_qc 对「无正文标题垃圾PDF」漏判假阳(171)

> **一句话**:三件本波实况沉淀——①「总指挥备忘录秘书」角色接手范式(状态文件 `.secretary_state.json` + 每10分钟六项自查提醒 + 会话漂移须每轮重认总指挥);②RSC 金OA **本波 8 条(新老混合、含 2025 `d5fd00172b`)route-B 全 0 净增**(`netgain=0/8`:7 `no-pdf-captured` + 1 `challenge-page`;**推翻「老文才失败」假设**、与 P 节单条 `d5ra08493h`(2025) 成功成悖论 → **route-B 对 RSC 单条 PoC ≠ 批量可复现**);③**`content_qc` 内容门对「正文无可比对标题的纯数据 PDF」漏判**——`c4ra02037e` 抓到 NIST `tuplehashxof_samples.pdf` 竟 `success:true` 落盘(是 R 节「内容QC说是正文≠真正文」更深一层:**门自身盲区**)。

### S.1 备忘录秘书(memo secretary)接手范式(角色·记忆·交接)
- **角色**:总指挥秘书长/备忘录秘书,非总指挥、非执行主力。三职:①**每10分钟提醒总指挥走六项治理自查**(合理调度组员/全局程序化/改跑读日志闭环/经验沉淀/复杂任务先检索开源/PDF口径最快最全不过度长尾——即《总指挥-关键节点快照模板-6项自查》);②被派活时兼 worker;③经验沉淀。
- **持久记忆 = 仓根 `.secretary_state.json`**:`{last_sent, target(总指挥 sessionId), role(secretary-NNN), interval_sec=600}`。接手步骤:读状态文件 → `list_sessions` 认当前总指挥(带［总指挥］标记者) → 发本轮六项自查提醒 → 回写 role/target/时间戳 → 维持10分钟节奏。历史演进 secretary-153→167→140→**171**。
- **坑:总指挥/成员会话 sessionId 会漂移**(本波总指挥 `6bf95794`→`733e5392`;发到已离组的旧会话报「对方未进组」)。故**每轮发提醒前必 `list_sessions` 重认当前总指挥**,别缓存旧 sessionId;`report_task` 自动路由回委派人、不受漂移影响,撞车/请示优先用它。

### S.2 RSC 金OA 本波 8 条 route-B **全 0 净增**(坐实 netgain=0/8;推翻「老文才失败」假设)
- **权威结果**(-156 生产脚本 `_routeb_rsc_launch_156.py` → `out/routeB_rsc_launch/summary.json`,已 exit0):`total=8, pdf_ok=0, **netgain=0/8**`。逐条:**7 条 `no-pdf: no-pdf-captured`**(过盾后没抓到 PDF 字节)+ **1 条(`c4ra14572k`)`blocked:challenge-page`**(CF 盾没过);每条耗 125–226s。生产脚本直调 `render_download_pdf_bytes(headless=False, pdf_url_fallbacks=build_static_candidates(doi), lock_path)`——与 -141/-152 验 b2-viewerfetch 同款路径。
- **8 条新老混合、全败**:老 `c4ra*`(c4ra00825a/02037e/14572k 2014、c5ra04969e 2015)与**新文**(`d0gc02302g` 2020、`d2gc02623f`、`d3ee02589f`、**`d5fd00172b` 2025**)**一并 0 净增**——**推翻本节初稿「老文才 CF 失败」假设**:连 2025 新文也 `no-pdf-captured`。
- **对照 P 节的悖论**:-141 早前单条 `d5ra08493h`(2025 RSC Advances)route-B **成功 484KB**,本波 8 条(含 2025 `d5fd00172b` Faraday Discuss)**全败**。差异疑来自:①CF 实时风控波动(PoC 单条 vs 本波连续开浏览器被加严);②期刊/CDN 路径差(ra vs fd vs gc vs ee);③浏览器/会话状态。**核心教训:route-B 对 RSC 的单条 PoC 成功 ≠ 批量可复现**;主要失败态 `no-pdf-captured`(过盾但没拿到 viewer 字节)与 P.1–P.4「签名 CDN 内联 viewer」根因一致,说明方法D b2-viewerfetch 本波未能稳定落字节。
- **另一路 `run_all --route-b all`(写 `fetch/` 子层)口径**:route-B 部分报 `cloudflare-challenge(http-403)`(触发路径不同、结论同为 0 真净增);websearch 兜底还引出 NIST 假阳(见 S.3)。⚠️ **两套跑法写同一根 `out/routeB_rsc_launch/`**:`_routeb` 脚本写**顶层** `summary.json`/`metadata.jsonl`(权威 route-B 结果)、`run_all` 写 **`fetch/` 子层**——读结论务必认顶层 `summary.json`。
- **真根因(-165 情报实锤,修正上文「疑 CF」)**:RSC 除 CF 外还有**第二道 `crawlprevention/governor` rate-gate**——`Validate User` reCAPTCHA(真机截图实锤),**第一篇过、第二篇即死**,且发的是 `Invalid domain for site key` 的**坏 reCAPTCHA(打码平台也解不了)**;触发器=**直接导航 `ArticlePdfHandler.ashx` + 速率**。这**完美解释本波 0/8 与 P 节单条 d5ra08493h 成功的悖论**:`_routeb_rsc_launch_156.py` 直调 `render_download_pdf_bytes` 导航 articlepdf 直链 + 连续 8 条快跑,正好踩 governor(第一篇过、第 2+ 篇全 `no-pdf-captured`/`challenge-page`)。补丁方案见《选型2026-route-B反RSC-governor补丁方案-165.md》:①governor 检测(`blocked:rsc-governor`)②先导航文章 landing 预热再页内 fetch(别直怼 PDF handler)③per-host 限速 30-90s + 命中后冷却退避。
- **ROI 结论**:本波 RSC 金OA(8 条)route-B 回收 = **0 真净增**,按用户「RSC 本波跑完前不扩 route-B」收口;RSC 金OA 子集**不宜再投 route-B 长尾**,除非先落 -165 的 governor 补丁(landing 预热 + per-host 30-90s 限速冷却 + governor 检测)后再小样本验证。

### S.3 content_qc 对「正文无可比对标题的纯数据 PDF」漏判假阳(铁证:c4ra02037e→NIST PDF)
- **铁证**:`c4ra02037e`(RSC 化学「steam reforming of bio-oil…」)经 websearch 抓到 `https://csrc.nist.gov/.../examples/tuplehashxof_samples.pdf`(NIST 密码学 hash 示例数据),`metadata.jsonl` 记 `success:true`、落盘 `pdfs/10.1039_c4ra02037e.pdf`(86307B)、`source_used=websearch`——**`content_qc` 未判 mismatch**(同条前面 jrheum 等 score48<50 均被判 mismatch,唯此纯数据 PDF 溜过 `ok:true`)。
- **根因推测**:content_qc 靠 `pypdf` 抽正文首页 + 元数据 title 做 rapidfuzz 标题模糊匹配;NIST 示例 PDF **无可比对标题文本**(通篇 hash 数据),疑抽不到有效文本 → score 未达 mismatch 阈值 → **误放行**。即门对「抽不到可比对内容」的处理非 fail-closed。
- **处置与待办**:已 heads-up 守门人 **-149**(『开卷读落盘PDF验expected-doi』回写强制门,验 `c4ra02037e` 必不 match→拦)、请其加 QC 黑名单并与回写手 -151 对齐——**回写侧已可拦、不会进 coverage**。但**下载侧 content_qc 门本身盲区待修**(-140/-147 门代码:抽不到可比对标题/正文 → 应 SUSPECT 拒收,而非放行)。是 R 节「内容QC说是正文≠真正文」的**门内自证**一例(R 节讲「别信 metadata」,S.3 是「门自己也会漏」)。
- **守门人开卷坐实(闭环,-149/-154 @6f0a4973,2026-07-02)**:开卷 `pdfs/10.1039_c4ra02037e.pdf`(md5 `dabd2c09`,86307B,20p)meta.title=`'Secure Hashing - TupalHashXOF-Samples'`、正文全 NIST SHA-3 TupleHashXOF 测试向量,expected-doi `c4ra02037e`/`02037e` 均 False → **SUSPECT-WRONG-CONTENT**,回写强制门(R.1 判据①)必拦。**关键安全事实**:`out/routeB_rsc_launch/fetch` 是**嵌套实验目录、默认不进权威 coverage 扫描**,故该假阳**当前未污染**权威库(它在权威 coverage 里本就是 miss=`success-metadata-but-pdf-missing-on-disk`、在 still_missing);-154 已转 -151 加黑名单(doi=`10.1039/c4ra02037e`、源 websearch)防经 `--nested/--extra-dirs` 回写,content_qc 盲区已同步 -147。**教训**:嵌套实验 out_dir 与权威 coverage 分离是道天然防线,但 `--nested/--extra-dirs` 合并回写时必须联动 QC 黑名单,否则此类门漏假阳会趁合并入库。

*出处:171(本会话,承接「总指挥备忘录秘书」secretary-140→171)｜S.1 据 `.secretary_state.json` + 接手实况;S.2/S.3 只读监控 -156 的 `out/routeB_rsc_launch/`(`metadata.jsonl`/`attempts.jsonl`,截至第3条、pid51976 在跑)｜S.2 route-B 8 条完整样本、S.3 下载侧门修复待后续补｜已 heads-up -149(假阳拦截)/总指挥 733e5392(进度+写锁A/B/C)｜-171 获《经验记录》整文件写锁串行追加,写毕即释放｜与 P 节(RSC 字节根因)/R 节(回写前置门)分节对齐、不重复。*

## T. 【本波定论七件·2026-07-03 收口】nl200722z 纠偏 + coverage churn 权威 recipe + CF 立场(避验证非破验证) + Wiley bug + A5 框架就绪 + QC 门位置权重弱点 + OpenAlex key 定位(145 单写沉淀;总指挥派 task-482abd43)

> **一句话**:把本波七条定论去重固化一处——①`nl200722z` 是真正文,SI 自动扣减用 mon176/33 不用 145/41(**防误杀**);②coverage churn(多人抢写致 385→362→…→~343)根因 = 漏参+抢写+allow 污染,权威 recipe = 单写者/`--no-write` 试算/写前备份/防回归 check(详见《coverage 口径重算规范与本波churn复盘-144.md》);③CF 人机验证立场——route-B 只过**软盾**(CF Turnstile/JS 质询)拿 OA 字节,**RSC governor / CF managed / JA3 绑定回放三类硬墙到顶**(-165 全套补丁真机 8 金OA **8/8 净增 0**、第一篇过第二篇即死),根解 = **A5 凭据用合法会话「避开」验证,而非「破」验证**;④Wiley `pdfdirect` URL 遗留 DOI 截断/编码 404 已修;⑤A5 机构订阅框架(auth-session + 安全凭据 config + route-B cookie 注入契约)已就绪,唯一 gate = 用户凭据;⑥QC 门弱点——开卷/`expected-doi-present` 把**参考文献区 DOI 误计 in-text**(`aic.690210612` 实锤),拟加**位置权重**;⑦OpenAlex key 作用 = **解限速/防 429 静默降级**,**不解锁订阅墙**(净增点估 ≈+1)。
>
> ✅ **coverage 数字已定版回填(2026-07-03,-155 纠偏写盘后)**:本节净覆盖以 `out/coverage.json` **`summary.success` = 339 / miss = 660 / total = 999 = 33.9%**(`generated_ts` **2026-07-03 01:27:42**;构成 `success_before_qc 514 − rejected 175(硬黑20+软黑155) + allow_override 10 = 339`)为**唯一权威**,与《基线口径冻结说明-388-173.md》header/§一 一致。**follow-up 上行(PDF 在盘/待迁,未并入 339)**:T0 终裁 **+3** · SciOpen **+1** · OCR14 **HOLD**。churn 时间线里的 385/376/343/340 等为**历史争写快照 / 中途快照**(证据留档),非当前权威。〔-145 承 task-482abd43,于 -155 定版广播后回补;原占位「339/预计+2后~341/待广播」已作废。⚠️ 数据卫生:冻结说明 §三/§五/footer 与 `项目索引.md` 仍残留 **340/00:42:19** 旧值,待属主(-159/-155)对齐到 339/01:27:42。〕

### T.1 nl200722z 是真正文——SI 自动扣减名单边界(防误杀)

- **铁证**:`10.1021/nl200722z` 在旧清单 `batch6_acs_si_reject_145.txt`(41 条)里被标 SI,但**真 PDF 重扫**(`_batch6_acs_si_scan_145.py`)与 **-176 稳健分类器**均判 **ARTICLE(真正文)**。
- **扣减铁律**:batch6 ACS SI 自动扣减**只用** `out/_mon176_acs_si_still_success.txt`(**33 条铁证 SI**),**勿整份吞旧 41**——多出的 8 条 = **7 条 DOUBT + `nl200722z` 真正文**,整份吞会**误杀 1 真正文 + 混入 7 存疑**。
- **口径联动**:本条即 -141 波审计《经验记录完整性审计-141波.md》标注的「缺失 T.1」;R.2 只纠了 4 条 `allow_override` 里的 3 条 SI-only,**未覆盖** batch6 仍 success 的 33 条 SI 清单边界与 nl200722z 保留规则,故在此单列成条。
- **属主**:SI33 扣减清单交 **-151 第二波 merge**(144 定序);176/145 **不擅自改盘**。

### T.2 coverage churn 权威 recipe(多人抢写复盘 + 防再犯)

- **churn 现象(历史争写快照,证据 `coverage 口径重算规范与本波churn复盘-144.md` §一)**:同一波 coverage 被反复 `--write` 覆盖,`success` 组成漂移:**385(干净基线)→ 362 → 368 → 380 → 343(误态)→ 重置回 385**;`allow_override` 一度从 10 **掉到 0**。
- **三大根因**:
  1. **漏参回归**:未传 `--qc-allow` → `allow_override` 10→0(MDPI7 / T0+2 / 真正文 acscatal 全被黑名单误剔);未传 `--extra-dirs` → `success_before_qc` **513→471**(二级 rerun 目录漏扫)。
  2. **多人抢写**:两个会话(如 174 与 155)并行 `--write`,三次覆盖 `coverage.json` → 组成漂移。
  3. **allow 清单污染**:混用多份 qc-allow 未排除**已驳回** `10.1116/1.5053761`(AIP 用户手册),又漏纳 `acscatal.0c04429`(-149 保留的唯一 ACS 真正文)与 SciOpen `10.26599/nr.2025.94907426`(-160 开卷翻案)。
- **权威 recipe(固化,防再犯)**:
  - **单写者制**:`out/coverage.json` 每波**仅 1 人写盘**(本波 -155),其他人只产黑名单 CSV / qc-allow 清单 / `--no-write` 试算;委派任务必须标注「只读试算」或「唯一写盘权」。
  - **写盘前三步**:①`--no-write --print-json` 试算,**逐 DOI 核对 `allow_override` 组成**(非只看总数)+ `success_before_qc` + rejected 数;②`Copy-Item out/coverage.json out/coverage.bak_<who>_<reason>.json` 备份;③**一次 `--write`**,写后 `generated_ts` + summary ping 总指挥定稿。
  - **防回归 check 清单**(节选):是否传 `--qc-allow`(漏传最严重)?是否传完整 `--extra-dirs`?allow 是否排除 `1.5053761` 与 -149 三条 SI?是否纳入 `acscatal.0c04429` + SciOpen?是否未纳入在途实验 batch(如 `p2_cf_soft`)除非明示?wave2 是否勿误杀 `nl200722z`?
- **详版**:配方全文(扫描集 / allow=14 组成 / QC 参数 / 阶段 A–D 命令模板 / 备份命名)见《coverage 口径重算规范与本波churn复盘-144.md》,**本节不重述**,只固化「单写者 + 三步 + 防回归」纪律。

### T.3 CF 人机验证立场:route-B 只过软盾、硬墙到顶、根解是「避验证」非「破验证」

- **三层分开看(承 N.2/N.3/N.8)**:①**软盾(可过)**= CF Turnstile / JS 质询——nodriver 真 Chrome 端到端已过(N.1/N.8,`acsomega.6c04195` CF 6.6s 过落 13.7MB %PDF);②**硬墙(到顶)**= (a) **RSC `crawlprevention/governor`** 应用层 rate-gate(`Validate User` reCAPTCHA,真机截图报 `Invalid domain for site key` 的**坏码**,**真人点不过、2captcha/anticaptcha 也解不了**,按速率/行为触发、**第一篇过第二篇即死**);(b) **CF Managed Challenge** 强站;(c) **JA3 绑定回放**(solve 拿 cf_clearance → 异客户端 curl_cffi 回放,JA3≠真 Chrome → 强 CF 站必 403)。
- **实码验证(-165 governor 全套补丁真机)**:落地 P1–P5 补丁(`blocked:rsc-governor` 检测 + articlepdf 前 articlelanding 预热 + per-host 30–90s 限速 + 命中 300s 冷却 + 去 `--disable-blink-features=AutomationControlled` + 点 "Take me to my Content")后,8 条 RSC 金 OA 真机复跑 **pdf_ok=0/8、真净增 0/8**(7× `blocked:rsc-governor` + 1× `no-pdf-captured`);补丁**可观测但未破墙**(本机会话/IP 仍被 governor 压制)。与 S.2(netgain=0/8)、-160《route-B RSC 0-8 归零因与收口结论》一致。
- **战略定论**:方向是**「不触发 governor」**,绝非「解那张验证码」——硬解是死路(坏 reCAPTCHA 无有效 site_key、域不匹配)。**真·根解 = A5 机构订阅**:用**合法凭据**走认证通道,在**已登录会话内**下载,既**避开**公开 IP 上的 CF/人机验证,又**突破**付费墙内全文——即「**避开验证,而非破验证**」。个别长尾/交互式 corner case 允许人工浏览器一键补下,**不**为此改造主线去「强破验证」。
- **边界**:RSC 在 batch6 口径净 MISS≈0(websearch 已从别处兜回),单独为 RSC 上 route-B 破盾边际≈0;RSC 订阅刊 ~59 条、Elsevier/ACS 主体仍唯 A5。

### T.4 Wiley `pdfdirect` URL bug:遗留 DOI 截断/编码 404 已修

- **问题**:遗留 Wiley DOI(含 `(` `)` `:` `<` `>` `;` 等特殊字符)构造 `onlinelibrary.wiley.com/doi/pdfdirect/…` 时**未 encode 后缀** → 浏览器/HTTP 栈把 `:` 等当路径分界 → **404 / 截断**。用户实证 `10.1002/1099-0739(200012)14:12<836::AID-AOC97>3.0.CO;2-C`。
- **修法**:保留 `prefix/suffix` 间 `/` 不编码,对 **suffix 整段** `urllib.parse.quote(suffix, safe="")`。三文件各新增 `_wiley_doi_path()`:`sources/publisher_oa.py`(`_wiley_openonline` 同产 **pdfdirect + pdf** 双链、均 encoded)、`sources/publisher_direct.py`(Wiley 模板填 encoded DOI)、`publisher_adapter.py`(`pdf_candidates()` 对 `key==wiley` encode)。**现代 DOI(无特殊字符)明文路径不变**。
- **验证**:三模块新增 legacy Wiley 断言,离线通过(`PUBLISHER_OA_OK` / `PUBLISHER_DIRECT_OK` / `PUBLISHER_ADAPTER_OK`、`compileall` exit 0);纯 bugfix,未做真网下载验证、未动 render_fetch/coverage/run_all。详见《Wiley pdfdirect URL bug 修复:改了啥+selftest.md》(-174,task-3a1d3e38)。

### T.5 A5 机构订阅框架就绪(机制无关层 + CLI 接入 + route-B 契约;唯一 gate = 用户凭据)

- **已实现层(离线 selftest 可验,新增 `fulltext_fetcher/institutional/` 包)**:
  | 模块 | 路径 | 职责 |
  |---|---|---|
  | 凭据加载 | `credential_store.py` | `FTF_*` env + 本地 `.ftf_institutional.local.json`;**严禁硬编码/日志泄露**,`__repr__` 自动 redact |
  | Cookie 持久化 | `cookie_store.py` | JSON + 过期,按 provider 分区 |
  | Auth Session | `auth_session.py` | 加载→注入 `Config`→`verify_live` 离线 gate;`bootstrap_institutional_config()` |
  | route-B 桥接 | `route_b_bridge.py` | `RouteBInjectionPlan` 契约、`plan_route_b_injection()`(纯数据计划) |
  | 离线自检 | `selftest_a5_framework.py` | 打印 `A5_FRAMEWORK_OK` |
  | 配置模板 | `.ftf_institutional.local.json.example` | 复制为 `.ftf_institutional.local.json`,**勿提交 git** |
- **CLI 接入(本波)**:`cli.py` / `run_all.py` 在 `Config` 构造后调 `bootstrap_institutional_config(cfg, cli_institutional=…)`(CLI 已填字段优先);启用后自动插 `publisher_direct` 源。route-B 注入 `inject_institutional_session` + `render_download_pdf_bytes(injection_plan=)` **已合入**(-141/-160,`run_all --selftest` RUN_ALL_OK)。
- **待补(机制层,-153 owner)**:`open_login_browser()` nodriver 可见 SSO、`verify_live` 联网 HEAD 探针、Shibboleth/OpenAthens/CARSI/WebVPN 登录启发式、Cookie 刷新。
- **定论**:**唯一 gate = 用户凭据,无凭据 ROI=0**;A5 可救前缀 **≈94% still_missing**(Elsevier+ACS+RSC 占 85%)。实现路径已定——**机构 cookie 注入 nodriver 真 Chrome → 复用 route-B 页内 `fetch()` 直下**,与现有 CF 过盾链焊成同一条 A5 链,不引第二套浏览器栈。详见《A5接入框架就绪-176.md》《检索成果-A5机构订阅框架CLI接入与route-B契约-174.md》。

### T.6 QC 门弱点:`expected-doi-present` 把参考文献区 DOI 误计 in-text(拟加位置权重)

- **弱点**:开卷复核 / 回写强制门的 `expected-doi-present` 正信号**只查「期望 DOI 是否出现在正文」,不看出现位置**——若一篇**错论文**在其**参考文献列表**里恰好引用了期望 DOI,全文扫描即判 `expected-doi-present=True` → 误当 match / 真正文。**实锤 `10.1002/aic.690210612`**(该 DOI 现已在 `out/batch6/rejected/`,系错文假阳)。
- **与现码呼应**:生产门②(`_qc_doi_publisher_conflict`)**已用「正文首 1200 字规避参考文献区」**、门③ body-embeds 窗口取 ~1500(-148 实测:cctc 需 ≥1500 才抓到、且 6 真正文不误杀)——**但这是"异 DOI 反证"侧的位置规避**;`expected-doi-present` **正信号侧尚未加同款位置权重**,是 R 节「内容 QC 说是正文≠真正文」的又一门内盲区(承 S.3「门自己也会漏」)。
- **拟修(位置权重)**:期望 DOI 须出现在**首部 / 标题相邻区**(而非仅出现在文末参考文献列表)才算强「就是这篇」信号;仅在参考文献区命中 → 降权、不足以单独判 match,须配 `has_body` / 首页标题吻合。落点交门属主 **-140**(承《选型2026-QC并集门增强建议-recover_b4_cf假阳-173.md》门③/④/⑤ 规格),并补 `tools/regress_qc_union_189.py` 负样本(`aic.690210612` 必判非-match)。

### T.7 OpenAlex key 作用定位:解限速/防 429 静默降级,不解锁订阅

- **背景**:OpenAlex 自 **2026-02-13** 起强制 API key(免费);本仓 `config.openalex_key` 字段已在、CLI 有 `--openalex-key`/`OPENALEX_KEY`。
- **作用(定位止损)**:**无 key = 100 credit/天(仅 demo)→ 主线定位源易 429/401/403 静默降级**;**免费 key = 100,000 credit/天 ≫ 本仓 999 DOI 用量**(singleton 按 DOI 单条查成本极低)→ 成本≈0、防降级。是**主线源运营止损项**(5 分钟可补)。
- **不解锁订阅(净增极低)**:-176 探针全量扫 still_missing **660 条 → 内容 QC 通过仅 Z=1/660(≈0.15%,`10.35848/1347-4065/ad280f` IOP)**。命中的多为 Elsevier 缩略图 JPG / ScienceDirect·Wiley·RSC 403 / landing 非 PDF——需 **route-B / A5** 才有二次转化,**不属 OpenAlex key 本身能解决**。即 **key 解限速、不解付费墙**。
- **坑(待修)**:`python -m fulltext_fetcher -f still_missing.txt` 的 **env→Config 闭合、key 生效** ✅;但 **`run_all.py` 批量仍不透传 `openalex_key`** ❌——若要一键正门用 key,需在 `run_all.py` Config 构造处补 `openalex_key=os.environ.get("OPENALEX_KEY")`(本波未改代码,交总指挥/运维)。运营层已 `setx OPENALEX_KEY`。详见《OpenAlex-key净增探针-176.md》。

*出处:145 task-482abd43(总指挥派,《经验记录》唯一单写者)｜七件定论源汇:①nl200722z = `_batch6_acs_si_scan_145.py` 重扫 + -176 分类器 + 《经验记录完整性审计-141波.md》缺失 T.1;②churn recipe = 《coverage 口径重算规范与本波churn复盘-144.md》(不重述,只固化纪律);③CF 立场 = N.1/N.2/N.3/N.8 + S.2 + 《RSC governor 攻坚结果》(-160) + 《选型2026-route-B反RSC-governor补丁方案-165.md》 + 项目总报告 §1.8;④Wiley bug = 《Wiley pdfdirect URL bug 修复》(-174);⑤A5 = 《A5接入框架就绪-176.md》+《检索成果-A5机构订阅框架CLI接入与route-B契约-174.md》(承 O.2/N.4 ROI);⑥QC 位置权重 = `download.py::_qc_doi_publisher_conflict`(首1200字)+ 《验证-门③异DOI反证-99PENDING-148.md》(body@1500)+ `aic.690210612` 实锤(现 `batch6/rejected/`),承 R/L.5/M/S.3,落地交 -140;⑦OpenAlex key = 《OpenAlex-key净增探针-176.md》(Z=1/660)+ 《选型2026-C4异步限速实现者参考-150.md》(限速头口径)｜**coverage 数字已定版回填 = `out/coverage.json` `summary.success` 339/999 = 33.9%(`generated_ts` 2026-07-03 01:27:42;514−175+10,allow10)**,churn 里 385/376/343/340 为历史/中途快照,原占位已作废(-145 于 -155 定版广播后回补;冻结说明 §三/§五/footer 与项目索引残留 340 待属主对齐)｜-145 获《经验记录》整文件写锁串行追加,写毕即释放｜与 O/P/Q/R/S 节分节对齐、不重复。*

## U. 【质量门·2026-07-03 收口】离线 selftest 必须旁路真限速 + 源文件工具伪影门 + 唯一数字源规范(158 秘书盘点 → 145 单写补登)

> **一句话**:本波质量门收口沉淀三条工程卫生定论——①离线 selftest **绝不能踩真 per-host 限速/退避/冷却 sleep**(render_fetch 自检 180s TIMEOUT 真因 = mock 路径未旁路 `_host_capture_gate`,非死锁);②AI 工具调用的 XML 标签(`</parameter>`/`</invoke>`/`antml`)误粘进 .py 源码尾部 → 非法 Python 破 `compileall`,回写/交接前须全仓伪影扫描;③一切覆盖率/miss/override 数字**以 `out/coverage.json` 为唯一真相源、展示文档引用而非手抄**——本波真相源一天内 340(00:42:19)→339(01:27:42)→326(OCR14 定版中) 连改,手抄必 off-by-one。

### U.1 render_fetch 自检 180s TIMEOUT 真因 = host-gate 未旁路(离线 selftest 铁律)
- **现象**:A5 注入合入后,`run_all_selftests.py` 中 `render_fetch` 单项 **exit=124 TIMEOUT after 180s**,套件 FAIL=1(154 全绿后的新回归)。
- **真因(142 实锤)**:`render_download_pdf_bytes(..., _capture_fn=mock, min_interval=0.0)` 第 2 次调用即卡 **56.35s**——是 `_host_capture_gate`(RSC per-host 限速)的 `time.sleep`;`min_interval=0` 只关 `_throttle`、**关不掉 host gate**。`_selftest_bytes` 对 `pubs.rsc.org` 有 5 次 mock 调用 → 累计 >180s。
- **误判排除**:`asyncio.run(_run_inject())`(MockTab) 与 item13 `_nodriver_capture_fn`(只建闭包不起浏览器)均秒回,非因;文件锁 item12 `_SINGLE_HEAD_STALE_SEC=600` < 预置陈旧 660s,立即接管不挂。
- **修法**:注入 mock `_capture_fn` 时**跳过 `_throttle`+`_host_capture_gate`**(生产不传 `_capture_fn`、字节路径不变)。修后 `render_fetch --selftest` = **3.07s / RENDER_OK**。
- **铁律**:**离线 selftest 绝不能踩真限速/退避/冷却 sleep**;凡有 per-host gate/backoff 的模块,mock 路径必须旁路真 sleep,否则「假死」表现为 TIMEOUT FAIL。(延伸嫌疑:`selftest_institutional` 67.9s 疑似同类,待另开工单。)

### U.2 源文件工具调用粘贴伪影 → compileall FAIL(防范门)
- **现象**:`fulltext_fetcher/institutional/ezproxy_login.py` 末尾混入 `</parameter>`/`</invoke>`(正确 EOF 应在 `print("EZPROXY_LOGIN_OK")`)——AI 工具调用 XML 被误粘进源码,**非法 Python → `python -m compileall` FAIL**,拖 run_all_selftests 红。
- **处置**:owner(149)删尾部伪影行;**全仓 `*.py` 扫描确认仅此一处**(`rg "/parameter|/invoke|antml"`)。
- **防范**:① `run_all_selftests` 的 compileall 门已能拦(守住即可);② 回写/交接前跑一次全仓伪影扫描;③ AI 写码后自查文件尾部无工具标签残留。

### U.3 唯一数字源 = out/coverage.json(防手抄漂移;本波 340→339→326 连改实例)
- **规范(写死)**:一切覆盖率/miss/override 数字**以 `out/coverage.json` `summary` 为唯一真相源**;展示文档(项目总报告、基线口径冻结说明、项目索引、still_missing 分桶)**引用而非手抄**,定版后由**单写者/秘书按分工一次性回填**,严禁多人各自手抄。
- **本波漂移实例(活生生的教训)**:一天之内主库被多次重算,真相源接连变化——`388(22:31)→385(22:42)→380(00:10)→340/659/override11(00:42:19)→339/660/override10(01:27:42)→326(OCR14 开卷:13 WRONG + 1 TRUE=acssuschemeng.4c05125,-144 --no-write 验证中/写盘前)`;展示文档屡屡滞后(冻结说明/项目索引一度停在 340 与自身 header 自相矛盾,-145 本波按当时现值对齐 340→339、随后按分工由 -158 一次性回刷 326)。
- **分工纪律(防两写相撞)**:coverage.json 单写者(本波 -144/-155)、展示文档 headline 回刷单写者(本波 -158)、《经验记录》单写者(-145)各司其职;跨文件依赖用 send_to_session/写锁串行,别抢定稿。
- **口径**:诚实净覆盖 ≈33.9%(339/999,写盘前现值)→ 326 定版后 ≈32.6%;免费手段天花板 ≈40–42%(承 T 节② / Q.5 双层口径)。**最终 headline 数以 `out/coverage.json` 定版为准,本节不硬编。**

### U.4(pointer)TLS 指纹开源专项已成文
- 见《过认证墙开源方案检索-TLS指纹-147.md》(-158):库选型沿用 -148(curl_cffi 已吸收为代码)、Go 系(azuretls/utls/tls-client/requests-go)不引入、RSC 唯 A5(=T 节③ CF 立场)。

*出处:U 节 = -158《经验落档缺口清单-158-2026-07-03.md》§二盘点草稿(U.1/U.2/U.3/U.4)→ -145 单写补登(task-482abd43,《经验记录》唯一单写者)｜U.1 render_fetch host-gate 真因 -142 实锤;U.2 ezproxy 伪影 owner -149;U.3 唯一数字源规范 + 本波 340→339→326 漂移(coverage.json 真相源,-144/-155 写盘、-158 headline 回刷)｜⚠️ U.3 未硬编最终 headline:待 -144 的 326 定版信号 + 最终 ts 后,T 节数字由 -145 一次性 339→326 回补｜-145 获《经验记录》整文件写锁串行追加,写毕即释放｜与 T 节质量门/回写前置门(R)分节对齐、不重复。*

## V. 【监管者巡检·2026-07-03 晚】主库口径不可一键复现契约 + single-writer-per-file 双改防治 + out/ gitignore 验盘盲区 + 总指挥角色漂移 + websearch假阳8仍计success/开卷门title误判 + claimed_no_pdf336全属QC移错(0可复下)(145 监管巡检单写沉淀;总指挥 -157 任命秘书#6「记录副手」派)

> **一句话**:本波监管巡检五条定论——①主库 headline **326 一键 run_all 复现不出**(默认口径现算 312、复刻 extra_dirs 现算 317),因 `run_all.py` 的 run_coverage 封装不传 extra_dirs/nested、且只写 RUNROOT 不写全局 `out/coverage.json` → **契约=一键须确定性复现自报数**;②无 sandbox 下多成员写同一文件必须 **single-writer-per-file + 写锁串行**;③`out/` 被 `.gitignore` 忽略 → ripgrep/Glob 默认扫不到,一度误判"主库不存在",**审计验盘必须用 Get-ChildItem/os.walk/`rg --no-ignore`**;④**总指挥身份以 list_sessions 的 ［总指挥］ 为准**,roster footer 会滞后(本波 footer 写 146、实为 157);⑤8 条 websearch 假阳仍计 success(326→318)、claimed_no_pdf 336 经查 **100% 属 QC 移错(真可复下=0)**。

### V.1 主库 headline 不可一键复现 = 口径陷阱 + 可复现契约(证据:`--no-write` 现算)
- **现象**:`out/coverage.json` headline success=**326**/999(`generated_ts` 2026-07-03 12:50:24),但一键默认口径现算只 **312**、复刻其 extra_dirs 现算 **317**(均 `python tools/build_coverage.py --no-write`,verify_allow=False)。三数不一。
- **根因**:① `out/coverage.json` 的 `_scan.caliber=experimental`,靠人工 `--extra-dirs rerun_elsevier_143/fetch,rerun_acs_144/fetch,rerun_wiley_144/fetch,t0_recover_156/fetch` 手跑;② `run_all.py:96` 的 `run_coverage` 封装**不传** extra_dirs/include_nested → 默认恒为一级权威口径(312);③ `run_all.py:741` 只写 `RUNROOT/coverage.json`,**从不写全局 `out/coverage.json`** → 全局主库是人工产物,一键跑不出。
- **326 vs 317(-9)**:verify_allow 只会减/持平 success(同态 True≤False),而 disk(True)=326 > extra(False,now)=317 → 差额为 12:50 后 out/ 被组员进程改动的**真漂移**(≥9),非口径差。
- **契约(交 -144 coverage 单写落地)**:一键 run_all 必须**确定性复现它自报的数**——二选一并写死:(a) 默认一级=权威(诚实数 ~312,扣 ≤8 假阳后 ~304–307);(b) 把已过 QC 的 extra_dirs 提级确定性写进 run_all。无论哪种 + 加写锁。承 T.2 churn recipe、U.3 唯一数字源。

### V.2 single-writer-per-file 协同双改防治(无 sandbox 铁律)
- **风险**:本波 list_sessions 见 5 python+7 powershell+3 node+20+ chrome 并发,多成员同跑 route-B / recover,写同一 `out/`;主库单文件 `coverage.json` 无写锁被人工重算 → 撞车(承 I.1 batch7 实锤、T.2)。
- **铁律**:凡写共享文件(coverage.json / 展示文档 headline / 经验记录)前 `acquire_write_lock(scope=文件路径)`,写毕 `release_write_lock`;**一文件一单写者**(coverage=-144/-155、headline=-158、经验记录=-145),跨文件依赖用 send_to_session 串行,别抢定稿。**本节即在写锁下追加**(言行一致)。

### V.3 out/ 被 gitignore → 审计验盘盲区(工具选择教训)
- **坑**:审计初期用 `Glob out/**/*` 与 `rg` 找 `coverage.json`/`attempts.jsonl` **全返 0**,一度误判"磁盘无主库、无任何 run 产物"。真因:`out/` 在 `.gitignore` 内,**ripgrep/Glob 默认遵守 .gitignore 跳过**。
- **对策**:审计"地面真相"(产物/PDF/日志)必须用**不忽略 gitignore 的工具**——`Get-ChildItem -Recurse` / `os.walk` / `rg --no-ignore`;`out/` 实为非空(2656 项/477KB,含 `coverage.json` 591KB + 各 RUNROOT `attempts.jsonl`)。**证据协议延伸**:真相源在 gitignore 里是常态,别用 git 感知工具下"不存在"结论。

### V.4 总指挥角色漂移:身份以 list_sessions 为准(roster footer 滞后)
- **坑**:任务附带的 roster footer 写"总指挥=146(sessionId=0eea4a09…)",但 `list_sessions` 显示该 sessionId 实属 **谷歌学术人机认证-157 ［总指挥］**,146 是另一 sessionId 的普通 worker。footer 缓存滞后 / 角色已轮换。
- **对策**:上报/请示前**必 list_sessions 确认当前 ［总指挥］ 与其 sessionId**,勿信 footer 缓存;本波据此把 NOTIFY 正确发给 157(非 146)。承 D 节协作经验。

### V.5 websearch 假阳 8 条仍计 success + 开卷门 title_ov 误判(承 L/R/T.6)
- **实锤(全 `--no-write` 试算)**:live `out/coverage.json` success=326 仍含 8 条 websearch 假阳(`_websearch_fp7_141` 7 ∪ `_mon157_websearch3` 3 去重 8),**均 status=success/source=websearch/qc=None、不在任何黑名单**;《监管者176-HOLD总汇总》"全已并"对这 8 条不成立(与 mon176_websearch9 是不同集)。并入后 **326→318 / miss 673→681**(delta 现算恰 -8)。
- **355744 三件套**:`10.1016/0021-9517(87)90366-6` / `0304-5102(82)85049-9` / `j.apcatb.2017.01.076` 磁盘实测均恰 355744 字节、开卷正文长均 12416 → **一份错 PDF(Hydrogenation lab safety Fact Sheet)套 3 DOI**,铁证。
- **开卷门新盲区**:`build_coverage._allow_openbook_verdict` 对 8 条中 **5 条误判 True**(title_ov≥0.5:0.70/0.50/0.60/0.60/0.62)——错文与真论文共享领域词汇致 title 重叠虚高;仅 3 条 verdict=False。**8 条全部 doi_present=False**。→ 可靠判据 = **doi_present=False + 151 人工 verdict=mismatch**,纯 title 重叠不足以判真(承 L.5/R/S.3/T.6「门自己也会漏」);故这 8 条**必须走黑名单并入,不能靠自动门捕获**。

### V.6 claimed_no_pdf 336 = 100% QC 移错(真可复下软缺口=0)
- **反直觉定论**:`out/coverage.json` `claimed_success_but_no_pdf=336`(> 净成功 326),初疑为大块可回收软缺口;逐条查:**在 `qc_rejected_manifest` 命中 336/336(100%)、union 黑名单 336/336(100%)、hard 48、uncertain 23、不属任何黑名单=0**。
- **含义**:336 全是 QC 判错、cleanup 物理移出 pdfs/ 的错文,PDF 缺失正是 cleanup 正确工作;**里面无隐藏净增**,不值得为其花回收波(同源重下只会再拿同一错文)。三方(manifest/union/coverage)内部一致 → 数据卫生健康。

### V.7(pointer)有头移屏外过 CF 已成文
- "过 CF 解题器【默认有头+移屏外】vs route-B 抓字节【默认无头】、统一 `FTF_HEADLESS` 翻转"已在 **N.9** 固化(-146/-157,`_verify_headless_157` 实证);本波不重述,仅登记为可复现契约的一部分(有头窗口移屏外 = 无人值守下过软盾的既定手法)。

*出处:V 节 = -145 监管巡检本波(总指挥 -157 任命秘书#6「记录副手」+ 单写锁派工)｜证据可复算:V.1 `python tools/build_coverage.py --no-write [--extra-dirs …]`(FLAT312/extra317/disk326)+ `run_all.py:96`(run_coverage 不传 extra_dirs)/`:741`(只写 RUNROOT);V.5 `out/_websearch_fp7_hardblack_append_141.csv`(7)+`out/_mon157_websearch3_hardblack_append.csv`(3)、开卷 `_openbook_pdf_body`+`_allow_openbook_verdict`、355744 三件套 os.path.getsize 实测;V.6 `qc_rejected_manifest.csv`∩claimed = 336/336｜质量门:build_coverage --selftest=COVERAGE_OK、run_all --selftest=RUN_ALL_OK 本波实跑通过｜-145 获《经验记录》整文件写锁串行追加,写毕即释放｜与 I(撞车)/L(websearch假阳)/N.9(有头)/R(回写开卷)/T.2(churn)/U.3(唯一数字源)分节对齐、不重复。*

---

## W. 【A5 路线B 断线补齐 + 门③落地 + 缺口补登·2026-07-03(-143 单写)】route-B 注入计划从 cfg 接回 render() + route-B 落盘 force=True 过 QC 门 + PreflightWarn 枚举 ValueError 洪水 + 门③ meta-doi-mismatch 落地

> **一句话**:-143 落四件事——①把 A5 机构订阅的 route-B「断线」接回(`download._browser_capture_fallback` 据 cfg 构 `RouteBInjectionPlan` 传 `render(injection_plan=)`,补离线 selftest);②补登 -140 审计里两项**至今未入正文**的真缺口(route-B 落盘 force=True 过门 / PreflightWarn ValueError bug);③**落地门③ meta-doi-mismatch**(PDF 内嵌元数据 DOI 反向比对——-140 审计「更正1」指出的 P0 缺失,此前误记「已补」,现真正实现,见 **W.4**)+ N.5 就地更正;④其余 -140 审计项(Network 域 vs 门控 Fetch 定论、coverage 二级扫描/口径)已被 **P/N.8/V** 节以更终版吸收,仅加指针不重抄(376/448 等为历史快照,以 `out/coverage.json` 为唯一数字源,不复活)。

### W.1 A5 路线B「断线」补齐:注入计划从 Config 接回 render()(-143,selftest 绿)
- **断线**(-177 核出):`download._browser_capture_fallback` 调 `render_download_pdf_bytes(...)` 时**漏传 `injection_plan=`** → route-A(HTTP EZproxy 改写)早已就绪、但 route-B 浏览器抓字节路径**注入不了机构 Cookie、不改写 EZproxy** → RSC/ACS/Wiley/ScienceDirect 这类 JA3 绑定强 CF 站在 route-B 上带不了机构会话。
- **修法**:新增纯函数 `route_b_bridge.plan_route_b_injection_from_config(cfg, url)`(离线、零副作用)——与路线A `http_client.needs_institution_access` **同口径守卫**:仅当配了 `ezproxy_prefix`/`institution_cookie` 且 host 命中 `institution_domains` 白名单(且非 OA 域)才产非空 plan,否则 `None`(与未启用逐字节一致)。`_browser_capture_fallback` 落盘前构 plan 并 `render(..., injection_plan=plan)` 传入;`_cookie_specs_from_header` 抽出复用「k=v; k2=v2」解析。
- **selftest(离线)**:`selftest_a5_framework` ⑤b(默认 Config→None、命中白名单→非空且 cookie 全解析、非白名单/无白名单→None)+ `download` ⑤e(默认 cfg→`injection_plan is None`、机构 cfg 命中→非空且 host/ezproxy 正确、host 未命中→None)。全套 `run_all_selftests` **PASS=46/FAIL=0/SKIP=2**。
- **铁律**:route-A 与 route-B 的机构访问守卫必须**同口径**(都过 `needs_institution_access`)——否则会出现「HTTP 改写了、浏览器没改写」或反之的半启用,既漏取又埋副作用。**唯一 gate 仍是用户真实凭据**(承 T.5),无凭据 → 恒 `None`、ROI=0、零副作用不变。

### W.2 route-B 落盘曾结构上绕过内容 QC 门 → 现 force=True 强制过门(147 核出、现已修、-143 补登)
- **坑(载荷性 P0)**:内容 QC 门原只在 ① `_download_pdf_core` 跑;route-B 的 **②b `_browser_capture_fallback`(B1/B2)** 与 **⑥ `_browser_pdf_download`(Akamai/MDPI)** 两出口曾校 `%PDF`+体积+结构后**直接 `_save()`、不过 `_content_qc_gate`** → 回收波 route-B 产出的 SI/错论文假阳会**裸奔落盘**(绕过 L/M/O 好不容易建的门)。
- **修法(现码;selftest ⑤e/⑤f 离线佐证)**:两出口落盘前均 `_content_qc_gate(..., force=True)`;`force=True` 是**关键**——route-B 源多为 DOI 绑定源、会被 `_source_needs_content_qc` 豁免,不强制则「接了门也等于没接」。回收波须置 `content_qc_non_article_hard_reject=True`(SI/目录/poster → mismatch 硬拒)。
- **铁律**:**新增任何「非 `_download_pdf_core` 的落盘出口」都必须显式过 `_content_qc_gate(force=True)`**;QC 门覆盖面要按「落盘出口」而非「主下载函数」盘点(承 N.5「联用」——那里只讲「要联用」,未讲「route-B 出口本身曾漏门」)。

### W.3 PreflightWarn 枚举 ValueError 洪水:nodriver typed CDP 事件的第二类崩(区别于 KeyError 字段缺失)(-143 补登,承 N.4/N.8)
- **区别于已记的 KeyError**:N.4/N.8 记的是 `render_fetch._patch_nodriver_cdp_compat._tolerant_from_json` 兜**字段缺失**(`KeyError:'localNetworkAccessRequestPolicy'`);**未记**的是另一类——**字段在、但枚举值是 nodriver 0.50.3 不认的新值**:Chrome 133 发 `PreflightWarn` → `LocalNetworkAccessRequestPolicy.from_json` 抛 **`ValueError`** 洪水(单跑约 270–上千行)→ `parse_json_event` 崩 → **网络事件被整批丢弃**。这正是「方法A 走 CDP Network 域时在 RSC 抓不到 `LoadingFinished`/body」的**直接机制之一**(与 P 节「Network 域取不到导航 articlepdf body」互为因果)。
- **修法**:`_tolerant_from_json` 的**枚举转换也包 `try/except`,未知枚举值降级 `ALLOW`**(与探针 `_route_b_b2_152.py::_tol` 的 `try/except→ALLOW` 写法对齐);不能只兜 KeyError。
- **铁律**:兜 CDP schema 漂移要**同时兜两类**——①字段缺失(KeyError)②字段在但枚举值不认(ValueError);只兜其一,另一类照样让 typed 事件解析崩、网络域静默失效。

### W.4 门③ meta-doi-mismatch 落地:PDF 内嵌元数据 DOI 反向比对(-143;-140 审计「更正1」P0 缺失补齐)
- **背景**:-140 审计核出门③(元数据 DOI≠期望)此前只「建议补」却被误记「已补」,现码实无——`_content_qc_*` 仅门①②④⑤ + `expected-doi-present`,读 PDF 自身 DOI 与期望反向比对的门③从未实现;唯一漏网 `cctc.200900261`(同题他刊)靠标题门①④⑤ 均拦不住。
- **实现**:① `_extract_pdf_meta_dois(data)` 从 PDF **XMP**(`prism:doi`/`dc:identifier`)+ **`/Info` 文档信息**抽内嵌 DOI(pypdf 多版本 XMP API 多路兜底、任何异常降级为 `[]`、绝不抛);② `_content_qc_verdict`(门②之后、标题 match 之前)判:期望 DOI 已知 且 PDF 自述 DOI【全部】≠期望、期望不在其中 → `meta-doi-mismatch` → mismatch;③ `_content_qc_gate` 抽 `meta_dois` 喂入。定稿提交 `61625a1`。
- **精度优先(不误杀真正文,三条硬约束)**:① 仅在**强正①未命中**(期望 DOI 不在 URL/正文区,否则上面已 return match)时才走门③——【有正证据就不据元数据翻案】;② 元数据**含期望** DOI → 不触发(它就是这篇);③ **无内嵌 DOI**(`meta_dois` 空)→ 不触发(与旧行为逐字节一致)。出版商 PDF 几乎必在 XMP 写自己 DOI,故「自述 DOI 全不同」是高置信「他篇」信号。
- **测试**:`download` selftest ⑦.1a 四例(负样本同题他刊→mismatch;元数据含期望 / 无内嵌 DOI / 强正优先 三正样本→不误杀);`tools/regress_qc_union_189._gate3_selfcheck` 确定性负/正样本(数据无关始终跑)+ `_replay` 把真数据落盘 PDF 的内嵌 DOI 也喂现门(189 同域桶回放并集全拒 189、旧交集仅 58)。
- **铁律**:内容 QC「这是不是这篇」要**多锚点**——标题(门①)/ URL·文内异 DOI(门②)/ **PDF 自述元数据 DOI(门③)**/ 版式(门④⑤);同题他刊专坑标题门,唯元数据 DOI 能一击识破,但必须让位于「期望 DOI 正证据」以免误杀(承 T.6)。

### W.5(pointer)已被现有分节吸收、仅登记指针,不重抄
- **route-B 方法A「Network 域 vs 过盾后门控 Fetch」A/B 定论**:见 **P 节(P.1–P.5,含 viewer-fetch/方法D 实证 484KB %PDF + P.5 方法C Fetch RESPONSE 放宽)+ N.8 #2**(5 条过盾铁律);批量 0/8 与单条成功的悖论由 **S.2/T.3** 的 RSC governor 二道墙定论(-165)解释。-140 审计建议的「N.9」草稿口径已被这两节以更终版覆盖,不再单列。
- **coverage 二级(嵌套)扫描机制 + 口径**:见 **V.1(caliber/extra_dirs 不透传/一键复现契约)+ L.4/L.6(净覆盖以 coverage.json 为准)+ O.4(黑名单双向纠错)**;-140 审计「P 节」草稿里的 376/623、+71→442/561、448/999 均为**历史/中途快照**,现权威 = `out/coverage.json`(承 U.3 唯一数字源),**不复活**。
- **归档**:两份 route-B 分叉暂存草稿(`经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md`、`选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md`)已由 -143 在文首标注为**「已并入·事实源指针」**(A/B 定论落 P/N.8、PreflightWarn 落 W.3),保留作原始探针证据,不再作为待办。

*出处:-143(承用户直派「修 A5 路线B 断线 + 合入经验记录 4 项待补登」;本波无协同工作组、独立主代理,经用户「方案A·定向合入」拍板)｜W.1 = `fulltext_fetcher/institutional/route_b_bridge.py::plan_route_b_injection_from_config` + `download.py::_route_b_injection_plan_for`/`_browser_capture_fallback(injection_plan=)` + selftest `a5_framework` ⑤b / `download` ⑤e,`run_all_selftests` PASS=46/FAIL=0/SKIP=2 实跑｜W.2 = `download.py::_browser_capture_fallback`/`_browser_pdf_download` 落盘前 `_content_qc_gate(force=True)`,selftest ⑤e/⑤f 离线佐证(承《验证-QC并集门对route-B产出无假阳无假杀-147.md》+《经验记录完整性审计-本波关键主题落档核对-140.md》缺失C)｜W.3 = 承 N.4/N.8 + 《经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md》PreflightWarn/`_tol` + 探针 `_route_b_b2_152.py`(缺失B)｜W.4 门③落地 = `download.py::_extract_pdf_meta_dois` + `_content_qc_verdict(meta_dois=)` + `_content_qc_gate` + selftest ⑦.1a + `tools/regress_qc_union_189._gate3_selfcheck`/`_replay`,定稿提交 `61625a1`(承《经验记录完整性审计-本波关键主题落档核对-140.md》更正1)+ N.5 就地更正｜数字口径一律以 `out/coverage.json` 为唯一真相源(U.3),不复活历史快照｜与 N/P/T/U/V 分节对齐、不重复。*

---

## X. 【免费 API 凭据接入 + OpenAlex Content 源 + 多 key 轮换池·2026-07-03 深夜(-147 单写)】环境变量别名回退 + Content API 计费/预算 429 语义 + 多 key 只增额度不增速 + 批量跑同出版商聚集触发看门狗

> **一句话**:-147 本波围绕「把本机已有的免费学术 API key 真正喂进 fulltext_fetcher」落六条实测定论——①项目 canonical 环境变量名(`S2_KEY`/`OPENALEX_KEY`/`CORE_KEY`)与业界通用名(`SEMANTIC_SCHOLAR_API_KEY`/`OPENALEX_API_KEY`)对不上时 key **被静默闲置**,补别名回退根治;②新增 `openalex_content` 源接 OpenAlex 官方缓存 PDF(~60M 篇),但它是**按成功下载计费**($0.01/篇、免费档 $1/天≈100 篇),429 分两种语义必须分治;③**多 key 轮换池只提升「额度」不提升「速率」**——速率瓶颈是单 IP/单 host 限速,不是 key;④Content API 候选 URL 绝不能带 `api_key`(防泄入产物),只在发请求瞬间单点注入;⑤Elsevier key 无机构订阅只能取 OA 全文,付费墙文章只回**首页预览**(响应头明写 `not entitled`);⑥批量跑输入若按 DOI 排序 → 同出版商前缀聚集 → 并发位全卡在同一慢域 → straggler 看门狗判整批卡死(0 成功),**打散乱序**是关键。

### X.1 环境变量 canonical 名 vs 通用别名对不上 → key 静默闲置(别名回退根治)
- **坑**:本机用户级环境变量按业界通用惯例存了 `SEMANTIC_SCHOLAR_API_KEY`(s2k-…,44 位)、`OPENALEX_API_KEY`(22 位)、`ELSEVIER_API_KEY`/`SCOPUS_API_KEY`(同一把 32 位)、`CLARIVATE_WOS_API_KEY`,但 `cli.py` 的 env 回退名是项目 canonical 的 `S2_KEY`/`OPENALEX_KEY`/`CORE_KEY` → **名字对不上,不传参跑批时这几把 key 实际全闲置**、静默降级到无 key 档(OpenAlex demo 100 credit/天、S2 共享池易 429)。用户完全不知道 key 没生效。
- **修法**:`cli.py` 新增 `_env_first(*names)`——按优先级取第一个非空环境变量并**裁剪两端空白**(粘贴的 key 常带换行/空格,会污染 HTTP 头),给三个 key 参数各配 canonical+通用别名回退链:`--openalex-key`=`OPENALEX_KEY`→`OPENALEX_API_KEY`;`--s2-key`=`S2_KEY`→`SEMANTIC_SCHOLAR_API_KEY`→`S2_API_KEY`;`--core-key`=`CORE_KEY`→`CORE_API_KEY`。优先级:显式 CLI 传参 > canonical env > 别名 env;空白值视同未设。selftest ⑨ 覆盖(别名兜底/canonical 优先/CLI 压 env/空白回退/裁剪)。
- **实测**:改后不传参 → `s2_key=s2k-pQ…`/`openalex_key=1THGg1…` 均自动拾取(`core_key=MISSING`,本机确未注册 CORE)。
- **铁律**:凡「从环境变量取凭据」都应**同时认 canonical 名 + 业界通用别名**并裁剪空白;否则用户按常识存的 key 会被静默忽略,且因「静默降级」极难发现(不报错、只是命中率莫名偏低)。承 C.3(S2/CORE 需 key)、T.7(OpenAlex key 解限速)。

### X.2 OpenAlex Content API 是「按成功下载计费」+ 429 两种语义必须分治(死磕 = 每条 miss 白等 2 分钟)
- **背景**:新增 `openalex_content` 源接官方缓存 PDF(`content.openalex.org/works/{W-id}.pdf`,~60M 篇)。它**不是免费的**:$0.01/篇、免费档每日 $1≈100 篇(未绑卡超预算即拒,不会超扣),404/未缓存不计费。
- **坑(实锤)**:首次端到端跑一条样本 DOI,`content.openalex.org` 连回 4 次 429、每次退避 30s,127s 后 MISS。探测响应头才看清:`X-RateLimit-Remaining-USD:0` + `Retry-After:28394`(≈到 UTC 午夜的秒数)+ body `Insufficient budget…Resets at midnight UTC`——**这是「日预算耗尽」不是「请求过快」**。而 HTTP 客户端退避上限才 30s,对这种「要等到明早」的 429 死磕重试,只会把批量跑的**每条 miss 拖慢 `30s×重试数`**。
- **两种 429 语义**:①**预算耗尽**(按 key,长 `Retry-After`,body 提 budget)——本次运行内对该 key 重试注定徒劳;②**请求过快**(按 IP,短 `Retry-After`)——退避重试有效。
- **修法**:`http_client.get` 对 429 判 `Retry-After>300s` → 不再 30s×N 死磕:多 key 池还有下一把 → 立即换 key 重试(host 不熔断);无可换 → 熔断该 host、快速失败、其余源照常兜底,预算次日重置后自愈。selftest ⑥b/⑥c 覆盖。
- **铁律**:**长 `Retry-After` 的 429 ≠ 普通限速**,是「配额/预算耗尽」硬信号,必须与短退避分治(换凭据或熔断,绝不按固定上限死磕);接任何「按量计费/按日配额」的 API 前先想清楚 429 的两种语义。

### X.3 多 key 轮换池只增「额度」不增「速率」——速率瓶颈是单 IP/单 host(实锤)
- **需求**:用户给了 28 把 OpenAlex key,问「多路并发是否更快」。
- **定论(必须分清额度与速率)**:**多 key 提升额度、不提升速率**。
  - **额度(已实现)**:$1/天预算**按 key 独立**,28 key ≈ 2800 篇缓存 PDF/天。轮换池是**串行轮换**(当前把耗尽→切下一把),解决的是额度。
  - **速率(基本不变)**:瓶颈是**单出口 IP / 单 host 限速**,不是 key——① 我们自己的 `HttpClient._respect_rate` 按**域**限速(每域 0.34s 最小间隔),28 把 key 打同一域照样被自己限成 ~3 req/s;② 服务端 per-IP 节流同理,28 key 同一出口 IP 照样被限。
  - **活证据**:本波批量跑日志里 `api.semanticscholar.org … 429 退避` **刷屏**——即便带 key,单 IP 高并发照样被服务端 per-IP 限。
- **真提速的杠杆**(都不是多 key):① 本地快照 `snapshot_db`(零联网零限速,最彻底);② 多 IP/代理池(才能绕单 IP 天花板);③ 调高并发 + 放宽 per-host 间隔(但会撞 per-IP 节流,需权衡)。
- **铁律**:回答「多 key/多账户能否提速」先拆**额度 vs 速率**两个维度——按量/按日配额型 API,多 key 只买额度;速率天花板由 IP 与 host 限速决定,除非换 IP,否则多 key 对速率净收益≈0(且多账户绕单 key 限速常违反 fair-use、有连坐封号风险,见 X.6)。

### X.4 计费 API 的 key 绝不能进候选 URL / 日志 / 产物——只在发请求瞬间单点注入
- **坑点**:`content.openalex.org/works/{id}.pdf?api_key=KEY` 若把带 key 的 URL 当候选存进 `PdfCandidate`,会一路泄进 `attempts.jsonl`/`results.csv`/`report.html`/`run.log`——**凭据落盘泄密**。
- **修法**:候选/日志/产物里的 URL **一律不带 `api_key`**;由 `HttpClient.get` 对 `content.openalex.org` 域**在真正发请求的这一刻**单点注入(取轮换池当前把),且「调用方已显式给 api_key(params 或 URL 内)」时绝不覆盖。selftest ⑨b + 端到端实跑后 `rg api_key` 扫四产物 = **零泄漏**验证。
- **铁律**:凭据注入点要**唯一且最靠近网络边界**(发请求瞬间),绝不进「会被序列化/落盘/进日志」的数据结构(候选、结果、事件);源连接器构造 URL 时把 key 留空,交传输层补。

### X.5 Elsevier key 无机构订阅只能取 OA 全文,付费墙只回「首页预览」(响应头明写)
- **实测**:`ELSEVIER_API_KEY`(=`SCOPUS_API_KEY` 同一把)对 Article Retrieval API 全部 200,但分两档——**OA 文章**:全文 XML(147KB)+ 真 PDF(903KB,`%PDF-`)都能取 ✓;**付费墙文章**(miss 清单里的 ApCatB):PDF 只 247KB/1 页/无 References,响应头 `X-ELS-Status: WARNING - Response limited to first page because requestor not entitled to resource`——**只给首页预览**。
- **含义**:这把 key **无机构订阅授权**,只能取 Elsevier OA 全文;miss 大头(付费墙 ApCatB/CEJ/Fuel)靠它打不开。要接 publisher TDM 源取订阅全文,需机构网/insttoken(承 T.5「唯一 gate=用户凭据」、O.2/W.1 A5 路线)。
- **铁律**:出版商 TDM API 的 key「能调通(200)」≠「能取全文」——付费墙内容会**降级为首页/摘要预览且响应头/字节数会露馅**,验收必须**核实体权限档位**(抽一篇已知付费墙 DOI 看是否 `not entitled`/页数/References),不能只看 HTTP 200。

### X.6 S2 /paper/batch 免费 key 可用、500 DOI/次、~1 req/s;批量端点 >> 多账户并发
- **实测**:`/paper/batch`(POST `ids:[DOI:…]`,`fields=title,openAccessPdf,externalIds`)免费 key 即可用,20 DOI 一次 1.18s、命中 17/20;无间隔连发 3 次 → 第 3 次 429,即稳定 ~1 req/s。单次上限 500 ID / 10MB。
- **定位**:提速正解是**批量端点**(1 次/秒 × 500 DOI ≈ 3 万 DOI/分)而非多账户并发——多账户绕限速违反 fair-use、有连坐风险,且吞吐远不如 batch。本仓现用逐 DOI 单查(`aggregators.SemanticScholar` 的 GET),真要提速改这里为 batch 预取+本地缓存即可。
- **对本项目 miss 的增益有限**:20 DOI 仅 2 带 `openAccessPdf`(机构库存档)——miss 主要是付费墙化学文献,S2 也只有 OA 指针。且 S2 在源序里排 unpaywall/openalex 之后,多数 DOI 轮不到它。
- **铁律**:「多账户 key 并发提速」在有批量端点时是下策——优先用官方 batch + 本地缓存;fair-use 型免费 API 的多账户绕限速有连坐封号风险。

### X.7 批量跑输入按 DOI 排序 → 同出版商聚集 → 并发位全卡同一慢域 → 看门狗判整批卡死(打散乱序根治)
- **坑(实锤)**:对 631 条 `still_missing.txt` 全源跑(并发 6),清单**按 DOI 字典序排**,前几百条全是 `10.1002/*`(Wiley)→ 6 个并发工作位**同时**卡在 `onlinelibrary.wiley.com/doi/pdfdirect/…` 同一个慢/难域上 → 240s 无任何输入完成 → `pipeline` 的 straggler 看门狗判定尾部卡死、**整批停在 0/631 成功**(metadata 全是 `straggler-timeout`)。attempts.jsonl 佐证:6 个在途项 download 事件 URL 全指向同一 Wiley pdfdirect 域。
- **根因**:并发调度按输入顺序取任务,输入同前缀聚集 → 并发位撞同一域 → 既触发 per-host 串行限速(互相排队)、又集体撞该域的 CF/慢响应 → 无进展 → 看门狗(合理地)判卡死。**是输入排列问题,不是源/网络问题**。
- **修法**:跑批前把清单**打散乱序**(628 条去重后 `Sort-Object {Get-Random}`),让并发位分散到不同域;并发提到 8、timeout 降到 20s。
- **铁律**:多源并发跑批,**输入顺序会显著影响吞吐与看门狗**——同出版商/同域聚集是隐性陷阱;喂给并发调度器前应**打散**(或按 host 轮转派发),避免 N 个并发位挤同一慢域。承 I.1(并发撞车)、pipeline straggler 看门狗设计。

*出处:-147(承用户直派「把本机免费 API key 接进项目并最大化取全文」系列追问,独立主代理、无协同工作组)｜X.1 = `cli.py::_env_first` + `--openalex-key/--s2-key/--core-key` 别名回退 + `_selftest` ⑨,`python -m fulltext_fetcher.cli --selftest`=CLI_OK;X.2/X.3/X.4 = `sources/aggregators.py::OpenAlexContent` + `http_client.py` 的 content 域 key 注入/多 key 轮换池(`_oa_current_key`/`_oa_rotate_key`)+ 长 Retry-After 熔断/换 key,`config.py::openalex_keys` + `DEFAULT_SOURCE_ORDER` 排 websearch 前,selftest AGGREGATORS_OK/HTTP_CLIENT_OK(⑥b/⑥c/⑥g/⑨b);端到端 `--sources openalex_content` 实跑 [OK] 落盘 5.0MB %PDF + 四产物 `rg api_key` 零泄漏;X.2 429 语义 = 探测 `content.openalex.org` 响应头 `X-RateLimit-Remaining-USD:0`+`Retry-After:28394`+body`Insufficient budget`;X.5 = 实测 Article Retrieval API OA(真 PDF 903KB)vs 付费墙(`X-ELS-Status: … not entitled` 首页 247KB);X.6 = 实测 `/paper/batch` 20 DOI/1.18s/命中17、连发第3次429;X.7 = `out/still_missing.txt` 按 DOI 排序全源跑 straggler 0/631(attempts.jsonl 6 在途全 Wiley pdfdirect)→ 打散乱序 `out/_still_missing_shuffled.txt` 重跑｜环境变量 `OPENALEX_API_KEYS`(28 把)写用户级注册表,项目经 `_split_keys` 解析成 28 把轮换池；本机 key 现状:S2✓/OpenAlex✓(28池)/邮箱`1659553158@qq.com`✓/Elsevier(仅OA)/CORE未注册｜数字口径仍以 `out/coverage.json` 为唯一真相源(U.3),本节不报覆盖率｜与 C(源时效)/T.5(唯一gate=凭据)/T.7(OpenAlex key解限速不解订阅)/I.1(并发撞车)分节对齐、不重复。*
