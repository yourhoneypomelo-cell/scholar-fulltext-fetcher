# 工作树三分类收口清单（A 可提交码 / B 正式文档 / C 临时脚本）· 只读产清单

> 交付：**谷歌学术人机认证-165**（worker）｜工单：`task-7e92b11e`（收口#5，用户指令：把工作树分三类，为后续 git 检查点做准备）｜2026-07-02。
> **边界（硬约束）**：只读盘 + 产清单。**未 `git add/commit/rm`、未 `git mv`、未改任何代码**。落地动作须总指挥 / 属主拍板。
> **快照说明**：本波仓内约 **30 个并发 worker 在实时产出临时件**，工作树是**移动靶**——本文计数为 **≈22:47 时点快照**，C 类只增不减、随时上漂；A/B/结论部分是结构性判断，较稳定。

---

## 〇、TL;DR（≈22:47 快照）

| 类别 | 计数 | 处置口径 |
|---|:--:|---|
| **A · 可提交代码**（tracked `M` 代码件） | **5** | 5/5 自测齐绿→可入下个 checkpoint；`download.py` 含 route-B 默认集成、**总指挥拍板保留·默认关**（勿回退，见 §四） |
| **B · 正式报告文档**（`*.md`） | **≈20** | 全部**保留入仓**，择时随 checkpoint 提交 |
| **C · 一次性探针 / 临时脚本 / 数据件** | **≈110** | 按《仓根临时脚本清库清单-152.md》口径**归档/清库**；本波新增 20 脚本见 §三增量表 |
| **OTHER** | 1 | `selftest.pdf`（自测游离产物，**-165 已清理**） |

- **HEAD**：`0b95502 checkpoint: 北极星主流程代码 + 本波交付/分析/选型/经验文档`。相比会话初快照，`config.py`/`pipeline.py`/`run_all.py`/`pyproject.toml` 及大量 md **已入库**，工作树已比 152 时干净。
- **download.py 专项核查（②）结论先说**：**无悬挂写锁**（`out/.route_b.lock` 是 **-141 正在跑的 route-B 活锁**，PID 49608 存活，切勿删）；**非 n1 残留、是 -157 完成的 route-B 默认集成成品**——**总指挥(-144)已拍板保留、默认 `False` 严格 gated（默认行为零变更）、勿回退**（-157 正把 flag 改 False）；单跑自测 5/5 PASS，唯一尾巴＝ run_all 内**偶发 1 次** `Event loop is closed`（Windows asyncio teardown 竞态，非阻塞）。详见 §四。

---

## 一、A 档 · 可提交代码（tracked `M`，5）

> 判据「已定稿可提交 / 半成品待落定」以 `python run_all_selftests.py`（离线 43 PASS/1 FAIL/2 SKIP）+ 单跑复核为客观依据。

| 代码件 | diff 量 | 内置自测 | 判定 | 说明 |
|---|:--:|:--:|:--:|---|
| `fulltext_fetcher/cli.py` | ~4 行 | `CLI_OK` ✅ | **已定稿可提交** | route_b 参数接线，小改 |
| `fulltext_fetcher/render_fetch.py` | ~433 行 | `RENDER_OK` ✅（5/5） | **已定稿可提交** | route-B 渲染 / 浏览器内抓字节引擎 + nodriver CDP 漂移兼容 + 单头串行锁；与 download.py 强耦合 |
| `fulltext_fetcher/sources/publisher_direct.py` | ~81 行 | `PUBLISHER_DIRECT_OK` ✅ | **已定稿可提交** | publisher_direct 补口 |
| `tools/build_coverage.py` | 并发新改 | `COVERAGE_OK` ✅ | **可提交·需对齐** | 本波被并发改动（coverage 口径属 **-151**）；提交前与 -151 对齐 |
| `fulltext_fetcher/download.py` | ~669 行 | `DOWNLOAD_OK` ✅ | **可提交·能力默认关待启用** | 含 **-157 已完成的 route-B 默认集成**（`_needs_browser_capture_host △ {mdpi.com}` + `_route_b_auto_fallback_enabled` + ⑤b 兜底，selftest 绿）。**总指挥(-144)已拍板保留、默认 `False` 严格 gated（默认行为零变更）**，-157 正把该 flag 改 False。**勿回退**。次要：run_all 内偶发 `Event loop is closed`（§四，非阻塞，可选收尾） |

- **归属提示**：`cli/download/render_fetch/publisher_direct` 均属 **route-B 落地（-144 口径）**；提交 / 回退请与 -144 统一协调，勿撞车。

---

## 二、B 档 · 正式报告文档（`*.md`，≈20，保留入仓）

- tracked `M` 与新 untracked 的 `*.md` 均为**交付 / 选型 / 经验 / 检索成果 / 数据核对**类正式文档，**全部保留入仓**。
- 代表：`谷歌学术人机认证-项目总报告.md`、`经验记录-踩坑与发现.md`（多人写，提交前协调）、`项目索引.md`、`基线口径冻结说明-388-173.md`、各 `检索成果-still_missing*.md`、`选型2026-A5-SSO实现复核-168.md`、`本波回收交付汇总.md` 等。
- 处置：择时随 checkpoint 提交即可；无需归档 / 删除。

---

## 三、C 档 · 一次性探针 / 临时脚本 / 数据件（≈110，归档/清库）

**复用现行权威清单**：《**仓根临时脚本清库清单-交付前三分类-152.md**》已把 61 个临时脚本细分为 **可删 20 · 归档 31 · 保留 9 · 待属主 1**（判据：纯 traceback / 用后即删 / 纯读打印 / 一次性导出＝可删；根因·门·QC·取样·冒烟＝归档留证；Runbook 依赖 / route-B 复验在途＝保留）。本文**沿用该口径**，仅做增量刷新。

### 3.1 增量（152 之后新出现的临时脚本，20 个，需补分类）

| 新脚本 | 建议档（沿用 152 判据） |
|---|---|
| `_agg_preprint_probe_149.py`、`_datahygiene_audit_148.py`、`_diff_succ_151.py`、`_probe_acs4_151.py`、`_verify_acs_151.py` | 归档（纯读审计/取样，结论已入 md） |
| `_mon169_identify_suspects.py`、`_mon169_qc_openbook_scan.py`、`_mon169_verify_plus2_probe.py` | 归档（监管#169 开卷 QC 审计，结论已入《经验记录-增补-监管#169…》） |
| `_t0_recover156_blsrc.py`、`_t0_recover156_diag.py`、`_t0_recover156_netgain.py`、`_t0_recover156_qc.py`、`_t0_adjudicate147_probe.py` | 归档（T0 回收/裁决一次性，产物已单独留存） |
| `_diag_mdpi_143.py`、`_diag_mdpi_b2_143.py`、`_batch_mdpi_b2_143.py`、`_allow_override_openbook_rescan_140.py`、`_naming_spotcheck_146.py`、`_route_b_download_probe_141.py` | 归档（诊断/取样，方法留证） |
| `_routeb_rsc_launch_156.py`、`_routeb_shard_healthcheck_142.py` | **保留**（route-B 发射/分片体检，复验/运维在途；与 152 的 route-B 6 保留同口径） |

### 3.2 数据件（`*_input.txt` / `*_out.json` / 分桶清单，≈28）

- `recover_*_input.txt`、`rerun*_input.txt`、`smoke*_input.txt`、`subset_*_input.txt`、`p3_longtail_153_input.txt`、`recover_p2_cf_soft_155_input.txt`、`batch7_*_dois.txt`、`routeB_{mdpi,rsc_goldoa,rsc_subscription}.txt`、`titles_probe*.{txt,json}` 等：属**批次/回收数据输入**，建议归 `inputs/` 或随批次目录（见 152 §六 C′）。
- `_*_report.txt` / `_*_out.*` / `_selftests_145_out.txt` / `_filelist.txt`：脚本临时产物，随对应脚本一并处置。

### 3.3 处置建议

- **落地须拍板**：`rm` / `git mv archive/scratch-2026-07/` / `.gitignore` 由总指挥或属主定。**route-B 复验脚本仍列保留**（-152 口径：待 RSC gate 齐绿后再降档）。
- **`.gitignore` 强烈建议**补 `_*.py` / `_tmp_*` / `_spike_*` / `_probe_*` / `_route_b_*` / `selftest.pdf`——本波 ~30 并发 worker 持续往仓根落临时件，不加约定会反复污染。

---

## 四、download.py 专项核查（任务②）

**问题**：除本就存在的未提交改动外，有无被**刚取消的 route-B 集成任务(n1)** 留下的半成品增量 / 悬挂写锁？

### 4.1 悬挂写锁 → **无（是活锁，切勿删）**
- `out/.route_b.lock` 存在，内容 `49608`（PID），`LastWriteTime 22:33:59`。
- **PID 49608 = 存活 python 进程**（`StartTime 22:21:03`）——即 **-141 正在跑的 route-B RSC 金OA 发射**持有的**单头串行锁**（全组共一机单头浏览器护栏）。
- 代码 `render_fetch._lock_is_stale/_acquire_file_lock` 自带**陈旧锁自动清理**（超时即 `os.remove` 重抢），即使持有进程崩溃也会自愈。
- **结论**：非悬挂锁，属正常运行态。**不要删**（删会打断 -141 的活）。

### 4.2 n1 半成品增量 → **非残留：是 -157 完成的成品，总指挥拍板保留**
- **总指挥(-144)权威裁定（22:4x）**：download.py 现含 **-157 已完成的 route-B 默认集成**（新增 `_needs_browser_capture_host △ {mdpi.com}` + `_route_b_auto_fallback_enabled` + ⑤b 兜底，selftest 绿）；**当初让我核查的"n1 残留"经确认不是残留、而是成品**，**已拍板保留、并设默认 `False`（严格 gated、默认行为零变更）**——-157 正在把那一行 flag 改 False。**故 download.py 归【可提交代码·已集成能力默认关、待 RSC 后启用】，不要建议回退。**
- 我的独立核查与此一致：download.py 的未提交改动是一套**结构完整**的 route-B 集成（host 门控 `_needs_browser_capture_host` + `_route_b_auto_fallback_enabled` 开关 + 单头锁 `lock_path` + **内嵌自测断言**如 `assert _bc_kw.get("lock_path")==…`、`blocked-shadow-library`）。
- **全仓扫 WIP 标记**（TODO/FIXME/XXX/WIP/NotImplementedError/breakpoint）：命中项全在 `base.py`/`http_client.py`/`fetcher.py` 等**未改动**旧骨架，**route-B 4 件里 0 命中**。
- **自测复核**：`python -m fulltext_fetcher.download` **单跑连测 5/5 全 PASS**（`DOWNLOAD_OK`，exit 0，无 event-loop 报错）。

### 4.3 唯一尾巴：run_all 内偶发 `Event loop is closed`
- `run_all_selftests.py` 全套（46 项）中 **download 唯一 FAIL**：`RuntimeError: Event loop is closed`（`asyncio/base_events.py:_check_closed`）；当时 **-141 的 route-B（PID 49608）正并发运行**。
- 但**单跑 5/5 干净**——故判定为 **Windows asyncio `ProactorEventLoop` 关闭期 `__del__` 竞态**（GC 时序相关、并发 route-B 时更易触发），**非 route-B 逻辑缺陷**、非 n1 残留半成品。
- **次要洁癖**：download 自测会在 **cwd 落一个 329B `selftest.pdf`**（本文已清理）——建议 selftest 改写临时目录，勿污染仓根。

### 4.4 download.py 建议
1. **保留现有未提交改动、勿回退**（总指挥已拍板：-157 的 route-B 默认集成成品，非残留）。**确认 flag 默认 `False`**（-157 在改）后即属【可提交·能力默认关待启用】，可随 checkpoint 入库。
2. （可选·非阻塞）owner 有余力时补一处 **async teardown 收尾**：`asyncio.run` 路径关闭事件循环前 await/cancel 悬挂任务，或对 `ProactorBasePipeTransport.__del__` 的 `Event loop is closed` 做抑制 / 改用 `WindowsSelectorEventLoopPolicy`——消除 run_all 偶发红。
3. 提交前建议在**无并发 route-B** 环境复跑一次 `run_all_selftests.py` 确认 download 稳定转绿（当前偶发红仅在并发 route-B 时出现）。

---

## 五、建议提交 / 归档动作（供总指挥 / 属主拍板，本文不执行）

1. **A 档**：`cli.py`/`render_fetch.py`/`publisher_direct.py` 自测齐绿 → 可纳入下个 checkpoint（与 **-144** route-B 属主对齐）；`build_coverage.py` 与 **-151** 对齐后提交；**`download.py` 总指挥已拍板保留（route-B 默认集成、默认关严格 gated），确认 flag=False 后即可入库·勿回退**（§4.4；async teardown 为可选非阻塞收尾）。
2. **B 档**：`*.md` 全部保留入仓，择时随 checkpoint 提交（`经验记录-踩坑与发现.md` 多人写，提交前协调）。
3. **C 档**：按 152 口径归档/清库；本波新增 20 脚本按 §3.1 补分类；route-B 复验脚本继续保留。
4. **`.gitignore`**：补 `_*.py`/`_tmp_*`/`_spike_*`/`_probe_*`/`_route_b_*`/`selftest.pdf`。
5. **护栏复述**：本文只读产清单，**未 git add/commit/rm、未 git mv、未改任何代码**。

---

*核验 2026-07-02 ≈22:47（含总指挥 22:4x 裁定刷新）｜-165 交付 · 工单 `task-7e92b11e`（收口#5）。快照口径（工作树随并发 worker 上漂）：A=5 · B≈20 · C≈110 · OTHER=1(已清)。download.py：无悬挂锁（活锁勿删）· 非 n1 残留而是 -157 route-B 默认集成成品（总指挥拍板保留·默认关·勿回退）· run_all 偶发 async teardown 为可选非阻塞收尾。*
