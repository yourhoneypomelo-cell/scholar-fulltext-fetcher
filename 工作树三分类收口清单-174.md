# 工作树三分类收口清单（A 可提交码 / B 正式文档 / C 临时脚本）· 174 波只读刷新

> 交付：**谷歌学术人机认证-159**（worker）｜工单：`task-dd56dffc`（174 波命名审计 + 工作树三分类，承接 173 收口）｜2026-07-02。
> **边界（硬约束）**：只读盘 + 产清单 + 跑 `_naming_audit.py` 写报告。**未 `git add/commit/rm`、未 `git mv`、未改任何生产代码**。落地动作须总指挥 / 属主拍板。
> **快照说明**：本波仓内仍有并发 worker 在产出临时件，工作树是**移动靶**——本文计数为 **≈当前时点快照**，C 类只增不减。

---

## 〇、TL;DR（174 快照 vs 173）

| 类别 | 173 快照 | **174 快照** | 漂移 |
|---|:--:|:--:|---|
| **A · 可提交代码**（tracked `M` 代码件） | 5 | **6** | **+1**：新增 `publisher_oa.py`（-173 口径 publisher OA 补口） |
| **B · 正式报告文档**（`*.md`） | ≈20 | **≈41** | tracked `M` 35 + untracked 6；全部**保留入仓** |
| **C · 一次性探针 / 临时脚本 / 数据件** | ≈110 | **≈122 untracked + 87 py** | 仓根 `_*.py` 87 个；数据件 28；较 173 上漂 |
| **命名审计** | （173 未单跑） | **544/544 硬合规 100%** | 见 §五 |

- **HEAD**（未变）：`0b95502 checkpoint: 北极星主流程代码 + 本波交付/分析/选型/经验文档`。
- **173→174 结构性变化**：A 档多 1 件 OA 补口；B 档 md 随多波交付持续累积；C 档新增 **154/155/157/167** 波探针（见 §三增量表）。

---

## 一、A 档 · 可提交代码（tracked `M`，6）

> 判据沿用 173：以各模块内置自测 + `run_all_selftests.py` 为客观依据；**本文未重跑全套 selftest**（只读边界）。

| 代码件 | 173 判定 | **174 刷新** | 说明 |
|---|---|---|---|
| `fulltext_fetcher/cli.py` | 已定稿可提交 | **维持** | route_b 参数接线 |
| `fulltext_fetcher/render_fetch.py` | 已定稿可提交 | **维持** | route-B 渲染引擎；与 download 强耦合 |
| `fulltext_fetcher/sources/publisher_direct.py` | 已定稿可提交 | **维持** | publisher_direct 补口 |
| `fulltext_fetcher/download.py` | 可提交·能力默认关 | **维持** | 总指挥(-144)已拍板保留 route-B 默认集成、**默认 `False` 严格 gated**；勿回退（173 §四） |
| `tools/build_coverage.py` | 可提交·需对齐 -151 | **维持·仍须与 -151 对齐** | coverage 口径属 -151 |
| `fulltext_fetcher/sources/publisher_oa.py` | — | **🆕 待落定** | 173 快照无此项；本波 tracked `M` 新增；提交前须单跑 `PUBLISHER_OA_OK` 并与 route-B 属主对齐 |

- **归属提示**：`cli/download/render_fetch/publisher_direct/publisher_oa` 均属 **route-B / publisher 补口（-144 口径）**；提交前与总指挥统一协调。

---

## 二、B 档 · 正式报告文档（`*.md`，≈41，保留入仓）

- **tracked `M`（35）**：各 `检索成果-*`、`经验记录-*`、`选型2026-*`、`项目索引.md`、`谷歌学术人机认证-项目总报告.md` 等交付 / 选型 / 经验 / 数据核对类文档。
- **untracked 新文档（6）**：

| 文档 | 建议 |
|---|---|
| `基线口径冻结说明-388-173.md` | 保留入仓（基线口径权威） |
| `工作树三分类收口清单-173.md` | 保留入仓（173 收口清单） |
| `检索成果-still_missing611机制横切分桶与下一波ROI-149.md` | 保留入仓 |
| `检索成果-开源过认证方案全网扫描-RSC-governor-时效热度维护-165.md` | 保留入仓 |
| `经验记录-增补-监管者169-回写必开卷SOP与websearch假阳9实锤-2026-07-02.md` | 保留入仓 |
| `选型2026-route-B反RSC-governor补丁方案-165.md` | 保留入仓 |

- **174 本文**：`工作树三分类收口清单-174.md`（本交付）。
- **处置**：全部保留入仓，择时随 checkpoint 提交；`经验记录-踩坑与发现.md` 多人写，提交前协调。

---

## 三、C 档 · 一次性探针 / 临时脚本 / 数据件（≈122 untracked）

**复用权威清单**：《**仓根临时脚本清库清单-交付前三分类-152.md**》+ 173 §三增量表。**174 仅刷新增量与计数**，不重复 152 全文。

### 3.1 173 之后新出现的临时脚本（174 增量，需补分类）

| 新脚本 | 建议档（沿用 152 判据） |
|---|---|
| `_naming_audit.py` | **归档**（本波只读 PDF 命名合规审计；产物 `_naming_audit_report.txt`） |
| `_qc_p2_cf_soft_155.py` | 归档（p2 CF soft 桶 QC，结论应入 md） |
| `_sweep154_fatcat_probe.py`、`_sweep154_prep.py` | 归档（154 波 Fatcat/预筛探针） |
| `_tmp_b4cf_fail_rootcause_157.py` | 归档（b4cf 失败根因一次性） |
| `_route_b_governor_probe_167.py` | **保留**（RSC governor 反制探针，与 165 选型在途；复验可能再用） |

### 3.2 173 已列、本快照仍存在的增量（20，维持 173 分类）

沿用 173 §3.1 表：`_agg_preprint_probe_149`、`_datahygiene_audit_148`、`_mon169_*`、`_t0_recover156_*`、`_t0_adjudicate147_probe`、`_diag_mdpi_*`、`_batch_mdpi_b2_143`、`_allow_override_openbook_rescan_140`、`_naming_spotcheck_146`、`_route_b_download_probe_141`、`_routeb_rsc_launch_156`（**保留**）、`_routeb_shard_healthcheck_142`（**保留**）等——**分类不变**。

### 3.3 152 基线 61 脚本中仍散落仓根的部分

当前 untracked **`_*.py` + `fs_shim_mainloop_141.py` = 87 个**。其中约 61 个已在 152 清单逐条分类（A 可删 20 / B 归档 31 / C 保留 9 / D 待属主 1）；**余下 ≈26 个**为 145–152 各波后续产出、152 清单未逐条覆盖——处置原则：

- 纯 traceback / 用后即删 / 纯读打印 / 一次性导出 → **可删或归档**
- route-B 复验 / Runbook 依赖 / governor 在途 → **保留**（与 152 §五 route-B 6 保留同口径）
- 属主明确（如 `_agg_recover_150.py` 属 -150）→ **交属主定夺**

### 3.4 数据件（28）

- **批次输入**：`recover_*_input.txt`、`rerun*_input.txt`、`smoke*_input.txt`、`subset_*_input.txt`、`p3_longtail_153_input.txt`、`recover_p2_cf_soft_155_input.txt`、`batch7_*`、`routeB_*.txt`、`titles_probe*.{txt,json}` 等 → 建议归 `inputs/` 或随批次目录。
- **脚本产物**：`_naming_audit_report.txt`、`_naming_spotcheck_146_report.txt`、`_batch6_final_out.txt`、`_selftests_145_out.txt`、`_filelist.txt`、`_agg_preprint_probe_149_out.json`、`_t0_recover156_input.txt`、`_run_all_demo_input.txt`、`_tmp_final_check.txt` → 随对应脚本一并处置。

### 3.5 处置建议（供拍板，本文不执行）

1. 按 152 + 173 + 本表 §3.1 合并清库；**route-B / governor 复验脚本继续保留**。
2. **`.gitignore` 强烈建议**补 `_*.py` / `_tmp_*` / `_spike_*` / `_probe_*` / `_route_b_*` / `selftest.pdf`（173 §3.3 已述，仍未落地）。
3. **护栏**：本文只读，**未 git add/commit/rm、未 git mv**。

---

## 四、174 待收口项清单（总指挥 / 属主拍板用）

| # | 待收口项 | 优先级 | 属主 |
|:--:|---|:--:|---|
| 1 | A 档 6 件代码随 checkpoint 入库（`download.py` 确认 flag 默认 `False` 后；`publisher_oa.py` 先单测绿） | 高 | -144 / -151 |
| 2 | B 档 ≈41 个 md 择批提交（协调 `经验记录-踩坑与发现.md` 多人写） | 中 | 总指挥 |
| 3 | C 档 87 脚本按 152/173/174 三表合并 `git mv archive/` 或 `rm` | 中 | 总指挥 |
| 4 | C 档 28 数据件归 `inputs/` 或随批次归档 | 低 | 各波属主 |
| 5 | `.gitignore` 补仓根临时件约定 | 中 | 总指挥 |
| 6 | 命名：布局外散落 PDF（146 spotcheck 报 11 条）是否纳入清库 | 低 | -146 / 总指挥 |

---

## 五、命名审计摘要（`_naming_audit.py` 只读复核）

> 标准：`target_name = sanitize_filename(doi.replace('/','_') | 'arxiv_'+id | title[:80]) + '.pdf'`（与 `download.py` 一致）。
> 执行：`python _naming_audit.py` → 产物 `_naming_audit_report.txt`（本波 freshly run）。

### 5.1 全量硬合规（`out/*/pdfs/`）

| 指标 | 结果 |
|---|:---:|
| pdfs 目录数 | **13** |
| 总 PDF 文件数 | **544** |
| 硬合规（自洽 + 不过长 + 无乱码） | **544/544 = 100.0%** |
| 非法字符 / 未 trim | **0** |
| 过长 (>144) | **0** |
| 乱码 / mojibake | **0** |
| 去重后缀 `_N` / `(N)` | **0** |
| metadata 同名覆盖风险 | **0** |
| 非 DOI 规范化（软发现） | **0** |
| 不以 `10.` 开头 | **0** |
| 跨 batch 同名（信息用，非冲突） | **69** 种 |

**各 batch 文件数**：batch4_p1=35, batch4_p2=38, batch4_p3=38, batch4_p4=40, batch4_p5=36, batch6=247, batch7=49, p2_cf_soft_155=5, recover_b4_cf=25, recover_b4_cf_smoke_179=4, routeB_mdpi=7, routeB_rsc_launch=0, title_probe=20。

### 5.2 与 146 spotcheck 交叉（软发现，非本次重跑）

`_naming_spotcheck_146_report.txt` 曾报：

- **抽样 60/60 = 100%** 硬合规；
- **布局外散落 PDF 11 条**（`out/runbook_b/`、`out/smoke_*`、`out/ja3_realfix_*` 等，含 3 条非 DOI 规范名如 `_FIXED` / `_viaMethodB` / `ja3_realfix_` 前缀）——**不在 `out/*/pdfs/` 契约目录**，全量审计脚本不扫；建议清库或归位时一并处理；
- metadata 缺 title/journal **3 条软发现**（可追溯性弱，不算文件名硬伤）。

### 5.3 命名结论

- **`out/*/pdfs/` 契约目录内命名健康：544/544 全绿，零覆盖风险。**
- **唯一待收口**：布局外 11 条散落 PDF + 3 条 metadata 软发现（见 §四 #6）。
- **无需改 `download.py` 命名逻辑**；审计脚本 `_naming_audit.py` 建议随 C 档归档留证。

---

## 六、建议提交 / 归档动作（供拍板，本文不执行）

1. **A 档**：5 件维持 173 口径 + **`publisher_oa.py` 单测绿后与 -144 对齐提交**。
2. **B 档**：≈41 个 md 全部保留，择 checkpoint 提交。
3. **C 档**：152 + 173 + §3.1 五件增量合并清库；route-B / governor 保留。
4. **命名**：契约目录已 100% 合规；布局外 PDF 待 §四 #6 决策。
5. **护栏复述**：只读产清单 + 命名审计报告，**未动生产代码与 git 索引**。

---

*核验 2026-07-02（174 波只读快照）｜-159 交付 · 工单 `task-dd56dffc`。A=6 · B≈41 · C≈122 untracked（87 py + 28 数据 + 其它）· 命名 544/544 硬合规 100% · 布局外散落 PDF 11 条待收口（146 交叉）。*
