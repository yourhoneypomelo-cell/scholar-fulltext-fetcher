# 选型2026 · ⑤ 采纳/淘汰总表 — 刷新预备包 + 路线图口径复核

> 智库预备（信息检索-智库专家 **-177**）｜2026-07-02｜**这是给总指挥 -156 后续派发的 ⑤「采纳/淘汰总表」的预备包**（待 -156 正式派 ⑤ 时据此定稿）。
> 关键前提（先读仓）：**⑤ 是「刷新」不是「重建」——`选型2026-总决策表.md`（-154, 2026-07-01）已是完整的 5 档采纳/整合/借鉴/保留/淘汰母表**（基于 6 份选型文档）。本预备包只做两件事：**① 汇总自 07-01 以来的新输入（7 份）+ 其决策 delta；② 复核路线图「已建 vs 待研究」口径**。不改任何 `.py`。

---

## A. ⑤ 应吸收的「新输入」（总决策表之后新增的 7 份，按模块归位）

> 总决策表原始输入 = 6 份（148 TLS / 158 浏览器 / 155 CAPTCHA·CF / 151 OA·PDF / 146 Scholar / 147 批量·化学）。以下为其后新增、⑤ 定稿须并入的 delta：

| # | 新文档（作者/日期） | 归入母表模块 | 决策 delta（建议档位） |
|---|---|---|---|
| N1 | **选型2026-scansci竞速引擎源码架构与并行化改造**（142, 07-02） | 并发/pipeline（母表未覆盖的新维度） | 🧩**借鉴**：单条内「源竞速」(Event 唤醒) + 两级并发预算 + 可疑PDF检测(pypdf) + EMA 自适应源排序。**不整包引入** scansci(Apache-2.0，Cython 核非必需)。P0/P1、默认灰度 `--race` |
| N2 | **选型2026-出版商前缀直链路由表-scansci对标补丁**（142, 07-02） | A3 出版商定位（母表 P0=借鉴 metapub 的**落地补丁**） | 🧩**借鉴**：`publisher_direct.py` +8 前缀直构(ACM/RoyalSoc/WorldSci/EMBO/AMS/AIP/AVS/IOP)、Atypon `epdf` 优先、Elsevier 过滤 `-mmc`；**新增** `data/publisher_access_catalog.json`(challenge_risk 分档，供 A5) |
| N3 | **选型2026-机构订阅与住宅代理方案**（156, 07-01） | A5 机构订阅（母表未列） | 🧩**借鉴** instsci 的 CloakBrowser+Cookie 持久化架构做 `sources/institutional.py`(WebVPN/CARSI/EZproxy/Shibboleth，默认关)；**CloakBrowser**(MIT,58 C++补丁,reCAPTCHA v3~0.9) 列 P2 待评估；住宅代理仅配置模板/导航 |
| N4 | **选型2026-机构订阅Cookie与Profile持久化源码架构-对141建议** | A5 机构订阅（实现架构） | 🧩借鉴：Cookie/Profile 持久化层实现细节（与 N3 同族，供实现者） |
| N5 | **选型2026-RSC-Cloudflare挑战绕行方案**（176, 07-02） | CF 绕过（母表原列 byparr/Docker） | 🔧**修订母表口径**：通用 CF 首选 = **点亮已内置的 `render_fetch.py` nodriver**(pip，免Docker)；patchright 次选；FlareSolverr/byparr 降为「cookie 引导」可选兜底 |
| N6 | **选型2026-D3自适应限速与熔断-开源选型与增量设计**（177, 07-02） | D3 限速/熔断（母表未列，且路线图误标未建） | 🛠️**保留自建+3零依赖增量**(jitter/AIMD/half-open)；🧩**借鉴** pyresilience(MIT/零依赖)算法当参考实现；PyrateLimiter 留作异步(C4)按域令牌桶 |
| N7 | **选型2026-FlareSolverr免Docker变体-给145**（177, 07-02） | CF 兜底服务（母表 byparr 只提 Docker） | 🔧**修订母表口径**：byparr 可 **免Docker**(`uv run main.py`)；补 FlareBypasser(zendriver,pip 一条龙) 作 `/v1` 兼容变体；三者皆免 uc3.5.5/Chrome 死结 |

> 另可并入的 **检索成果类**（非选型但含决策）：角度8(出版商 TDM/被引口径/ROI)、绿色OA客户端A1·出版商SDK源码A3、补遗-publisherTDM 伪代码(Wiley/Elsevier/Springer keyed-TDM)、E2 本地检索选型、batch4/6 失败分桶。

---

## B. ⑤ 定稿建议结构（复用母表 5 档，补 3 处）

沿用 `选型2026-总决策表` 的 **✅采用 / 🔷整合 / 🧩借鉴 / 🛠️保留自建 / ❌不用·仅登记** 五档，只需：
1. **新增行**：D3 限速/熔断（N6）、单条内竞速+EMA+可疑PDF（N1）、机构订阅 A5（N3/N4）、publisher_access_catalog（N2）。
2. **修订行**：CF 绕过口径（N5/N7：nodriver 免Docker 首选 + byparr 免Docker/FlareBypasser 变体，FlareSolverr 降兜底）。
3. **刷新 P0–P4 优先级路线**：把 N1(竞速 P0/P1)、N2(publisher_direct +8 前缀 P1)、A5(N3/N4 P0-P1)、D3(N6 P2 实现波次) 排进去。

> ⑤ 定稿仍应恪守母表总原则：**外部库一律「可选依赖+函数内延迟导入+缺库优雅降级」，强制依赖只保 `requests`**。

---

## C. 路线图「已建 vs 待研究」口径复核（继 D3 更正后的系统抽查）

> 对照 `检索成果-路线图覆盖现状与剩余空白`(-160, 07-01) + `功能优化与新增功能建议`(路线图原文) vs 我已核验的源码。**诚实结论：除 D3 外无重大误标，母图基本健康**；仅 2 处需修订：

| 项 | 现有口径 | 实际（源码核验） | 修订 |
|---|---|---|---|
| **D3** 自适应限速/熔断 | 路线图/覆盖图均标「未建·未深研·当前固定 0.34s」 | `http_client.py` **已有**：按域间隔 + **表头自适应收紧**(`_maybe_adapt_rate`) + **连续失败熔断**(`_note_fail`/`_host_down`) + 429/5xx 退避尊重 Retry-After | ⚠️**主更正**：D3 应为「**已建~60%**，仅缺 jitter/AIMD/half-open」（详 N6） |
| **A3** 出版商适配器 | 覆盖图标「设计就绪、待实现」 | `publisher_direct.py`(144) **已实现 20 个 DOI 前缀直构**（Nature/Wiley/ACS/Springer/Elsevier/Atypon 系…含 %PDF 校验+selftest） | 🔧小更正：**直链构造已建**（20 前缀）；「待实现」仅指 keyed-TDM(`publisher_tdm`) 与 +8 前缀增强（N2） |
| 其余 A1/A2/A4/A5/B1/B2/B3/C1–C6/D1/D2/E1/E2 | 覆盖图口径 | 与源码一致（E1/B1/D2/A4/C1/C6 已建；A5/E2/publisher_tdm 设计就绪待实现；C4 aio.py 原型；B3 未建） | ✅ 口径准确，无需改 |

**复核结论**：`检索成果-路线图覆盖现状`(-160) 是一张可信的当前母图，**唯一实质错误是 D3 低估**（已由 N6 更正），A3 属轻微低估（直链已建）。建议 ⑤ / 路线图下次修订时同步这两处口径。

---

## D. 给 -156 的三句话
1. **⑤ 不用重建**：`选型2026-总决策表`(-154) 已是母表；⑤ = 并入本预备包 §A 的 7 份新输入 + §C 的 2 处口径修订。
2. **可立即排的实现波次**（回收后）：N1 单条内竞速(P0/P1,纯加速可灰度)、N2 publisher_direct +8 前缀(P1,低风险)、D3 三增量(N6,P2)、A5 机构订阅(N3/N4)。
3. **我待命**：-156 一派 ⑤ 我即据此定稿；或继续新的检索/读源码题。

---
*核验 2026-07-02｜信息检索-智库专家 -177｜仅汇总/复核/预备，不改 .py 及既有文档。核心：⑤=刷新总决策表(并入7份新输入)；路线图口径唯一实质误标=D3(已更正)，A3 轻微低估(直链已建)。*
