# 仓根临时脚本 · 三分类补齐清单（152/173/174 未覆盖增量）· 只产提案不执行

> 交付：**谷歌学术人机认证-149**（worker）｜承接：总指挥(157)指派、原属工程卫生秘书(142→147 均掉线)，认领两块：①152 清单未覆盖的后续波 `_*.py` 三分类补齐；②数据件归 `inputs/`。｜2026-07-03。
> **边界（硬约束，与 152/174 一致）**：**只产分类提案，不实际删除 / 不 `git mv` / 不改任何 `.py` 或生产代码 / 不动 git 索引**。落地（`rm` / `git mv` / `.gitignore`）须总指挥或属主拍板；**删除一律先 `git mv` 进 `archive/` 留一版**（北极星：留证）。
> **基线**：《仓根临时脚本清库清单-交付前三分类-152.md》（61 个已分类：A删20 / B归档31 / C保留9 / D待属主1）+《工作树三分类收口清单-173/174.md》增量。本文只补 **152/173/174 均未逐条覆盖的 ~45 个新脚本**，不重复已分类项。
> **快照**：工作树是移动靶（并发 worker 仍在产出）；本文为 **2026-07-03 时点快照**，仓根现存 `_*.py` ≈129 个（174 记 87，本波 146/154/156/157/159/174/176 新增多件）。

---

## 〇、TL;DR

| 档 | 数量 | 处置口径 |
|---|:--:|---|
| **A · 可删（先 archive 留一版）** | 8 | 纯 `_tmp_*` 一次性 traceback/取样/结算，结论已入 md/生产码，无残值 |
| **B · 应归档 `archive/scratch-2026-07/`** | 30 | 探针/审计/QC/取样，方法或证据可复用 → `git mv` 保 git 历史 |
| **C · 保留（在途，别动）** | 7 | 本波在途流程依赖（OCR14 处置 / 无头浏览器修复 / OpenAlex-key 接入 / Wiley 回收） |
| **合计** | **45** | 另附数据件归位建议（§四） |

- **判据沿用 152**：可删＝纯 traceback／用后即删／纯读打印结算／一次性导出（产物已单独留存）；应归档＝根因验证／门验证／内容 QC 审计／取样／冒烟，方法可复用或需留证；保留＝复验/接入流程在途或被 Runbook/文档引用。
- **保守原则**：凡属**当前活跃波（156/157/159/174/176）或明确在途主题**（OCR14、无头浏览器、OpenAlex-key 接入、Wiley 回收、RSC governor）→ 一律 **C 保留 / B 归档，绝不列 A 删**。

---

## 一、A 档 · 可删（8，先 `git mv archive/` 留一版再议 `rm`）

| 脚本 | 推断用途（据命名+波次） | 依据 |
|---|---|---|
| `_tmp_sweep_probe_149.py` | 149 波 sweep 一次性探针 | 纯探针；结论应已入 sweep 系 md |
| `_tmp_sweep_analyze_149.py` | 149 sweep 结果分析（纯读打印） | 用后即算 |
| `_tmp_sweep_oaset_149.py` | 149 sweep OA 集取样（一次性） | 一次性取样 |
| `_tmp_allow_diag.py` | allow/override 诊断（纯读打印） | 无波次后缀，明显临时 |
| `_tmp_v2_compare.py` | v2 口径对比（一次性核对） | 一次性对账 |
| `_tmp_v2_allow11.py` | v2 allow 11 条核对（一次性） | 同上 |
| `_tmp_reject_verify.py` | reject 结果一次性复核 | 纯读打印 |
| `_tmp_final_recipe_155.py` | 155 波"最终配方"临时脚本 | 结论应固化进配方 md |

> A 档均 `_tmp_*` 前缀、无长期引用；仍建议**先归档留一版**，属主确认无引用后再 `rm`。

---

## 二、B 档 · 应归档 → `archive/scratch-2026-07/`（30）

### 2.1 websearch / 开卷 / 假阳审计探针（结论已入检索成果/经验 md，方法留证）
| 脚本 | 推断用途 |
|---|---|
| `_ws_probe_141.py` / `_ws_probe_struct_145.py` | websearch 结果探针 / 结构化探针 |
| `_ws_openbook_141.py` / `_ws_openbook_qc_145.py` / `_ws_openbook_qc_159.py` | 开卷取证 + QC（141/145/159 波） |
| `_ws_dedup_141.py` | websearch 去重 |
| `_websearch_fp_openbook_141.py` | websearch 假阳开卷复核 |
| `_mon157_websearch_openbook_scan.py` / `_mon157_deepread_highrisk.py` | 监管157 开卷扫 / 高危深读 |
| `_mon176_agg_openbook_scan.py` / `_mon176_openaire_suspect_probe.py` / `_mon176_secondary_openbook_scan.py` | 监管176 聚合/OpenAIRE/二次开卷扫 |
| `_p3_openbook_153.py` / `_openbook_t0plus3_151.py` | P3 / T0+3 开卷探针 |

### 2.2 发布商（AIP/Wiley）探针与 QC（方法留证；见 §三注：Wiley 回收若在途则暂 C）
| 脚本 | 推断用途 |
|---|---|
| `_aip_oa_confirm_157.py` / `_aip_ja3_probe2_157.py` / `_aip_tandf_ja3_probe_147.py` | AIP OA 确认 / JA3 探针 |
| `_wiley_404_probe_148.py` / `_wiley_404_browser_148.py` | Wiley 404 根因探针 |
| `_probe_wiley_resolve_155.py` | Wiley 解析探针 |

### 2.3 OpenAlex-key 净增探针（多波；见 §三注：key 接入若在途则暂 C）
| 脚本 | 推断用途 |
|---|---|
| `_openalex_key_probe_140.py` / `_openalex_key_probe_160.py` / `_openalex_key_probe_176.py` / `_openalex_key_probe_phase2_174.py` | OpenAlex key 净增探针（140/160/174phase2/176 波） |

### 2.4 ROI 分桶 / sweep154 / ACS-SI / 其它（结论已入对应 md）
| 脚本 | 推断用途 |
|---|---|
| `_roi_bucket_probe_152.py` / `_roi_bucket_probe2_152.py` | ROI 分桶探针（结论入 ROI md） |
| `_sweep154_report.py` / `_sweep154_qc.py` / `_sweep154_extract_hits.py` | 154 sweep 报告/QC/抽取（同 174§3.1 的 sweep154 族口径→归档） |
| `_qc_reverify_acs_si_145.py` / `_gen_acs_si_union_append_145.py` | ACS SI 假阳复核 / SI 并集 append（审计留证） |
| `_probe_fast_oa_155.py` | 快速 OA 探针（155 波） |
| `_repro339_147.py` | issue #339 复现（根因写作参考） |

---

## 三、C 档 · 保留（7，在途别动）

| 脚本 | 保留理由 | 降档条件 |
|---|---|---|
| `_verify_headless_157.py` | **在途**：无头浏览器验证（正解决用户反馈的弹窗问题，总指挥在切默认无头） | 无头切换收口后 → 归档 |
| `_ocr14_dryrun_146.py` / `_ocr14_prep_probe_146.py` | **在途**：本波 OCR14 桶处置（秘书144 backup+13条append+--no-write验326 进行中） | OCR14 收口后 → 归档 |
| `_mon176_ocr_bucket_retry.py` | **在途**：OCR14 桶只读复抽（已读证实为只读探针），OCR14 复核链路仍用 | OCR14 收口后 → 归档 |
| `_wiley_realprobe_159.py` / `_wiley_qc_159.py` | 159 波 Wiley 真机探针 + QC，**若 Wiley 回收在途**（问 -159 属主） | Wiley 回收收口后 → 归档 |
| `_openalex_key_probe_176.py`（若 key 接入在途，见 §2.3） | 若 A5 机构订阅/OpenAlex-key 接入仍在预研（141/174 波有接入文档），最新一波探针留复验用 | key 接入定论后 → 归档 |

> **注（待属主确认，勿单方处置）**：
> - §2.2 Wiley/AIP 与 §2.3 OpenAlex-key：若对应回收/接入流程**在途**，请属主（-159 / A5 接入 owner）把最新一波探针**上提为 C 保留**；本文默认非最新波列 B 归档、最新波列 C。
> - `_mon176_*` 监管探针属**监管**产出，归档前知会监管。

---

## 四、数据件归位建议（附，非脚本主体）

> 处置：**批次/回收输入清单 → `git mv` 归 `inputs/`（非破坏）**；**脚本产物（`_*_out.txt`/`_*_report.txt`/`*_out.json`）→ 随对应脚本一并处置**。均**待拍板**，本文不执行。

- **批次/回收输入 → `inputs/`**：`recover_*_input.txt`、`rerun141_other_cf_input.txt`、`smoke5_140_input.txt`/`smoke154_live_input.txt`/`smoke170_input.txt`、`subset_elsevier_probe_149.txt`、`batch7_failed_dois.txt`/`batch7_subset_149.txt`、`p3_longtail_153_input.txt`、`recover_p2_cf_soft_155_input.txt`、`_t0_recover156_input.txt`、`_smoke_wiley_legacy_155_input.txt`、`_run_all_demo_input.txt`。
- **route-B 分桶输入**：`routeB_mdpi.txt`/`routeB_rsc_goldoa.txt`/`routeB_rsc_subscription.txt` → 随 route-B 复验保留或归 `inputs/`。
- **探针输入/真值**：`titles_probe.txt`/`titles_probe_truth.json` → 随 `_title_probe_content_qc.py`（152 A 档）一并处置。
- **脚本产物（随对应脚本）**：`_naming_audit_report.txt`、`_naming_spotcheck_146_report.txt`、`_batch6_final_out.txt`、`_selftests_145_out.txt`、`_filelist.txt`、`_agg_preprint_probe_149_out.json`、`_tmp_final_check.txt`、`_batch6_final_out.txt`。

---

## 五、机器可读清单（供非破坏迁移脚本消费；`bucket<TAB>file`，仅供拍板后使用）

```
A	_tmp_sweep_probe_149.py
A	_tmp_sweep_analyze_149.py
A	_tmp_sweep_oaset_149.py
A	_tmp_allow_diag.py
A	_tmp_v2_compare.py
A	_tmp_v2_allow11.py
A	_tmp_reject_verify.py
A	_tmp_final_recipe_155.py
B	_ws_probe_141.py
B	_ws_probe_struct_145.py
B	_ws_openbook_141.py
B	_ws_openbook_qc_145.py
B	_ws_openbook_qc_159.py
B	_ws_dedup_141.py
B	_websearch_fp_openbook_141.py
B	_mon157_websearch_openbook_scan.py
B	_mon157_deepread_highrisk.py
B	_mon176_agg_openbook_scan.py
B	_mon176_openaire_suspect_probe.py
B	_mon176_secondary_openbook_scan.py
B	_p3_openbook_153.py
B	_openbook_t0plus3_151.py
B	_aip_oa_confirm_157.py
B	_aip_ja3_probe2_157.py
B	_aip_tandf_ja3_probe_147.py
B	_wiley_404_probe_148.py
B	_wiley_404_browser_148.py
B	_probe_wiley_resolve_155.py
B	_openalex_key_probe_140.py
B	_openalex_key_probe_160.py
B	_openalex_key_probe_phase2_174.py
B	_roi_bucket_probe_152.py
B	_roi_bucket_probe2_152.py
B	_sweep154_report.py
B	_sweep154_qc.py
B	_sweep154_extract_hits.py
B	_qc_reverify_acs_si_145.py
B	_gen_acs_si_union_append_145.py
B	_probe_fast_oa_155.py
B	_repro339_147.py
C	_verify_headless_157.py
C	_ocr14_dryrun_146.py
C	_ocr14_prep_probe_146.py
C	_mon176_ocr_bucket_retry.py
C	_wiley_realprobe_159.py
C	_wiley_qc_159.py
C	_openalex_key_probe_176.py
```

---

## 六、执行建议（供总指挥 / 属主拍板，本文不执行）

1. **先建 `archive/scratch-2026-07/`**（仓根尚无 `archive/`）→ B 档 30 个 `git mv` 入档；A 档 8 个**保守也先 `git mv` 留一版**，属主确认无引用后再 `rm`。
2. **C 档 7 个别动**：待各自在途流程（OCR14 / 无头浏览器 / Wiley 回收 / OpenAlex-key 接入）收口后再降档归档；§三注中"最新波是否上提 C"请属主确认。
3. **数据件**：`inputs/` 目录归位（§四），非破坏 `git mv`。
4. **`.gitignore`**：随 152/174 建议一并补 `_*.py` / `_tmp_*` / `_spike_*` / `_probe_*` / `_route_b_*`，减少后续污染。
5. **护栏复述**：本文只读产提案，**未删 / 未移 / 未改任何脚本或生产代码 / 未动 git 索引**。

---

*核验 2026-07-03｜-149 交付 · 承接 142→147（均掉线）工程卫生秘书。补 152/173/174 未覆盖 ~45 脚本：A可删8 · B归档30 · C保留7（在途）+ 数据件归位建议。只读三分类提案，删除一律先 archive 留证、待拍板。*
