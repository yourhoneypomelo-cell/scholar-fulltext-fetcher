# 下一波回收 ROI 预排 · still_missing 660 机制分桶（141 工单）

> 交付：**谷歌学术人机认证-152**（worker）→ 总指挥 **-141**｜2026-07-03｜taskId=`task-9036c85a-1f14-446b-b046-e999fa934a97`
> 边界：**纯读析** —— 只读 `out/coverage.json` + 既有交付文档，**只新建本 1 份 md**；未改任何 `.py`/coverage/still_missing/经验记录/git、未跑网络、未发射 route-B。
> 权威源：`out/coverage.json`（`generated_ts=2026-07-03 12:50:24`：**success 326 / miss 673 / 999 = 32.63%**）、`out/still_missing.txt`（头注 **673**，三方一致）。分析脚本 `_roi_bucket_probe_152.py` / `_roi_bucket_probe2_152.py`（纯读、分析后可删）。
> 口径纪律（贯穿全文）：**发射 / 过 CF / success ≠ 净增**；任何重跑 success 必过内容 QC 门③④⑤（websearch 68.5% 假阳、ACS `/doi/pdf` ~93% SI 的前车之鉴）；净增带诚实区间、不虚抬。

---

## 〇、TL;DR（一页给总指挥 -141）

- **权威地板**（12:50:24 定版）：净成 **326 / 999 = 32.63%**、**miss = 673**。vs 174 波（340/659）**Δ−14 success / +14 miss**（其中 **−13 = OCR14 不可验证 success 卫生改判并入 miss**、−1 净纠偏，**非新墙**），定版三方一致（`coverage`=`still_missing.txt`=673）。
- **miss 的机制结构**（**01:27:42 的 660 快照分桶**；定版 673 = 660 + 13 OCR14 卫生，见 §二/§四）：一堵**免费物理到顶的订阅付费墙（404，A5-only、无凭据 ROI=0）** + **CF 托管质询站的 OA 极小子集（159，走量=0/仅子集）** + **RSC 双门（70，卡 governor）** + **长尾（27）**；外加**横切 OCR/不可验证 16 条（数据卫生）**。
- **下一波真净增只来自 4 个正交小池**（全零/近零凭据）：
  1. **p2_cf_soft_155 在途收口**（Wiley/AIP/T&F OA）→ 过 QC 后 **+3~7**。**最高即时 ROI**。
  2. **T0 翻案 / allow 池维护**（QC 改判 miss 188 中经内容QC 核实真正文者）→ 近零成本 **+2~8**。
  3. **RSC 金 OA ~8**（route-B）→ **现 0/8 卡 governor**，需 **-165 补丁**后小样本再验，点估 **+0~8**。
  4. **长尾 OA 子集**（p3_longtail 收口 + publisher_direct 补口）→ **+1~3**。
- **数据卫生（非净增，但口径诚实优先）**：176 波 **OCR 14 条"不可验证 success"** 曾全部计 success（websearch 扫描件/图片/加密）+ miss 内图片 2 条 → **定版已并入 OCR14 −13 卫生改判**（13 改判 miss、1 真值留 success），success **339→326** 已落盘，换来"可验证净覆盖"真实。
- **免费天花板**：现 32.63% → 在途+近零成本池全兑现（且扣 OCR 卫生）后 **~33~35%** 点估；**大头 Elsevier 377 + 订阅子集 ≈ 570 只能靠 A5 机构订阅**（凭据永久 gate、现 ROI=0）。破 ~40% **唯 A5**。

| 优先级 | 行动 | 桶 | 免费净增（诚实） | 归属 |
|:--:|---|---|:--:|---|
| **P0·收口** | p2_cf_soft_155 开卷 QC + 真净增结算 | CF-managed（Wiley/AIP/T&F OA） | **+3~7** | -149 QC / -155 |
| **P0·诊断+补丁** | RSC governor 反制（-165 ①②③）→ 小样本≥3 验 | RSC 双门 | **+0~8**（现 0） | -141/-156/-165 |
| **P1·近零成本** | T0 翻案剩余 + allow 白名单维护 | 跨桶（QC 改判 miss 188） | **+2~8** | -147/-149 |
| **P1·数据卫生** | OCR 14 不可验证 success 改判 miss + 换源（定版已并入 −13） | OCR 横切 16 | 口径诚实（success −13 已落盘） | -169/-149 |
| **P2·长尾** | p3_longtail 收口 + publisher_direct 补口 | 其他长尾 27 | **+1~3** | -152/-141 |
| **P3·gate** | A5 机构订阅（主体 ~570） | 订阅无OA 404 | **0 →（+30~40pp）** | -153/用户 |
| **❌ 不做** | FS 走量 ACS 128 / browser_search·wayback 扩 Elsevier / 双开 shim·route-B 锁 | — | ≈0 | — |

---

## 一、规模与三方一致性（673 定版）

| 口径 | 数值 | 来源 |
|---|---:|---|
| total_unique_dois | **999** | `coverage.summary` |
| 净成功 success（QC 后） | **326** | `coverage.summary`（12:50:24） |
| **still_missing / miss** | **673** | `coverage.summary` = `still_missing.txt` 去 `#` |
| 净覆盖率 | **32.63%** | 326/999 |
| success_before_qc | 514 | 原始去重成功 |
| rejected_total | **188** | 硬 33 + 软 155 |
| allow_override（白名单免剔） | 10 | 内容QC 核实真正文 |

> **三方一致（定版 12:50:24）**：`coverage.miss=673`、`still_missing.txt` 头注 **673**、`records` 逐条 miss **673**，0 条互差（01:27:42 快照曾三方 660，OCR14 −13 后同步 673）。
> **vs 174 漂移**：174 定版 340/659 → **定版 326/673（12:50:24）**，**Δ−14 success**（**−13 = OCR14 不可验证 success 卫生改判**、−1 净纠偏；无新墙、机制结构不变）。

---

## 二、机制横切分桶主表（互斥求和 = 660 · 01:27:42 快照）

> 桶按"破盾机制/墙型"切（与出版商桶正交但可换算，见主前缀）。**"免费可救"= 免费手段真净增潜力**，非"有 handler / 能发射 / 能过 CF"。

| # | 机制桶 | 条数 | 口径 | 主前缀（出版商） | 墙型（实证） | 免费可救？ | 建议路径 | 免费净增（诚实） |
|:-:|---|:--:|:--:|---|---|:--:|---|:--:|
| **1** | **CF-managed（CF 托管质询·不绑 JA3）** | **159** | 互斥 | ACS 128(10.1021)｜Wiley 23(10.1002/10.1111)｜AIP 5(10.1063/10.1116)｜T&F 3(10.1080) | Managed Challenge / Just-a-moment；FS-shim 解 CF 后 curl_cffi/cookie **可回放**（N.3） | 仅 OA/authorchoice/OnlineOpen 真正文**子集**（ACS 走量=0，SI 陷阱） | **开源**：单干净 FS-shim 串行 + 内容 QC 门 | **+3~7**（p2 收口，见 §三-1） |
| **2** | **RSC 双门：CF-JA3 门① + governor 门②** | **70** | 互斥 | RSC 10.1039 | 门① cf_clearance **绑 JA3/TLS**，curl_cffi 回放必 403（N.3 锤死）；门② `crawlprevention/governor` rate-gate（route-B 页内直下也归零，160 波 0/8） | 仅金 OA ~8 子集（卡 governor） | **开源**：-165 governor 补丁（landing 预热+per-host 限速/冷却+检测）+ 隐身/住宅IP | **+0~8**（本波实测 0，需补丁） |
| **3** | **订阅无OA（IP/登录墙 + 常规订阅 no-pdf）** | **404** | 互斥 | Elsevier 377(10.1016 372/10.1006 5)｜Springer 23(10.1007/10.1023/10.1134)｜IOP 4(10.1088/10.1070/10.1149/10.35848) | 数据中心 IP/登录 403（**非 CF**）+ link.springer landing-no-embedded-pdf（订阅落页） | ❌ 免费物理到顶 | **A5 唯一**（Crossref-PII→`/pdfft`、`content/pdf/{doi}`） | **0**（无凭据）／有凭据 **+30~40pp** |
| **4** | **其他长尾** | **27** | 互斥 | JNN 10.1166(4)｜CSJ 10.1246(4)｜IEEE 10.1109(2)｜中日俄小社/单条大刊(17) | 订阅小社为主，少数 OA | ⚠️ 部分 | **人工一键**：常规重跑 + publisher_direct 补口；无解封存 | **+1~3**（OA 子集，小） |
| **—** | **［横切］OCR/不可验证** | **16** | **横切·不并入求和** | miss 内 2(Elsevier 图片)｜success 内 14(待卫生) | 扫描件/图片 PDF（alnum=0）+ 加密 PDF | OCR 复验/换源 | **人工一键 + 数据卫生**（改判 miss） | 口径诚实（success −14），净增≈0 |

**互斥求和校验**：159 + 70 + 404 + 27 = **660** ✓（本表机制分桶基于 **01:27:42 的 660 快照**）。**12:50:24 定版 miss = 673 = 660 + 13**（+13 = §四 OCR14 "不可验证 success" 卫生改判并入 miss，落 OCR 横切/相应出版商桶；机制结构不变、细分重桶待 -169/-149 卫生线）。

> **⚠️ 关于任务列的「CF-JA3」桶**：全网**仅 RSC(10.1039) 绑 JA3**（N.3 实锤：ACS 不绑、cookie 可回放；RSC 绑、curl_cffi 回放必 403），故 **CF-JA3 ≡ 桶 2 的 RSC 门①**，无独立出版商条目——已并入桶 2，不另计。桶 2 标题保留「CF-JA3 门① + governor 门②」以呈现两道门。
> **末次原因镜头 vs 机制镜头**：`coverage` 末次原因（660，01:27:42 快照）= no-candidates 248 / qc_soft_reject 155 / cloudflare 69 / landing-no-pdf 69 / claimed-no-pdf 59 / qc_hard 20 / http-403 11 / governor 5 / 余零星。**规划用机制镜头（本表），不用末次镜头**——末次多被 recover/websearch 触碰覆盖了真墙（-143/-142 已证），且 QC 改判 miss 188 是卫生纠偏、**非可捞池**。

---

## 三、逐桶 ROI 详解（可救性 + 净增 + 路径）

### 桶 1 · CF-managed（159）——免费净增只在极小 OA 子集，ACS 走量=0

- **细分**（按主前缀 error 交叉；括注为出版商全量与主前缀差）：ACS 128（qc_soft 89 + cloudflare 32 + qc_hard 6 + no-response 1）｜Wiley 22×10.1002 +1×10.1111=**23**（cloudflare 13 + qc_soft 7 + qc_hard 2）｜AIP 4×10.1063 +1×10.1116=**5**（cloudflare 3 + qc_hard 1）｜T&F 3（cloudflare 3）。合 **159**。
- **可救性（诚实校准）**：ACS `/doi/pdf` 过 CF 后 **~93% 是免费 SI 冒充正文**（O.3）；-145 逐页看 56 条**仅 4 真正文（~12%）**→ **ACS 正文归 A5-only、FS/route-B 边际≈0，严禁走量**（会把 SI 当净增）。真免费净增只在 **Wiley OnlineOpen + AIP/T&F OA** 的极小子集，且**必须逐页过内容 QC**。
- **在途**：`p2_cf_soft_155`（Wiley+AIP+T&F OA 29 条）——174 波开局 10/29、5 raw OK 待 QC 剔假阳；FlareSolverr 侧 `OSError [Errno 22]`（Windows Proactor 老 bug），CF 路径预期 MISS，5×OK 走 publisher_oa/crossref/websearch。
- **ROI**：**+3~7**（p2 收口结算，过 QC）。**建议路径 = 开源**（单干净 FS-shim 串行、**不双开**、过 QC 门）。

### 桶 2 · RSC 双门（70）——机制可通、生产未就绪，卡 governor

- **细分**：claimed/no-pdf-captured **49**（route-B viewer 未落字节）+ qc_soft 12 + **governor 显式 5** + qc_hard 3 + no-downloadable 1。→ route-B 触 governor 未落字节是主体（54/70）。
- **归零主因**（160 波 0/8 实锤）：**非 JA3 回放**（route-B 已页内直下绕过），而是 **门② `crawlprevention/governor` rate-gate**——`_routeb_rsc_launch_156.py` 8 条连续串行 + 直导航 `articlepdf` 直链，完美踩中触发；坏 reCAPTCHA（`Invalid domain for site key`）**不可硬解**，只能不触发。单条 PoC（`d5ra08493h` 484KB）成功 ≠ 批量可复现。
- **可救性**：金 OA **~8** 上限（现 0）；订阅 **~59 唯 A5**；route-B 对订阅墙 `no-pdf` 是**正确行为**。
- **ROI**：**+0~8**（需 -165 补丁：P1 governor 检测 / P2 landing 预热勿直导 articlepdf / P3 per-host 30–90s 限速+命中冷却 5–30min / P5 坏 reCAPTCHA 不硬解；开源借力 zendriver/Camoufox 降 bot 判定 + 住宅 IP）。**当前 defer**，补丁后小样本 ≥3 篇再验。**建议路径 = 开源（补丁+隐身+住宅IP）**。

### 桶 3 · 订阅无OA（404）——660 的主体，免费到顶，唯 A5

- **细分**：Elsevier 372（no-candidates 242 + landing 44 + qc_soft 36 + cloudflare 15 + http-403 11 + no-response 7 + …）+ 10.1006 老 Elsevier 5｜Springer 23（landing-no-pdf 主体）｜IOP 4（landing）。
- **可救性**：**❌ 免费物理到顶**——browser_search 0/10、wayback 0/12、常规重跑 landing-no-embedded-pdf 反复证顶；`no-candidates 242` 即"无免费候选 URL"，是 IP/登录墙在末次口径的投影。
- **ROI**：**0（无凭据）**；**有凭据 = 最大增量**（A5 到手后 Elsevier `/pdfft` via Crossref-PII 放量，点估 **+30~40pp**，是唯一能真正清空主体的途径）。代码地基已就绪零副作用（A5-150），缺会话持久化层。**建议路径 = A5 机构订阅（凭据永久 gate，无凭据 ROI=0、不投人日）**。

### 桶 4 · 其他长尾（27）

- **构成**：JNN 10.1166(4)、CSJ 10.1246(4)、IEEE 10.1109(2)、中日俄小社/单条大刊(17，含 10.11862/10.3866/10.7503/10.26599 等)。
- **可救性**：订阅小社为主，少数 OA。publisher_direct 补口（同族可复用：10.1006/RSC-legacy/AIP-IOP 家族）；中日俄小社多无免费全文、无模板、A5 未必覆盖 → **封存**。
- **ROI**：**+1~3**（OA 子集小）。`p3_longtail_160`（29 条）在途，与 route-B/FS **不撞锁**。**建议路径 = 人工一键（常规重跑 + 补口）**；长尾无解封存不投人日。

---

## 四、OCR/不可验证 + 数据卫生专节（横切 16，非净增但口径优先）

> **176 波实锤**：`_mon176_ocr_bucket_still_success.txt` **14 条全部仍计 success**（源均 websearch），全页重抽 **14/14 无有效文本**（full_chars≈页数、alnum=0 → 扫描件/图片 PDF；1 条 `10.1021/acs.iecr.4c02255` 加密 NotImplementedError）。按 169 SOP §N.2 规则 4「**无法判定不得计 success**」→ 应改判 miss（非实锤假阳，是"不可验证 success"）。

| 子类 | 条数 | 明细 | 处置 |
|---|:--:|---|---|
| **success 内·扫描件/图片（chars=0）** | 13 | 10.1002/anie.201203185、10.1002/smll.201401464、10.1016/j.matlet.2015.09.140、10.1039/c5cy00124b … | OCR 后开卷复验，或换源；否则改判 miss |
| **success 内·加密 PDF** | 1 | 10.1021/acs.iecr.4c02255（27.7 MB，NotImplementedError） | 解密后复验或改判 miss |
| **miss 内·抓到 JPEG 图片（非 PDF）** | 2 | 10.1016/j.ces.2018.08.038、10.1016/j.ijhydene.2019.03.015（head=`ÿØÿà JFIF`） | 已在 miss（订阅桶 Elsevier）；换源/A5 |

- **另**：miss 内 **content-mismatch 9 条**（全 Elsevier，抓到错文/顶包，score<50 或 cross-publisher DOI 冲突）——已正确判 miss，属订阅桶。
- **ROI 性质**：**数据卫生、非净增**。**定版已并入 OCR14 −13**：success **339→326**（净覆盖 **32.63%**），headline 已降、换"**可验证净覆盖**"真实（诚实优先于好看）。**建议路径 = 人工一键 OCR/换源 + 数据卫生改判**（余 encrypted/图片条待复核），交 pipeline 属主（-169/-149 卫生线）。

---

## 五、下一波 ROI Top-N 优先级（供总指挥 -141 直接排工）

| 优先级 | 行动 | 对象（660 口径） | 机制/桶 | 预估净增 | 撞车域 | 归属 |
|:--:|---|---|---|:--:|---|---|
| **P0·收口** | **p2_cf_soft_155 开卷 QC + 真净增结算** | 29 CF-soft OA（Wiley/AIP/T&F） | 桶1 | **+3~7** | 等 PID 自然收尾，**勿双开** | **-149** QC / **-155** |
| **P0·诊断+补丁** | **RSC governor 反制补丁 → 小样本≥3 验** | 桶内金 OA ~8 | 桶2 | **+0~8**（现 0） | `out/.route_b.lock` 单头 | **-141/-156/-165** |
| **P1·近零成本** | **T0 翻案剩余 + allow 白名单维护** | QC 改判 miss 188 中真正文子集 | 跨桶 | **+2~8** | 无网络 | **-147/-149** |
| **P1·数据卫生** | **OCR 14 不可验证 success 改判 miss + 换源**（定版已并入 −13） | OCR 横切 16 | 横切 | 口径诚实（success −13 已落盘） | 无网络 | **-169/-149** |
| **P2·长尾** | **p3_longtail 收口 + publisher_direct 补口** | other 27 + 10.1006(5)/RSC-legacy | 桶4 | **+1~3** | run_all 常规（不撞锁） | **-152/-141** |
| **P3·gate** | **A5 机构订阅（主体）** | Elsevier 377 + Springer 23 + RSC 订阅 ~59 + ACS 正文订阅 ~90 + 长尾 ~20 ≈ **~570** | 桶3 | **0 →（+30~40pp）** | 凭据永久 gate | **-153/用户** |
| **❌ 不做/勿高估** | FS 走量 ACS 128 / browser_search·wayback 扩 Elsevier / 双开 shim·route-B 锁 / 直接报翻案上限 | — | — | ≈0 | 多 shim thrash | — |

**推荐排期节奏**：**先收口在途（P0 p2，禁另起跑 p2/route-B 双开锁）→ 近零成本并行（P1 T0 翻案 + OCR 卫生，零网络）→ 汇总真净增（务必过内容 QC 门）→ RSC governor 补丁小样本验（P0′ 诊断线，defer 放量）→ P2 长尾 → P3 A5 待凭据（不投人日）。**

---

## 六、诚实天花板结论（防高估，供决策）

- **免费手段（当前无机构凭据）净增天花板**：在途 p2（+3~7）+ T0 翻案（+2~8）+ RSC 金 OA（+0~8，需补丁）+ 长尾（+1~3），去重后乐观上沿 ~+15~25（**OCR14 卫生 −13 已并入定版 32.63%**）。**净覆盖点估从 32.63% 经免费回补上探 ~33~35% 区间**。
- **660 的主体（订阅无OA 404 + CF/长尾订阅子集 ≈ 570）是免费物理到顶的付费墙**——已被 browser_search 0/10、wayback 0/12、常规重跑 landing-no-embedded-pdf、RSC route-B 0/8 反复证顶，**唯一根本解是 A5 机构订阅（无凭据 ROI=0）**。
- **别把「发射数 / 过 CF 数 / success 数 / QC 改判 miss 数」当净增**：真命中 = 过盾/取到 + 内容 QC 门③④⑤ 双关；QC 改判 miss 188 是卫生纠偏、**不是可捞池**；OCR 14 "不可验证 success" **定版已下修 −13（339→326）**。
- **一句话给 -141**：**先把在途 p2 收口 + T0 翻案 + OCR 卫生这三笔近零成本做干净（P0/P1，过 QC）；RSC governor 补丁 defer 到小样本验（别拿算力硬撞 0/8）；主体订阅墙 404+ 锁 A5 待凭据（P3，ROI=0 不投人日）；长尾封存。免费天花板 ~33~35% 净覆盖，破此上限只能靠机构订阅 A5。**

---

## 七、来源与方法

- **权威地板**：`out/coverage.json`（12:50:24，summary: total 999 / success 326 / miss 673 / 32.63%、qc rejected_total 188(硬33+软155) / allow_override 10 / before_qc 514）、`out/still_missing.txt`（头注 673）。
- **本文分析（纯读、可删）**：`_roi_bucket_probe_152.py`（前缀×error 分布 + 交叉）、`_roi_bucket_probe2_152.py`（OCR 清单交叉 + RSC 细分 + QC 改判前缀）。
- **墙型/ROI 实证**：`经验记录-踩坑与发现.md` **N.3**（ACS 不绑 JA3 / RSC 绑 JA3）、**O.3**（ACS `/doi/pdf` ~93% SI）、**L/M**（websearch 假阳 + recover_b4_cf 真净增≈4%）、**S.2/P**（RSC governor + CDN viewer）。
- **RSC governor**：`route-B RSC 0-8 归零因与收口结论-160.md`、`选型2026-route-B反RSC-governor补丁方案-165.md`、`检索成果-开源过认证方案全网扫描-RSC-governor-时效热度维护-165.md`。
- **OCR/聚合源**：`检索成果-监管者176-OCR桶与聚合源扩扫.md`、`out/_mon176_ocr_bucket_still_success.txt`、`out/_mon176_agg_openbook_scan.csv`。
- **对齐引用（不重画）**：`检索成果-still_missing分桶统计刷新与下一波ROI-174.md`（174 定版 340/659）、`检索成果-still_missing620机制横切分桶与下一波ROI优先级-142.md`、`检索成果-still_missing628全量ROI与优先级排序-150.md`、`基线口径冻结说明-388-173.md`、`A5机构订阅现状与still_missing可救前缀梳理-150.md`。

---
*核验 2026-07-03｜-152 worker · 工单 -141｜纯读析，未改 .py/coverage/still_missing/经验记录/git、未跑网络、未发射 route-B｜互斥机制分桶求和 660（01:27:42 快照）｜**定版 326/673/32.63%@12:50:24（OCR14 −13 已并入、rejected 188）**；下一波 = P0 p2 收口(+3~7) + RSC governor 诊断/补丁(+0~8 defer) → P1 T0 翻案(+2~8) + OCR 卫生(定版已 success−13) → P2 长尾(+1~3) → P3 A5 gate(0→+30~40pp)；免费天花板 ~33~35%，主体 ~570 唯 A5；发射/过CF/success/QC改判 ≠ 净增，每笔过内容 QC 门。*
