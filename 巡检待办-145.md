# 巡检待办清单 · -145(持续巡检与总指挥审计 AI)

> 用途:审计 AI 巡检中发现、但**不属于审计角色自行修改**(涉及生产码/属主在飞文件)的待办项,移交属主会话接手。
> 纪律:审计 AI 默认只读 + 非破坏扫描;本文件仅登记待办,不代改生产码。
> 生成:2026-07-03 23:50 · -145

---

## P2 · 代码健康(非阻塞)

### 1. nodriver `get_all_cookies` DeprecationWarning(属主待办)

- **现象**:`python -m tools.flaresolverr_nodriver --selftest` 联网自检时打印
  `DeprecationWarning: Call to deprecated function (or staticmethod) get_all_cookies. -- Deprecated since version 1.3.`
  位置:`tools/flaresolverr_nodriver.py:166` `raw = await self._tab.send(cdp.network.get_all_cookies())`。
- **影响**:**非阻塞**。自检仍 `FLARESOLVERR_NODRIVER_OK` 通过(该调用已有 `try/except` 兜底 → `self._browser.cookies.get_all()`)。仅为 nodriver ≥1.3 的弃用告警,升级 nodriver 后可能变错误。
- **建议修复**:把非弃用 API(`self._browser.cookies.get_all()`)提为首选路径、`cdp.network.get_all_cookies()` 降为兜底;或按 nodriver 1.3+ 新签名改调用。改后跑 `RUN_ONLINE_SELFTESTS=1 python -m tools.flaresolverr_nodriver --selftest` 复验无 warning。
- **归属**:route-B/浏览器栈属主会话(近期活跃于 `63ce5f7`/`b892eca`/`df1554d`)。
- **状态**:⬜ 待属主处理。审计 AI 未改该文件(生产码 + 可能与属主在飞改动冲突)。

---

## 已核销(移入下方存档,勿重复处理)

- ✅ **监管者176 开卷扩扫 HOLD**:六子项逐 DOI 核对已并入 368 定版,文档已关闭 HOLD(-145,commit `bd32ad6`)。
- ✅ **2 个 env-gated SKIP 自检**:`regress_qc_union_189`(REGRESS_UNION_189_OK)+ `flaresolverr_nodriver --selftest`(FLARESOLVERR_NODRIVER_OK)双通过(-145 补跑)。
- ✅ **36 条 pdf-missing 回收清单**:`out/_pdfmissing36_recover_145.txt` 已生成(-145)。

---

*-145 巡检登记｜审计 AI 非破坏原则:仅登记待办,不代改生产码/属主在飞文件。*
