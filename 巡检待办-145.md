# 巡检待办清单 · -145(持续巡检与总指挥审计 AI)

> 用途:审计 AI 巡检中发现、但**不属于审计角色自行修改**(涉及生产码/属主在飞文件)的待办项,移交属主会话接手。
> 纪律:审计 AI 默认只读 + 非破坏扫描;本文件仅登记待办,不代改生产码。
> 生成:2026-07-03 23:50 · -145

---

## P2 · 代码健康(非阻塞)

### 1. nodriver `get_all_cookies` DeprecationWarning ✅ 已由属主修复(commit `46f0b37`)

> **2026-07-04 00:34 属主已修复**:`_get_cookies` 首选非弃用 `self._browser.cookies.get_all()`,`cdp.network.get_all_cookies()` 降为兜底(行为不变、仅换序),commit `46f0b37` 明确引用「巡检待办-145 P2」。本项**关闭**。下方原始记录留档。

<details><summary>原始待办记录(已解决)</summary>

- **现象**:`python -m tools.flaresolverr_nodriver --selftest` 联网自检时打印
  `DeprecationWarning: Call to deprecated function (or staticmethod) get_all_cookies. -- Deprecated since version 1.3.`
  位置:`tools/flaresolverr_nodriver.py:166` `raw = await self._tab.send(cdp.network.get_all_cookies())`。
- **影响**:**非阻塞**。自检仍 `FLARESOLVERR_NODRIVER_OK` 通过(该调用已有 `try/except` 兜底 → `self._browser.cookies.get_all()`)。仅为 nodriver ≥1.3 的弃用告警,升级 nodriver 后可能变错误。
- **建议修复**:把非弃用 API(`self._browser.cookies.get_all()`)提为首选路径、`cdp.network.get_all_cookies()` 降为兜底;或按 nodriver 1.3+ 新签名改调用。改后跑 `RUN_ONLINE_SELFTESTS=1 python -m tools.flaresolverr_nodriver --selftest` 复验无 warning。
- **归属**:route-B/浏览器栈属主会话(近期活跃于 `63ce5f7`/`b892eca`/`df1554d`)。
- **状态**:✅ 已由属主 `46f0b37`(00:34)修复。审计 AI 未改该文件。

</details>

---

## 轮次#3 发现(2026-07-04 01:05)

- ✅ **775.5MB 交付 zip 未 gitignore 风险 → 已由属主关闭**:`谷歌学术全文交付-368-enriched.zip`(consolidate_delivery --enrich 产物)曾在仓根未被忽略,一次 `git add -A` 即可膨胀仓库。属主已提交 `.gitignore` 规则 `/*交付*.zip` + `/*delivery*.zip` 覆盖之(`git check-ignore` 命中 `.gitignore:66`)。审计 AI 曾临时加 `/*.zip` 试封、复核发现属主规则已生效遂**还原 .gitignore 到 HEAD**(无残留)。**风险关闭**。
- 📌 **openalex_content 源 · 真实净增=0(如实)**:属主新增源已注册 + 离线 selftest 过(A 级代码),但 `by_source` 无该源、`openalex_key` 未配置 → 需付费 key($0.01/篇)且未在语料上真跑;无 key 时零请求零计费(gate 正确)。**能力已交付、净增未兑现**——属"潜在能力"非"已实现净增"。
- 📌 **build_coverage 方案C · 对当前368贡献=0(如实)**:代码 + selftest 已入库,但权威 368 由 writeback149 物理解隔离产出(generated_ts 22:07:22),非方案C。方案C 是**未来翻案免手工解隔离**的长期改进,对当前 KPI 净增 0。
- 📌 **A5 已由用户拍板暂缓(重要口径变化)**:属主《交接待办-A5暂缓与后续增强-149.md》记录用户明示「A5 暂不在本项目落实」。**A5 从「唯一硬阻塞」重定性为「用户拍板的交接/暂缓项」**——本项目免费边界内已收口,A5 万事俱备只欠凭据、移交未来/他项目。本会话《A5放量执行顺序-441分片-route分类-145.md》作为凭据到位后的放量补充,与交接待办互补不冲突。
- ℹ️ **自检口径小差**:本会话实测 `PASS=47/FAIL=0/SKIP=2`;交接待办-149 记 `SKIP=1`。差 1 = `regress_qc_union_189`(data-regress gated,近期注册);非失败,口径以实测为准。

## 已核销(移入下方存档,勿重复处理)

- ✅ **监管者176 开卷扩扫 HOLD**:六子项逐 DOI 核对已并入 368 定版,文档已关闭 HOLD(-145,commit `bd32ad6`)。
- ✅ **2 个 env-gated SKIP 自检**:`regress_qc_union_189`(REGRESS_UNION_189_OK)+ `flaresolverr_nodriver --selftest`(FLARESOLVERR_NODRIVER_OK)双通过(-145 补跑)。
- ✅ **36 条 pdf-missing 回收清单**:`out/_pdfmissing36_recover_145.txt` 已生成(-145)。
- ✅ **36 条 pdf-missing 隔离真跑**(承用户 GO):**净增 0/36**(30 cf-403 RSC + 4 dl-fail + 2 no-src),`--verify` REPRODUCIBLE,权威 coverage 未动。结论见《回收实测-pdfmissing36-146.md》——该桶归 A5/route-B,**免费路线到顶,原预估 +1~3pp 修正为 0pp**。
- ✅ **A5 无凭据冒烟**(承用户选 opt-dryrun,隔离 `out/a5_smoke_nocred_145`):18 条 → **0/18 success、0 PDF 落盘、权威 coverage 逐字未动**,`--verify` REPRODUCIBLE。分桶 paywall(Elsevier http-403)=8 / timeout(straggler,ACS/RSC 尾部)=8 / cf-403(ACS)=2。**结论:框架凭据 gate 行为正确——无凭据时订阅直链 401/403/cf-403 被 %PDF 校验过滤,不产假成功、不污染 coverage、零副作用。** 真联调仍待用户创建 `.ftf_institutional.local.json`。

## P3 · 数据卫生(轻微,非阻塞)

### 2. still_missing 直通 straggler 条目残留输入行内联注释

- **现象**:`out/a5_smoke_nocred_145/still_missing.txt` 中 8 条 straggler-timeout 条目保留了输入文件的**行内 `#` 注释**(如 `10.1021/ja00136a029       # ja  JACS (旧刊)`),而正常 resolve 过的条目 DOI 干净。
- **影响**:**轻微**。隔离 RUNROOT;DOI 仍是行首 token、回喂 `-f` 可解析。仅不整洁。
- **根因推测**:straggler 强制记终态路径记录的是**原始输入行**,未走 DOI 规范化(去行内注释)。
- **建议**:straggler 记录前对输入行做与正常路径一致的 DOI 规范化(strip 行内 `# …`)。归 pipeline 属主。
- **状态**:⬜ 登记备查(非阻塞)。

---

*-145 巡检登记｜审计 AI 非破坏原则:仅登记待办,不代改生产码/属主在飞文件。*
