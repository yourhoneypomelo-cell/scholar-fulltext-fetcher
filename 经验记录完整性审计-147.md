# 经验记录完整性审计 · 147（只读缺口清单）

> 交付：**谷歌学术人机认证-153**（worker）｜taskId=`task-5444962f-777e-40ec-8da2-b3aa403e8ab5`｜2026-07-03
> 触发：总指挥派单落实用户准则#4——程序实现路径/原理、踩坑/成功经验/新发现须完整记录；只读审计《经验记录-踩坑与发现.md》及相关经验文档，核实本波(140-147)关键定论是否完整落档，产①缺口清单 ②每条应补写要点 ③结构/去重建议，**不改正文**（实际单写落档由指定单写者执行）。
> 审计对象：`经验记录-踩坑与发现.md`（A–T 全 20 节）、`经验记录-增补-本轮五大定论-2026-07-02.md`（-150）、`经验记录-增补-监管者169-回写必开卷SOP…-2026-07-02.md`（-169）、`经验记录完整性审计-141波.md`（-160）、`经验记录完整性审计-本波关键主题落档核对-140.md`（-140）。
> **代码事实核实**：门③/route-B 落盘过门两处易漂移点，以**工作树 `fulltext_fetcher/download.py`（未提交）实测**为准，不采信文档旧口径。
> **边界**：只读；仅新建本 1 份 md；**未改**《经验记录》正文 / 代码 / coverage / 黑名单 / git。
>
> ℹ️ **coverage 口径**：本文净覆盖数字以 `out/coverage.json` `summary` 为唯一权威。成文时最近落盘权威 = **340/999 ≈ 34.03%**（`本波回收交付汇总.md`，`generated_ts 2026-07-03 00:42:19`）／**343/656/34.3%**（`纠偏重算配方-155.md`，01:03:41，p2 一级扫入 +5）。主文档 T 节仍用占位「**339/999，预计 +2 后 ~341**」——见结构建议§三·4。
>
> ⚠️ **【历史快照】上述 340/343/339 均为成文时快照；当前权威 = `out/coverage.json`：326 / 673 / 32.63% @2026-07-03 12:50:24, allow=10**（OCR14 −13 已并入；数字勿当当前）。

---

## 〇、TL;DR

### A. 总指挥点名的 8 大主题（本波 140-147）

| # | 点名主题 | 落档结论 | 正文位置 | 动作 |
|---:|---|---|---|---|
| 1 | **CF-JA3 实锤仅 RSC 绑**（ACS 回放可过 / RSC 回放必 403） | **已落且充分** | N.3 + T.3 | 无需补 |
| 2 | **RSC governor**（应用层 rate-gate、坏 reCAPTCHA、补丁 8/8 净增 0） | **已落且充分** | S.2 + T.3（+指针 -165/-160） | 无需补 |
| 3 | **CF-soft 翻案 +2**（recover_p2_cf_soft OA 子集回收结果） | **缺失** | 主文档无专条（仅 ROI 预排/155 churn 侧提及） | **补条**（并核定「+2」真数） |
| 4 | **Wiley pdfdirect 修复**（遗留 DOI 截断/编码 404） | **已落且充分** | T.4 | 无需补 |
| 5 | **FS-shim Errno22 根因**（多 shim 重载 thrash、非 FS 真挂） | **已落且充分** | N.7（现象）+ N.8 #1（根因定论） | 无需补 |
| 6 | **开卷 QC 参考文献区假阳**（expected-doi-present 误计 in-text） | **已落**（拟修位置权重属待办、非落档缺口） | T.6（`aic.690210612` 实锤） | 无需补 |
| 7 | **OpenAlex key 防 429**（解限速不解订阅、Z=1/660） | **已落且充分** | T.7 + 增补 M.4 | 无需补 |
| 8 | **route-B 免费到顶**（软盾可过、硬墙到顶、根解唯 A5） | **已落且充分** | S.2 + T.3 + Q.5 + O.2 | 无需补 |

**8 项结论**：**6 已落充分 / 1 已落(6·待办另论) / 1 缺失(CF-soft)**。点名主题里唯一真缺口 = **CF-soft OA 子集回收结果（主题 3）**。

### B. 前序审计(-140/-141波)遗留开口 · 当前状态复核

| 来源 | 开口 | 当前状态（本波复核） | 结论 |
|---|---|---|---|
| -140 | **N.9**（route-B 方法A：同源 B1 / 跨源过盾后门控 Fetch；Network 域取不到导航 articlepdf body）+ PreflightWarn `ValueError` bug | 主文档 N 节**止于 N.8**，无 N.9 | **仍缺失（P0/P1）** |
| -140 | route-B 落盘 **force=True 过门**（曾结构性绕过内容 QC） | **代码已落**（`download.py` L1157/1362 实证 `force=True`），**正文 N.5 未记** | **文档缺口** |
| -140 | N.5「**已补门③**」与代码不符 | `download.py` 无 `meta-doi`/`prism:doi` 反查（门③元数据侧未实现，T.7 出处标「门③异DOI反证-99PENDING」） | **更正仍待改** |
| -140 | **coverage 二级(嵌套)扫描机制** + 「+71」证伪 + stale 黑名单双向错（原建议 P 节） | P 字母已被 **RSC route-B**（本文 P 节）占用；机制/口径坑仅散见 T.2 指针，主文档无专节 | **部分缺失** |
| -141波 | **T.1 nl200722z** 真正文 + SI 扣减 mon176/33 非 145/41 | 已落 **T.1** | **已闭合** ✓ |
| -141波 | **T.2** SI33 + websearch9 → ~343 下修链 | churn recipe 已落 T.2；SI33 见 T.1；下修终值 340 见《本波回收交付汇总》 | **主体已落**（数字待广播回补） |
| -141波 | **更正 O.1 首段**（真正文 4→1） | O.1 首段(L371)**仍写「真正文 4」**，仅 R.2(L488)前向更正为 1 | **前向不一致仍在** |
| -141波 | **补 Q.5** 双层口径（诚实地板 vs 免费天花板） | Q.5 数字链停在 388→391→~400，未接 340 地板 | **待补一句 + 更新数字链** |

---

## 一、8 大点名主题逐条核对

### 1. CF-JA3 实锤仅 RSC 绑 —— 【已落且充分】
- **正文**：**N.3**「ACS 不绑 JA3（回放可过）/ RSC 绑 JA3（回放必 403）—— 分水岭」——机理（solve 拿 cf_clearance→curl_cffi 回放，强 CF 站另绑 JA3/TLS）、ACS 实测落 4.41MB、RSC 3 条 articlepdf 全 403、curl_cffi「非解题真 Chrome 的 JA3」背景齐全；**T.3** 再固化为「JA3 绑定回放」硬墙一类。**充分。**

### 2. RSC governor —— 【已落且充分】
- **正文**：**S.2**（-165 情报实锤：CF 之外第二道 `crawlprevention/governor` rate-gate、`Validate User` 坏 reCAPTCHA「Invalid domain for site key」、第一篇过第二篇即死、完美解释 0/8 与单条 d5ra08493h 成功的悖论、补丁三件套指针）；**T.3**（governor 全套补丁真机 8 金OA **8/8 净增 0**、可观测未破墙）。指针 `选型2026-route-B反RSC-governor补丁方案-165.md`、`route-B RSC 0-8 归零因与收口结论-160.md`。**充分。**

### 3. CF-soft 翻案 +2 —— 【缺失】（点名主题里唯一真缺口）
- **应落内容**：`recover_p2_cf_soft`（单 shim CF-soft OA 子集回收，脚本 `_qc_p2_cf_soft_155.py` / 输入 `recover_p2_cf_soft_155_input.txt`）的**回收结果与真净增**。
- **正文现状**：《经验记录》**无任何 `p2_cf_soft` / CF-soft 回收结果条目**。CF-soft 仅以**前瞻 ROI**出现在分桶文档（`…620-142`「P2 CF-soft OA 子集 +3~11」、`…611-149`「+3~9，须防 ACS-SI 陷阱」），以及 churn 侧 `纠偏重算配方-155.md`「**p2_cf_soft_155 一级扫入 +5**」。主文档 T.3 只写「软盾可过」的机制立场，未落该桶**实测净增结果**。
- **⚠️ 数字待核**：点名「**+2**」与 155 实测「一级扫入 **+5**」、ROI 估计「+3~9/+3~11」**三者不一致**——需单写者据 `out/` 证据路径（`_qc_p2_cf_soft_155.py` 产物 / 对应 summary）核定「过内容 QC 门后的**真净增**」到底是几条，勿把「跑批 success / 一级扫入」当真净增（承 M.2/O.2「success ≠ 真净增」铁律）。
- **补登建议**：新增一条（并入 T 节或 S 节，草稿见§四），写清：桶=CF-soft OA 子集、机制=单 shim 过软盾、**真净增 = N 条（据证据核定，防 ACS-SI 陷阱）**、与 155 churn「一级扫入 +5」的口径区别。

### 4. Wiley pdfdirect 修复 —— 【已落且充分】
- **正文**：**T.4**（`(` `)` `:` 等特殊字符未 encode 致 404 根因、`_wiley_doi_path()` 对 suffix 整段 `quote(safe="")` 修法、三文件 `publisher_oa/publisher_direct/publisher_adapter` 各新增断言、现代 DOI 明文路径不变、纯 bugfix 未做真网验证）。指针 `Wiley pdfdirect URL bug 修复.md`。**充分。**

### 5. FS-shim Errno22 根因 —— 【已落且充分】
- **正文**：**N.7**（现象：进程仍 listening 但每次 solve 报 `OSError:[Errno 22]`、`flaresolverr_recovered=0`、acs 跑到 ~28 条 exit_code=1）+ **N.8 #1**（根因定论：同机并发 7 个 shim〔3×flaresolverr + 4×fs_shim_141〕thrash → nodriver 拖垮，**非 FS 真挂/非 CF 真破**；-144 kill 一个 shim 释放 ~10 chrome；修法=**单 shim 单桶串行**，降载后 acs CF 200 @6.9s）。**充分。**

### 6. 开卷 QC 参考文献区假阳 —— 【已落】（拟修属待办）
- **正文**：**T.6**「`expected-doi-present` 把参考文献区 DOI 误计 in-text（拟加位置权重）」——弱点机理（正信号只查是否出现、不看位置；错论文引用了期望 DOI 即误判 match）、实锤 `10.1002/aic.690210612`（现 `batch6/rejected/`）、与门②首 1200 字/门③ body@1500 位置规避的呼应、拟修（期望 DOI 须出现在首部/标题相邻区）、落点交 -140 + 补 `regress_qc_union_189.py` 负样本。
- **说明**：「拟加位置权重」是**待落地代码 TODO**（属主 -140），非**落档缺口**——踩坑与修法方向已完整记录。**充分。**

### 7. OpenAlex key 防 429 —— 【已落且充分】
- **正文**：**T.7**（无 key=100 credit/天易 429/401/403 静默降级；免费 key=100k credit/天 ≫ 999 DOI 用量、成本≈0；**解限速不解订阅**：-176 探针 660 条 → 内容 QC 通过仅 **Z=1/660**；坑：`run_all.py` 仍不透传 `openalex_key`，待补）+ 增补 **M.4**（mailto 礼貌池 2026-02 已废、免费 key 是唯一动作、Crossref 礼貌池仍有效别混改）。**充分。**

### 8. route-B 免费到顶 —— 【已落且充分】
- **正文**：**S.2**（RSC 金 OA 8 条 route-B netgain=0/8）+ **T.3**（route-B 只过软盾、硬墙三类到顶、8/8 净增 0、根解「避验证非破验证」= A5）+ **Q.5**（免费天花板 39–41%、miss≈600 唯 A5）+ **O.2**（FS 三桶真破 CF=0、真正文仍需 A5）。指针 `回收实测结论-CF与免费路线到顶.md`。**充分。**

---

## 二、前序审计遗留开口 · 逐条现状（避免重复劳动、只报增量）

> -141波审计的 7 主题里，**route-B RSC 0/8·A5 唯一解·FS 真破 CF=0·acs-authorchoice 机制** 已确认充分（不再重列）；-140 审计的 CF 根因·JA3·QC 主体 亦充分。以下只列**当前仍开**的口。

### 2.1 【仍缺失】N.9 · route-B 方法A 定论 + PreflightWarn bug（承 -140 缺失 A+B，P0/P1）
- **现状**：主文档 N 节止于 **N.8**，**无 N.9**。-140 审计备好的成段草稿（同源 PDF→B1；跨源/viewer 导航 PDF〔RSC articlepdf〕→ 过盾后门控 `Fetch.enable` RESPONSE；**Network 域取不到导航 PDF 的 body**；-154「整体删 Fetch 改纯 Network 域」对同源 OK、对 RSC 不成立需引回过盾后门控 Fetch）**未合入**。
- **附带必修 bug（PreflightWarn）**：`_tolerant_from_json` 只兜 `KeyError`（字段缺失）、**不兜「字段在但枚举值 nodriver 0.50.3 不认」**（Chrome133 发 `PreflightWarn` → `LocalNetworkAccessRequestPolicy.from_json` 抛 `ValueError` 洪水 → 网络事件被丢弃 = A 端 Network 域抓不到 body 的直接机制），亦未落档。
- **建议**：合入 **N.9**（草稿见§四·A，直接取 -140 审计§三成稿即可，去重 2 份暂存草稿）。**注意 P 字母已被占用**，此定论应挂 N.9，不要再叫「P 节」。

### 2.2 【文档缺口】route-B 落盘 force=True 过门（承 -140 缺失 C）
- **代码实证（本波复核工作树 `download.py`）**：`_browser_capture_fallback`（L1103，落盘前 L1157–1159 `_content_qc_gate(..., force=True)`）与 `_browser_pdf_download`（L1340，L1361–1362 同款 `force=True`）**均已强制过门**；`force=True` 注释明写「越过 DOI-keyed 源豁免（route-B 经浏览器导航）」。**代码已修、正确。**
- **落档缺口**：N.5 只讲「FS/路线B 回收必须与内容 QC 联用」，**未记**「route-B 三出口（②b B1/B2、⑥ Akamai/MDPI）落盘路径**本身曾结构性绕过门、现已 `force=True` 接门**」这一踩坑+修法。
- **建议**：N.5 末补一条（草稿见§四·B）。

### 2.3 【更正仍待改】N.5「已补门③」与代码不符（承 -140 更正 1，P0）
- **正文**：N.5(L315)写「-173 建议 + 142 复核 + 147 实测**已补门③（文内/元数据 DOI≠期望）**/门④/门⑤」。
- **代码实证**：`download.py` 有门①（content-title-mismatch）、门②（url-doi-conflict，`_qc_doi_publisher_conflict`）、门④（SI）、门⑤（citation-report/poster/TOC）；`_content_qc_verdict`(L700) 的 DOI 强正只查「期望 DOI 是否在正文/URL」，**grep 全文无 `meta-doi`/`prism:doi`** → **门③的「元数据 `/doi` 反向比对」侧未实现**（与 T.7 出处「门③异DOI反证-99PENDING-148」自洽）。
- **建议**：N.5 表述更正为「**门④⑤ 已落地；门③（文内/元数据 DOI≠期望，meta-doi-mismatch）仍未实现（P0，`cctc.200900261` 一类漏网）**」，别把「建议补」写成「已补」。（此为正文修订，需《经验记录》写锁属主执行；本审计不改正文。）

### 2.4 【部分缺失】coverage 二级(嵌套)扫描机制 + 「+71」证伪 + stale 黑名单双向错（承 -140 缺失 D）
- **现状**：`build_coverage.list_batch_dirs` 早已 `os.walk` 递归收 `out/<dir>/fetch/metadata.jsonl`（`include_nested` 默认 True、`--no-nested` 逃生、`--extra-dirs` 定向、demo/smoke 剔除）——「修扫描」实为已完成态；口径坑「+71→442/561 在 QC 口径不成立」、stale 黑名单**误杀 4 真全文 / 漏 3 假阳 md5 碰撞**——这些**机制/踩坑**主文档无专节，仅 T.2 以指针形式引 `coverage 口径重算规范…144.md`。
- **建议**：优先级 P1。可**并入 T.2** 一小段（机制一句 + 口径锚定「净数字唯认 `coverage.json summary.qc.success_after_qc`，dry-run 中间态勿当终值」），不必再单开一节（避免与 churn recipe 重复）。

### 2.5 【前向不一致】O.1 首段「真正文 4」（承 -141波 更正 O.1）
- **现状**：O.1(L371)**仍写「真正文 4」**，R.2(L488) 已开卷推翻为「真正文 1 / +1」，《本波回收交付汇总》亦确认「QC 净增真正文 ≈1」。读者先读 O.1 会拿到过时数字。
- **建议**：O.1 首段改「真正文 **1**（经 R.2 开卷复核推翻，`acscatal.0c04429`）」并加 pointer→R.2；O.1「真净增率 4/95」同步更正。

### 2.6 【待补一句 + 更新数字链】Q.5 双层口径（承 -141波 补 Q.5）
- **现状**：Q.5 数字链停在 `388→391→~400`、免费天花板「39–41%」；当前权威已下修到 **340/999≈34.03%**（00:42:19）→ churn 到 343（01:03:41）。Q.5 未接 340 诚实地板、未区分「诚实地板 ~34%」vs「免费手段天花板 ~40–42%」。
- **建议**：Q.5 或 T.2 末加一句——「**诚实地板（SI33+ws9 落盘后）≈ 340/999 ≈ 34.0%；免费手段天花板（T0/route-B/CF-soft 摘果后）~40–42%，破此唯 A5**」，并把主文档所有净覆盖占位「339/~341」按 T.7 约定待广播后由 -145 回补至 `coverage.json` 现值。

---

## 三、结构 / 去重建议

1. **【小节字母碰撞·优先】增补文档与主文档 M/N 节重号**：
   - `经验记录-增补-本轮五大定论-150.md` 用 **M.1–M.5**，与主文档 **M 节**（M.1–M.2「同题非全文四类假阳」）**同号异义**；
   - `经验记录-增补-监管者169.md` 用 **N.0–N.6**，与主文档 **N 节**（N.1–N.8「CF/JA3/route-B」）**同号异义**。
   - 建议：合入主文档时改用**唯一续号**；未合入前，在两份增补文件顶部**显著标注**「本文编号为增补内部序，勿与《经验记录》正文 M/N 节混引」。

2. **【审计文件三份去重】** `完整性审计-140` / `完整性审计-141波` / 本 `完整性审计-147`：三份逐波推进、口径连续。建议：**前两份保留为历史事实源**，以本 -147 为**当前开口口径**（本文§二已逐条复核前两份开口的现状：T.1 已闭合、N.9/force/门③/O.1/Q.5 仍开）。写锁属主落档后，可在本文末追记「已落 → 闭合」。

3. **【T 节体量】** T 节已含七件定论（T.1–T.7）且体量大；本波新落 CF-soft（主题 3）建议**并入 T 节或 S 节**（S 节已是 route-B/CF 实测族），不新开字母，避免主文档字母膨胀。

4. **【净覆盖数字单一事实源】** 主文档 T 节占位「339/~341」、《本波回收交付汇总》340（00:42）、`纠偏重算配方-155` 343（01:03）——churn 属实、已由 T.2 记录。建议正文**一律指针到 `out/coverage.json` `summary.qc.success_after_qc` + `generated_ts`**，广播定版后由单写者一次性回补，勿在多处硬写具体数（防再 churn）。

---

## 四、补登草稿（供写锁属主 copy，本审计不改正文）

**A · 新增 N.9（承 -140 缺失 A+B，去重 2 份暂存草稿后取单份）**
```
### N.9 route-B 方法A 定论：同源 B1 / 跨源过盾后门控 Fetch（Network 域取不到导航 articlepdf body）（2026-07-02，A/B 收口；源 -152 真机·-140/-142 起草）
- 分叉：过盾后抓 PDF 字节两条 CDP 实现——(A) -154「删 Fetch.enable、纯 Network 域」；(B) 探针「过盾后门控 Fetch.enable RESPONSE、同 tab」。
- A/B 实测（同机单头 nodriver0.50.3 + Chrome133 有头）：ACS-OA acsomega.6c04195 → B1 页内 fetch=PASS(13.78MB)；RSC d5ra08493h · A(Network 域)=FAIL(size=0、no-pdf-captured、188s)、B(过盾后门控 Fetch RESPONSE)=PASS(484,829B、%PDF-1.6、QC match)。
- 定论：同源 PDF→B1；跨源/viewer 导航 PDF（RSC articlepdf）→ 过盾后门控 Fetch.enable RESPONSE（仅 cf_clearance 后 enable、全同一 tab、抓到 %PDF 即 disable）；Network 域对导航 PDF 取不到 body。-154「整体删 Fetch」对同源 OK、对 RSC 需把过盾后门控 Fetch 引回。落地归 -144/-154。
- 附带必修 bug（PreflightWarn）：_tolerant_from_json 只兜字段缺失(KeyError)，未兜「字段在但枚举值 nodriver0.50.3 不认」（Chrome133 发 PreflightWarn → LocalNetworkAccessRequestPolicy.from_json 抛 ValueError 洪水 → 网络事件被丢弃 = A 端抓不到 body 的直接机制）。修法：枚举转换包 try/except、未知值降级 ALLOW（探针 _route_b_b2_152.py 的 _tol 即此写法）。
- 出处：路线B-真机冒烟结果-152.md；经验待归档-routeB抓字节Network-vs-Fetch分叉-142.md；选型2026-routeB-Fetch域删除vs过盾后门控Fetch-154vs152分岔-140.md（合入后此二份可归档为指针）。
```

**B · N.5 末补一条（承 -140 缺失 C，force=True 过门）**
```
- route-B 落盘曾结构上绕过内容 QC 门（147 核出、现已修）：内容 QC 门原只在 ① _download_pdf_core 跑；route-B 走 ②b _browser_capture_fallback(B1/B2) 与 ⑥ _browser_pdf_download(Akamai/MDPI)，二者曾直接 _save() 不过门。现已修：两函数落盘前均 _content_qc_gate(..., force=True)（download.py L1157/L1362）；force=True 是为压过「route-B 源多为 DOI 绑定源会被 _source_needs_content_qc 豁免」，否则接了也豁免。回收波须置 content_qc_non_article_hard_reject=True。
```

**C · N.5 门③表述更正（承 -140 更正 1）**
```
将 N.5「已补门③(文内/元数据 DOI≠期望)/门④/门⑤」更正为：
「门④(SI)⑤(citation-report/poster/TOC) 已落地 end-to-end；门③（文内/元数据 DOI≠期望，meta-doi-mismatch 读 XMP/Info /doi 反向比对）仍未实现（P0，cctc.200900261 一类同题他刊漏网，见《验证-门③④⑤拦截率-147》《验证-门③异DOI反证-99PENDING-148》）。」
```

**D · CF-soft OA 子集回收结果（主题 3，数字待单写者据证据核定）**
```
- CF-soft OA 子集（recover_p2_cf_soft，单 shim 过软盾）真净增 = <N> 条（据 _qc_p2_cf_soft_155.py 产物 / summary 过内容 QC 门后核定，防 ACS-SI 陷阱）。
- 口径区别：155 churn recipe 记「p2_cf_soft_155 一级扫入 +5」是「一级目录扫入的落盘 success」，须再过 QC 才是真净增；点名「+2」与「+5/+3~9」需以证据路径统一。承 M.2/O.2「跑批 success ≠ 真净增」。
```

**E · O.1 首段 / Q.5 更正**（承 -141波）
```
O.1 首段「真正文 4」→「真正文 1（经 R.2 开卷复核推翻，唯 acscatal.0c04429；真净增率 1/95）」+ pointer R.2。
Q.5 末补：「诚实地板（SI33+ws9 落盘后）≈340/999≈34.0%（coverage.json 00:42:19）；免费手段天花板（T0/route-B/CF-soft 摘果后）~40–42%，破此唯 A5。」净覆盖占位待广播后 -145 回补现值。
```

---

## 五、给写锁属主 / 总指挥的执行清单（按优先级）

1. **P0 更正** N.5「已补门③」→「门③(meta-doi) 未实现」（补登草稿 C）——防误导后续放弃 `cctc.200900261` 一类回收判断。
2. **P0/P1 新增 N.9**（route-B 方法A 定论 + PreflightWarn bug，草稿 A），并归档 2 份暂存分叉草稿为指针（去重）。
3. **P1 补 N.5** route-B 落盘 force=True 过门一条（草稿 B，坑+修法已发生在代码、未落档）。
4. **P1 补 CF-soft** 回收结果条（草稿 D，**先由属主核定真净增 N 条**再落，勿抄「+2/+5」歧义数）。
5. **P2 更正** O.1 首段真正文 4→1、补 Q.5 双层口径（草稿 E）；coverage 二级扫描机制并入 T.2 一小段（§二.4）。
6. **P2 结构** 增补 M/N 重号标注、净覆盖数字统一指针 `coverage.json`（§三）。
- 以上均需《经验记录》**写锁**；本审计只读、未改正文，草稿已备好可直接粘贴。

---

## 六、边界声明
- **未改**任何 `.py`、未改《经验记录-踩坑与发现.md》正文、未改 coverage/黑名单、未动他人产物、未 git 提交；仅新增本审计报告 1 份。
- 门③状态、route-B 落盘是否过门两点，以**工作树 `download.py`（未提交）**实测为准（门③ meta-doi 侧未实现、route-B 落盘已 `force=True` 过门 @ L1157/L1362）；若本审计出具后 `download.py` 再被并发改动，§二.2/§二.3 需按最新代码复核。
- CF-soft「+2」真数**未在本审计内核定**（属回收属主职责），本文只标「需据证据统一口径」。

*核验 2026-07-03｜谷歌学术人机认证-153｜task-5444962f｜只读审计，未改正文/代码/coverage/git｜8 点名主题：6 已落充分 / 1 已落(6) / 1 缺失(CF-soft)；前序开口 N.9·force 过门·门③更正·O.1·Q.5 仍待写锁属主落定，T.1 已闭合。*
