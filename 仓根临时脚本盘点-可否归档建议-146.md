# 仓根临时脚本盘点 · 用途 + 可否归档建议(非破坏性)

> 交付:**谷歌学术人机认证-146**｜2026-07-02｜工单来源:总指挥(经验固化任务 `task-fad9e549`)。
> 边界:**只盘点、只产建议,先不删、不移、不改任何 `.py`**。归档动作(移入 `archive/` 或删除)须由总指挥/属主拍板后再执行。
> 口径:仓根 `_*.py` + `fs_shim_mainloop_141.py` 共 **33 个临时脚本**;逐个读了文件头 docstring 判用途。另附临时 `.txt/.json` 产物一览。

---

## 〇、TL;DR

- **33 个临时脚本**,按可归档性分三档:**A·可归档 27 · B·保留/待转正 6**(+ C 档为数据 txt,另表)。
- **绝大多数(A 档)是"改程序→跑→读日志"闭环里的一次性诊断/验证脚本**,多数自述「用完即删 / 不进仓 / 可删 / throwaway / SPIKE」;其结论**已固化进 `经验记录-踩坑与发现.md`(H/J/K/L/M/N 节)与各专项 md**,脚本本身留着只占仓根。
- **B 档 6 个别删**:① `_smoke_render_bytes.py`(被 `路线B-...Runbook-173.md` 第2步引用);② `fs_shim_mainloop_141.py`(修好了 Windows [Errno 22] 的**可用** shim 变体,应评估合入 `tools/`);③ `_naming_audit.py` + ④⑤⑥ `_find_acs_oa_152.py`/`_gap_pubdirect_152.py`/`_gap_pubdirect_152_detail.py`(可复用只读审计/扫描,且后三者归 **-152** 在跑的 publisher_direct 活,勿单方处置)。
- **建议的非破坏性归档法**:新建 `archive/scratch-2026-07/`,把 A 档整体 `git mv` 进去(保留 git 历史、随时可回);B 档留仓根或转正到 `tools/`。**本文件不执行,仅建议。**

---

## 一、A 档 · 可归档(结论已固化的一次性脚本,27 个)

### 1. FlareSolverr / nodriver shim 诊断(-141,定位 Windows [Errno 22])
| 脚本 | 用途(文件头自述) | 自述处置 | 归档 |
|---|---|---|:--:|
| `_tmp_fs_probe_141.py` | 复现 `flaresolverr_nodriver._solve` 各 CDP 步骤,打全 traceback 定位 `[Errno 22]` | 用完即删 | A |
| `_tmp_fs_probe2_141.py` | 诊断2:复现 shim 并发模型(Proactor 主线程 run_forever + 工作线程 run_coroutine_threadsafe),定位跨线程 `[Errno 22]` | 用完即删 | A |
| `_tmp_fs_probe3_141.py` | 诊断3:真实 `shim.Solver._solve` 全流程 traceback | 用完即删 | A |
| `_tmp_fs_probe5_141.py` | 诊断5:精确复现 shim HTTP 路径(HTTPServer+Handler+solve→run_coroutine_threadsafe),定位真实失败行 | 用完即删 | A |

> 这 4 条的**结论已产出** = `fs_shim_mainloop_141.py`(B 档,主线程 loop 变体)+ 经验记录 N.1 shim 坑位。诊断脚本可归档。

### 2. JA3 / 浏览器内直下 PDF 探针(-144 / spike)
| 脚本 | 用途 | 自述处置 | 归档 |
|---|---|---|:--:|
| `_ja3_probe_144.py` | JA3 只读探针:验证「浏览器内 `fetch().arrayBuffer()` 能否绕 JA3 拿 PDF 字节」,逐步日志 + CDP 最小补丁 | 一次性 | A |
| `_tmp_ja3_diag_patched.py` | 应用 `ClientSecurityState.from_json` 容忍缺字段 monkeypatch 后逐阶段定位 `--capture-bytes` 卡点 | 一次性 | A |
| `_tmp_ja3_monkeypatch_probe.py` | 运行期 monkeypatch 证实「Chrome↔nodriver CDP schema 漂移」是 `--capture-bytes` 抓不到 PDF 根因 | 不进仓 | A |
| `_tmp_ja3_realfix_probe.py` | 实证真修复:不启 Fetch 域拦截,过 CF 后同会话同 JA3 `fetch` 正文 PDF(排除 SI) | 不进仓 | A |
| `_spike_browser_capture.py` | SPIKE:浏览器内 PDF 字节捕获(避开 CDP Network 域,导航+页内 fetch+`%PDF` 校验) | throwaway | A |
| `_spike_bc_smoke.py` | SPIKE:nodriver 能否在本环境启动+抓页 | throwaway | A |
| `_diag_capture.py` | 逐步打印浏览器内抓字节各阶段定位卡点(单 URL) | 不进仓 | A |
| `_probe_cdp_api.py` | 打印 nodriver `Tab.add_handler/evaluate/get` 等签名(API 探查) | 一次性 | A |

> 结论已由 `render_fetch.py`(仓内正式件)落地 + 经验记录 N.3/N.4;探针可归档。

### 3. 内容 QC / 门③④⑤ 验证与回归(-142 / -147 / -157)
| 脚本 | 用途 | 归档 |
|---|---|:--:|
| `_tmp_verify_b4cf_qc_142.py` | 142 复核:独立重算 recover_b4_cf 34 条 verdict,与 -144 `recover_b4_cf_qc.json` 对账 | A |
| `_tmp_manual_check4_142.py` | 142 人工核验:4 条 uncertain/scanned + 1 SI 边界,抽首页文本 | A |
| `_tmp_b4cf_fail_rootcause_157.py` | recover_b4_cf 失败 DOI 根因分析,产 `out/recover_b4_cf_fail_rootcause_157.md` | A |
| `_tmp_verify_gates345_147.py` | 只读:173 门③④⑤ 对 recover_b4_cf 拦截率/必要性/误杀 | A |
| `_tmp_verify_gate_live_147.py` | 只读定论:34 条 success 跑当前真门 end-to-end,输出真实拦截率 | A |
| `_tmp_verify_escape_mech_147.py` | 只读:区分 15 条 active-match 两种逃逸机制(整源豁免 vs 进门 match) | A |
| `_tmp_regress_nofalsekill_147.py` | 只读:广样本不误杀回归(跨目录真全文过解耦门④⑤+hard_reject=0 误杀) | A(建议并入 `tools/regress_qc_union_189.py`) |
| `_tmp_regress_fixtures_147.py` | 只读:§9.7 四条不误杀正例 fixture 断言 | A(建议并入 selftest) |
| `_tmp_verify_cov_147.py` | 校验 recover_b4_cf 黑名单(12+8=20)是否已从 coverage 净口径剔除 | A(自述可删) |
| `_title_probe_content_qc.py` | title_probe 20 条内容级 QC(复用 `qc_content_match.classify`),写 `out/title_probe/_content_qc.json` | A(功能已在 `tools/qc_content_match.py`) |

### 4. QC 审计集口径复原(临时统计)
| 脚本 | 用途 | 自述 | 归档 |
|---|---|---|:--:|
| `_tmp_qc_bucket_stats.py` | 统计 union 错论文审计集 URL host / 桶分布 | 用后即删 | A |
| `_tmp_qc_bucket189.py` | 复原「同域错论文 189 条(publisher 61 + repository 128)」口径 | 临时 | A |
| `_tmp_qc_report_probe.py` | 探查 `qc_content_report.json`/`qc_rejected_manifest.csv` 结构 | 临时 | A |

### 5. 批次切分(一次性数据工具)
| 脚本 | 用途 | 自述 | 归档 |
|---|---|---|:--:|
| `_split_batch4.py` | 把 batch_4 500 条切 5 chunk txt 供并行 | 用完可删 | A |
| `_split_batches.py` | 把测试集 csv/xlsx 切 N chunk txt | 用完可删 | A |

---

## 二、B 档 · 别删 / 待转正(6 个)

| 脚本 | 为什么保留 | 建议动作 |
|---|---|---|
| `_smoke_render_bytes.py` | **被 `路线B-浏览器内直下PDF验证Runbook-173.md` 第2步显式引用**(`python _smoke_render_bytes.py` 冒烟 ACS-OA+RSC 各1条) | 保留仓根;或随 Runbook 迁 `tools/` 并同步 Runbook 路径 |
| `fs_shim_mainloop_141.py` | **修好了 Windows 工作线程 Proactor 的 `[Errno 22]`** 的可用 shim 变体(主线程 loop 跑 + HTTP 守护子线程),`importlib` 复用原 shim 逻辑不改共享文件 | **评估合入 `tools/`**(或把该修复并进 `tools/flaresolverr_nodriver.py`);合入前别删 |
| `_naming_audit.py` | 只读审计 `out/*/pdfs/` 文件名是否符合 `download.py` 命名标准——**可复用运维工具** | 转正到 `tools/` 或 `scripts/` |
| `_find_acs_oa_152.py` | 找真金 OA 的 ACS 文章证路线B 端到端(Crossref ACS Omega→Unpaywall)——归 **-152** 在跑的活 | **交 -152 定夺**(见下方协作备注),勿单方处置 |
| `_gap_pubdirect_152.py` | P1 `publisher_direct` 缺口扫描(still_missing 前缀 vs 覆盖能力)——**-152 活** | 同上,交 -152 |
| `_gap_pubdirect_152_detail.py` | 缺口明细:逐条 gap DOI 便于建模板 backlog——**-152 活** | 同上,交 -152 |

---

## 三、C 档 · 临时 `.txt/.json` 产物(附带,随对应任务归档)

| 文件 | 性质 | 建议 |
|---|---|---|
| `_filelist.txt` / `_naming_audit_report.txt` / `_tmp_final_check.txt` / `_run_all_demo_input.txt` | 脚本临时输出/输入 | 可随 A 档一并归档或删 |
| `subset_elsevier_probe_149.txt` / `titles_probe.txt` / `titles_probe_truth.json` | 探针输入/真值 | 探针归档则一并归档 |
| `recover_*_input.txt` / `recover_b4_cf_smoke10.txt` / `batch7_*.txt` 等 | 回收任务**数据输入清单**(非脚本) | 属数据,建议归 `inputs/` 或随批次目录,**不在本脚本盘点范围** |

---

## 四、执行建议(供总指挥/属主拍板)

1. **A 档 27 个** → `git mv` 进 `archive/scratch-2026-07/`(保 git 历史、随时可回);**不建议直接 `rm`**(北极星:留证)。
2. **B 档 `_smoke_render_bytes.py`** 保留仓根(Runbook 依赖);**`fs_shim_mainloop_141.py`** 走"合入 `tools/` 或并进 shim"评审后再决定去留;**`_naming_audit.py`** 转正 `tools/`。
3. **`_find_acs_oa_152.py` / `_gap_pubdirect_152*.py`** 属 **-152** 活,已 `send_to_session` 知会 -152 自行决定归档/转正(不单方移动)。
4. 归档后仓根 `_*.py` 应清空,`.gitignore` 可加 `_*.py`/`_tmp_*`/`_spike_*`/`_probe_*` 约定,减少后续临时件污染。

---

*核验 2026-07-02｜-146｜工单 `task-fad9e549` 交付项2:临时脚本非破坏性盘点。只读盘点+建议,未删/未移/未改任何脚本。*
