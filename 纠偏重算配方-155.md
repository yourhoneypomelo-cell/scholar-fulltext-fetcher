# 纠偏重算配方 + 验证 · 155（coverage 回归修复备料）

> 交付：**谷歌学术人机认证-155**｜2026-07-02 24:25｜承总指挥紧急扩项（−151 offline，−144 执行）  
> 边界：本稿 + staging QC + `--no-write` 试跑；**正式写盘由 −144 按 SOP 执行**（−155 可代跑 backup，不擅自覆盖 production 除非总指挥确认）。

---

## 〇、回归根因（23:59:39 / 00:02:50 误跑）

| 对比项 | `coverage.bak_174_pre_websearch9_merge.json`（正确 385） | 误跑后 live |
|---|---|---|
| generated_ts | 22:42:59 | 23:59:39 → 00:02:50 |
| success | **385** | 362 → **370** |
| success_before_qc | **513** | 471 → **533** |
| **allow_override** | **10** | **0** ⚠️ |
| rejected_hard / soft | **6 / 122** | 22 / 141（黑名单膨胀） |

**根因**：`build_coverage` **漏传 `--qc-allow` 与 `--extra-dirs`**，MDPI7 / T0+2 / `acscatal.0c04429` 等 allow 保护全丢；且 websearch9/openaire3/SI33 **提前并入** qc CSV 但配方不对，造成双重漂移。

---

## 一、385 基线复现配方（--no-write 已验证）

### 1.1 先还原 QC + 反推扫描口径

**backup 真相**（`coverage.bak_174_pre_websearch9_merge.json` → `_scan`）：

```json
{ "include_nested": true, "extra_dirs": [], "n_nested_dirs": 11 }
```

即 −151 当时用 **`--nested`（非 `--extra-dirs`）** 扫二级 `*/fetch/`。但 **现盘 nested 会过扫**（2026-07-02 试跑 → success **388** / before **533**，偏高），故 **纠偏改用显式 `--extra-dirs`**，对齐 backup `by_success_batch` 贡献：

| extra-dir | backup 净成贡献 |
|---|---:|
| `rerun_elsevier_143/fetch` | 6 |
| `rerun_acs_144/fetch` | 1 |
| `routeB_mdpi` | 7（一级目录，默认扫） |
| `recover_b4_cf` | 4（一级，默认扫） |

**Staging QC**（去掉 premature merge）：`out/_qc_hard_baseline_staging_155.csv`（highconf−9−3）、`out/_qc_soft_baseline_staging_155.csv`（union−33）。

### 1.2 build_coverage 命令（纠偏定稿 · 勿用 live 362 投影）

**Step0 基线（allow=10，无 SciOpen）** — `--no-write` 实测 **384 / 513 / 10**（差 1=crossref1 假阳，可接受）：

```bash
python tools/build_coverage.py \
  --qc-hard out/_qc_hard_baseline_staging_155.csv \
  --qc-soft out/_qc_soft_baseline_staging_155.csv \
  --qc-allow out/rerun_acs_144_whitelist_back_145.txt,out/_t0_recover156_writeback_qcallow_immediate2_151.txt,out/routeB_mdpi_qcallow_151.txt \
  --qc-extra-reject out/rerun_acs_144_deduct_dois_145.txt \
  --extra-dirs rerun_elsevier_143/fetch,rerun_acs_144/fetch
```

**Step0+SciOpen（allow=11，success=385）** — 在 Step0 上 **追加** allow 文件 + p3 落盘：

```bash
# 同上，但：
# --qc-allow 增 out/_sciopen_allow_160.txt
# --extra-dirs 改为 rerun_elsevier_143/fetch,rerun_acs_144/fetch,rerun_wiley_144/fetch,p3_longtail_160/fetch
```

> ⚠️ **禁止用 live 362/325 投影**——那是漏 `--qc-allow` 的回归态。⚠️ **禁止现盘 `--nested`**——会过扫到 **388**。

### 1.3 --no-write 实测（2026-07-02 24:30 · v2 SciOpen）

| 指标 | 目标 | 实测（+SciOpen） | 判定 |
|---|---:|---:|:---:|
| success_before_qc | 513→514 | **514** | ✅（+1 SciOpen PDF） |
| allow_override | **10→11** | **11** | ✅ |
| **success（净成）** | **385** | **385** | ✅ |
| miss | 614 | **614** | ✅ |

（无 SciOpen 时：before=513 allow=10 success=**384**，差 1=crossref1 假阳 `10.1252/jcej.38.807`。）

---

## 二、分步 merge 投影（**从 385 诚实地板**，非 live 362）

| 步 | 动作 | Δsuccess | 投影 success | allow | 备注 |
|:--:|---|---:|---:|---:|---|
| 0 | Step0 基线（staging QC + elsevier+acs extra） | — | **384~385** | **10** | crossref1 假阳 ±1 |
| 0b | + **SciOpen allow** + p3 extra | **+1** | **385~386** | **11** | −160 翻案 |
| 1 | append **websearch9** → highconf | **−9** | **~377** | 11 | 自 386−9 |
| 2 | append **Tier-A 37**（SI33 union + openaire3 + crossref1） | **−37** | **≈340** | 11 | overlap ∅ |
| HOLD | **OCR14** 14 条 | — | — | — | 勿 auto merge |

> ~~旧 manifest 从 live **362** 投影 **325**~~ **作废**（回归态 allow=0）。

**overlap**：Step1~4 DOI 集合 **两两 ∅**；**勿误杀 `nl200722z`**（不在 SI33 33 条内）。

**OCR14**：`out/_mon176_ocr_bucket_still_success.txt` 14 条 + retry 日志 alnum≈0 — **勿 auto merge**（−176 HOLD）。

---

## 三、−144 执行 SOP（写盘顺序）

1. **Backup**：`coverage.json` → `coverage.bak_pre_fix_155.json`；`qc_merge_highconf_wrong.csv` / `qc_merge_union_wrong.csv` → `.bak_pre_fix_155`  
2. **Revert QC** 至 baseline staging 内容（或按 §1.1 删行）  
3. **Write Step0**：上节配方 **去掉 `--no-write`** → 验 `allow_override=10`、`success∈{384,385}`  
4. **Write Step1**：并入 websearch9 append（9 行）→ build → **验 376**  
5. **Write Step2**：并入 `_acs_si33_union_append_145.csv`（33 行 union）→ build → **验 343**  
6. **Write Step3~4**：openaire3 + crossref1 hardblack → build → **验 ~339~340**  
7. 更新《基线口径冻结说明-388-173.md》+ ping 总指挥  

---

## 四、机器可读

- `out/_merge_wave2_manifest_155.json`（第一波备料 live 交叉，基于误跑 362 地板，**仅供历史参考**）  
- `out/_qc_hard_baseline_staging_155.csv` / `_qc_soft_baseline_staging_155.csv`（385 复现 staging）  
- `out/_trial385.json`（含 rerun_acs extra-dir 的误配方试跑，**勿用**）

---

## 五、与旧 manifest 关系

《第二波merge清单-155.md》中 **362/325 投影作废**——该数来自漏 `--qc-allow` 的误跑。**以本稿 384→376→343 阶梯为准**。

---

## 六、总指挥补正 · openaire3 + crossref1 显式核验（2026-07-03 01:00）

### 6.1 生产 hardconf 实况（勿假设未 merge）

`out/qc_merge_highconf_wrong.csv` **L65–68 已含** openaire3 三条 + crossref1 一条（与 `out/_mon176_openaire3_hardblack_append.csv` + crossref1 一致）：

| DOI | 行号 | live coverage status |
|---|---|---|
| `10.1016/j.susc.2014.02.019` | 65 | **miss** |
| `10.1016/j.jcou.2019.07.021` | 66 | **miss** |
| `10.1016/j.apcata.2013.07.028` | 67 | **miss** |
| `10.1252/jcej.38.807` | 68 | **miss** |

> 144 在 00:10:44 可能只广播了 websearch9，但 **现盘 hardconf 四行均在**；live `coverage.json`（`generated_ts` **00:54:00**）四 DOI 均已 miss。

### 6.2 显式 extra-reject 备料（belt-and-suspenders）

`out/_qc_extra_reject_openaire_crossref_155.txt`（4 DOI，与上表同集）。

### 6.3 `--no-write` 复验（含三文件 extra-reject）

| 指标 | 目标 | 实测 |
|---|---:|---:|
| success | 340–343 | **343** |
| miss | — | **656** |
| success_rate | — | **34.33%** |
| allow_override | **14** | **14** |
| before_qc | — | **515** |
| rejected_hard | — | **19**（+4 与显式 openaire/crossref 文件计数一致，与 hardconf 重叠不双杀 success） |

### 6.5 −174 补遗 · before_qc 复现 CLI 史（**FROZEN 不写盘**）

| 跑次 | 命令要点 | 结果 |
|---|---|---|
| **BAD** | `python tools/build_coverage.py --out-root out`（漏 allow/extra-dirs） | 362/637 **allow=0** ⚠️ |
| **GOOD（interim）** | 长 `--extra-dirs`（含 t0_recover_156、routeB_rsc_launch 等）+ 4-file `--qc-allow` | **380→343**；**before_qc=517**（非 513） |
| **155 终稿** | `--qc-allow _coverage_allow_final_155.txt` + 三文件 extra-reject + 4 extra-dirs | **343/656 allow=14**；before_qc=**515** |

**before_qc 517 vs 513/515 解释**：interim GOOD 跑时 **`p2_cf_soft_155` 作为一级目录被默认扫入**（进行中 run 的 PDF 落盘），故 before_qc 偏高 +2~4；155 终稿 extra-dirs **未显式含 p2**，且 p2 仍在跑/未全落盘 → before_qc=515。

### 6.6 总指挥终稿 · ONE write @01:03:41（144 交底配方）

**--no-write 预检**：success=**343** miss=656 before_qc=**517** allow文件**14条** allow_override=**13**（1条未触 reject 无需 override）OCR14 in success=**0**

```bash
python tools/build_coverage.py --out-root out \
  --extra-dirs rerun_cf_savable_140/aip/fetch,rerun_cf_savable_140/wiley/fetch,rerun_cf_savable_141/aip/fetch,rerun_cf_savable_140/acs/fetch,rerun_cf_savable_140/cf_other/fetch,rerun_acs_144/fetch,rerun_wiley_144/fetch,rerun_aip_144/fetch,rerun_elsevier_143/fetch,t0_recover_156/fetch,routeB_rsc_launch/fetch \
  --qc-allow out/_coverage_allow_final_155.txt \
  --qc-extra-reject out/rerun_acs_144_deduct_dois_145.txt
```

- wave2 黑名单已由 144 append 至 production CSV（**155 不再 append**）
- Backup：`out/coverage.bak_pre_final_155_v2.json`
- Live：`generated_ts` **2026-07-03 01:03:41** → **343 / 656 / 34.3%**；p2_cf_soft_155 一级扫入 +5

**allow 14 清单**（`out/_coverage_allow_final_155.txt`）：

1. `10.1002/anie.201406637`（T0终裁）
2. `10.1016/j.apcatb.2021.119925`（T0终裁 apcatb）
3. `10.1016/j.jcou.2022.102356`（T0立即）
4. `10.1016/j.jechem.2020.06.007`（T0立即）
5. `10.1016/s1872-2067(17)62899-7`（T0终裁 s1872）
6. `10.1021/acscatal.0c04429`（ACS 真全文）
7. `10.26599/nr.2025.94907426`（SciOpen）
8–14. MDPI7：`10.3390/app14114959` `catal10070741` `catal13091244` `catal15111028` `catal16020163` `catal16030270` `en11092276`

### 6.8 v2 基线定版 · ONE write @01:27:42（339/allow11 · 总指挥最终指令）

```bash
python tools/build_coverage.py --out-root out \
  --extra-dirs rerun_elsevier_143/fetch,rerun_acs_144/fetch,rerun_wiley_144/fetch,t0_recover_156/fetch \
  --qc-allow out/_coverage_allow_v2_11_155.txt \
  --qc-extra-reject out/rerun_acs_144_deduct_dois_145.txt
```

- Backup：`out/coverage.bak_pre_final_155.json`（写前）+ `coverage.bak_pre_final_155_v2_allow11.json`（342→339 回滚点）
- Live：**339 / 660 / 33.9%** | allow_override=**10** | before_qc=514
- **follow-up 上行（未计入）**：T0终裁3（+3，PDF 在盘）· SciOpen（+1，PDF 在 rejected/）

---
*定版 2026-07-03 01:27:42｜-155·FROZEN｜339/660/allow11*
