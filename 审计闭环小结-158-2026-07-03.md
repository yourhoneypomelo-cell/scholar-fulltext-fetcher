# 审计闭环小结 · 谷歌学术人机认证-158（持续巡检与总指挥审计 AI）

> 会话：**谷歌学术人机认证-158**（审计AI/worker）｜2026-07-03｜边界：只读巡检 + 写本 md + 写 1 份检索交付；未改核心码/coverage/未动 git。
> 用途：本会话第 1–2 轮审计闭环的交接/复盘存档。**主指标真相源 = `out/coverage.json`（唯一数字源）**。
> 路径校正：角色说明写 `D:\AI project\文献下载`（本机不存在）→ 实际审计对象 = `e:\AI项目\谷歌学术人机认证`。
>
> ⚠️ **【历史快照·当前权威见 `out/coverage.json`：326 / 673 / 32.63% @2026-07-03 12:50:24, allow=10】**（本文 §一 339/660@01:27:42 为巡检当时快照·【历史】；方法/结论有效、数字勿当当前）。

---

## 一、第 1 轮 · 完整核查（NOTIFY）

### 1.1 业务进度（真相源 `out/coverage.json`，generated_ts 2026-07-03 01:27:42）
- **total=999 / success=339 / miss=660 / 净成功率 33.93%**；`claimed_success_but_no_pdf=336`。
- `out/still_missing.txt` = 660 DOI（+2 注释行；与 coverage.miss 一致，已核）。
- QC 链：success_before_qc=514 → 剔 175（硬20+软155）→ 净 339；**allow_override=10**。
- 主力源：websearch=142 / unpaywall=53 / s2=37 / openalex=31 / europe_pmc=22 / crossref=14 / publisher_oa:acs-authorchoice=10 / browser_pdf_download(route-B 真下)=7。
- 免费天花板 ≈40–42%；差额=假阳/OCR14 HOLD，非加反爬可填。

### 1.2 NOTIFY 三项（第 1 轮 flag）
1. 🔴 **质量门 RED**：`run_all_selftests.py` = PASS=44/**FAIL=1**/SKIP=2，render_fetch selftest 180s TIMEOUT。→ 已修（见第 2 轮）。
2. ⚠️ **真相源漂移**：展示文档（项目总报告、基线口径冻结说明-388-173）写 340/659/34.03%@00:42:19、override=11；真相源已 339/660/33.93%@01:27:42、override=10。→ 总指挥交 142 全仓回填映射表统一收口（定版~341 一次性回填），采纳「唯一数字源=out/coverage.json」写进规范。**待回填**。
3. ⚠️ **工程卫生**：仅 4 个 checkpoint commit，核心 WIP 全未提交（render_fetch/institutional/run_all/download/publisher_*）+ 根目录 100+ 未跟踪 `_tmp_/_route_b_/_mon_/_qc_` 临时脚本。→ 总指挥派 159 产只读清理预案（提交清单+.gitignore+归档），排质量门绿后；**提交核心码待用户授权**。

---

## 二、第 2 轮 · 质量门验收（① 已独立复验真绿）

| 项 | 结果（可复算） |
|---|---|
| `python -m fulltext_fetcher.render_fetch --selftest` | **RENDER_BYTES_OK + RENDER_OK / 7.08s / rc=0**（<10s） |
| `python run_all_selftests.py` | **PASS=45 FAIL=0 SKIP=2 / exit=0**；compileall PASS 0.2s、render_fetch PASS 1.6s；总 118s（原 373s） |
| 全仓 `*.py` 伪影扫描 | 无残留 `</parameter></invoke>` |

- **render_fetch 180s TIMEOUT 根因（142 定，我认账）**：`_host_capture_gate`（RSC per-host 限速）的 `time.sleep`——mock 调 `render_download_pdf_bytes('pubs.rsc.org/a', min_interval=0.0)` 第 2 次即卡 56.35s，`_selftest_bytes` 对该 host 5 次调用累计 >180s；`min_interval=0` 只关 `_throttle`、关不掉 host gate。（我上版归因 item13/asyncio 已撤回。）
- **修法（142）**：mock `_capture_fn` 时跳过 `_throttle`+`_host_capture_gate`（生产不传、字节不变）。
- **ezproxy_login.py 伪影（我发现 + 149 删）**：L323-324 曾有 `</parameter>`/`</invoke>` 工具调用粘贴伪影（正确 EOF L322）→ 破坏 compileall；owner 149 已删，全仓仅此一处。
- → 总指挥定的「质量门绿=合并+提交硬前置」**已满足**。

### 2.2 待办（第 2 轮 ②，未完成）
- 定版并入（~341）后核 **`out/coverage.json` ↔ `still_missing.txt` ↔ 展示文档 三方一致（override=10）**——硬依赖总指挥 merge 前 ping；merge 又卡在 150 websearch 净减「疑似卡死」（已上报）。

---

## 三、检索交付 · TLS/JA3 指纹开源专项（总指挥派单，已 report_task）

产物：`过认证墙开源方案检索-TLS指纹-147.md`。核心：
- **防重复造轮子**：库选型前作 `选型2026-隐身HTTP与TLS指纹客户端.md`(-148) 已定 curl_cffi 主采/primp A-B/tls-client 备胎，且 **curl_cffi 已吸收为代码**（`http_client.py` 可选 impersonate 取回 + `FTF_IMPERSONATE_HTTP=1` 开关 + 离线 selftest ⑩a-g；scholar 引擎）。
- **补 Go 系候选**（任务点名、前作未覆盖）：azuretls-client / utls / bogdanfinn tls-client / requests-go → 统一评分后**均判「不引入」**（Go 栈不适配本项目 requests-only Python；Python 侧 curl_cffi 已等效覆盖）。
- **核心裁决（三源印证）**：**纯 TLS/JA3 指纹库救不了 RSC 0/8**。RSC 门 = Managed Challenge(需 JS/浏览器) + 应用层 governor rate-gate + CDN viewer + 订阅墙，**无一是 passive-JA3**。① 代码 `config.py` L108「curl_cffi TLS 伪装也过不了 RSC CF JS 质询」；② `160` 实测 0/8 主因 = governor 非 JA3；③ 2026 web 实证 curl_cffi 无 JS 引擎过不了 managed challenge。→ RSC 真解 = route-B 真浏览器 + 165 governor 补丁 + 住宅 IP；订阅唯 A5；TLS 指纹库对 RSC ROI≈0。

---

## 四、协作日志（本会话经手）

| 事件 | 对象 | 结果 |
|---|---|---|
| 第 1 轮 NOTIFY 上报 | 总指挥-141 | 已收，逐项处置 |
| render_fetch 根因（初版 item13/asyncio） | 142 | 被 142 反证纠正，我认账 |
| ezproxy_login.py 伪影确认+全仓扫描 | 142/总指挥 | 149 已删 |
| TLS 指纹检索交付 | 总指挥（report_task done） | 已回报 |
| 质量门真绿独立复验 | 142 + 总指挥 | 双向确认，硬前置解锁 |
| 风险上报：150 websearch 净减「疑似卡死」 | 总指挥 | 已建议探询/改派 |

---

## 五、开放项 / 遗留观察

1. **[待总指挥]** 定版并入~341 → merge 前 ping 我核三方一致（第 2 轮 ②）。
2. **[待总指挥]** 150 websearch 净减「疑似卡死」，阻塞 144 一次性并入。
3. **[待 142 映射表]** 展示文档 340/659 → 真相源 339/660 回填（定版后一次性）。
4. **[待用户授权]** 核心 WIP git 提交（总指挥不擅自 commit）。
5. **[非阻塞·可另开工单]** `selftest_institutional` 67.9s 为全套最慢单项（非 FAIL）。**探针**：`selftest_institutional.py` 本体是 mock/无网络（假 Session），慢因**疑似**被测 `HttpClient` 重试/退避的真 `time.sleep` 被触发（标**推测**）；建议同 render_fetch 思路，selftest 注入零退避/假时钟提速。

---

## 六、证据路径（可复算）

- 真相源：`out/coverage.json`（339/660，01:27:42）、`out/still_missing.txt`（660）。
- 质量门：`run_all_selftests.py`（PASS=45/FAIL=0/SKIP=2）、`fulltext_fetcher/render_fetch.py`（`_selftest`/`_selftest_bytes`/`_host_capture_gate`）。
- 代码 RSC 裁决支点：`fulltext_fetcher/config.py` L107-117、`fulltext_fetcher/http_client.py`（可选 impersonate 取回 + selftest ⑩a-g）。
- 交付：`过认证墙开源方案检索-TLS指纹-147.md`；前作 `选型2026-隐身HTTP与TLS指纹客户端.md`、`route-B RSC 0-8 归零因与收口结论-160.md`。

---

*小结 2026-07-03｜-158（审计AI）｜只读+写md，未动核心码/coverage/git｜状态：第 1 轮 NOTIFY 已处置、第 2 轮① 质量门真绿已验、第 2 轮② 待定版 merge ping。*
