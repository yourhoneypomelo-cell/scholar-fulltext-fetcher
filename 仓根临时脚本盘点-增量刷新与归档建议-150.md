# 仓根临时脚本盘点 · 增量刷新 + 归档建议（承 -146，非破坏性）

> 交付：**谷歌学术人机认证-150**（worker · 信息检索-专家智库岗）｜2026-07-02｜用户「继续执行」后自决项（总指挥 148 暂忙、共享看板项目件已全 done）。
> 边界：**只盘点、只产建议，先不删、不移、不改任何 `.py`**。归档动作（`git mv` 进 `archive/` 或删除）须由总指挥 / 属主拍板后再执行。
> 关系：本文是 `仓根临时脚本盘点-可否归档建议-146.md` 的**增量刷新**——-146 盘的是当时 **33 个**；本轮 git 现接又多出 **10 个**（route-B/CF 根因诊断 + coverage 聚合），总数升至 **43 个**。**-146 的 33 条判定仍然成立**，本文只补「-146 之后新增的 10 条」+ 汇总口径刷新，不复述 -146 逐条。

---

## 〇、TL;DR

- **仓根临时脚本现 43 个**（-146 盘 33 + 本轮新增 10），另加 16 个临时 `.txt/.json`（数据输入/产物，C 档）。
- **新增 10 条全是 route-B / CF 过盾根因诊断（-144/-152）+ 1 条 coverage 聚合（-150）**：
  - **结论均已固化**进 `经验记录-踩坑与发现.md` **N.4 / N.8**（route-B 卡 CF 真因＝过盾期开 CDP 拦截；FS 0 产出＝多 shim 重载争用）与 route-B 各专项 md。route-B 路径**已闭环实证**（`10.1021/acsomega.6c04195` CF 6.6s 过、B1 落 13.7MB %PDF）。
  - 因此这批诊断脚本**原则上均可归档**；**但它们属 -144/-152 的在跑/新收尾工作区**，按 -146 既定纪律（「-152 的活交 -152 定夺、勿单方处置」）——**本文只标"可归档候选"，实际 `git mv` 须 -144/-152/总指挥拍板**。
- **1 条例外偏保留**：`_agg_recover_150.py`（我方产的 coverage 回收聚合审计，只读/可 `--write`）——若后续还要做「独立 out 目录回收并入净覆盖」的周期性体检，值得转正 `tools/`；否则随本波归档。
- **归档法（不变，非破坏性）**：新建 `archive/scratch-2026-07/`，A 档整体 `git mv` 进去（保 git 历史、随时可回）；B 档留仓根或转正 `tools/`。**本文件不执行，仅建议。**
- **另注（非脚本、但交付前需总指挥协调）**：git 有 **4 个已改未提交的生产 `.py`**（`config.py`/`download.py`/`pipeline.py`/`render_fetch.py`＝route-B 落地，属 -144）+ `经验记录-踩坑与发现.md`（多人写）。这些**不在临时脚本范围**，但提交/回退动作应由总指挥统一协调，避免与 -144 撞车。

---

## 一、增量：-146 之后新增的 10 条脚本（逐条）

> 归属取自文件头 `task-*` 标记与内容；「结论固化处」指其发现已写入的权威文档，脚本本体因此可归档。

### 1. route-B / CF 过盾根因诊断（-152，task-c56ceacf）—— 5 条

| 脚本 | 用途（文件头自述） | 结论固化处 | 归档 |
|---|---|---|:--:|
| `_route_b_cfwait_152.py` | 验证 route-B 卡 CF 是否因**过盾期开着 CDP Fetch 拦截**：对照 FS shim 全程不开拦截、只轮询 `cf_clearance` | **N.8 #2 的关键实证**（真因＝过盾期开拦截） | A（属 -152） |
| `_route_b_probe_152.py` | 运行时 monkeypatch `ClientSecurityState.from_json` 容缺，验 CDP 版本错配是否 no-pdf-captured 根因 | N.4 / N.8 #2、#3（双键兼容） | A（属 -152） |
| `_route_b_diag_152.py` | 分步诊断 no-pdf-captured 卡在哪一步（最终 URL/标题/是否 CF/body/PDF 链/页内 fetch） | N.8 #2 | A（属 -152） |
| `_route_b_b2_152.py` | RSC 端到端：过盾（不开拦截）+ 过盾后 B2/方法A（`Fetch.enable` RESPONSE + 导航 PDF），验 B1→B2 兜底链 | N.8 #2（B1/B2 分域：ACS 走 B1、RSC 走 B2） | A（属 -152） |
| `_route_b_qc_152.py` | 对 route-B 落盘 PDF 做内容 QC（抽首页文本核 DOI/标题防假阳） | 功能已在 `tools/qc_content_match.py` | A（属 -152；同 -146 对 `_title_probe_content_qc.py` 的判法） |

### 2. route-B / FS 单 shim 冒烟 + OA 样本查找（-144，task-144 / task-c56ceacf）—— 4 条

| 脚本 | 用途 | 结论固化处 | 归档 |
|---|---|---|:--:|
| `_fs_test_144.py` | 经**单 shim(8199)** 测 1 篇 ACS 能否过 CF（`cf_clearance`+200） | **N.8 #1**（单干净 shim → ACS CF 200 / 6.9s；0 产出真因＝多 shim 争用） | A（属 -144） |
| `_rb_test_144.py` | 聚焦测 route-B 方法B（页内 `fetch().arrayBuffer()`）在**不开 Fetch 拦截、低负载**下能否落 %PDF | N.4 / N.8 #2 | A（属 -144） |
| `_oa_check_144.py` | 查 Unpaywall 找 RSC 金OA 且 PDF 托管在 `pubs.rsc.org`（JA3-bound）的样本，供 route-B RSC 验证 | route-B RSC 样本（`10.1039/c4ra00825a` 等） | A（属 -144） |
| `_find_rsc_oa_152.py` | 一次性：从 RSC Advances（全金OA刊 ISSN 2046-2069）近作经 Unpaywall 核 `is_oa`，选 RSC-OA 样本 | 同上（route-B rsc 桶样本源） | A（属 -152） |

> 说明：这 9 条（1+2）是「route-B 从『本机未证』→『已实证闭环』」这条主线的诊断/取样脚本。**主线已在 N.8 定论**，脚本使命完成；但归 -144/-152，**归档交其/总指挥定夺**。

### 3. coverage 回收聚合审计（-150，本会话早前产）—— 1 条

| 脚本 | 用途 | 现状 | 归档 |
|---|---|---|:--:|
| `_agg_recover_150.py` | 把各 `out/rerun_*/**/coverage.json` 成功集并入 999 语料净覆盖（按 DOI 并集去重、剔 QC 黑名单、**只 DOI-bound 源、拒 websearch/search-type**）；默认只读，`--write` 出 `out/coverage_merged_150.json` | 本波各独立 out 目录**已被扁平扫描计入**（0 增，见 `本波回收交付汇总.md §四`）→ 一次性使命已完成 | **A/B 之间**：随本波归档即可；若要做**周期性**回收并入体检，转正 `tools/` 更合适 |

---

## 二、汇总口径刷新（A/B/C 档，并入 -146）

| 档 | -146 判定 | 本轮增量 | 现合计 | 处置 |
|---|:--:|:--:|:--:|---|
| **A · 可归档（结论已固化的一次性件）** | 27 | +9（route-B/CF 诊断，属 -144/-152） | **~36** | `git mv` → `archive/scratch-2026-07/`；**属 -144/-152 的 9 条须其/总指挥拍板** |
| **B · 保留/待转正** | 6 | +1（`_agg_recover_150.py`，偏保留/转正） | **~7** | 见 -146 §二 + 本文 §一.3 |
| **C · 临时 `.txt/.json` 数据** | 若干 | +16（见 §三） | — | 随对应任务/批次归档，或归 `inputs/` |

> **B 档 6 条（-146，仍成立）**：`_smoke_render_bytes.py`（Runbook-173 第2步引用）、`fs_shim_mainloop_141.py`（修好 Windows [Errno 22] 的可用 shim 变体，待评估合入 `tools/`）、`_naming_audit.py`（可复用只读审计）、`_find_acs_oa_152.py` / `_gap_pubdirect_152.py` / `_gap_pubdirect_152_detail.py`（-152 活）。本轮 `_agg_recover_150.py` 为 B 档新候选。

---

## 三、C 档：本轮新增临时 `.txt/.json`（16 个，附带）

- **回收任务数据输入清单（非脚本）**：`recover_a_inst_elsevier20_input.txt`、`recover_b4_cf_input.txt`、`recover_b4_cf_smoke10.txt`、`recover_b6_els_wayback_input.txt`、`recover_b6_tail_input.txt`、`recover_regular_140_input.txt`、`rerun141_other_cf_input.txt`、`smoke170_input.txt`、`subset_elsevier_probe_149.txt`、`batch7_failed_dois.txt`、`batch7_subset_149.txt`。→ 属**数据输入**，建议归 `inputs/` 或随批次目录，**不随脚本归档**。
- **探针输入/真值**：`titles_probe.txt`、`titles_probe_truth.json`。→ 探针（`_title_probe_content_qc.py`）归档则一并归档。
- **脚本临时输出/输入**：`_filelist.txt`、`_run_all_demo_input.txt`、`_tmp_final_check.txt`。→ 可随 A 档一并归档或删。

---

## 四、执行建议（供总指挥 / 属主拍板）

1. **A 档（含 -146 的 27 + 本轮 route-B/CF 诊断 9）** → `git mv` 进 `archive/scratch-2026-07/`（保 git 历史、随时可回）；**不建议 `rm`**（北极星：留证）。**其中 route-B/152 + 144/152 取样共 9 条属 -144/-152，落 mv 前请 `send_to_session` 知会其属主或由总指挥统一批**（同 -146 对 -152 脚本的既定纪律）。
2. **B 档**：`_smoke_render_bytes.py` 留仓根（Runbook 依赖）；`fs_shim_mainloop_141.py` 走「合入 `tools/` 或并进 shim」评审；`_naming_audit.py` 转正 `tools/`；**`_agg_recover_150.py`**——本波已 0 增、可随归档，若要周期性回收并入体检则转正 `tools/`（我方 -150 可承接）。
3. **C 档数据 `.txt/.json`**：回收输入清单归 `inputs/`；探针真值随探针；脚本临时输出可删。
4. **归档后**：仓根 `_*.py` 应清空，`.gitignore` 加 `_*.py`/`_tmp_*`/`_spike_*`/`_probe_*`/`_route_b_*` 约定，减少后续临时件污染。
5. **⚠️ 非脚本、但交付前需协调**：git 有 4 个已改未提交生产件（`config.py`/`download.py`/`pipeline.py`/`render_fetch.py`＝route-B 落地，属 -144）+ `经验记录-踩坑与发现.md`（多人写）。提交/回退请总指挥统一协调，勿与 -144 撞车。

---

## 五、来源

- 脚本枚举：仓根 `_*.py`（42）+ `fs_shim_mainloop_141.py`，逐个读文件头 docstring / `task-*` 标记判用途。
- 结论固化处：`经验记录-踩坑与发现.md` **N.1 / N.4 / N.8**（CF/JA3/route-B 根因）、`路线B-真机冒烟结果-152.md`、`路线B-浏览器内直下PDF验证Runbook-173.md`、`选型2026-route-B默认能力集成方案-设计草案-150.md`、`本波回收交付汇总.md §四`（coverage 已扁平扫描计入）。
- 承接：`仓根临时脚本盘点-可否归档建议-146.md`（-146 的 33 条基线判定）。

---
*核验 2026-07-02｜-150｜「继续执行」自决项：临时脚本盘点增量刷新。只读盘点 + 建议，未删 / 未移 / 未改任何脚本或 `.py`。仓根临时脚本 33→43（+10 全为 route-B/CF 根因诊断，属 -144/-152，结论已固化 N.4/N.8，route-B 已闭环实证），归档动作交属主 / 总指挥拍板。*
