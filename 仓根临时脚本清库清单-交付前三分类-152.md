# 仓根临时脚本 · 交付前清库清单（三分类 · 只产清单不执行）

> 交付：**谷歌学术人机认证-152**（worker）｜工单来源：总指挥 `task-4916d4f3`｜2026-07-02。
> 合并去重自《仓根临时脚本盘点-可否归档建议-142.md》（43 条基线，「取代 146 版」）、`-146.md`（33 条原始盘点）、`-150.md`（增量刷新到 43）。本文把仓根现存 **61 个临时脚本**全部纳入，并**由 -152 亲自拍板了原先挂『待确认（他人在飞）』的 route-B/-152 脚本**（我即 -152，见 §五）。
> **边界（硬约束）**：**只产分类清单，不实际删除 / 不移动 / 不改任何 `.py` 或生产代码**。落地（`rm` / `git mv` / `.gitignore`）须总指挥或属主拍板。

---

## 〇、TL;DR

- 仓根现存 **61 个临时脚本**（`_*.py` + `fs_shim_mainloop_141.py`），四类分流：

| 类别 | 数量 | 处置口径 |
|---|:--:|---|
| **A · 可安全删除** | 20 | 一次性探针/导出/纯读打印，结论已 100% 落进生产码 + 经验记录/交付 md，无残值；拍板后可直接 `rm`（保守做法：先 `git mv` 进归档留一版） |
| **B · 应归档** | 31 | 方法/证据有参考价值 → `git mv` 进 `archive/scratch-2026-07/`（保 git 历史、随时可回），**不建议 `rm`**（北极星：留证） |
| **C · 保留 / 待转正** | 9 | 留仓根（Runbook 依赖 / route-B 复验在途）或转正 `tools/` |
| **D · 待属主确认** | 1 | `_agg_recover_150.py` 属 -150，交其定夺（-150 自评：可随本波归档，若周期性体检则转正 `tools/`） |
| **合计** | **61** | |

- **判据**：可删＝纯 traceback / 「用后即删」/ 纯读打印结算 / 一次性导出器（产物已单独留存）；应归档＝根因验证、门验证、内容 QC 审计、取样、冒烟，方法可复用或需留证；保留＝仍被文档引用或复验流程在途。
- **本波 vs -142 的两处实质推进**：① 补齐 -142 未覆盖的 **18 个新脚本**（-145 内容 QC 结算 ×9、-146 冒烟 ×2、-147 门/回归 ×2、-148 T0 预筛 ×2、-152 真机验收 ×3）；② **-152 亲自收口 route-B/-152 的 12 个脚本**（原先各盘点均标「勿单方处置」）——见 §五。
- **route-B 收口状态（-152 定论）**：机制**已闭环实证**（ACS-OA 走 B1、RSC 走 B2，两桶均落真 %PDF + QC match，见《路线B-真机冒烟结果-152.md》）；**但 RSC 生产路径 gate 尚未齐绿**——待 -144 落地「过盾后门控 Fetch.enable 兜底 + 修 PreflightWarn 容错」后，由 -152 真机复验两样本。**故 route-B 的验收/QC 脚本暂列 C 保留（复验要用），根因诊断/取样脚本列 B 归档（结论已固化）**。

---

## 一、A 档 · 可安全删除（20）

> 结论已 100% 固化进生产码 / 经验记录 / 交付 md，脚本本体无残余参考价值。

### 1.1 FlareSolverr / nodriver shim 诊断（-141，结论＝`fs_shim_mainloop_141.py` + 经验记录 N.1）
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_fs_probe_141.py` | 复现 `_solve` 各 CDP 步定位 [Errno 22] | 纯 traceback，结论已产出可用 shim |
| `_tmp_fs_probe2_141.py` | 诊断并发模型跨线程 [Errno 22] | 同上 |
| `_tmp_fs_probe3_141.py` | 真实 `_solve` 全流程 traceback | 同上 |
| `_tmp_fs_probe5_141.py` | 精确复现 shim HTTP 路径失败行 | 同上 |

### 1.2 CDP/JA3/字节捕获一次性探测（结论已并入 `render_fetch.py`）
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_probe_cdp_api.py` | 打印 nodriver `Tab.add_handler/evaluate/get` 签名 | 纯 API 探查，一次性 |
| `_diag_capture.py` | 逐步打印浏览器内抓字节各阶段卡点 | 机制已落 `render_download_pdf_bytes` |
| `_spike_bc_smoke.py` | nodriver 能否在本环境启动+抓页 | throwaway 冒烟 |
| `_spike_browser_capture.py` | 浏览器内 PDF 字节捕获 SPIKE | 已落 `render_fetch.py` 第 396–404 行 |

### 1.3 -144 一次性单样本核验
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_rb_test_144.py` | route-B 方法B 页内 fetch 聚焦测试 | 单样本，结论已并 `render_fetch.py` |
| `_oa_check_144.py` | 单 shim 测 1 篇 ACS 过 CF | 一次性诊断 |
| `_fs_test_144.py` | 找 1 条 RSC 金 OA 样本 | 一次性取样，已被 `_find_rsc_oa_152` 取代 |

### 1.4 QC 口径复原 / 结算临时统计（自述「用后即删」/ 纯读打印）
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_qc_bucket_stats.py` | 统计 union 错论文审计集 host/桶分布 | 自述「用后即删」 |
| `_tmp_qc_bucket189.py` | 复原「同域错论文 189 条」口径 | 自述「临时」 |
| `_tmp_qc_report_probe.py` | 探查 `qc_content_report` 结构 | 自述「临时」 |
| `_qc_wiley_aip_145.py` | wiley/aip 桶全页复核净增结算 | 纯读打印、回报总指挥即完成使命 |
| `_wiley_aip_confirm_145.py` | 同法对 wiley/aip 桶确认结算 | 与上近重复，纯读打印一次性 |

### 1.5 一次性导出器（产物 txt/json 已单独留存，脚本可复现即弃）
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_export_si_reject_145.py` | 从 reclass json 导 51 条 SI DOI → `out/rerun_acs_144_si_reject_145.txt` | 一次性导出，产物 txt 已留 |
| `_batch6_si_reject_export_145.py` | 导 batch6 acs SI 假阳 → `out/batch6_acs_si_reject_145.txt` | 同上 |

### 1.6 其它已被生产吸收
| 脚本 | 用途 | 为何可删 |
|---|---|---|
| `_tmp_verify_cov_147.py` | 校验 b4cf 黑名单是否已从 coverage 剔除 | 自述可删，一次性校验 |
| `_title_probe_content_qc.py` | title_probe 内容级 QC | 功能已在 `tools/qc_content_match.py` |

---

## 二、B 档 · 应归档 → `archive/scratch-2026-07/`（31）

> 方法/证据有参考价值：`git mv` 保 git 历史、随时可回；**不建议直接 `rm`**。

### 2.1 JA3 / 浏览器内直下 根因与真修复探针（-144，方法可复用）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_ja3_probe_144.py` | JA3 只读探针（浏览器内 fetch 绕 JA3 拿 PDF） | 结论已落 `render_fetch.py`，方法留证 |
| `_tmp_ja3_diag_patched.py` | ClientSecurityState 容缺 monkeypatch 后定位卡点 | 根因验证参考 |
| `_tmp_ja3_monkeypatch_probe.py` | 证实 CDP schema 漂移是抓不到 PDF 根因 | 同上 |
| `_tmp_ja3_realfix_probe.py` | 实证不启 Fetch 拦截同会话 fetch 正文 PDF | 同上 |

### 2.2 内容 QC / 门③④⑤ 验证与回归（-142/-147/-157）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_tmp_verify_b4cf_qc_142.py` | 独立重算 recover_b4_cf 34 条 verdict 对账 | 审计证据 |
| `_tmp_manual_check4_142.py` | 4 条 uncertain/scanned + 1 SI 边界人工核验 | 审计证据 |
| `_tmp_b4cf_fail_rootcause_157.py` | b4cf 失败 DOI 根因分析（产 md） | 根因写作参考 |
| `_tmp_verify_gates345_147.py` | 门③④⑤ 拦截率/必要性/误杀 | 门验证证据 |
| `_tmp_verify_gate_live_147.py` | 34 条 success 跑真门 end-to-end 拦截率 | 同上 |
| `_tmp_verify_escape_mech_147.py` | 区分两种逃逸机制（整源豁免 vs 进门 match） | 同上 |
| `_tmp_routeb_cf_paywall_147.py` | 补验三类假阳（CF页/付费墙/错文）在哪道闸被拦 | 门验证证据 |
| `_tmp_regress_nofalsekill_147.py` | 广样本不误杀回归 | 建议并入 `tools/regress_qc_union_189.py` 后归档 |
| `_tmp_regress_fixtures_147.py` | §9.7 四条不误杀正例 fixture 断言 | 建议并入 selftest 后归档 |
| `_tmp_routeb_qc_regress_147.py` | route-B 产出 QC 无假阳/无假杀回归 | 建议并入 `tools/regress_qc_union_189.py` 后归档 |

### 2.3 -145 内容 QC 重分类审计（ACS authorchoice SI 假阳排查，喂 QC 黑名单）
> 均复用生产门函数（`download._content_qc_*`）、纯读产 json/csv，结论固化进《验证-rerun_acs_144-真全文净增与SI假阳-145.md》+ QC 黑名单 txt。方法可复用于后续桶结算 → 留证归档。
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_acs_reclassify_145.py` | rerun_acs_144 全 56 条全页稳健重分类（ARTICLE/SI_COVER/DOUBT） | 审计方法留证 |
| `_qc_scan_acs144_145.py` | 复用生产门对 56 条逐条 verdict（产 qc_scan json + md） | 审计证据 |
| `_batch6_acs_final_145.py` | batch6 acs-authorchoice success 权威终判（产 json+reject txt） | 审计证据 |
| `_screen56_145.py` | rerun_acs_144 甄别表 + 真净增 N + 扣除 DOI 清单 | 审计证据 |
| `_batch6_acs_si_scan_145.py` | 把 SI 假阳排查扩到 batch6 + recover_b4_cf 主语料 | 审计证据 |

### 2.4 冒烟（-146，方法可复用；若纳入 Runbook 则转 C 保留）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_smoke_routeb_146.py` | route-B ②b 生产路径小批串行冒烟（直调 `render_download_pdf_bytes`） | 结论固化经验记录 N.8 + 冒烟结果 md；冒烟骨架可复用 |
| `_smoke_mdpi_146.py` | MDPI ⑥ 路径（Akamai）有头串行冒烟（直调 `_nodriver_fetch_pdf_bytes`） | 独立于 render_fetch，方法可复用 |

### 2.5 T0 预筛 / 门③落地实测（-148，审计 + 交付背书）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_t0_prescreen_99_148.py` | 99 条 PENDING 翻案候选跑生产门预分类（turnkey 清单，产 csv + md） | 交付证据留证 |
| `_t0_gate3_probe_148.py` | 门③四变体对 99 条实测选窗口，给 -161 落地背书 + 扩回归样本 | 根因/门验证证据 |

### 2.6 route-B 根因诊断 / 取样 / 缺口扫描（-152，结论已固化，见 §五）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_route_b_probe_152.py` | CDP 版本错配 monkeypatch 证伪（no-pdf 根因） | 已固化经验记录 N.4/N.8 |
| `_route_b_diag_152.py` | route-B 分步诊断（定位 no-pdf-captured 卡点） | 已固化 N.8 |
| `_find_acs_oa_152.py` | 找真金 OA 的 ACS 样本（证路线B 端到端） | 一次性取样，样本已定，方法可复用 |
| `_find_rsc_oa_152.py` | 找真金 OA 的 RSC 样本 | 同上 |
| `_gap_pubdirect_152.py` | P1 publisher_direct 缺口扫描（汇总） | 结论固化《…publisher_direct缺口扫描-152.md》；补口落地后可复扫 |
| `_gap_pubdirect_152_detail.py` | 缺口逐条明细（建模板 backlog） | 同上 |

### 2.7 批次切分（一次性数据工具）
| 脚本 | 用途 | 归档理由 |
|---|---|---|
| `_split_batch4.py` | batch_4 500 条切 5 chunk | 用完可删，留作切分模板 |
| `_split_batches.py` | 测试集 csv/xlsx 切 N chunk | 同上 |

---

## 三、C 档 · 保留 / 待转正（9）· 别删

| 脚本 | 为什么保留 | 建议动作 |
|---|---|---|
| `_smoke_render_bytes.py` | **被《路线B-浏览器内直下PDF验证Runbook-173.md》第 2 步显式引用** | 留仓根；或随 Runbook 迁 `tools/` 并同步路径 |
| `fs_shim_mainloop_141.py` | **修好 Windows 工作线程 Proactor [Errno 22]** 的可用 shim 变体 | 评估合入 `tools/`（或并进 `tools/flaresolverr_nodriver.py`）；合入前别删 |
| `_naming_audit.py` | 只读审计 `out/*/pdfs/` 文件名是否合 `download.py` 命名标准——可复用运维工具 | 转正 `tools/` 或 `scripts/` |
| `_route_b_verify_152.py` | route-B gate 真机验收（render 层，旁路 QC）——**RSC 复验在途** | 保留至 RSC gate 齐绿；复验后归档 |
| `_route_b_verify_prod_152.py` | route-B gate 真机验收（**生产路径**，过 QC 门 + `_save`）——本波 P0 复验用 | 同上 |
| `_route_b_b2_152.py` | RSC B2/方法A 端到端探针（过盾后 Fetch.enable RESPONSE）——复验基线 | 同上 |
| `_route_b_diag_rsc_152.py` | RSC 生产路径失败根因诊断（直调 render 打印 error/note） | 保留至 RSC gate 齐绿；届时归档 |
| `_route_b_cfwait_152.py` | 「过盾期不开拦截」决定性实证 + 最干净过盾骨架——复验/回归基线 | 保留至 RSC gate 齐绿；届时归档 |
| `_route_b_qc_152.py` | route-B 落盘 PDF 内容 QC 验收（抽首页核 DOI/标题） | 保留（复验要用）；后续可转正为 route-B 验收工具 |

---

## 四、D 档 · 待属主确认（1）

| 脚本 | 归属 | 现状 / 建议 |
|---|:--:|---|
| `_agg_recover_150.py` | -150 | 回收覆盖聚合（dry-run，`--write` 出审计）。本波各独立 out 目录已被扁平扫描计入（0 增），一次性使命完成。-150 自评：可随本波归档；若要「周期性回收并入净覆盖」体检则转正 `tools/`。**交 -150 定夺，勿单方处置。** |

---

## 五、-152 收口说明：route-B/-152 的 12 个脚本（本波关键推进）

-142/-146/-150 均把 route-B/-152 脚本标为「待确认（他人在飞）· 勿单方处置」。**我即 -152，现予收口**：

- **route-B 机制已闭环实证**（《路线B-真机冒烟结果-152.md》）：ACS-OA `10.1021/acsomega.6c04195` 走 **B1**（页内 fetch，13.7MB，QC match）；RSC `10.1039/d5ra08493h` 走 **B2/方法A**（过盾后 Fetch.enable RESPONSE，484KB，QC match）。根因（过盾期开 CDP Fetch 拦截会被 CF 卡死）已固化经验记录 **N.4/N.8**。
- **但 RSC 生产路径 gate 未齐绿**：待 -144 落地「① 过盾后门控 Fetch.enable 兜底引回 `render_fetch._nodriver_capture_fn`；② 修 `_patch_nodriver_cdp_compat` 的 `PreflightWarn` 未知枚举容错」后，由 **-152 真机复验 ACS-OA + RSC 两样本齐绿**方算端到端通。
- **因此**：
  - **验收/复验/QC 脚本（6 个）暂列 C 保留**：`_route_b_verify_152` / `_route_b_verify_prod_152` / `_route_b_b2_152` / `_route_b_diag_rsc_152` / `_route_b_cfwait_152` / `_route_b_qc_152`——复验流程要用，**RSC gate 齐绿后再降为归档**。
  - **根因诊断 / 取样 / 缺口扫描脚本（6 个）列 B 归档**（§2.6）：结论已固化 md + 经验记录，方法留证即可。
- **协作提示**：C 保留的 6 个复验脚本调用 `render_fetch._nodriver_capture_fn` / `download._browser_capture_fallback`，与 **-144** 的 route-B 落地强耦合；-144 落地后需 `send_to_session` 知会 -152 复验，避免脚本与生产码接口漂移撞车（此依赖已在本清单登记，非埋进 report）。

---

## 六、C′ 档 · 临时 `.txt/.json` 产物（附带，非脚本盘点主体）

| 文件 | 性质 | 建议 |
|---|---|---|
| `recover_a_inst_elsevier20_input.txt` / `recover_b4_cf_input.txt` / `recover_b4_cf_smoke10.txt` / `recover_b6_els_wayback_input.txt` / `recover_b6_tail_input.txt` / `recover_regular_140_input.txt` / `rerun141_other_cf_input.txt` / `batch7_failed_dois.txt` / `batch7_subset_149.txt` / `subset_elsevier_probe_149.txt` / `val500_input.txt` | 回收/批次**数据输入清单**（非脚本） | 属数据，建议归 `inputs/` 或随批次目录 |
| `routeB_mdpi.txt` / `routeB_rsc_goldoa.txt` / `routeB_rsc_subscription.txt` | route-B 分桶输入清单 | 数据，随 route-B 复验保留或归 `inputs/` |
| `titles_probe.txt` / `titles_probe_truth.json` | 探针输入/真值 | 随 `_title_probe_content_qc.py`（A 档）一并处置 |
| `_naming_audit_report.txt` | `_naming_audit.py` 输出 | 随脚本转正则重生成，可删 |
| `_filelist.txt` / `_run_all_demo_input.txt` / `_batch6_final_out.txt` / `_tmp_final_check.txt` | 脚本临时输出/输入 | 随对应 A 档脚本一并处置 |

> 注：git 快照中的 `smoke170_input.txt` 当前 glob 未见（疑已删），不计入。

---

## 七、附：过时盘点 md 去重建议

本文合并去重 `-142/-146/-150` 三份盘点后：**-146 已被 -142 显式「取代」**、**-150 是 -146 的增量刷新**，二者均被本文（-152）进一步纳并。建议：`仓根临时脚本盘点-可否归档建议-146.md`、`-150.md`（及被取代的 `-142.md`）在本清单确认后 `git mv` 进 `archive/docs-2026-07/` 或 `docs/`，仓根只留本文一份**现行**清库清单，减少后续读者踩到过时口径。

---

## 八、执行建议（供总指挥 / 属主拍板，本文不执行）

1. **A 档 20 个** → 拍板后可 `rm`（结论已固化）；保守做法仍是先 `git mv` 进 `archive/scratch-2026-07/` 留一版。
2. **B 档 31 个** → `git mv` 进 `archive/scratch-2026-07/`（仓根尚无 `archive/`，需先 `mkdir`）；其中 `_tmp_regress_*_147.py` / `_tmp_routeb_qc_regress_147.py` 三条建议先并入 `tools/regress_qc_union_189.py` / selftest 再归档。
3. **C 档 9 个** → `_smoke_render_bytes.py` 留仓根（Runbook 依赖）；`fs_shim_mainloop_141.py`、`_naming_audit.py` 走转正 `tools/` 评审；**route-B 6 个保留至 -144 落地 + -152 复验 RSC gate 齐绿后再降档归档**。
4. **D 档 `_agg_recover_150.py`** → 交 -150 确认（归档或转正 `tools/`）。
5. **`.gitignore` 补充**：现行 `.gitignore` 未覆盖 `_*.py`；归档后建议加约定 `_*.py` / `_tmp_*` / `_spike_*` / `_probe_*` / `_route_b_*`，减少后续临时件污染仓根。
6. **⚠️ 非脚本、交付前需总指挥协调**：git 有 7 个已改未提交生产件（`cli.py` / `config.py` / `download.py` / `pipeline.py` / `render_fetch.py` / `sources/publisher_direct.py` / `tools/build_coverage.py`＝route-B 落地相关，属 -144）+ `pyproject.toml` + `经验记录-踩坑与发现.md`（多人写）。提交/回退请统一协调，勿与 -144 撞车。

---

*核验 2026-07-02｜-152 交付 · 总指挥 `task-4916d4f3`｜合并 -142/-146/-150 + 补 18 新脚本 + -152 收口 route-B。只读盘点 + 三分类清单，未删 / 未移 / 未改任何脚本或生产代码。仓根临时脚本 61 个：可删 20 · 归档 31 · 保留 9 · 待属主 1。*
