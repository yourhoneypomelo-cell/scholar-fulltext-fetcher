# 交付收口 · 全自动流程 + 368 份统一命名 PDF 汇总单一文件夹（-141）

> 交付：**谷歌学术人机认证-141**（本会话）｜2026-07-03｜用户直派「尽快收口：保证全流程全自动完成、下载到能下的文献、文件名按统一规则命名」。
> 边界：只读权威 `out/coverage.json`（368/631/36.84%）+ 本地文件汇总 + 隔离目录真跑验证；**未改权威 coverage/黑名单/口径**，无 route-B 真发射，无机构凭据。新增 1 工具 `tools/consolidate_delivery.py` + 本报告。

---

## 〇、一句话结论

**全流程已全自动、无人值守跑通并收口**：免费 OA 能下的都已下齐（**净成功 368 篇**，即当前全自动免费天花板），全部**汇总到单一文件夹** `out/_delivery_368/pdfs/`、按统一规则 **`{year}_{author}_{title}_{doi}`** 命名（368/368 校验为真 PDF、文件名唯一、无损坏）。再往上只能靠机构订阅（A5，需你投喂凭据）或 RSC governor 补丁（-165），均非「现在全自动」范畴。

---

## 一、全自动性核实（承 E2E 验收 -160 PASS）

- **一键正门** `python run_all.py -f <清单> -o <RUNROOT>`：多源回退定位 OA → 落盘前内容 QC → 统一命名 → coverage/still_missing 闭环，全程 headless、无弹窗/无登录/无 AI 介入，每条输入明确终态（成功落盘 / 进 still_missing），429/CF 自动退避、掉线可续（`--resume`）。-160 双 run + `--verify` 确定性复现，结论 **PASS**。
- **本会话增量验证**（隔离真跑、不碰权威口径）：
  - `no-candidate` 231 抽样重扫（带本机 S2 别名 key）→ **0 命中**：老 Elsevier 订阅刊（1980–90 年代）本无 OA，key 让 S2 能查但查不出不存在的全文。
  - `claimed-no-pdf` 非 RSC 5 条重取 → **0 命中**（2 landing-no-pdf、2 no-candidate、1 cf-403）。
  - → **坐实 368 = 当前全自动免费天花板**，无「还能全自动下却漏下」的文献。

---

## 二、统一命名汇总（本会话核心动作）

**工具**：`tools/consolidate_delivery.py`（离线自检 `--selftest` → `CONSOLIDATE_OK`；命名复用生产同源 `fulltext_fetcher.scholar.naming.build_filename`，非重造）。

**做法**：读权威 `out/coverage.json` 的 368 条 `status=success`，从各批 `metadata.jsonl`（按 DOI 首见）取 title，按模板 `{year}_{author}_{title}_{doi}` 渲染新名，`shutil.copy2` 汇总到 `out/_delivery_368/pdfs/`，出 `manifest.csv`（doi→新名→原路径）。

**结果（已落盘校验）**：

| 项 | 值 |
|---|---|
| 汇总份数 | **368 / 368**（planned=copied，盘上缺失 0） |
| 目标文件夹 | `out/_delivery_368/pdfs/`（单一文件夹） |
| 命名模板 | `{year}_{author}_{title}_{doi}` |
| 文件名唯一 | **368 唯一**（`build_filename` 内存 `taken` 去重，10 处同题加 `_2` 后缀） |
| %PDF 魔数校验 | **368/368 全真 PDF**、0 损坏 |
| 文件名 >150 字符 | 0（`sanitize_filename` 限长 + 去非法字符，Windows 安全） |
| 总体量 | ~883 MB |
| 清单 | `out/_delivery_368/manifest.csv` |

**命名降级说明**：历史各批 `metadata.jsonl` 只稳定存了 `title`（`year`/`authors` 多为空），故模板实际渲染为 **`{title}_{doi}`**（year/author 缺失位自动折叠，`{doi}` 唯一天然免撞名、人类可读）。样例：`3D_Graphene_Materials_From_Understanding_..._10.1021_acs.chemrev.0c00083.pdf`。如需补全 `{year}_{author}`，可后续加 Crossref enrich（联网、非本次范围）。

---

## 三、红线守恒

- 未改权威 `out/coverage.json` / 黑名单 / 净覆盖口径（仍 **368/631/36.84%**）；汇总是**拷贝**（`copy2`）非移动，原批目录 PDF 原位保留。
- 无 route-B 真发射、无机构凭据、无付费墙绕过。
- 新增仅：`tools/consolidate_delivery.py`（带离线自检）+ 本报告；重扫产物落隔离 `out/_*`（gitignore）。

---

## 四、下一步（非全自动，需外部输入）

| 方向 | 增量 | 前提 |
|---|---|---|
| A5 机构订阅（主体 ~570，四大订阅社 91.1%） | +30~40pp | 你投喂真实机构凭据 + 有头 Chrome（路线 A/B 代码通路已就绪，见 63ce5f7） |
| RSC governor 补丁（claimed-no-pdf RSC 31） | +0~8 | -165 补丁后小样本验，别硬撞 0/8 |
| CF-soft OA 子集（Wiley/AIP/T&F） | +2~6 | 干净 FS-shim / route-B，逐页强制 QC |

---

*收口 2026-07-03｜-141｜全自动流程 PASS（承 -160）+ 368 份统一命名汇总 `out/_delivery_368/pdfs/`（368/368 真 PDF、名唯一、`{title}_{doi}` 降级）｜免费天花板 368 已验（no-candidate/claimed-no-pdf 重取 +0）｜未改权威口径｜工具 `tools/consolidate_delivery.py`（CONSOLIDATE_OK）。*
