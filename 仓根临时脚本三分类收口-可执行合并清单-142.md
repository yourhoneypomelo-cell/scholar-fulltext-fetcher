# 仓根临时脚本三分类收口 · 可执行合并清单（保留 / 归档 / 删除）

> 交付：**谷歌学术人机认证-142**（工程卫生 秘书/副手）｜工单来源：**-146**（`task-78acf835`，承 152/173/174 收口）｜2026-07-03。
> **边界（硬约束·北极星「留证」）**：**本文只产可执行清单、不实际删除 / 不移动 / 不改任何 `.py` 或生产代码**；**绝不动** `out/` 产物 / `coverage.json` / `qc_merge_*` / QC 黑名单。`rm` / `git mv` / `.gitignore` 落地须**总指挥 / 属主拍板后**再执行。
> **口径来源**：合并去重《仓根临时脚本清库清单-交付前三分类-152.md》（权威 61 条：可删20/归档31/保留9/待属主1）+《工作树三分类收口清单-173.md》§3.1（+20 增量）+《工作树三分类收口清单-174.md》§3.1（+5 增量），并对 **152/173/174 未逐条覆盖的 64 个新脚本**按同一判据补分类。
> **快照说明**：仓内多 worker 实时产临时件，工作树是**移动靶**——本表为 **2026-07-03 ≈12:0x 时点快照**（`_*.py` 只增不减）。

---

## 〇、TL;DR

**当前仓根临时脚本 = 128 个**（`_*.py` **127** + `fs_shim_mainloop_141.py` 1），三分类：

| 类别 | 数量 | 处置口径（拍板后执行） |
|---|:--:|---|
| **删除 (D)** | **20** | 结论已 100% 固化进生产码 + 经验记录/交付 md，无残值。可 `rm`；**保守做法：先 `git mv` 进 `archive/scratch-2026-07/` 留一版再删** |
| **归档 (A)** | **95** | 根因/门/QC/取样/冒烟/审计探针，方法或证据有参考价值 → `git mv archive/scratch-2026-07/`（保 git 历史、随时可回），**不建议直接 `rm`** |
| **保留 (K)** | **13** | Runbook 依赖 / route-B·governor 复验在途 / 可复用运维工具 / 待属主确认 → 留仓根或转正 `tools/`，**别删** |
| **合计** | **128** | |

**判据（沿用 152/173/174）**：删除＝纯 traceback / 用后即删 / 纯读打印结算 / 一次性导出（产物已单独留存）；归档＝根因验证 / 门验证 / 内容 QC 审计 / 取样 / 冒烟 / 监管开卷扫描（方法可复用或需留证）；保留＝仍被文档引用（Runbook）/ route-B·governor 复验流程在途 / 可复用只读运维工具 / 属主未定。

**数据件（`.txt/.json`，另表§四）**：非脚本，`recover_*/rerun*/smoke*/subset_*/batch7_*/routeB_*` 等批次输入建议归 `inputs/`；`_*_out.*`/`_*_report.txt` 随对应脚本处置。**均不删**。

---

## 一、删除 (D) · 20 个 · 结论已固化、无残值

> 全部来自 152 §一 A 档（已逐条严判：纯 traceback / 自述「用后即删」/ 纯读打印 / 一次性导出且产物已留）。执行仍建议先 `git mv archive/` 留一版（北极星留证），确认无引用后再 `rm`。

| # | 脚本 | 原判据（152 §一） |
|:--:|---|---|
| 1 | `_tmp_fs_probe_141.py` | FS/shim `[Errno 22]` 复现 traceback，结论＝`fs_shim_mainloop_141.py`+经验 N.1/N.7 |
| 2 | `_tmp_fs_probe2_141.py` | 同上（并发模型跨线程） |
| 3 | `_tmp_fs_probe3_141.py` | 同上（真实 `_solve` 全流程） |
| 4 | `_tmp_fs_probe5_141.py` | 同上（shim HTTP 路径失败行） |
| 5 | `_probe_cdp_api.py` | 纯 nodriver API 签名探查，一次性 |
| 6 | `_diag_capture.py` | 机制已落 `render_fetch.render_download_pdf_bytes` |
| 7 | `_spike_bc_smoke.py` | throwaway 冒烟（nodriver 能否启动） |
| 8 | `_spike_browser_capture.py` | 已落 `render_fetch.py`（浏览器内抓字节 SPIKE） |
| 9 | `_rb_test_144.py` | 单样本，结论已并 `render_fetch.py` |
| 10 | `_oa_check_144.py` | 一次性诊断（单篇 ACS 过 CF） |
| 11 | `_fs_test_144.py` | 一次性取样，已被 `_find_rsc_oa_152` 取代 |
| 12 | `_tmp_qc_bucket_stats.py` | 自述「用后即删」（union 错论文桶分布） |
| 13 | `_tmp_qc_bucket189.py` | 自述「临时」（同域错论文 189 口径复原） |
| 14 | `_tmp_qc_report_probe.py` | 自述「临时」（探 `qc_content_report` 结构） |
| 15 | `_qc_wiley_aip_145.py` | 纯读打印、回报即完成使命 |
| 16 | `_wiley_aip_confirm_145.py` | 与上近重复，纯读打印一次性 |
| 17 | `_export_si_reject_145.py` | 一次性导出，产物 `out/rerun_acs_144_si_reject_145.txt` 已留 |
| 18 | `_batch6_si_reject_export_145.py` | 一次性导出，产物 txt 已留 |
| 19 | `_tmp_verify_cov_147.py` | 自述可删（一次性校验黑名单已剔除） |
| 20 | `_title_probe_content_qc.py` | 功能已在 `tools/qc_content_match.py` |

---

## 二、归档 (A) · 95 个 · `git mv archive/scratch-2026-07/`

### 2.1 来自 152 §二 B 档（31）— 维持归档

**JA3/浏览器内直下 根因与真修复探针（4）**：`_ja3_probe_144.py`、`_tmp_ja3_diag_patched.py`、`_tmp_ja3_monkeypatch_probe.py`、`_tmp_ja3_realfix_probe.py`

**内容 QC / 门③④⑤ 验证与回归（10）**：`_tmp_verify_b4cf_qc_142.py`、`_tmp_manual_check4_142.py`、`_tmp_b4cf_fail_rootcause_157.py`、`_tmp_verify_gates345_147.py`、`_tmp_verify_gate_live_147.py`、`_tmp_verify_escape_mech_147.py`、`_tmp_routeb_cf_paywall_147.py`、`_tmp_regress_nofalsekill_147.py`、`_tmp_regress_fixtures_147.py`、`_tmp_routeb_qc_regress_147.py`
> 归档前建议：`_tmp_regress_nofalsekill_147`/`_tmp_regress_fixtures_147`/`_tmp_routeb_qc_regress_147` 三条**先并入 `tools/regress_qc_union_189.py` / selftest**（152 §八.2）再归档。

**-145 内容 QC 重分类审计（5）**：`_acs_reclassify_145.py`、`_qc_scan_acs144_145.py`、`_batch6_acs_final_145.py`、`_screen56_145.py`、`_batch6_acs_si_scan_145.py`

**冒烟-146（2）**：`_smoke_routeb_146.py`、`_smoke_mdpi_146.py`

**T0 预筛/门③落地-148（2）**：`_t0_prescreen_99_148.py`、`_t0_gate3_probe_148.py`

**route-B 根因诊断/取样/缺口扫描-152（6）**：`_route_b_probe_152.py`、`_route_b_diag_152.py`、`_find_acs_oa_152.py`、`_find_rsc_oa_152.py`、`_gap_pubdirect_152.py`、`_gap_pubdirect_152_detail.py`
> 注：`_find_acs_oa_152`/`_gap_pubdirect_152*` 曾被 146 标「-152 属主待确认」，**-152 已亲自收口列归档**（152 §五）；可归档。

**批次切分（2）**：`_split_batch4.py`、`_split_batches.py`

### 2.2 来自 173 §3.1 增量（16）— 维持归档

| 组 | 脚本 |
|---|---|
| 聚合/数据卫生审计 | `_agg_preprint_probe_149.py`、`_datahygiene_audit_148.py` |
| 监管#169 开卷 QC | `_mon169_identify_suspects.py`、`_mon169_qc_openbook_scan.py`、`_mon169_verify_plus2_probe.py` |
| T0 回收/裁决一次性 | `_t0_recover156_blsrc.py`、`_t0_recover156_diag.py`、`_t0_recover156_netgain.py`、`_t0_recover156_qc.py`、`_t0_adjudicate147_probe.py` |
| MDPI 诊断/取样 | `_diag_mdpi_143.py`、`_diag_mdpi_b2_143.py`、`_batch_mdpi_b2_143.py` |
| 开卷/命名/route-B 诊断 | `_allow_override_openbook_rescan_140.py`、`_naming_spotcheck_146.py`、`_route_b_download_probe_141.py` |

> 173 §3.1 另列 `_diff_succ_151`/`_probe_acs4_151`/`_verify_acs_151` —— **当前快照已不在仓根**（疑已清），不计入。

### 2.3 来自 174 §3.1 增量（3）— 维持归档

`_sweep154_fatcat_probe.py`、`_sweep154_prep.py`、`_qc_p2_cf_soft_155.py`

### 2.4 152/173/174 未覆盖的新脚本（45）— 本波 -142 补分类为归档

> 判据：均为一次性探针 / 监管开卷扫描 / 桶 QC / key 探针 / 出版商专项探针 / `_tmp_*` 诊断，结论已（或应）入对应 `检索成果-*` / `选型2026-*` / `数据卫生核对-*` md；方法留证 → 归档（非保留、非删除，保守可回溯）。

| 组 | 脚本 |
|---|---|
| 监管176 开卷扩扫 | `_mon176_secondary_openbook_scan.py`、`_mon176_ocr_bucket_retry.py`、`_mon176_agg_openbook_scan.py`、`_mon176_openaire_suspect_probe.py` |
| 监管157 深读/开卷 | `_mon157_deepread_highrisk.py`、`_mon157_websearch_openbook_scan.py` |
| websearch 假阳/开卷探针 | `_ws_probe_141.py`、`_ws_probe_struct_145.py`、`_ws_dedup_141.py`、`_ws_openbook_141.py`、`_ws_openbook_qc_145.py`、`_ws_openbook_qc_159.py`、`_websearch_fp_openbook_141.py` |
| OpenAlex key 探针 | `_openalex_key_probe_140.py`、`_openalex_key_probe_160.py`、`_openalex_key_probe_176.py`、`_openalex_key_probe_phase2_174.py` |
| 154 sweep 系列 | `_sweep154_qc.py`、`_sweep154_extract_hits.py`、`_sweep154_report.py` |
| 149 sweep/tmp | `_tmp_sweep_probe_149.py`、`_tmp_sweep_oaset_149.py`、`_tmp_sweep_analyze_149.py` |
| 开卷/长尾扫描 | `_p3_openbook_153.py`、`_openbook_t0plus3_151.py`、`_repro339_147.py` |
| Wiley 专项探针 | `_wiley_realprobe_159.py`、`_wiley_qc_159.py`、`_wiley_404_probe_148.py`、`_wiley_404_browser_148.py` |
| AIP/T&F JA3 探针 | `_aip_oa_confirm_157.py`、`_aip_ja3_probe2_157.py`、`_aip_tandf_ja3_probe_147.py` |
| ROI 分桶/快速 OA 探针 | `_roi_bucket_probe_152.py`、`_roi_bucket_probe2_152.py`、`_probe_fast_oa_155.py`、`_probe_wiley_resolve_155.py` |
| ACS SI 生成/复核 | `_gen_acs_si_union_append_145.py`、`_qc_reverify_acs_si_145.py` |
| `_tmp_*` 诊断/比对 | `_tmp_v2_compare.py`、`_tmp_v2_allow11.py`、`_tmp_allow_diag.py`、`_tmp_reject_verify.py`、`_tmp_final_recipe_155.py` |
| CF 有头默认实证 | `_verify_headless_157.py`（验证 -157「CF 解题器默认有头+移屏外」；结论已固化经验记录 **N.9**；可复用为 CF 有头默认回归检查，故归档留证而非删） |

> **说明**：2.4 的 45 条以命名族 + 结论已固化 md 为据判归档；其中若干 `_tmp_*`（如 `_tmp_v2_*`、`_tmp_allow_diag`、`_tmp_reject_verify`）**很可能属可删（纯读打印/用后即删）**，但因未逐条读 docstring 确认「产物已留」，**保守列归档**（archive 可回溯，误判成本低）。如需降为删除，可指派逐条开卷确认。

---

## 三、保留 (K) · 13 个 · 别删

| # | 脚本 | 为什么保留 | 建议动作 |
|:--:|---|---|---|
| 1 | `_smoke_render_bytes.py` | **被《路线B-浏览器内直下PDF验证Runbook-173.md》第 2 步显式引用** | 留仓根；或随 Runbook 迁 `tools/` 并同步路径 |
| 2 | `fs_shim_mainloop_141.py` | **修好 Windows 工作线程 Proactor `[Errno 22]`** 的可用 shim 变体 | 评估合入 `tools/`（或并进 `tools/flaresolverr_nodriver.py`）；合入前别删 |
| 3 | `_naming_audit.py` | 只读审计 `out/*/pdfs/` 文件名合 `download.py` 命名标准——可复用运维工具（174 §五 544/544 全绿即此脚本产） | 转正 `tools/` 或 `scripts/` |
| 4 | `_route_b_verify_152.py` | route-B gate 真机验收（render 层，旁路 QC）——**RSC 复验在途** | 保留至 RSC gate 齐绿，复验后降档归档 |
| 5 | `_route_b_verify_prod_152.py` | route-B gate 真机验收（**生产路径**，过 QC 门 + `_save`） | 同上 |
| 6 | `_route_b_b2_152.py` | RSC B2/方法A 端到端探针（过盾后 `Fetch.enable` RESPONSE）——复验基线 | 同上 |
| 7 | `_route_b_diag_rsc_152.py` | RSC 生产路径失败根因诊断 | 同上 |
| 8 | `_route_b_cfwait_152.py` | 「过盾期不开拦截」决定性实证 + 最干净过盾骨架——复验/回归基线 | 同上 |
| 9 | `_route_b_qc_152.py` | route-B 落盘 PDF 内容 QC 验收 | 保留；后续可转正 route-B 验收工具 |
| 10 | `_routeb_rsc_launch_156.py` | [-156] RSC 金OA A集发射（生产路径，跑 `routeB_rsc_goldoa.txt`）——route-B 发射/复验在途（173 §3.1） | 保留至 route-B 收口 |
| 11 | `_routeb_shard_healthcheck_142.py` | route-B 发射前 ready-to-fire 输入分片健康性复检（只读、可复跑运维）（173 §3.1） | 保留（可转正 `tools/`） |
| 12 | `_route_b_governor_probe_167.py` | RSC「governor / Validate User」第二关绕过探针，**与《选型2026-route-B反RSC-governor补丁方案-165.md》在途**（174 §3.1） | 保留至 governor 方案收口 |
| 13 | `_agg_recover_150.py` | 回收覆盖聚合（dry-run）——**属主 -150**，一次性使命已完成但周期性体检可转正 | **交 -150 定夺**（归档或转正 `tools/`），勿单方处置 |

> **route-B 6 条（#4–9）+ #10–12** 调用 `render_fetch._nodriver_capture_fn` / `download._browser_capture_fallback`，与 **-144** 的 route-B 落地强耦合；-144 落地 / RSC gate 齐绿后需知会复验属主，避免脚本与生产码接口漂移撞车。

---

## 四、数据件（`.txt/.json`）· 非脚本、附带处置（均不删）

| 组 | 文件 | 建议 |
|---|---|---|
| 批次/回收数据输入 | `recover_a_inst_elsevier20_input.txt`、`recover_b4_cf_input.txt`、`recover_b4_cf_smoke10.txt`、`recover_b6_els_wayback_input.txt`、`recover_b6_tail_input.txt`、`recover_regular_140_input.txt`、`recover_p2_cf_soft_155_input.txt`、`rerun141_other_cf_input.txt`、`batch7_failed_dois.txt`、`batch7_subset_149.txt`、`subset_elsevier_probe_149.txt`、`p3_longtail_153_input.txt`、`smoke5_140_input.txt`、`smoke154_live_input.txt`、`smoke170_input.txt` | 归 `inputs/` 或随批次目录 |
| route-B 分桶输入 | `routeB_mdpi.txt`、`routeB_rsc_goldoa.txt`、`routeB_rsc_subscription.txt` | **随 route-B 复验保留**或归 `inputs/`（被 #10 `_routeb_rsc_launch_156` 依赖） |
| 探针输入/真值 | `titles_probe.txt`、`titles_probe_truth.json` | 随 `_title_probe_content_qc`（删除档）一并处置或归 `inputs/` |
| 脚本临时产物 | `_filelist.txt`、`_naming_audit_report.txt`、`_naming_spotcheck_146_report.txt`、`_batch6_final_out.txt`、`_selftests_145_out.txt`、`_run_all_demo_input.txt`、`_tmp_final_check.txt`、`_smoke_wiley_legacy_155_input.txt`、`_t0_recover156_input.txt`、`_agg_preprint_probe_149_out.json` | 随对应脚本一并处置（脚本转正则重生成，可删） |
| ⚠️ 非临时·建议入仓 | `.ftf_institutional.local.json.example` | **A5 机构订阅本地凭据 example 模板**，非临时件 → **保留入仓**（勿扫入清库） |

---

## 五、可执行动作（供拍板 · 本文不执行）

> 以下为**建议命令，本文未运行**；须总指挥 / 属主拍板后执行。执行前确认无并发 worker 正引用相关脚本。

```bash
# 0) 归档目录（仓根尚无 archive/）
mkdir -p archive/scratch-2026-07

# 1) 归档 95 个（保 git 历史、随时可回）—— 用 git mv，勿 rm
#    （删除档 20 个的保守做法同样先 git mv 留一版再删）
#    完整文件名见 §一(20) + §二(95)；建议脚本化：读本清单表格逐条 git mv。

# 2) 保留 13 个：不动（route-B/governor 复验在途 / Runbook 依赖 / 待属主 / 可复用工具）
#    可选转正：git mv _naming_audit.py tools/  （及 fs_shim / _routeb_shard_healthcheck 评审后转正）

# 3) .gitignore 补仓根临时件约定（强烈建议，防并发 worker 反复污染）
#    追加：_*.py  _tmp_*  _spike_*  _probe_*  _route_b_*  _*_out.*  _*_report.txt  selftest.pdf
```

**收口待办（供总指挥 / 属主拍板）**：
1. **删除 20 + 归档 95** → 拍板后 `git mv archive/scratch-2026-07/`（删除档确认无引用后可再 `rm`）。
2. **保留 13** → route-B/governor 复验收口后再降档；`_naming_audit`/`fs_shim_mainloop_141`/`_routeb_shard_healthcheck_142` 评审转正 `tools/`；`_agg_recover_150` 交 **-150** 定夺。
3. **数据件** → 归 `inputs/`；`.ftf_institutional.local.json.example` 入仓。
4. **`.gitignore`** → 补临时件约定（152/173/174 三波均建议、仍未落地）。
5. **过时盘点 md 去重**（152 §七）：`仓根临时脚本盘点-可否归档建议-142/146.md`、`-150.md`、`工作树三分类收口清单-173/174.md` 已被本合并清单纳并，确认后可归 `archive/docs-2026-07/`，仓根只留本一份现行清单。

---

## 六、计数对账

| 口径 | `_*.py` | + `fs_shim` | 删除 | 归档 | 保留 |
|---|:--:|:--:|:--:|:--:|:--:|
| 152（权威基线） | 60 | 61 | 20 | 31 | 9(+1 待属主) |
| 173 §3.1 增量 | +18 | | 0 | +16 | +2 |
| 174 §3.1 增量 | +? | | 0 | +3 | +1 |
| **-142 补分类新脚本** | +45 | | 0 | +45 | 0 |
| **本表合计** | **127** | **128** | **20** | **95** | **13** |

> 20 + 95 + 13 = **128** ✓（127 `_*.py` + `fs_shim_mainloop_141.py`）。

---

*核验 2026-07-03（-142 工程卫生秘书快照）｜工单 `task-78acf835`（来源 -146）｜合并 152/173/174 + 补 64 新脚本三分类。只读产清单 + 命令建议，**未删 / 未移 / 未改任何脚本或生产代码，未动 out\/coverage\/qc_merge\/黑名单**。仓根临时脚本 128：删除 20 · 归档 95 · 保留 13。*
