# 检索成果 · 路线图覆盖现状与剩余空白（截至 2026-07-01）

> 智库汇总（供总指挥 145 派工）。整理人：谷歌学术人机认证-160（信息检索-智库专家）｜2026-07-01。
> 目的：在多路并行、会话频繁更替的当下，给一张**「哪些已建 / 已研究 / 仅剩实现 / 仍是空白」**的单页地图，避免重复造轮子、便于精准派工。
> 依据：《功能优化与新增功能建议》(路线图 A1–E2) + 《角度8》(ROI) + 本会话已产出的 E1/E2/A3/补遗 + 通读源码。**本文只做覆盖盘点，不改代码。**

---

## 一、单页覆盖地图

| 项 | 现状 | 覆盖（文档/成员/代码） | 剩余空白 | 建议 |
|---|---|---|---|---|
| **A1** CORE key + 绿色OA | **多已建** | `green_oa.py`(BASE/OSF/ScienceOpen)、`aggregators.Core` | 申请免费 CORE key(运维)；**核实 BASE 是否需 key**(见《补遗》→ -156) | 运维/-156 收尾 |
| **A2** 落地页解析增强 | **部分已建** | `landing.py`(JSON-LD/meta/link 已加) | 出版商专属 selector + 重定向链跟踪 | 低优先，实现者 |
| **A3** 出版商适配器/keyed-TDM | **设计就绪、待实现** | `publisher_direct.py`(公网模板)；**`publisher_tdm` 伪代码+骨架已给(《补遗》)** | **实现 `publisher_tdm`**(Wiley/Elsevier/Springer) | **实现者(建议 -153，与 A5 合并)** |
| **A4** 渲染兜底 | **已建(默认关)** | `render_fetch.py`(Playwright/nodriver) | — | — |
| **A5** 机构订阅接入 ★ | **研究中** | 《机构订阅集成设计》、-153 | 实现(EZproxy/Shibboleth/OpenAthens/CARSI + keyed-TDM 合并) | **-153 主线** |
| **B1** Scholar 被引/版本 | **已建** | `scholar_serpapi.py`/`citations.py` | 微增量(`citations_per_year`/`cites_id`) | 白捡，非阻塞 |
| **B2** 反爬栈产品化 | 骨架(默认关) | `poc/self_built_stack/` | 产品化(仅确需直抓时) | 灰色、按需 |
| **B3** 代理池 | **未建** | 《角度6》研究 | 实现(仅服务 B2/高频) | **最低**，不建议主线 |
| **C1** Excel/CSV 入口 | **✅ 已建** | `cli.py` | — | — |
| **C2** 失败分类重试 | **未建/部分** | 断点续跑已有 | 按 error 分类永久失败不重试 | 小、实现者 |
| **C3** Zotero 直写 | **研究/实现中** | PoC 已有、-164 | 移植进生产 | **-164** |
| **C4** 异步并发 | **研究中** | -168 | httpx/aiohttp 重构评估 | **-168** |
| **C5** Web/GUI | **未建** | — | 轻量面板(P2) | 中长期 |
| **C6** 运行报表 | **✅ 已建** | `report.py` | — | — |
| **D1** 测试/CI | **部分** | 各模块 selftest | 连接器 mock 测试 + GitHub Actions | 中 |
| **D2** PDF 完整性校验 | **✅ 已建** | `download.py`(pypdf) | — | 备忘录秘书已定稿 |
| **D3** 自适应限速/熔断 | **未建·未深研** ← 空白 | `http_client` 固定间隔 | per-host 动态间隔 + 令牌桶 + 退避 | **当前最大「未研究」空白，中 ROI** |
| **E1** 快照线 | **✅ 已建** | `snapshot_bootstrap/snapshot/ingest`；**guide 补丁已给(本会话)** | 仅并入 guide 三句(可选) | 白捡 |
| **E2** 本地全文检索 | **设计就绪、待实现** | **选型+设计+PDF抽取选型已给(本会话)** | 实现 `localsearch.py`(FTS5) | 实现者，非急 |

---

## 二、结论（给 145 的三句话）

1. **研究面几乎已满覆盖**：路线图 A1–E2 中，绝大多数已建或已研究/已设计；本会话补齐了 E1(补丁)/E2(设计)/A3(伪代码)/A1(BASE 核实+CORE key)/PDF 抽取选型。
2. **唯一「仍未深入研究」的空白 = D3 自适应限速/熔断**（中 ROI；与 -168 的 C4 异步相邻但不同层——C4 是并发模型、D3 是按 host 实时退避）。如需我继续，D3 是最该研究的下一个。
3. **其余多是「实现待排」而非「研究待补」**：`publisher_tdm`(A3→-153)、`localsearch.py`(E2)、A5 机构接入(-153)、C2/C3/C4/D1——设计/研究已就绪，等 145 排实现波次。

---

## 三、来源
- 《功能优化与新增功能建议》（路线图 A1–E2 + 进度标注）、《检索成果-角度8》（ROI）、《检索成果-00 聚合总报告》、《项目索引》、本会话产出（E2/E1/A3/补遗）+ 通读 `fulltext_fetcher/` 源码（`sources/`、`download.py`、`render_fetch.py`、`snapshot*.py`）。

---

*核验 2026-07-01｜仅覆盖盘点，不改代码。最大「未研究」空白 = D3 自适应限速/熔断；其余为「实现待排」。*
