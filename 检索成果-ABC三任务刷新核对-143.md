# A/B/C 三任务刷新与核对（still_missing 分桶 / publisher_direct 缺口 / 仓根临时脚本）

> 交付：**谷歌学术人机认证-143（worker）**｜2026-07-02｜来源：用户直派「A 刷新分桶+vs173漂移核对 / B publisher_direct 缺口扫描+可救前缀 / C 仓根临时脚本盘点归档」。
> 边界：**只读核对 + 只新建本 1 份 md，未改任何 `.py` / 未删移任何脚本 / 未动他人文档**。
> 与既有交付**不重复、只增量核对**：A 详本见 `检索成果-still_missing628分桶统计刷新-vs173漂移核对-143.md`；B 详本见 `检索成果-publisher_direct缺口扫描-152.md`（-152）；C 底本见 `仓根临时脚本盘点-可否归档建议-146.md`（-146）。本文只做**当前数据复跑核验 + 增量**。
>
> ⚠️ **净覆盖率口径统一（173 冻结）**：本文 §〇 复跑数字 **success 371 / miss 628 / 37.14%**（@12:37:20 快照）为**【历史口径】**。**【历史快照】当前权威见 `out/coverage.json`：326 success / 673 miss / 999 = 32.63%**（generated_ts 2026-07-03 12:50:24, allow_override=10）。A/B/C 核验方法与漂移定性仍有效，唯净覆盖/miss 总数以 388/611 为准。唯一权威见 **《基线口径冻结说明-388-173.md》**。

---

## 〇、TL;DR

1. **A（分桶/漂移）无新漂移**：最新权威 `out/coverage.json`（12:37:20）= **success 371 / miss 628 / 999 = 37.14%**，与 -143 分桶文档（当时 12:13:09 口径）**逐项一致**；`_shard_stats.json` 八桶求和=628、三方一致。vs-173 的漂移（净覆盖 −7.7pp、still_missing +77）**已在 -143 文档定性完毕，未再变动**。
2. **B（publisher_direct 缺口）现场复跑数字与 -152 完全吻合**：`static=194 / xref=381 / 有handler=575（91.6%）/ 真缺口=53（8.4%）`，24 个纯缺口前缀。**最高 ROI 补口 1+2+3（RSC遗留期+Springer兄弟前缀+老Elsevier）= +24，缺口 53→29**（复用同域模板，回归风险低）。
3. **C（临时脚本）已从 -146 的 33 个增至 48 个**（47 个 `_*.py` + `fs_shim_mainloop_141.py`），**新增 15 个**（-146 之后产生，主要是 route-B 诊断 -152/-147 + 回收聚合 -150 + 漂移/嵌套核查 -151）。本文补齐这 15 个的用途与归档分档。
4. **新发现（值得 144 关注）**：`out/_audit149_netgain_unmerged.csv` 记录 **71 个已落盘 PDF 尚未并入净覆盖**（67 条仍在 still_missing + 4 条 route-B 新产），`coverage_merged_150.json` 的 `new_recovered_dois=[]`、`blacklist_size=424` → 这批**回收产物卡在 QC/黑名单未回灌**，是「628 能否再降」的直接抓手（须过内容 QC 门后再并）。

---

## 一、任务 A — still_missing 628 分桶统计刷新 + vs-173 漂移核对（现值复核）

**权威口径（直读 `out/coverage.json.summary`，生成 12:37:20）**：

| 指标 | 值 | 
|---|---:|
| total_unique_dois | 999 |
| success（净·真唯一正确论文） | **371** |
| miss / still_missing | **628** |
| success_rate | **0.3714（37.14%）** |
| claimed_success_but_no_pdf | 343 |
| success_after_qc | 371（去重 463 − 剔抓错 92：硬黑 0 + 软黑 92） |

**分桶（`out/still_missing_shards/_shard_stats.json`，628，八桶）**：elsevier 379 / acs 95 / rsc 67 / springer 23 / wiley 22 / iop 4 / aip 3 / other 35；`shard_sum_equals_total=true`、`no_duplicates_across_buckets=true`、`all_still_missing_sharded=true`。

**核对结论**：
- 与 -143 分桶文档（`…统计刷新-vs173漂移核对-143.md`）的 **371/628/999、37.1%、八桶数值逐项一致** → coverage 时间戳虽从 12:13:09 前移到 12:37:20，但**净口径无变化，无新漂移**。
- vs-173 的两处漂移（净覆盖 **44.8%→37.1% −7.7pp**、still_missing **≈551→628 +77**，+77=audit156 漏派并入）与 CF 桶去伪（真出版商 CF≈187 而非 307，Elsevier 镜像 CF 不计入）**已在 -143 文档完成，本次无新增漂移**，此处不复述细节。

---

## 二、任务 B — publisher_direct 缺口扫描 + 可救前缀（现场复跑 `_gap_pubdirect_152.py`）

**复跑输出（对 628 条 still_missing 逐条判定，判定源 `sources/publisher_direct.py`）**：

| 覆盖类别 | 条数 | 占比 |
|---|---:|---:|
| static（离线可推直链） | 194 | 30.9% |
| xref（需 1 次 Crossref 取 PII/坐标） | 381 | 60.7% |
| **有 handler 合计** | **575** | **91.6%** |
| **真缺口（无模板）** | **53** | **8.4%** |

**分桶**：acs 95 / wiley 22 = ✅COVERED；elsevier 379（gap 5=老10.1006）、rsc 67（gap 10=a/b/tf老期）、springer 23（gap 9=10.1023×6+10.1134×3）、other 34（gap 21）= PARTIAL；aip 4 / iop 4 = ❌GAP。

**24 个纯缺口前缀**（合计 43 条）：`10.1023(6)`、`10.1006(5)`、`10.1166(4)`、`10.1246(4)`、`10.1063(3)`、`10.1134(3)`，其余 18 前缀各 1 条（Cambridge/OUP/IUCr/IEEE/DeGruyter/AIP-AVS/IOP家族/中日俄小社等）。

> **口径警示（与 -152 一致）**：「有 handler」≠「能免费下到 PDF」。publisher_direct 是**机构订阅路径**（`--institutional` 门控），无订阅回 401/403/HTML 被 `%PDF` 魔数闸过滤、不产假阳。故 575 是**机构订阅理论上限**、非免费可得；最大桶 Elsevier `10.1016`(374) 属 IP/登录墙最低 ROI，主力交机构订阅。

**最高 ROI 补口建议（复用现有同域模板、近零成本、可加离线 selftest）**：① RSC 扩 a/b/tf 遗留期 **+10**；② Springer 加兄弟前缀 `10.1023`(Kluwer)+`10.1134`(Pleiades) **+9**；③ 老 Elsevier `10.1006` 并入 `_elsevier` Crossref-PII 分支 **+5**。**先做 1+2+3 = +24，缺口 53→29（腰斩）**；余长尾（AIP/IOP/CSJ/JNN/中日俄各 1~4）ROI 低，押后或交机构订阅 / websearch（走 QC union 门）。**注：改 `publisher_direct.py` 属代码活，本文只标缺口、不落地。**

---

## 三、任务 C — 仓根临时脚本盘点（增量：-146 之后新增 15 个）

**总量刷新**：仓根 `_*.py` = **47** 个 + `fs_shim_mainloop_141.py` = **48** 个（-146 盘点时为 33 个）。下表只列 **-146 未覆盖的 15 个新脚本**（其余 33 个分档见 -146 文档，结论不变）。

| 新脚本 | 用途（文件头自述） | 关联会话 | 归档建议 |
|---|---|---|:--:|
| `_route_b_probe_152.py` | 路线B 根因探针：monkeypatch `ClientSecurityState.from_json` 容旧，证 Chrome133↔nodriver0.50.3 CDP 字段错配（`localNetworkAccessRequestPolicy` KeyError）饿死网络层 | 152 | A（结论已入经验 N.4） |
| `_route_b_diag_152.py` | 路线B 分步诊断：定位 `no-pdf-captured` 卡在哪步（最终URL/标题/CF质询/PDF链/页内fetch） | 152 | A |
| `_route_b_cfwait_152.py` | 验 -148 假设：过盾期开 `Fetch.enable` 是否把 CF 卡死；纯导航+轮询 cf_clearance（不开拦截）对照 | 152 | A |
| `_route_b_b2_152.py` | RSC 端到端闭环：过盾 + B2/方法A（Fetch RESPONSE 拦截+导航PDF）能否落 %PDF | 152 | A |
| `_route_b_qc_152.py` | 对路线B 落盘 PDF 做内容 QC（抽首页文本核 DOI/标题，防假阳） | 152 | A（功能已在 `tools/qc_content_match.py`） |
| `_tmp_routeb_cf_paywall_147.py` | 只读：补验三类假阳（CF挑战页/付费墙HTML/错文章）各在哪道闸被拦 | 147 | A |
| `_tmp_routeb_qc_regress_147.py` | 只读·回收波前置：route-B 产出内容 QC 无假阳/无假杀回归 | 147 | A（建议并入 `tools/` 回归集） |
| `_rb_test_144.py` | 聚焦测试：route-B 方法B（页内 `fetch().arrayBuffer()`）不开拦截+低负载能否落 %PDF | 144 | A |
| `_ja3_probe_144.py`※ | JA3 只读探针（※-146 已列 A 档，仍在仓根） | 144 | A |
| `_fs_test_144.py` | 一次性：单 shim(8199) 测 1 篇 ACS 能否过 CF（cf_clearance+200） | 144 | A |
| `_oa_check_144.py` / `_find_rsc_oa_152.py` | 查 Unpaywall 找「RSC 金OA 且 PDF 托管 pubs.rsc.org（JA3绑定CF）」样本，供路线B 净测 | 144/152 | A |
| `_probe_acs144_qc_145.py` | 探针：盘点 `rerun_acs_144` 56 条 success 的 QC 字段与来源分布 | 145 | A |
| `_agg_recover_150.py` | 回收聚合（默认 dry-run）：把 `rerun_*/**/coverage.json` 成功集并入 999 净覆盖、去重、剔 QC 黑名单；`--write` 产 `coverage_merged_150.json` | 150 | **B**（可复用回收核账工具，建议转正 `tools/`） |
| `_probe_drift_151.py` | 校验嵌套 `rerun_*` 目录成功集 vs 主 coverage 的漂移 | 151 | A |
| `_probe_nested_dois_151.py` | 盘点嵌套(depth>0) metadata 目录的 DOI 命中 | 151 | A |

> ※`_ja3_probe_144.py` 在 -146 已归 A 档，这里因它仍在仓根一并列出，避免读者以为遗漏。实际「-146 之后真·新增」= 除它外的 14 个。

**归档建议（承 -146，非破坏性、须属主/总指挥拍板后再执行）**：
1. **A 档**（结论已固化的一次性诊断/验证）：14 个新脚本中 12 个可随 -146 的 A 档一并 `git mv` 进 `archive/scratch-2026-07/`（保 git 历史、可回溯，不建议 `rm`）。
2. **B 档（别删/待转正）**：`_agg_recover_150.py`（回收核账，复用价值高）建议评估转正 `tools/`；`_tmp_routeb_qc_regress_147.py` 建议并入回归集。
3. **route-B 系（`_route_b_*_152.py` 5 个 + `_rb_test_144.py`）属 -152/-144 在跑的活**，虽结论已入经验 N.4，归档前建议 `send_to_session` 知会 -152，**勿单方处置**。
4. `.gitignore` 可加 `_*.py`/`_tmp_*`/`_spike_*`/`_probe_*`/`_route_b_*` 约定，减少后续临时件污染仓根。

---

## 四、新发现 — 71 个已落盘 PDF 尚未并入净覆盖（628 的直接抓手）

`out/_audit149_netgain_unmerged.csv`（71 行）：

| 来源目录 | 条数 | status |
|---|---:|---|
| rerun_acs_144 | 56 | in_still_missing（有 PDF 但仍在 628） |
| rerun_elsevier_143 | 8 | in_still_missing |
| rerun_cf_savable_140 | 2 | in_still_missing |
| rerun_wiley_144 | 1 | in_still_missing |
| runbook_b（route-B 新产） | 4 | new |

- `coverage_merged_150.json`：`main_success=371`、`merged_success=371`、`new_recovered_dois=[]`、`blacklist_size=424` → 这 67 条**已下载 PDF 却未计入净覆盖**（多因落在 QC 黑名单 424 内 / 未过内容 QC），另 4 条 route-B 产物为「new」待核。
- **含义**：628 里至少有一批是「物理已回收、卡在 QC 回灌」而非「真下不到」。**若这批经内容 QC 判为真全文并回灌，still_missing 可实质下降**——这是比破新盾更低成本的净增来源。
- **建议给 144**：排一个「对 `rerun_acs_144`(56)/`rerun_elsevier_143`(8) 等已落盘 PDF 跑内容 QC 门 → 通过者并入 coverage、更新 still_missing」的收口任务（纯核账 + QC，无需破盾/联网破 CF）。**注：并入动作会改 coverage 产物，属代码/数据活，本文只提示、不执行。**

---

## 五、来源与复现

- 数据：`out/coverage.json`(12:37:20 summary)、`out/still_missing.txt`(628)、`out/still_missing_shards/_shard_stats.json`(628)、`out/coverage_merged_150.json`、`out/_audit149_netgain_unmerged.csv`。
- 脚本（复跑，未改）：`python _gap_pubdirect_152.py`（B 复现）；仓根 `_*.py` 清单经 `Get-ChildItem` 盘点。
- 既有交付（只引不复述）：`检索成果-still_missing628分桶统计刷新-vs173漂移核对-143.md`、`检索成果-publisher_direct缺口扫描-152.md`、`仓根临时脚本盘点-可否归档建议-146.md`、`检索成果-still_missing-CF-JA3桶ROI深挖-173.md`、`A5机构订阅现状与still_missing可救前缀梳理-150.md`。

---

*核验 2026-07-02｜谷歌学术人机认证-143｜A/B/C 三任务现值复核 + 增量｜只新建本 1 份 md，未改任何 .py / 未删移脚本 / 未动他人文档。*
