# 交接待办 · A5 机构订阅【暂缓】+ 后续增强移交清单（-149）

> 交付：**谷歌学术人机认证-149**｜2026-07-04｜承用户拍板落档。
> 性质：**交接文档**——记录「什么被暂缓、暂缓在哪个状态、接手人从哪一步继续、还有哪些小项移交」。
> 数字纪律：KPI 唯一权威 = `out/coverage.json`（**368/631/999 = 36.84%** @2026-07-03 22:07:22）+《基线口径冻结说明-388-173.md》；本文不另造数字。

---

## ⓪ 用户拍板（置顶）：A5 机构订阅**暂不在本项目落实**

2026-07-04 用户明示：**「A5 暂时不在这个项目中落实」**。免费边界内已全部收口（368 定版），A5 相关工作全部停在**「万事俱备、只欠凭据」**态——代码/测试/文档/输入集均已就绪并入库，唯一缺的是用户机构凭据。**本项目内不再推进 A5 联调**；未来在本项目重启、或移植到其他项目时，按下述状态直接接手，**不需要重做任何准备工作**。

### A5 就绪度快照（截至本文，全部已入库）

| 件 | 状态 | 位置 |
|---|---|---|
| 机构订阅框架（credential_store / auth_session / assisted_auth / ezproxy_login / login_browser / route_b_bridge） | 4 项离线 selftest 全绿：`A5_FRAMEWORK_OK` / `EZPROXY_LOGIN_OK` / `ASSISTED_AUTH_OK` / login_browser OK | `fulltext_fetcher/institutional/` |
| 路线A（HTTP 直链：publisher_direct + EZproxy 改写 + Cookie 注入） | 已进主流程，`--institutional` 一键启用（run_all 与核心 CLI 同口径） | `fulltext_fetcher/http_client.py` / `sources/publisher_direct` |
| 路线B（浏览器内注入机构会话抓字节，破 RSC/ACS/Wiley/ScienceDirect 等 JA3 绑定强 CF 站） | **断线已修复入库**（`63ce5f7`：cfg→`plan_route_b_injection_from_config`→`render(injection_plan=)`；route-A/B 白名单同门守卫；落盘前强制内容 QC） | `fulltext_fetcher/download.py` / `institutional/route_b_bridge.py` |
| 冒烟输入集 | 18 条订阅墙确证 DOI（Elsevier 8 / ACS 6 / RSC 4，取自 a5_only∩still_missing） | 仓根 `a5_smoke_input.txt`（另 `out/_a5_testset_176.txt` 同源） |
| 凭据模板与安全 | `.gitignore` 已忽略 `.ftf_*.local.json`；`InstitutionalCredentials.__repr__` 自动脱敏 | 仓根 `.ftf_institutional.local.json.example` |
| Runbook | 3 步即跑速查 / 详版 how-to / 联调验收清单 | 《A5即跑清单-189.md》/《A5用户凭据接入指引-141.md》/《A5联调就绪核-credential-in-run验证与联调清单-177.md》 |
| **放量分片清单**（冒烟达标后用） | `a5_shards/` 12 分片共 441 条 = **真实缺口全集**（-149 逐条核验：`_all_441.txt` 与分片并集均与 coverage 复算的 441 缺口**逐 DOI 一致**，零重叠零漏）；每分片一条命令即跑 | `a5_shards/*.txt` +《交付-A5即跑分片清单-441真实缺口-157.md》 |

### 接手 3 步（未来有凭据时，从这里继续）

1. **合法登录**机构入口（EZproxy/SSO/CARSI/WebVPN 皆可）→ 浏览器 Cookie-Editor 导出 Header String（`k1=v1; k2=v2`）。
2. `Copy-Item .ftf_institutional.local.json.example .ftf_institutional.local.json` → 填 5 字段（`enabled:true` / `provider` / `ezproxy_prefix`（非 EZproxy 留 null）/ `institution_cookie` / `institution_domains`）。
3. `python run_all.py -f a5_smoke_input.txt --route-b cf-only -o out/_a5_smoke_<会话号>`——先 18 条冒烟，命中 ≥70% 再对 still_missing 订阅分片放量（`-c 2 --per-host-interval 1.0` 防 bulk 风控）。

### 为什么值得接手（量化依据，`b6d76fe` 统计）

- 当前 631 miss 中，剔除 QC 正确剔除的 190 条抓错论文后**真实缺口 441**；其中 **~395（90%）集中四大订阅社**（Elsevier 308 / RSC 46 / ACS 28 / Wiley 13；若把 10.1006 老 Academic Press 4 条并入 Elsevier 计 312，即《157 分片清单》口径的 **399/90.5%**——两处数字同底，仅前缀归属差异）——主体是付费墙全文。
- **免费路线已实测到顶**：36 条 pdf-missing 隔离重取 **0/36**（`1291a88`）；no-candidate 231 带 key 重扫 **+0**；FlareSolverr 对 still_missing 真救回 ≈0。
- 故 A5 是唯一 **+30~40pp 量级**的净增路径；本项目一切 A5 准备工作的 ROI 都保存在上表里，不因暂缓而流失。

### 🔴 红线（重启时依然生效）

Cookie/前缀只落本地 gitignore 文件；绝不进 chat / `run.log` / `metadata.jsonl` / 任何产物 / git；仅对**有合法访问权**的内容使用；批量走低并发礼貌节流。

---

## ① 本波已收口（接手人**不需要**重做）

| 项 | 证据 |
|---|---|
| 净覆盖定版 **368/631/999 = 36.84%**（writeback149：+50 gold −8 websearch 假阳；46 份误拒 gold 解隔离；写前备份留存） | `out/coverage.json` @22:07:22、`8e466b3`、《coverage收口149-干跑复核与阻断报告.md》 |
| build_coverage **方案C**：qc_allow 白名单命中项额外认 `<batch>/rejected/`（复活留证 `pdf_in_rejected`、照过回写开卷门、非白名单永不认、pdfs/ 实盘压过复活、零 allow 行为逐字节不变）——**未来翻案 gold 不再需要手工解隔离** | `788e8db`；selftest 场景 a–e；全配方干跑对账 368 逐字不变 |
| 一键正门 API key 透传 + 环境变量别名回退（OpenAlex/S2/CORE；canonical 优先、`SEMANTIC_SCHOLAR_API_KEY` 等别名兜底） | `b892eca`（cli）/ `61625a1`（run_all）/ `46f0b37`（README） |
| 内容QC **门③ meta-doi-mismatch**（PDF 自述 DOI 反向比对，专拦"同题他刊"）落地+定稿 | `61625a1` / `2be22ae`；`REGRESS_UNION_189_OK` |
| **openalex_content 新源**（官方缓存 PDF；key 单点注入不落产物）+ http_client 429 超长 Retry-After 熔断 | `788e8db` |
| **交付夹**：`out/_delivery_368/pdfs/` 368 份统一命名 `{year}_{author}_{title}_{doi}` + `manifest.csv` 逐行可溯源 | `tools/consolidate_delivery.py`（368/368 拷贝校验；-149 随机 10 份开卷抽检全过：6 doi_in_body + 4 title≥0.5、0 可疑；重复的 `out/_delivery` 已清理，两夹全量比对逐字节等价后删除） |
| 一键正门 E2E 验收 PASS + **非空 RUNROOT 重跑告警**（E2E-160 观察项①收口：`--no-resume` 重跑同 `-o` 会累积 `_N` 副本，现启动即告警并指引 `--resume`/换新 `-o`；只警不阻断） | 《E2E验收结论-160.md》；本笔 run_all `_nonempty_runroot_warning` + selftest ⑨ |
| 全套离线自检 | `run_all_selftests.py` **PASS=47 / FAIL=0 / SKIP=1**（唯一 SKIP=flaresolverr_nodriver，online-gated） |

## ② 移交待办（小项 · 非阻塞）

| # | 项 | 级别 | 说明 |
|---|---|---|---|
| 1 | **scholar 真实抓取凭据 gate**：SerpApi key 或住宅代理 | D（用户凭据） | 与 A5 同类"等凭据"项：代码/离线测试全就绪（`fulltext_fetcher/scholar/`），数据中心 IP 直抓 Scholar 必 403；有 key/代理时即可真跑验收 |
| 2 | C1 报表半项：**带状态结果 xlsx 报表** | P2 | `.xlsx` 入口已建（`cli._read_xlsx`）；输出侧现有 `results.csv`/`report.html` 可用，"贴合验证用的 xlsx 状态表"未做（体验项） |
| 3 | nodriver `get_all_cookies` 弃用告警 | ✅已修待复验 | `46f0b37` 已把非弃用 API 提为首选；接手人升级 nodriver 后跑 `RUN_ONLINE_SELFTESTS=1 python -m tools.flaresolverr_nodriver --selftest` 复验无 warning 即可关闭（巡检待办-145 #1 同源） |
| 4 | 大规模开卷内容 QC 仍靠 AI 轮次 | C | 生产门①–⑤已程序化；再有"翻案波"（黑名单假阳纠偏）仍需人/AI 逐条开卷复核 → 走《coverage收口149》同款流程：只读复核 → `--no-write` 干跑对账 → 白名单 + 方案C 复活 → 单写者真写（写前备份） |
| 5 | （可选）交付夹命名补全 | P3 | `consolidate_delivery --enrich` 可联网 Crossref 补 year/author 后重汇总（当前缺字段条目已按 `{title}_{doi}` 优雅降级，不影响使用） |
| 6 | RSC governor 二道墙补丁（-165 方案） | 暂缓 | claimed-no-pdf 桶 31 条 RSC 卡 governor rate-gate；属 route-B 免费侧，用户已冻结免费扩张——与 A5 一并留给未来（route-B 授权会话叠加时顺带验证） |

## ③ 接手人须知的三条纪律

1. **唯一数字源**：净覆盖只认 `out/coverage.json`（368/631/36.84% @22:07:22）与《基线口径冻结说明-388-173.md》；文档里任何 326/339/377/388/448… 都是【历史口径】，勿复活（经验记录 U.3）。
2. **coverage 回写协议**：单写者 = `tools/build_coverage.py`；写前 `Copy-Item out/coverage.json out/coverage.bak_<ts>.json`；先 `--no-write` 全配方干跑对账再真写；白名单命中项自动过回写开卷门。
3. **落盘出口必过内容 QC 门**：新增任何非 `_download_pdf_core` 的落盘出口都必须显式 `_content_qc_gate(force=True)`（经验记录 W.2 铁律）。

---

*-149｜2026-07-04｜承用户「A5 暂时不在这个项目中落实，着重写入交接待办」拍板落档；A5 就绪度=代码/测试/Runbook 全入库（63ce5f7/788e8db 等），唯一 gate=用户凭据；免费侧收口证据=`1291a88`(36条重取0)/`b6d76fe`(631失败全统计)/交付夹 368/368+抽检10/10；自检 PASS=47/FAIL=0/SKIP=1。与《A5即跑清单-189》《基线口径冻结说明-388-173》《巡检待办-145》对齐、不重复。*
