# 交付终验收 · 免费侧闭环（干净·可信·提交后验收）· 368/999 = 36.84%

> 执行：**谷歌学术人机认证-157（总指挥）**｜2026-07-04｜承用户令「把最后的 run_all.py 改动定稿 → 跑一轮最终 fresh E2E + selftest → 冻结为最终交付证据」。
> 唯一权威净覆盖：`out/coverage.json` **success 368 / miss 631 / 999 = 36.84%** @generated_ts 2026-07-03 22:07:22（本波未动 coverage，沿用 writeback149 定版）。

---

## 一、代码定稿（提交前已 selftest 验证）

本波把三处在飞改动（并发成员遗留、经复核为一致单元）定稿入库：

| 文件 | 改动 | 验证 |
|---|---|---|
| `run_all.py` | 一键正门新增 `--s2-key` / `--core-key`，默认经 `cli._env_first` 环境别名回退（`SEMANTIC_SCHOLAR_API_KEY`/`S2_API_KEY`/`CORE_API_KEY`，canonical 优先、裁剪空白），透传入 `Config`；补 selftest ⑥b | `run_all --selftest`→RUN_ALL_OK |
| `fulltext_fetcher/download.py` | 新增 `_extract_pdf_meta_dois`（XMP/Info 抽内嵌 DOI，异常安全、绝不抛）+ 内容QC **门③ meta-doi-mismatch**：强正未命中且 PDF 内嵌 DOI 全≠期望→判 mismatch，专拦「同题他刊」title 假匹配；有正证据/无内嵌 DOI/含期望→不触发，绝不误杀；补 selftest ⑦.1a（4 例含「强正 body DOI 优先」） | `download`→DOWNLOAD_OK |
| `tools/regress_qc_union_189.py` | 门③ 回归：确定性负/正 `_gate3_selfcheck`（数据无关）+ 真数据回放喂内嵌 DOI | REGRESS_UNION_189_OK |

提交（本地 main，单一提交者=总指挥，作者沿用仓内约定 `谷歌学术人机认证 <scholar-fetcher@local>`，未改 git config）：

- `61625a1` feat: 一键正门透传 s2/core key(env 别名回退) + 内容QC 门③ meta-doi-mismatch 定稿
- `3adc1f9` docs: 631/368 基线 ROI 预排(-141) + 巡检待办登记(-145)
- `26d3bb2` chore: 撤除误入库运行日志 `_rescan_nocand_141_run.log` + `.gitignore` 补 `/_*.log`

---

## 二、selftest（提交前 + 提交后一致全绿）

```
python run_all_selftests.py
SUMMARY: PASS=46  FAIL=0  SKIP=2  (total=48)
SKIPPED (WIP): flaresolverr_nodriver(online), regress_qc_union_189(data-regress)
```

- 2 个 SKIP 为环境门控（联网 / 数据回归），符合预期。
- 数据回归单独补跑：`RUN_DATA_REGRESS=1 python tools/regress_qc_union_189.py` → **REGRESS_UNION_189_OK**（门③负/正样本 OK；189 同域错论文桶拒 189；34 title 假匹配桶漏 0）。

---

## 三、最终 fresh E2E（全新隔离 RUNROOT · 真跑）

`python run_all.py -f examples/sample_dois.txt --email test@example.com -o out/_final_e2e_140`

```
run_all 一页式总结  (RUNROOT=out/_final_e2e_140, 2026-07-04 00:04:34)
输入 7 → 去重 7 → 待跑 7
本次结果 : 成功 4 / 处理 7(miss 3)，用时 211.6s
本次命中源 : openalex=2, europe_pmc=1, websearch=1
RUNROOT 覆盖(可信) : 唯一 DOI 7 | 净成功 4 | still_missing 3 | 净成功率 57.1% (QC 剔抓错 0)
失败原因分桶 : no-source=2, download-fail=1
```

**成功逐条（统一命名 `{year}_{author}_{title}_{doi}` 落单一文件夹）**：

| DOI | 源 | 文件名 |
|---|---|---|
| 10.1038/nature14539 | websearch | `2015_LeCun_Deep_learning_10.1038_nature14539.pdf` |
| 10.1038/s41467-019-09234-6 | openalex | `2019_Zhou_Metascape_..._10.1038_s41467-019-09234-6.pdf` |
| 10.1371/journal.pone.0000217 | openalex | `2007_Tenaillon_Quantifying_Organismal_Complexity_..._10.1371_journal.pone.0000217.pdf` |
| 10.7717/peerj.4375 | europe_pmc | `2018_Piwowar_The_state_of_OA_..._10.7717_peerj.4375.pdf` |

**失败逐条（每条恰好一个终态，无悬空）**：

| DOI | 桶 | error |
|---|---|---|
| 10.1007/978-3-031-84300-6_13 | download-fail | landing-no-embedded-pdf(text/html) |
| 10.5040/9781350101272.00000005 | no-source | no-candidates |
| 10.9999/nonexistent.doi.20260703 | no-source | no-candidates（坏 DOI，预期）|

> 说明：nature14539（订阅刊）经 websearch 找到自存稿并通过内容 QC → 合法 success（演示免费路线找自存副本的能力）；2 条 title/arXiv 输入被 title 检索解析到「同题他作」，但**未采纳任何错 PDF**、干净落到 no-source/download-fail 终态——正是门③/内容QC 的守门价值。

**复现自证**：`python run_all.py --verify -o out/_final_e2e_140`

```
原报(summary)  : 唯一 7 | 净成功 4 | miss 3
复算(snapshot) : 唯一 7 | 净成功 4 | miss 3
QC 快照完整性  : OK
复现结论       : REPRODUCIBLE(数一致)
```

---

## 四、交付验收结论

| 验收项 | 结果 |
|---|---|
| 一键正门（DOI/标题→下载可得 PDF→统一命名→单文件夹→无需 AI） | ✅ `run_all.py` 真跑闭环 |
| 每条输入终态齐全（success / still_missing 分桶，无悬空） | ✅ |
| selftest 全绿 | ✅ PASS=46/FAIL=0/SKIP=2 |
| 内容 QC 门③（meta-doi-mismatch）+ 回归 | ✅ 已定稿并 REGRESS_UNION_189_OK |
| fresh E2E 真跑 + `--verify` 可复现 | ✅ 7/4/3 REPRODUCIBLE |
| 权威净覆盖 | ✅ 368/999 = 36.84%（22:07:22 定版，本波未动）|
| git 工作树 | ✅ 干净，HEAD `26d3bb2` |

**结论：免费/OA 侧交付已闭环，且经「干净·可信·提交后」的最终验收闭环确认。**

**下一阶段（用户 gate，非本闭环必选）**：A5 机构订阅付费墙全文（需 EZproxy/Shibboleth/CARSI 凭据；route-B 机构注入已于 `63ce5f7` 补线）；远端推送（本仓无 remote，待地址）。

---

*交付终验收 2026-07-04｜-157（总指挥）｜selftest 46/0/2 · data-regress OK · fresh E2E 4/7 REPRODUCIBLE · coverage 368/36.84% · git 干净 HEAD 26d3bb2｜out/_final_e2e_140 为隔离验收产物（gitignore 不入库）。*
