# 交付 · A5 即跑分片清单（441 真实缺口 · 按出版商）

> 执行：**谷歌学术人机认证-157（总指挥）**｜2026-07-04｜承用户令「按出版商把 441 真实缺口拆成 A5 即跑分片清单（凭据到位即可跑）」。
> 数据源（只读）：`out/coverage.json`（`generated_ts` 2026-07-03 22:07:22；success 368 / miss 631）。真实缺口 = miss 631 − QC 剔抓错论文 190 = **441**。
> 生成脚本：`_a5_shards_140.py`（gitignore 纯读产物）。分片文件在 **`a5_shards/`**（已入库，`{doi}  # 标题` 格式，`run_all` 直接可读）。

---

## 一、分片总览（按量降序）

| 分片文件 | 条数 | 出版商 | A5 主路线 |
|---|---:|---|---|
| `a5_shards/elsevier.txt` | **312** | Elsevier（ScienceDirect） | route-B 机构注入（JA3 强 CF） |
| `a5_shards/rsc.txt` | **46** | RSC | route-B（+ governor 补丁 -165 待落） |
| `a5_shards/acs.txt` | **28** | ACS | route-B 机构注入 |
| `a5_shards/springer.txt` | **14** | Springer/Nature-Link | route-A 直链 / route-B |
| `a5_shards/other.txt` | 14 | 杂（含 1006/1023/1360 等） | 视域名而定 |
| `a5_shards/wiley.txt` | **13** | Wiley（OnlineLibrary） | route-B 机构注入 |
| `a5_shards/aip.txt` | 3 | AIP | route-A/B |
| `a5_shards/taylorfrancis.txt` | 3 | Taylor & Francis | route-B |
| `a5_shards/pleiades.txt` | 3 | Pleiades/Springer | route-A/B |
| `a5_shards/csj.txt` | 3 | 日本化学会 | route-A/B |
| `a5_shards/iop.txt` | 1 | IOP | route-A |
| `a5_shards/ecs.txt` | 1 | ECS | route-A |
| **合计** | **441** | — | — |
| `a5_shards/_all_441.txt` | 441 | 全量（纯 DOI） | 一把梭 |

**四大订阅社 = Elsevier 312 + RSC 46 + ACS 28 + Wiley 13 = 399（占 441 的 90.5%）**，是 A5 阶段的绝对主体。

---

## 二、凭据到位即跑（每分片一条命令）

前置：①填本地凭据 `cp .ftf_institutional.local.json.example → .ftf_institutional.local.json`（enabled/provider/ezproxy_prefix 或 institution_cookie/institution_domains；已 gitignore，勿提交），或 env `FTF_INSTITUTIONAL=1 + FTF_EZPROXY_PREFIX/FTF_INSTITUTION_COOKIE`；②有头 Chrome（route-B 需 nodriver）。

```powershell
# 单社（推荐分社跑，便于观察命中率、礼貌限速）
python run_all.py -f a5_shards/elsevier.txt --institutional --route-b cf-only -o out/a5_elsevier
python run_all.py -f a5_shards/rsc.txt      --institutional --route-b cf-only -o out/a5_rsc
python run_all.py -f a5_shards/acs.txt      --institutional --route-b cf-only -o out/a5_acs
python run_all.py -f a5_shards/wiley.txt    --institutional --route-b cf-only -o out/a5_wiley
python run_all.py -f a5_shards/springer.txt --institutional -o out/a5_springer   # 多为 route-A 直链

# 全量一把梭（也可）
python run_all.py -f a5_shards/_all_441.txt --institutional --route-b cf-only -o out/a5_all
```

> ⚠️ **A5 route-B 接线**：`63ce5f7` 已补 `download→injection_plan` 机构注入线（离线 selftest 过）；真机效果需你给凭据实测。RSC 46 里 claimed-no-pdf 大头受 governor 影响，建议 -165 补丁后再冲。

---

## 三、口径与红线

- 分片仅含**真实缺口**（已排除 QC 剔的 190 抓错论文——那是「下到了但不是它」，不是 A5 目标）。
- A5 仅供**已合法获取机构访问权**者使用；凭据严禁入库/入日志（`credential_store` 已 redact，`.ftf_*.local.json` 已 gitignore）。
- 跑完以 `run_all --verify` 自证、并入权威 coverage 前经内容 QC 门（含门③ meta-doi-mismatch）复验，防再引入假阳。

---

*A5 即跑分片 2026-07-04｜-157｜441 真实缺口按出版商 12 分片（四大社占 90.5%）｜`a5_shards/`（入库）+ `_all_441.txt`｜凭据到位即跑、route-B 注入线已就绪（63ce5f7）｜脚本 `_a5_shards_140.py`（gitignore）。*
