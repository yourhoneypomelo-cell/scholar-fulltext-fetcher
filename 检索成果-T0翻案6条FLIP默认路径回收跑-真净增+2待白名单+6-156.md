# 检索成果 · T0 翻案 6 条 FLIP 默认路径回收跑(真净增 +2 / 完整可救 +6)

> 交付：**谷歌学术人机认证-156**（worker）｜2026-07-02｜总指挥 -149 派发 P0 任务「拿 148 内容 QC 认可的可救子集跑默认路径下载 + 并集门 QC，报真净增」。
> 边界：**只跑不改源码**——复用 `run_all.py`（默认路径、`--route-b off`）+ 生产内容 QC 门（`download._content_qc_*`，同 148 口径 O.5）+ 权威 `tools/build_coverage.py`（黑名单感知 + `qc_allow` 白名单）；**未改任何核心 `.py`、未回写 `coverage.json`**（交属主）。
> 承接：《检索成果-T0预筛still_missing翻案99条-内容QC逐条-148.md》§二（A 档 6 条真正文候选，cctc.200900261 已在 §三纠正为错文剔除）。

---

## 〇、TL;DR（诚实口径）

- **本波默认路径回收跑 · 真净增 = +2**（`10.1016/j.jcou.2022.102356`、`10.1016/j.jechem.2020.06.007`）：`run_all`（route-b off）websearch 兜底复现了净源真全文，**本次产物 + 在盘净源 双重内容 QC = match/100 / expected-doi-present**，铁净增，可立即回写。
- **QC 并集门零假阳**：本次 websearch 兜底把另 3 条抓成**错文**（apcatb→DOE 评审报告、s1872→他文、anie→arxiv 2025 错论文），全被生产内容 QC 判 `uncertain/partial-title-overlap` 拦下（coverage 层 soft/hard_reject），**未污染 coverage**——验证任务③「防假阳」的门有效。
- **完整可救面 = +6**：148 §二认定的 6 条净源真全文**本岗逐条复核全部 match/100**（在盘 `rerun_elsevier_143/fetch` + `rerun_wiley_144/fetch` + `recover_b4_cf`）。它们 miss 的**唯一根因是 DOI 键黑名单假阳（真全文被误杀）**，正确处置是 **`qc-allow` 白名单纠假阳（非重下）**。
- **落地口径**（官方 `coverage.json` 现状 success=**379**/999=**37.9%**，6 条均在 620 still_missing 内、qc=soft/hard_reject）：立即回写 +2 → **379 → 381（38.1%）**；-147 终裁采纳全 6 条 → **379 → 385（38.5%）**。（注:`build_coverage` 纯 flat 复算 baseline=371，与官方 379 差 8 为 -151 已回写二级目录的口径差，**不影响本 6 条增量**。）
- 与 148 预期对齐：148「确认净增地板 +5」（expected-doi-present 强正 5 条）；本岗复核 6 条全 match/100（含 apcatb 在盘 openalex 净源版 expected-doi-present=True）→ **地板 +6 ≥ 148 的 +5，一致且略高**。

---

## 一、方法（三步端到端，全复用生产件）

1. **默认路径下载**（任务②）：`python run_all.py -f _t0_recover156_input.txt --email <占位> -o out/t0_recover_156 -c 3 --route-b off`。resume 默认开；6 条在全局是 miss（黑名单剔除→不在 success 集）→ 不跳过 → 全部实下。
2. **过 QC 并集门**（任务③）：对**本次产物**与**在盘净源版本**双跑生产内容 QC 门（`_content_qc_non_article_reject` + `_content_qc_verdict`，rapidfuzz，match_hi=70/mismatch_lo=50），逐条判 verdict/score/exp_doi_present——防 SI/citation-report/错文假阳。
3. **真净增测算**（任务④）：权威 `build_coverage.build`（黑名单感知 + `qc_allow`），精确追踪 6 条 status 变化（baseline / 本次产物 / 在盘净源三场景），只算不落盘。

---

## 二、6 条逐条结果（双口径对比·证据 `out/_t0_recover156_netgain.csv`）

| DOI | 桶 | 本次默认路径(websearch) | 在盘净源 | 净增判定 |
|---|---|---|---|---|
| `10.1016/j.jcou.2022.102356` | elsevier | **match/100** 2062633B (idus.us.es) | **match/100** 2062633B (unpaywall) | **✅ 立即 +2** |
| `10.1016/j.jechem.2020.06.007` | elsevier | **match/100** 1825032B (sciengine 官方) | **match/100** 1825032B (websearch) | **✅ 立即 +2** |
| `10.1016/j.apcatb.2021.119925` | elsevier | ✗ uncertain/62.9（9.4MB DOE 评审报告，错文） | **match/100** 1765161B (openalex) | ⏳ 建议 +4（白名单） |
| `10.1016/s1872-2067(17)62899-7` | elsevier | ✗ uncertain/62.7（ir.ciomp 他文，3 页） | **match/100** 2187222B (openaire) | ⏳ 建议 +4（白名单） |
| `10.1002/anie.201406637` | wiley | ✗ uncertain/57.1（**arxiv 2506.12931** 2025 错论文） | **match/100** 579727B (semantic_scholar) | ⏳ 建议 +4（白名单） |
| `10.1116/1.5053761` | aip | ✗ MISS（cloudflare-challenge http-403） | **match/100** 5084557B (crossref) | ⏳ 建议 +4（白名单） |

**读法**：前 2 条本次 websearch 抓到的 PDF 与在盘净源**同字节、同 match/100**（同一篇真全文）→ 铁净增。后 4 条本次默认路径**未复现**：3 条 websearch 源漂移抓成错文（内容 QC 正确拦下）、1 条 aip 被 CF 挡（route-b off 无法过 JA3 型强 CF）——但它们的真全文早已在盘净源批，是黑名单假阳。

---

## 三、QC 并集门防假阳验证（任务③核心）

本次 websearch 兜底暴露的 3 条错文，**全部被生产内容 QC 门拦住，零漏网**：

| DOI | websearch 抓到 | 内容 QC 判定 | coverage 层 |
|---|---|---|---|
| apcatb.2021.119925 | chemcatbio「beto-12-peer-review」评审报告(38 页 9.4MB) | uncertain 62.9 partial-overlap, exp_doi_present=False | soft_reject（union）|
| s1872-2067 | ir.ciomp「A macro library for monatomic catalysts」(3 页) | uncertain 62.7 partial-overlap, exp_doi_present=False | hard_reject（highconf）|
| anie.201406637 | **arxiv.org/pdf/2506.12931**（2025 年新论文≠2014 Angew） | uncertain 57.1 partial-overlap, exp_doi_present=False | soft_reject（union）|

→ 结论：**默认路径靠 websearch 兜底质量不可靠（本波 6 条里 3 条抓错），但生产 QC 并集门 100% 挡住，coverage 无假阳**。这印证 148/153「uncertain 池整池拒收」策略与门④/门②的有效性。

---

## 四、真净增测算（证据 `out/_t0_recover156_netgain.log`）

| 场景 | 本 6 条 success | 净增 | 全局 success |
|---|---:|---:|---:|
| baseline（权威默认 flat + 全黑名单） | 0 | — | 371 |
| **A：本次默认路径产物 + `qc-allow` 本次 match 2 条** | 2 | **+2** | 373 |
| **B：在盘净源真全文 + `qc-allow` 148 的 6 条** | 6 | **+6** | 381※ |

※ 表内全局 success（371/373/381）为 `build_coverage` **纯 flat 复算**口径；官方 `coverage.json`（-151）基线 success=**379**（差 8 为 -151 已回写二级目录的口径差）。场景 B 全局 +10 而非 +6：`--extra-dirs` 纳入整个源目录，顺带回收目录内**其他**已下净真全文（+4，超本任务范围）。**本任务净增以「本 6 条 success 数」为准 → A=+2、B=+6**；映射到官方口径即 **379→381（+2）、379→385（+6）**。

---

## 五、待回写 coverage 清单（交属主·本岗不回写）

清单文件：`out/_t0_recover156_writeback_qcallow.txt`（`build_coverage --qc-allow` 可直接消费）。

- **立即可回写（+2·高置信）**：`10.1016/j.jcou.2022.102356`、`10.1016/j.jechem.2020.06.007`——本次默认路径实跑 && 在盘净源 双重 match/100，无需人核。
- **建议回写（+4·须 -147 终裁）**：`10.1016/j.apcatb.2021.119925`、`10.1016/s1872-2067(17)62899-7`、`10.1002/anie.201406637`、`10.1116/1.5053761`——在盘净源 match/100，属黑名单假阳；本次默认路径未复现（websearch 漂移/CF），建议直接白名单纠假阳而非再重下。

回写命令（属主执行）：
```
python tools/build_coverage.py \
  --qc-allow out/_t0_recover156_writeback_qcallow.txt \
  --extra-dirs rerun_elsevier_143/fetch,rerun_wiley_144/fetch
```
（`qc_allow` 优先级最高、只对落盘真成功生效、绝不凭空造成功；`recover_b4_cf` 为一级目录已在默认扫描内。）

---

## 六、待办 / 依赖（交接）

- [ ] **-147 终裁**：确认建议回写的 4 条（apcatb/s1872/anie/1.5053761）从 DOI 键黑名单移除或入 `--qc-allow`；尤其 s1872 在 **hard 黑名单**（batch4_p1 曾 websearch 抓成 nature IL-17，后 openaire 抓对，典型「先错后对」）。
- [ ] **coverage 回写**（-151/-140）：先落 +2（立即档）→ 371→373；-147 确认后落 +6 → 371→377。
- [ ] **门/源加固建议**（-140/-161）：本波再证 **websearch 兜底源漂移严重**（6 条里 3 条抓错文，含把 anie 2014 抓成 arxiv 2025）。可考虑：websearch 命中且 `exp_doi_present=False` 时**降级为 uncertain 不落 success**（现已由 QC 并集门兜住，但源头可更严）。
- [ ] **aip 强 CF**（route-B 属主）：`10.1116/1.5053761` route-b off 恒 `cloudflare-challenge`，真全文已在盘（crossref 净源），无需 route-B 重救。

---

## 七、产物清单（本波新写，均在 `out/` 或仓根，未改核心码）

| 文件 | 说明 |
|---|---|
| `_t0_recover156_input.txt` | 6 条 FLIP 输入清单 |
| `out/t0_recover_156/` | `run_all` 独立 RUNROOT（fetch/ + coverage.json + still_missing.txt + run_all_summary.json）|
| `out/t0_recover_156_runall.log` | 默认路径下载可读日志（5/6 成功，194.9s，全 websearch）|
| `out/t0_recover_156_qc.log` | 内容 QC 门双跑对比日志 |
| `out/t0_recover_156_netgain.log` | 真净增测算日志（baseline/A/B）|
| `out/_t0_recover156_netgain.csv` | **真净增清单**（6 条双口径 verdict + 回写档位）|
| `out/_t0_recover156_writeback_qcallow.txt` | **待回写 qc-allow DOI 清单**（分立即 +2 / 建议 +4）|
| `_t0_recover156_diag.py` / `_blsrc.py` / `_qc.py` / `_netgain.py` | 一次性审计脚本（只读，不改核心码）|

---

*核验 2026-07-02｜谷歌学术人机认证-156｜只跑不改源码、复用生产下载路径 + 内容 QC 门 + 权威 build_coverage、未回写 coverage｜承 148（A 档 6 候选）、142（T0 P0 ROI）｜终裁交 -147、回写交 -151/-140｜已同步总指挥 149。*
