# 下一波回收 ROI 预排 · still_missing **631** 分桶（368 基线刷新 · -141）

> 交付：**谷歌学术人机认证-141**（本会话）｜2026-07-03｜纯读析刷新。
> 边界：**只读** `out/coverage.json`（`generated_ts=2026-07-03 22:07:22`：**success 368 / miss 631 / 999 = 36.84%**）+ 逐条 `records` 分桶；仅新建本 1 份 md。未改任何 `.py`/coverage/still_missing/git、未跑网络、未发射 route-B。分析脚本 `_roi631_probe_out.txt`（纯读产物，可删）。
> 关系：本文是《下一波ROI预排-141.md》（-152，673-miss/326 基线）的**基线刷新增补**——writeback149 定版后 miss 673→631、success 326→368。机制结论沿用前文，本文只刷新数字 + 标注两条**本会话新增的免费杠杆**（route-B 断线补齐 63ce5f7、API key 别名回退 b892eca），不重画机制论证。

---

## 〇、TL;DR（一页）

- **权威地板**：净成 **368 / 999 = 36.84%**、**miss = 631**（22:07:22 定版；writeback149 +50 gold −8 websearch 假阳并入）。
- **631 的结构 = 91.1% 是四大订阅社**（Elsevier 357 + ACS 127 + RSC 62 + Wiley 24 = 575/631），**主体是免费物理到顶的付费墙**——与前文（-152）结论一致：破 ~40% **唯 A5 机构订阅**（凭据永久 gate，现 ROI=0）。
- **免费真净增只来自 4 个正交小池**（全零/近零凭据），去重后诚实上沿 **+4~19**，净覆盖点估从 36.84% 上探 **~37.5~39%** 区间：
  1. **claimed-no-pdf（36，RSC 31）**——元数据 success 但盘上无 PDF（route-B viewer 未落字节）；**最便宜**、但 RSC 卡 governor，**+1~5**。
  2. **CF-challenge OA 子集（73）**——Wiley OnlineOpen / AIP / T&F OA 极小子集（**ACS 走量=0，93% SI 陷阱**）；route-B（**本会话已补机构注入线**）/ 干净 FS-shim + 强制 QC，**+2~6**。
  3. **landing-no-pdf render 子集（62，Elsevier 41 / Springer 17）**——render_fetch 抽 JS 注入直链；大头仍付费墙，**+1~3**。
  4. **no-candidate 带 key 重扫（231，Elsevier 224）**——**本会话 API key 别名回退（b892eca）**若命中你本机的 `OPENALEX_API_KEY`/`SEMANTIC_SCHOLAR_API_KEY` 等，可能新浮出个位数 OA 候选，**+0~5**。
- **qc-reject（190 = soft 160 + hard 30）不是可捞池**——是已正确剔除的抓错论文（websearch/ACS SI 假阳），免费 ROI≈0，别当增量。

| 优先级 | 行动 | 桶 | 免费净增（诚实） | 需要 |
|:--:|---|---|:--:|---|
| **P0·最便宜** | claimed-no-pdf 重取（先 5 非 RSC；RSC 待 -165 governor 补丁） | claimed-no-pdf 36 | **+1~5** | route-B（已就绪） |
| **P0·带 key 重扫** | 用别名回退喂 key 重跑 no-candidate（增量识别 OA 候选） | no-candidate 231 | **+0~5** | 你的 API key(env) |
| **P1** | CF-soft OA 收口（Wiley/AIP/T&F，逐页强制 QC） | cf-challenge 73 | **+2~6** | 干净 FS-shim / route-B |
| **P2** | landing-no-pdf render 抽链子集 | landing 62 | **+1~3** | `[render]` |
| **P3·gate** | A5 机构订阅（主体 ~570） | no-candidate+paywall+CF/长尾订阅 | **0 →（+30~40pp）** | 你的机构凭据 |
| **❌ 不做** | ACS `/doi/pdf` 走量、qc-reject 当池、Elsevier browser_search/wayback 扩 | — | ≈0 | — |

---

## 一、631 末次原因分布（当前定版逐条 records）

| 末次原因 | 条数 | 主出版商 | 定性 | 免费可救？ |
|---|:--:|---|---|:--:|
| **no-candidate** | **231** | Elsevier 224 | 无免费候选 URL（IP/登录墙投影） | ❌ 主体（带 key 重扫或有微量） |
| **qc_soft(wrong-paper)** | **160** | ACS 90 / Elsevier 42 / RSC 12 | 已正确剔的抓错论文（audit-union） | ❌ ≈0（非池） |
| **cf-challenge(403)** | **73** | ACS 27 / Elsevier 14 / Wiley 13 / RSC 8 | Cloudflare 质询 | ⚠️ 仅 OA 子集 |
| **landing-no-pdf** | **62** | Elsevier 41 / Springer 17 | 落地页无内嵌 PDF | ⚠️ 部分（render） |
| **claimed-no-pdf** | **36** | RSC 31 | 元数据 success 但盘上无 PDF | ✅ 最便宜（RSC 卡 governor） |
| **qc_hard(wrong-paper)** | **30** | ACS 9 / Elsevier 7 / Wiley 6 | 已正确剔的抓错论文（title-mismatch） | ❌ ≈0 |
| **paywall-403** | **11** | Elsevier | 订阅 403（非 CF） | ❌ A5 |
| content-mismatch | 9 | Elsevier | 抓到错文/顶包，正确判 miss | ❌ ≈0 |
| no-response | 7 | Elsevier | 重试耗尽/连接失败 | ⚠️ 重试或微量 |
| rsc-governor | 5 | RSC | governor rate-gate 显式挡 | ⚠️ 需 -165 补丁 |
| http-404/202/412/not-pdf 等 | ~7 | 零星 | 死链/异常 | ❌ ≈0 |

> **合计 631**。**规划用机制镜头（§二）**，末次原因仅作规模参照——recover/websearch 已触碰覆盖真墙，qc 改判 miss 是卫生纠偏、非可捞池（承 -152 §二末注）。

---

## 二、免费可救 4 池详解（诚实校准 · 每笔过内容 QC 门）

### 池 1 · claimed-no-pdf（36；RSC 31 + 非 RSC 5）——最便宜，但 RSC 卡 governor
- **构成**：`success-metadata-but-pdf-missing-on-disk` 36 条，`source=None`（元数据记 success，但 `<batch>/pdfs/` 无文件）。按批：batch6×18、batch4_*×16、batch7×1、recover_b4_cf×1。按社：**RSC 31**、其余 5（10.1109/10.1146/10.1166/10.1595/10.2138 各 1）。
- **性质**：与 writeback149 解隔离的 46 份 gold **不同**（那批已翻 success 计入 368）；这 36 是 route-B viewer 抓取过、字节未落盘的 RSC 为主残留。
- **可救性**：**非 RSC 5 条**可先常规重取（最快）；**RSC 31** 走 route-B 页内直下，但 **0/8 归零主因是 governor rate-gate（-152 §桶2 实锤）**，不上 -165 补丁批量重取仍大概率再撞。单条 PoC 可成（`d5ra08493h` 484KB），批量不可复现。
- **本会话相关**：route-B 断线补齐（63ce5f7）只解**机构注入**通路；这 31 条是**免费 OA-but-CF**，吃的是 governor 而非机构墙，故仍 defer 到 -165 governor 补丁。
- **诚实净增**：**+1~5**（5 非 RSC + RSC 少量单发）。

### 池 2 · CF-challenge OA 子集（73）——ACS 走量=0，真增在 Wiley/AIP/T&F
- **构成**：ACS 27 / Elsevier 14 / Wiley 13 / RSC 8 / AIP 3 / T&F 3 / CSJ 3 / 余 2。
- **可救性（承 O.3/N.3 实锤）**：**ACS `/doi/pdf` 过 CF 后 ~93% 是免费 SI 冒充正文**（逐页看 56 条仅 4 真正文）→ **ACS 归 A5-only、走量=0**；Elsevier CF 14 多为订阅；真免费净增只在 **Wiley OnlineOpen + AIP/T&F OA** 极小子集，**必须逐页过强制内容 QC**（route-B 落盘 force=True 门已就位，见经验记录 W.2）。
- **诚实净增**：**+2~6**（干净 FS-shim 串行或 route-B，禁双开锁）。

### 池 3 · landing-no-pdf render 子集（62）——大头付费墙，render 抠 JS 直链拿小头
- **构成**：Elsevier 41 / Springer 17 / 余 4。
- **可救性**：Elsevier/Springer landing-no-embedded-pdf **大头是订阅落页**（browser_search 0/10、wayback 0/12 反复证顶）；少数 OA 落地页把 PDF 直链藏在 JS 里，`[render]` 抽链可捡。
- **诚实净增**：**+1~3**。

### 池 4 · no-candidate 带 key 重扫（231）——本会话别名回退可能微量解锁
- **构成**：Elsevier 224 + Elsevier-legacy 4 + 余 3。**免费物理到顶的主体**。
- **本会话新杠杆**：`cli.py` API key 别名回退（b892eca）——若你本机把 key 存成 `OPENALEX_API_KEY`/`SEMANTIC_SCHOLAR_API_KEY`/`CORE_API_KEY`（而非 canonical `OPENALEX_KEY`/`S2_KEY`/`CORE_KEY`），过去被静默闲置，现自动兜底生效 → OpenAlex/S2/CORE 限速放宽、**可能新浮出个位数 OA 候选**。
- **诚实净增**：**+0~5**（纯增量识别；Elsevier 主体仍订阅墙，唯 A5）。

---

## 三、诚实天花板（防高估）

- **免费净增上沿**：池1(+1~5) + 池2(+2~6) + 池3(+1~3) + 池4(+0~5)，去重后 **+4~19**；净覆盖点估 **36.84% → ~37.5~39%** 区间。
- **631 主体（575 = 四大订阅社 91.1%）是免费物理到顶的付费墙**——`no-candidate 231`/`paywall-403 11`/CF 订阅子集/landing 订阅落页均已反复证顶。**破 ~40% 唯 A5 机构订阅**（凭据永久 gate、无凭据 ROI=0、不投人日）。
- **别把「发射数/过 CF 数/success 数/qc 改判 miss 数」当净增**：真命中 = 过盾取字节 + 内容 QC 门③④⑤双关；**qc-reject 190 是已正确剔的抓错论文，不是可捞池**。

---

## 四、推荐排期（供直接排工）

1. **P0（最便宜，今天）**：claimed-no-pdf 的 **5 条非 RSC** 常规重取 + **no-candidate 带 key 重扫**（先确认你本机 key env 名，别名回退已就位）→ 过 QC 结算净增。
2. **P1**：CF-soft OA 收口（Wiley/AIP/T&F，干净 FS-shim/route-B，逐页 force QC）。
3. **P2**：landing-no-pdf 的 render 抽链子集。
4. **defer**：RSC claimed-no-pdf 31 + rsc-governor 5 → 等 **-165 governor 补丁**后小样本 ≥3 验，别拿算力硬撞 0/8。
5. **P3·gate**：A5 机构订阅（主体 ~570）——**待你投喂真实机构凭据 + 有头 Chrome**；route-B 机构注入线本会话已补齐（63ce5f7），凭据到位即可跑 `a5_smoke_input.txt` 验活体。

---

## 五、来源与方法

- **权威地板**：`out/coverage.json`（22:07:22，success 368 / miss 631 / 36.84%）逐条 `records` 分桶。
- **本文分析（纯读、可删）**：`_roi631_probe_out.txt`（publisher×reason 交叉 + claimed-no-pdf 细分）。
- **机制/ROI 实证（不重画，指针）**：《下一波ROI预排-141.md》（-152，673 基线机制分桶）、《基线口径冻结说明-388-173.md》（368 定版）、《A5联调就绪核-...-177.md》（路线 A/B 就绪 + 本会话补齐）、经验记录 **N.3/O.3/L/M/S.2/P/W** 节。
- **本会话新增免费杠杆**：`63ce5f7`（route-B 机构注入线补齐）、`b892eca`（API key 别名回退）。

---
*核验 2026-07-03｜-141｜纯读析刷新，未改 .py/coverage/still_missing/git、未跑网络、未发射 route-B｜631 = no-candidate 231 + qc_soft 160 + cf 73 + landing 62 + claimed-no-pdf 36 + qc_hard 30 + paywall 11 + 余；四大订阅社 575/631=91.1%｜免费净增 +4~19（点估 ~37.5~39%），破 40% 唯 A5；qc-reject 190 非池；数字一律以 out/coverage.json 为唯一真相源。*
